{
  "project": "ADR-049 PR-7 — crypto-state move (key-move)",
  "description": "The atomic migration of all per-context deps.crypto.* call sites off the supervisor-owned MlsCryptoProvider onto the actor-owned &mut PerContextState crypto state, wiring take_crypto_state at the actor spawn seam. Final slice of the ADR-049 PR-4 -> PR-6 -> PR-7 read-authority/state-ownership sequence. See ADR-049 Decision 15.",
  "gates": [
    {
      "id": "gate-cryptomove-1",
      "name": "Atomic crypto-state move",
      "stories": ["SCP-CRYPTOMOVE-001"]
    }
  ],
  "stories": [
    {
      "id": "SCP-CRYPTOMOVE-001",
      "title": "Move per-context crypto state from MlsCryptoProvider onto actor-owned PerContextState (take_crypto_state at spawn)",
      "gate": "gate-cryptomove-1",
      "priority": "P0",
      "severity": "critical",
      "status": "pending",
      "files": [
        "crates/scp-runtime/src/context/lifecycle_helpers.rs",
        "crates/scp-runtime/src/context/messaging_helpers.rs",
        "crates/scp-runtime/src/context/governance_helpers.rs",
        "crates/scp-runtime/src/context/ttl_close_helpers.rs",
        "crates/scp-runtime/src/context/trust_recovery_helpers.rs",
        "crates/scp-runtime/src/context/broadcast_helpers.rs",
        "crates/scp-runtime/src/context/actor/handlers/lifecycle.rs",
        "crates/scp-runtime/src/context/actor/handlers/trust_recovery.rs",
        "crates/scp-runtime/src/context/actor/state.rs",
        "crates/scp-runtime/src/context/actor/deps.rs",
        "crates/scp-runtime/src/context/supervisor/supervisor.rs",
        "crates/scp-runtime/src/crypto/mls/provider.rs"
      ],
      "description": "PR-7 (the 'key-move') of the ADR-049 PR-4 -> PR-6 -> PR-7 sequence. PR-6 (merged at origin/main HEAD 8e2110f93) flipped read-authority for the Class-M epoch/recv floors onto the Supervisor-owned ContextFloors registry and deleted the provider mirrors. This story does the state move: it migrates ALL ~32 per-context deps.crypto.* call sites (lifecycle 16, messaging 5, governance 4, ttl_close 3, trust_recovery 3, broadcast 1) off the Arc<MlsCryptoProvider> and onto the actor-owned &mut PerContextState crypto state (ContextCryptoState in context/actor/state.rs), and wires take_crypto_state(context_id) at the actor spawn seam so the crypto material is moved into the actor exactly once at spawn. Per ADR-049 Decision 15 this MUST be ONE atomic PR: take_crypto_state is atomic and one-way per context (it DashMap::removes the context from the provider and records it in taken_context_ids; afterward every deps.crypto.* on that context returns CryptoFailed('context state owned by actor')), so a per-domain move would break the un-migrated domains at runtime for the life of the context. Scope exception (recorded, per prd.md story-scope guidance): this story is deliberately larger than the ~15-30 min single-iteration norm because the move is indivisible by construction (Decision 15) — it cannot be split into independently-mergeable stories without shipping a broken build between merges. FLOORS ARE EXCLUDED from the move and STAY in the ContextFloors registry (Class-M); the actor's dormant recv_sequence_tracker field (state.rs) MUST stay dead (Decision 15, PR-6 annotation); the floor-gate seams (deps.supervisor.check_and_advance_* and mirror_forward_local_sender_epoch) are unchanged. The durable-blob crypto export migrates too: export_crypto_state becomes an inherent method on PerContextState/ContextCryptoState (floors already come from the registry per PR-6), so no per-context deps.crypto.export_crypto_state call survives in the domain helpers. PR-7 relocates OWNERSHIP of existing per-context crypto state (mint-or-restore-then-move); it does NOT relocate group CREATION — the provider stays the birth/restore seam, so create_mls_group / add_member remain permitted provider calls at the Create/Join spawn/birth seam, and their removal is deferred to the separable §6 provider-dissolution end-state (explicitly NOT PR-7). local_did stays a node/provider-level constant (not per-context state) and is exempt from the move. The moved sender_key_epoch counter is the local node's key-generation counter and is Class-S (sync-persist fail-closed on rotation per ADR-049 Decision 9), with the registry floor for local_did continuing to be raised and never regressing on a coalesce-window rollback. send_sequence seeds PerContextState::send_tracker via SendSequenceTracker::from_persisted (there is no ContextCryptoState.send_sequence field). Respawn/restore reconstitutes the actor crypto state directly from the snapshot into the new actor (never round-tripping the provider), preserving the one-way take invariant.",
      "acceptanceCriteria": [
        "Zero steady-state per-context provider crypto calls remain in the six domain helpers: grep -rE 'deps\\.crypto\\.(seal|open|advance_epoch|rotate_sender_key|remove_member|destroy_mls_group|destroy_sender_key|mls_encrypt_management|local_sender_key_epoch|validate_key_package|export_crypto_state)' over crates/scp-runtime/src/context/{lifecycle_helpers,messaging_helpers,governance_helpers,ttl_close_helpers,trust_recovery_helpers,broadcast_helpers}.rs AND crates/scp-runtime/src/context/actor/handlers/{lifecycle,trust_recovery}.rs returns 0 matches; those operations (including export) are rewritten to operate on the actor-owned &mut PerContextState crypto state.",
        "The birth/restore seam calls create_mls_group and add_member remain PERMITTED (not migrated by PR-7): the provider still constructs the MLS group at the Create/Join handler before take_crypto_state moves it, and their removal is the separable §6 provider-dissolution follow-on — PR-7 does not delete create_mls_group / add_member from the provider.",
        "local_did stays a node/provider-level constant and is exempt: deps.crypto.local_did calls may remain (they read node identity, not per-context crypto state) and are excluded from the zero-grep in the first criterion.",
        "Pre-existing-group spawn seams seed the actor crypto PRE-dispatch (before the actor accepts commands): for restore_context, respawn_from_snapshot, cold restart (restore_all_contexts), and the spawn-from-Welcome path where the provider installed the joined group before spawn, the actor's ContextCryptoState.mls_group.is_some() holds immediately after spawn and before the first command — verified by async tests. On the restore/respawn/cold-restart legs the crypto is seeded from the owned material returned by restore_crypto_state (NOT a re-take): the test asserts no take_crypto_state is called on the already-taken context and the context never re-appears in provider.contexts.",
        "Fresh create builds the group in-dispatch, so the provider-build + take runs AFTER the actor accepts commands: for a fresh create the MLS group is constructed inside the Create handler (crates/scp-runtime/src/context/actor/handlers/lifecycle.rs) in the actor's dispatch loop (ContextCryptoState.mls_group is None between spawn and that handler — state.rs), and take_crypto_state moves the provider-built group into the actor within that handler — verified by an async test asserting mls_group.is_none() immediately after spawn and mls_group.is_some() only AFTER the Create command completes (not at spawn). The seam enumeration therefore includes the in-dispatch Create/Join handler, not only the spawn_actor_* functions; a grep confirms take_crypto_state is reached from BOTH the pre-existing-group spawn seams (spawn_actor_with_state / restore_context / respawn_from_snapshot / spawn_actor_from_welcome) AND the in-dispatch Create/Join handler.",
        "The move payload send_sequence seeds the send tracker, not a crypto-state field: the take path routes OwnedMlsCryptoState.send_sequence into PerContextState::send_tracker via SendSequenceTracker::from_persisted, and context/actor/state.rs::ContextCryptoState declares no send_sequence field — verified by grep over the type definition and the take-seam call site.",
        "The dormant recv anti-replay mirror stays dead: grep -rn '\\.recv_sequence_tracker' over crates/scp-runtime/src (excluding the field definition and doc-comment in context/actor/state.rs, and excluding #[cfg(test)] modules) returns 0 write or read sites — the authoritative recv floor is read only from the ContextFloors registry.",
        "Floors never appear on the move payload or the actor crypto state: OwnedMlsCryptoState (provider.rs) has exactly the fields {mls_group, sender_key, sender_key_store, sender_key_epoch, send_sequence, pending_distributions, nonce_dedup, member_wrapping_keys}, and neither OwnedMlsCryptoState nor context/actor/state.rs::ContextCryptoState declares any field of type ReceiveFloor or named sender_epochs / recv_sequence — verified by grep over both type definitions.",
        "The live floor-gate seams are unchanged: git diff over PR-7 shows no modification to the check_and_advance_epoch / check_and_advance_recv_sequence gate calls in the decrypt_and_dispatch receive path nor to mirror_forward_local_sender_epoch (the move relocates sender-key material, not the gate).",
        "Sender-key rotation on the moved state is Class-S sync-persist fail-closed AND raises the registry floor: an async test rotates the sender key on a migrated context and asserts (a) sender_key_epoch is durably persisted before the rotation acks, (b) an injected persist failure (via the one-shot rotation persist-failure test seam) causes the rotation to return Err (fail-closed, not Ok), and (c) the ContextFloors registry entry for local_did is raised and a subsequent simulated coalesce-window rollback of the actor snapshot does not regress that registry floor.",
        "Cross-domain reachability of the migrated state: an async test creates a context, takes its crypto state into the actor at spawn, then drives one lifecycle-domain operation and one governance-domain operation against the SAME context and asserts both succeed operating on the actor-owned crypto state (proving the move did not orphan any domain's reader), and asserts a raw deps.crypto.with_context on that context id returns CryptoFailed('context state owned by actor').",
        "Respawn preserves the one-way take invariant via returned owned material (not a provider round-trip): an async test spawns an actor for a context, simulates an actor crash + respawn_from_snapshot, and asserts (a) the respawned actor's ContextCryptoState is reconstituted DIRECTLY from the material returned by restore_crypto_state (mls_group.is_some()), (b) restore_crypto_state performed NO provider.contexts insert and NO re-take, (c) the provider's taken_context_ids marker for the context is unchanged (still taken), and (d) the context never re-appears in provider.contexts at any point.",
        "ADR-049 provenance is intact: crates/scp-runtime/src/context/actor/state.rs::ContextCryptoState::recv_sequence_tracker retains its DORMANT PR-7 annotation, and the ADR-049 Decision 15 heading exists and is cited by the moved-handler modules or their module docs.",
        "Full CI is green on every commit of the PR: cargo fmt --all -- --check; cargo clippy --workspace --all-targets --features scp-ffi-uniffi/allow_in_memory_custody,scp-ffi/allow_in_memory_custody,scp-ffi-napi/allow_in_memory_custody,scp-core/testing,scp-runtime/testing -- -D warnings; cargo check -p scp-protocol --target wasm32-unknown-unknown; DYLD_LIBRARY_PATH=$(python3.12 -c \"import sysconfig; print(sysconfig.get_config_var('LIBDIR'))\") cargo test --workspace; python3.12 scripts/check-block-in-place.py; bash scripts/check-deleted-primitives.sh; python3.12 scripts/check-protocol-sync.py; bash scripts/check-protocol-deps.sh — all exit 0.",
        "python3.12 scripts/validate-prd.py exits 0 with this story present."
      ],
      "actionItems": [
        "Land ADR-049 Decision 15 and the §9 read-authority-switch PR-7-clause amendment (this docs PR) FIRST so this story's sources resolve; implementation branches from the merged docs.",
        "Rewrite the 16 lifecycle_helpers.rs deps.crypto.* sites (destroy_mls_group, destroy_sender_key, remove_member, rotate_sender_key, mls_encrypt_management, validate_key_package, as_ref) to operate on the actor-owned &mut PerContextState crypto state.",
        "Rewrite the 5 messaging_helpers.rs sites: rotate_sender_key, local_sender_key_epoch, and export_crypto_state migrate onto the actor state (export becomes an inherent PerContextState/ContextCryptoState method); local_did stays a provider/node constant and is NOT migrated.",
        "Rewrite the 4 governance_helpers.rs (advance_epoch, rotate_sender_key), 3 ttl_close_helpers.rs (export_crypto_state, as_ref), 3 trust_recovery_helpers.rs (advance_epoch, export_crypto_state, seal), and 1 broadcast_helpers.rs (export_crypto_state) sites, plus the actor/handlers/{lifecycle,trust_recovery}.rs sites, onto the actor state.",
        "Wire take_crypto_state into Supervisor::spawn_actor_with_state and the create/restore/respawn/welcome entrypoints so the OwnedMlsCryptoState payload seeds the actor's ContextCryptoState, and route send_sequence into PerContextState::send_tracker via SendSequenceTracker::from_persisted, exactly once at spawn.",
        "Change MlsCryptoProvider::restore_crypto_state to RETURN the owned crypto material (OwnedMlsCryptoState) alongside RestoredFloors instead of self.contexts.insert(...)ing it (it already constructs the ContextCryptoState at the build site — return that owned payload rather than installing it), and split restore_crypto_state_with_floor_guard into (a) merge snapshot floors into the ContextFloors registry (unchanged from PR-6) and (b) seed the new actor's ContextCryptoState directly from the returned owned material (wrapping mls_group/sender_key in Some(..), routing send_sequence into send_tracker). On a warm respawn the context is already in taken_context_ids, so restore MUST NOT re-take (a take on a taken context returns CryptoFailed('context state owned by actor')). Keep the provider as the birth seam only: create_mls_group / add_member stay provider calls at the Create/Join handler — their removal is the deferred concrete-MlsCryptoProvider-struct dissolution follow-on (consistent with §6's crypto-provider-trait replacement), NOT PR-7.",
        "Keep the ContextCryptoState::recv_sequence_tracker field dormant (no reads/writes) and update the ActorDeps::crypto doc-comment (deps.rs) to reflect the completed steady-state dissolution — the provider is now a birth/restore seam only.",
        "Add an injectable one-shot persist-failure test seam for sender-key rotation, analogous to the existing #[cfg(any(test, feature = \"testing\"))] force_export_failure AtomicBool one-shot seam in provider.rs (~line 494), so the 'rotation Class-S fail-closed' criterion (injected persist failure => rotation returns Err) is testable end-to-end without a broken production build.",
        "Add the async tests enumerated in the acceptance criteria (pre-dispatch-seed for restore/respawn/welcome, fresh-create in-dispatch take timing, send_sequence-seeds-send_tracker, cross-domain reachability, rotation Class-S fail-closed + registry-floor-no-regress, respawn one-way-invariant via returned owned material, provider-returns-owned-by-actor).",
        "Run the full ADR-049 Verification command block plus the review roster to double-zero before opening the PR."
      ],
      "blockedBy": [],
      "sources": [
        { "file": ".docs/adrs/ADR-049-actor-per-context.md", "section": "### 15. PR-7 crypto-state move is one atomic PR, not per-domain" },
        { "file": ".docs/adrs/ADR-049-actor-per-context.md", "section": "### 1. ContextActor owns state by move" },
        { "file": ".docs/adrs/ADR-049-actor-per-context.md", "section": "### 6. `ContextCryptoProvider` (26 methods) is deleted; replaced by two narrow traits" },
        { "file": ".docs/adrs/ADR-049-actor-per-context.md", "section": "### 9. Persistence: coalesced by default, sync for safety-critical" },
        { "file": ".docs/adrs/ADR-049-actor-per-context.md", "section": "### 10. Actor panic recovery" }
      ],
      "details": {
        "phase": "2 (ADR-049)",
        "crate": "scp-runtime",
        "adr": "ADR-049",
        "pr": "PR-7",
        "scopeException": "single atomic story by construction — Decision 15 (take_crypto_state is atomic/one-way per context); not decomposable into independently-mergeable stories"
      }
    }
  ]
}
