{
  "project": "scp",
  "description": "Cooperative mode HTTP binding for bridge connectors: implement the six-endpoint REST API specified in §12.10 as HTTP handlers in scp-node. Platforms implement the shadow/message/attest/status/webhook endpoints; scp-node hosts the bridge node that mediates between these endpoints and SCP protocol operations. Authentication via DID-signed bearer tokens. Webhook receiver for platform-initiated events. Integration with existing bridge submodules (registration, shadow, claiming, provenance).",
  "gates": [
    {
      "id": "gate-bridge-http-binding",
      "name": "Cooperative Mode HTTP Binding Endpoints",
      "priority": "P1",
      "status": "in-progress",
      "stories": [
        "SCP-BCH-001",
        "SCP-BCH-002",
        "SCP-BCH-003",
        "SCP-BCH-004",
        "SCP-BCH-005",
        "SCP-BCH-006",
        "SCP-BCH-007",
        "SCP-BCH-014",
        "SCP-BCH-015",
        "SCP-BCH-016"
      ]
    },
    {
      "id": "gate-bridge-credential-lifecycle",
      "name": "Bridge Credential Lifecycle",
      "priority": "P1",
      "status": "done",
      "stories": [
        "SCP-BCH-008",
        "SCP-BCH-009"
      ]
    },
    {
      "id": "gate-bridge-sender-key",
      "name": "Bridge Sender Key Encryption Model",
      "priority": "P1",
      "status": "done",
      "stories": [
        "SCP-BCH-010",
        "SCP-BCH-011",
        "SCP-BCH-012",
        "SCP-BCH-013"
      ]
    }
  ],
  "stories": [
    {
      "id": "SCP-BCH-001",
      "title": "Implement DID-signed bearer token authentication for bridge HTTP endpoints",
      "gate": "gate-bridge-http-binding",
      "priority": "P0",
      "severity": "critical",
      "status": "done",
      "files": [
        "crates/scp-node/src/bridge_auth.rs",
        "crates/scp-node/src/lib.rs"
      ],
      "description": "Implement the authentication layer specified in §12.10.2. The bridge operator authenticates using DID-signed JWTs in the Authorization header. The JWT contains iss (operator DID), aud (node URL), iat, exp, scp_bridge_id, and scp_context_id. The node verifies the JWT signature against the operator's DID document (§3.2). Token lifetime MUST NOT exceed 1 hour. For webhook callbacks (platform to bridge node), verify Ed25519 signatures in the X-SCP-Signature header against the platform's pre-registered public key.\n\nThis story is foundational — all other bridge HTTP endpoints depend on it for authentication.",
      "acceptanceCriteria": [
        "BridgeAuthMiddleware extracts bearer token from Authorization header and validates JWT structure",
        "JWT signature is verified against the operator's DID document resolved via scp-identity",
        "JWT exp field is checked and tokens older than 1 hour are rejected",
        "JWT scp_bridge_id and scp_context_id fields are validated against the registered bridge",
        "Webhook signature verification extracts X-SCP-Signature and X-SCP-Platform-Key-Id headers",
        "Webhook signature is verified as Ed25519 over the raw request body",
        "Invalid or missing authentication returns 401 with error code BRIDGE_NOT_AUTHORIZED",
        "Suspended bridges return 403 with error code BRIDGE_SUSPENDED",
        "Tests verify valid JWT acceptance, expired JWT rejection, and wrong-key JWT rejection",
        "Tests verify webhook signature acceptance and rejection"
      ],
      "actionItems": [
        "Define BridgeAuthMiddleware as an axum middleware layer",
        "Implement JWT parsing and DID document resolution for signature verification",
        "Implement webhook signature verification from X-SCP-Signature header",
        "Define BridgeAuthError enum with appropriate variants",
        "Add bridge_auth module declaration to lib.rs",
        "Add unit tests for all authentication paths"
      ],
      "blockedBy": [],
      "sources": [
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.10.2 Authentication"
        },
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.10.3 Error Format"
        },
        {
          "file": ".docs/adrs/phase-5.md",
          "section": "## ADR-023: Bridge Connector Protocol"
        }
      ],
      "details": {},
      "blockedByIssues": [
        377
      ]
    },
    {
      "id": "SCP-BCH-002",
      "title": "Implement POST /v1/scp/bridge/shadow endpoint for shadow identity creation",
      "gate": "gate-bridge-http-binding",
      "priority": "P1",
      "severity": "critical",
      "status": "done",
      "files": [
        "crates/scp-node/src/bridge_handlers.rs",
        "crates/scp-node/src/lib.rs"
      ],
      "description": "Implement the shadow creation endpoint specified in §12.10.4. Accepts platform_handle, platform_user_id, and optional metadata. Delegates to create_shadow() in scp-core/bridge/shadow.rs. Returns the created shadow identity with shadow_id, attributed_role (observer), and created_at. Idempotent: if a shadow with the same platform_user_id already exists, returns 200 with the existing shadow instead of 201.",
      "acceptanceCriteria": [
        "POST /v1/scp/bridge/shadow accepts JSON body with platform_handle (required), platform_user_id (required), metadata (optional)",
        "Delegates to create_shadow() in scp-core/bridge/shadow.rs",
        "Returns 201 Created with shadow_id, platform_handle, platform_user_id, attributed_role, created_at on success",
        "attributed_role defaults to 'observer' per §12.3",
        "Idempotent: existing shadow for same platform_user_id returns 200 OK with existing shadow data",
        "Returns 400 INVALID_REQUEST when required fields are missing",
        "Returns 401 BRIDGE_NOT_AUTHORIZED when authentication fails",
        "Shadow creation is recorded as a context event in the Merkle log",
        "Tests verify successful creation, idempotent re-creation, and missing field rejection"
      ],
      "actionItems": [
        "Define CreateShadowRequest and CreateShadowResponse structs",
        "Implement handler function delegating to create_shadow()",
        "Add idempotency check by platform_user_id lookup",
        "Wire handler into axum router at /v1/scp/bridge/shadow",
        "Add unit tests for all response paths"
      ],
      "blockedBy": [
        "SCP-BCH-001"
      ],
      "sources": [
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "#### POST /v1/scp/bridge/shadow"
        },
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "## 12.3 Shadow Identities"
        }
      ],
      "details": {}
    },
    {
      "id": "SCP-BCH-003",
      "title": "Implement POST /v1/scp/bridge/message endpoint for shadow message emission",
      "gate": "gate-bridge-http-binding",
      "priority": "P1",
      "severity": "critical",
      "status": "done",
      "files": [
        "crates/scp-node/src/bridge_handlers.rs"
      ],
      "description": "Implement the message emission endpoint specified in §12.10.4. Accepts shadow_id, content, content_type, optional platform_message_id, and optional platform_timestamp. The bridge node constructs the SCP envelope with BridgeProvenance (§12.5) via mark_bridge_provenance() from scp-core/bridge/provenance.rs and publishes to the context. Returns 202 Accepted with message_id, sequence, and bridge_provenance. Handles claimed shadows by reflecting the claimed provenance status.",
      "acceptanceCriteria": [
        "POST /v1/scp/bridge/message accepts JSON body with shadow_id (required), content (required), content_type (required), platform_message_id (optional), platform_timestamp (optional)",
        "Constructs BridgeProvenance via mark_bridge_provenance() with correct originating_platform, bridge_mode, operator_did, and shadow_status",
        "Returns 202 Accepted with message_id, sequence, and bridge_provenance object",
        "bridge_provenance in response includes originating_platform, bridge_mode, shadow_status, and operator_did",
        "Returns 404 SHADOW_NOT_FOUND when shadow_id does not exist",
        "Returns 400 INVALID_REQUEST when required fields are missing",
        "Claimed shadows produce provenance with shadow_status 'Claimed'",
        "platform_timestamp is preserved in provenance as platform-reported time",
        "Tests verify successful message emission, shadow-not-found rejection, and provenance construction"
      ],
      "actionItems": [
        "Define EmitMessageRequest and EmitMessageResponse structs",
        "Implement handler that looks up shadow, constructs BridgeProvenance, and publishes envelope",
        "Wire handler into axum router at /v1/scp/bridge/message",
        "Add unit tests for all response paths including claimed shadow handling"
      ],
      "blockedBy": [
        "SCP-BCH-001",
        "SCP-BCH-002"
      ],
      "sources": [
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "#### POST /v1/scp/bridge/message"
        },
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "## 12.5 Trust and Provenance for Bridged Content"
        }
      ],
      "details": {}
    },
    {
      "id": "SCP-BCH-004",
      "title": "Implement POST /v1/scp/bridge/attest endpoint for platform identity attestation",
      "gate": "gate-bridge-http-binding",
      "priority": "P1",
      "severity": "major",
      "status": "done",
      "files": [
        "crates/scp-node/src/bridge_handlers.rs"
      ],
      "description": "Implement the attestation endpoint specified in §12.10.4. The platform vouches for a user's identity by providing attestation evidence (evidence_type, verification_method, verified_at, platform_confidence, optional additional_signals). The bridge node produces an IdentityLink attestation (§3.5) signed by the bridge operator's DID. This attestation feeds into the shadow claiming flow — a user who later joins SCP can present it to claim their shadow. Attestation expiry defaults to 24 hours.",
      "acceptanceCriteria": [
        "POST /v1/scp/bridge/attest accepts JSON body with platform_handle (required), platform_user_id (required), attestation_evidence (required)",
        "attestation_evidence must contain evidence_type, verification_method, verified_at, platform_confidence",
        "Produces an IdentityLink attestation signed by the bridge operator's DID",
        "Returns 201 Created with attestation_id, status ('active'), platform_handle, issued_at, expires_at",
        "Attestation expiry defaults to 24 hours (86400 seconds) from issued_at",
        "Returns 400 INVALID_REQUEST when required attestation_evidence fields are missing",
        "Attestation is stored and retrievable for subsequent claim operations",
        "Tests verify attestation creation, signature validity, and missing field rejection"
      ],
      "actionItems": [
        "Define AttestRequest and AttestResponse structs",
        "Implement handler that creates IdentityLink attestation signed by operator DID",
        "Wire handler into axum router at /v1/scp/bridge/attest",
        "Add unit tests for attestation creation and validation"
      ],
      "blockedBy": [
        "SCP-BCH-001"
      ],
      "sources": [
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "#### POST /v1/scp/bridge/attest"
        },
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "## 12.3 Shadow Identities"
        }
      ],
      "details": {}
    },
    {
      "id": "SCP-BCH-005",
      "title": "Implement GET /v1/scp/bridge/status and DELETE /v1/scp/bridge/shadow/{shadow_id} endpoints",
      "gate": "gate-bridge-http-binding",
      "priority": "P1",
      "severity": "major",
      "status": "done",
      "files": [
        "crates/scp-node/src/bridge_handlers.rs"
      ],
      "description": "Implement the bridge status and shadow deletion endpoints specified in §12.10.4. The status endpoint returns bridge_id, status, platform, mode, operator_did, registered_at, shadow_count, rate_limits, and shadows array. The delete endpoint removes a shadow identity (historical actions remain in event log), returns 204 on success, 404 if not found, 409 if claimed. Deletion is idempotent (re-deleting returns 204). Both endpoints delegate to existing scp-core/bridge/ functions.",
      "acceptanceCriteria": [
        "GET /v1/scp/bridge/status returns 200 with bridge_id, status, platform, mode, operator_did, registered_at, shadow_count, rate_limits, shadows array",
        "shadows array includes shadow_id, platform_handle, attributed_role, provenance_status, created_at for each shadow",
        "DELETE /v1/scp/bridge/shadow/{shadow_id} returns 204 No Content on successful deletion",
        "DELETE returns 404 SHADOW_NOT_FOUND when shadow does not exist",
        "DELETE returns 409 SHADOW_ALREADY_CLAIMED when shadow has been claimed (bound to a DID)",
        "DELETE is idempotent: deleting an already-deleted shadow returns 204",
        "Shadow deletion is recorded as a context event in the Merkle log",
        "Historical actions attributed to deleted shadow remain in event log with original provenance",
        "Tests verify status response structure, shadow deletion, claimed shadow rejection, and idempotency"
      ],
      "actionItems": [
        "Define BridgeStatusResponse and DeleteShadowResponse structs",
        "Implement status handler delegating to list_bridges() and list_shadows()",
        "Implement delete handler with claimed-shadow check",
        "Wire handlers into axum router",
        "Add unit tests for all response paths"
      ],
      "blockedBy": [
        "SCP-BCH-001",
        "SCP-BCH-002"
      ],
      "sources": [
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "#### GET /v1/scp/bridge/status"
        },
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "#### DELETE /v1/scp/bridge/shadow/{shadow_id}"
        }
      ],
      "details": {}
    },
    {
      "id": "SCP-BCH-006",
      "title": "Implement POST /v1/scp/bridge/webhook receiver for platform-initiated events",
      "gate": "gate-bridge-http-binding",
      "priority": "P1",
      "severity": "critical",
      "status": "done",
      "files": [
        "crates/scp-node/src/bridge_handlers.rs",
        "crates/scp-node/src/bridge_webhook.rs",
        "crates/scp-node/src/lib.rs"
      ],
      "description": "Implement the webhook receiver specified in §12.10.4. Accepts platform-initiated events with event_type (message, presence, identity_update, user_departed, message_edit, message_delete), event_id (for deduplication), timestamp, and event-specific payload. Authentication via X-SCP-Signature header (§12.10.2). Deduplication by event_id. Must respond within 5 seconds; events requiring longer processing are queued internally. Returns accepted/rejected status with reason.",
      "acceptanceCriteria": [
        "POST /v1/scp/bridge/webhook accepts JSON body with event_type (required), event_id (required), timestamp (required), payload (required)",
        "Supports event types: message, presence, identity_update, user_departed, message_edit, message_delete",
        "Verifies X-SCP-Signature header via Ed25519 signature over raw request body",
        "Deduplicates events by event_id — repeated delivery of same event_id returns accepted: true without reprocessing",
        "Returns 200 with accepted: true and event_id on successful acceptance",
        "Returns 200 with accepted: false, event_id, and reason when event is rejected (e.g., unknown user)",
        "Response time is under 5 seconds; longer-running processing is queued internally",
        "message events create SCP envelopes with BridgeProvenance via mark_bridge_provenance()",
        "identity_update events update shadow metadata (platform_handle, display_name)",
        "user_departed events trigger shadow removal workflow",
        "Tests verify each event type processing, deduplication, and signature rejection"
      ],
      "actionItems": [
        "Define WebhookRequest, WebhookResponse, and WebhookPayload structs with event-type-specific variants",
        "Implement webhook handler with signature verification and event routing",
        "Implement event deduplication store keyed by event_id",
        "Implement internal queue for events requiring async processing",
        "Wire handler into axum router at /v1/scp/bridge/webhook",
        "Add unit tests for all event types and deduplication"
      ],
      "blockedBy": [
        "SCP-BCH-001",
        "SCP-BCH-002"
      ],
      "sources": [
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "#### POST /v1/scp/bridge/webhook"
        },
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.10.2 Authentication"
        }
      ],
      "details": {}
    },
    {
      "id": "SCP-BCH-007",
      "title": "Wire bridge HTTP endpoints into scp-node router and integration test end-to-end flow",
      "gate": "gate-bridge-http-binding",
      "priority": "P1",
      "severity": "major",
      "status": "done",
      "files": [
        "crates/scp-node/src/http.rs",
        "crates/scp-node/src/lib.rs",
        "crates/scp-node/tests/bridge_integration.rs"
      ],
      "description": "Wire all bridge HTTP handlers into the scp-node axum router under the /v1/scp/bridge/ prefix. Add integration tests that exercise the full lifecycle: register bridge, create shadow, emit message, attest identity, check status, receive webhook event, delete shadow. Verify provenance chain is correctly constructed end-to-end. Verify authentication is enforced on all endpoints. Verify error responses conform to §12.10.3.",
      "acceptanceCriteria": [
        "All bridge endpoints are mounted under /v1/scp/bridge/ in the scp-node axum router",
        "Bridge auth middleware is applied to all bridge endpoints",
        "Integration test exercises full lifecycle: register bridge -> create shadow -> emit message -> attest identity -> check status -> webhook event -> delete shadow",
        "Integration test verifies BridgeProvenance is attached to messages with correct originating_platform, bridge_mode, operator_did, and shadow_status",
        "Integration test verifies authentication rejection on all endpoints with invalid tokens",
        "Integration test verifies error responses use the SCP error format (code + message + details)",
        "Integration test verifies claimed shadows cannot be deleted (409 response)",
        "All endpoints return correct Content-Type: application/json header"
      ],
      "actionItems": [
        "Add bridge route group to scp-node axum router in http.rs",
        "Apply BridgeAuthMiddleware to the bridge route group",
        "Create bridge_integration.rs with end-to-end lifecycle test",
        "Add test helpers for constructing DID-signed JWTs",
        "Verify all error responses conform to §12.10.3 format"
      ],
      "blockedBy": [
        "SCP-BCH-002",
        "SCP-BCH-003",
        "SCP-BCH-004",
        "SCP-BCH-005",
        "SCP-BCH-006"
      ],
      "sources": [
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "## 12.10 Cooperative Mode HTTP Binding"
        },
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.10.6 Bridge Node Lifecycle"
        },
        {
          "file": ".docs/adrs/phase-5.md",
          "section": "## ADR-023: Bridge Connector Protocol"
        }
      ],
      "details": {}
    },
    {
      "id": "SCP-BCH-008",
      "title": "Implement BridgeCredentialStore trait with lifecycle methods",
      "gate": "gate-bridge-credential-lifecycle",
      "priority": "P1",
      "severity": "major",
      "status": "done",
      "files": [
        "crates/scp-core/src/bridge/credentials.rs",
        "crates/scp-core/src/bridge/mod.rs"
      ],
      "description": "Implement the BridgeCredentialStore trait specified in §12.11. The trait defines the credential lifecycle: provision (store new credentials), retrieve (get credentials for use), rotate (replace credentials), revoke (destroy all credentials for a bridge instance), and list (enumerate credential types for a bridge). Credentials are encrypted at rest using a key derived from the bridge operator's identity key material via HKDF with a bridge-specific salt. The trait is async and returns Result types. On BridgeStatus::Revoked, revoke() destroys all credentials (overwrite with zeros, delete record). On BridgeStatus::Suspended, credentials are retained but retrieve() returns an error indicating the bridge is suspended. Credential access is scoped to bridge instance — cross-bridge credential sharing is prohibited. Add a BridgeCredential struct holding encrypted_data, credential_type, created_at, expires_at, and bridge_id. Add a CredentialType enum with variants OAuthAccessToken, OAuthRefreshToken, ApiKey, WebhookSecret, and Custom(String).",
      "acceptanceCriteria": [
        "BridgeCredentialStore trait is defined with async methods: provision, retrieve, rotate, revoke, list",
        "BridgeCredential struct has fields: encrypted_data (Vec<u8>), credential_type (CredentialType), created_at (u64), expires_at (Option<u64>), bridge_id (String)",
        "CredentialType enum has variants: OAuthAccessToken, OAuthRefreshToken, ApiKey, WebhookSecret, Custom(String)",
        "provision() accepts bridge_id, credential_type, plaintext credential, and operator key material; stores encrypted credential",
        "retrieve() returns decrypted credential for a given bridge_id and credential_type; returns error if bridge is suspended",
        "rotate() replaces existing credential with new value for a given bridge_id and credential_type",
        "revoke() destroys all credentials for a bridge_id: overwrites encrypted_data with zeros before deletion",
        "list() returns all credential types stored for a given bridge_id",
        "Encryption uses HKDF-SHA256 with IKM=bridge_credential_key (per-bridge CSPRNG random), salt=SHA-256(\"SCP-BRIDGE-CREDENTIAL-V1\"), info=\"scp-bridge-credential:\" || bridge_id",
        "Cross-bridge credential access is rejected: retrieve with wrong bridge_id returns NotFound",
        "All types derive Serialize, Deserialize, Debug, Clone",
        "Unit tests verify provision-retrieve roundtrip, rotate replaces credential, revoke destroys all, suspended bridge blocks retrieve"
      ],
      "actionItems": [
        "Define CredentialType enum in bridge/credentials.rs",
        "Define BridgeCredential struct in bridge/credentials.rs",
        "Define BridgeCredentialStore async trait with 5 methods",
        "Define BridgeCredentialError enum with variants NotFound, BridgeSuspended, EncryptionError, StorageError",
        "Implement HKDF key derivation helper for credential encryption",
        "Add credentials module declaration to bridge/mod.rs",
        "Add unit tests for all trait contract requirements"
      ],
      "blockedBy": [],
      "sources": [
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "## 12.11 Bridge Credential Lifecycle"
        },
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.11.2 Requirements"
        },
        {
          "file": ".docs/adrs/phase-5.md",
          "section": "## ADR-023: Bridge Connector Protocol"
        }
      ],
      "details": {}
    },
    {
      "id": "SCP-BCH-009",
      "title": "Implement OAuth 2.0 reference binding for BridgeCredentialStore",
      "gate": "gate-bridge-credential-lifecycle",
      "priority": "P1",
      "severity": "major",
      "status": "done",
      "files": [
        "crates/scp-core/src/bridge/credentials.rs",
        "crates/scp-core/src/bridge/oauth.rs",
        "crates/scp-core/src/bridge/mod.rs"
      ],
      "description": "Implement the OAuth 2.0 reference binding specified in §12.11.3. This provides a concrete OAuthBridgeFlow struct that orchestrates the Authorization Code + PKCE flow and integrates with BridgeCredentialStore for token storage. The flow is: (1) generate PKCE code verifier and challenge, (2) build authorization URL, (3) exchange authorization code for access + refresh tokens, (4) store both tokens via BridgeCredentialStore::provision, (5) auto-refresh access token when 80% of lifetime has elapsed, (6) on refresh failure, retry with exponential backoff (initial 1s, max 60s, 5 retries). On bridge revocation, call the platform's RFC 7009 token revocation endpoint for both tokens before destroying local copies. Scope minimization: Relay mode requests read-only scopes, Puppet mode requests read+write. The implementation uses reqwest for HTTP calls and is async.",
      "acceptanceCriteria": [
        "OAuthBridgeFlow struct is defined with fields: client_id, redirect_uri, token_endpoint, authorization_endpoint, revocation_endpoint (optional), scopes",
        "generate_pkce() returns a (code_verifier, code_challenge) tuple using S256 method",
        "build_authorization_url() returns a URL with response_type=code, client_id, redirect_uri, scope, code_challenge, code_challenge_method=S256",
        "exchange_code(authorization_code, code_verifier) calls token_endpoint and returns access_token + refresh_token + expires_in",
        "Tokens are stored via BridgeCredentialStore::provision with CredentialType::OAuthAccessToken and CredentialType::OAuthRefreshToken",
        "refresh_token() uses the stored refresh token to obtain a new access token; stores the new token via rotate()",
        "refresh_token() is called automatically when 80% of the access token's lifetime has elapsed",
        "On refresh failure, retries with exponential backoff: initial 1s, max 60s, 5 retries",
        "revoke_tokens(bridge_id) calls the platform's revocation endpoint (RFC 7009) for both access and refresh tokens, then calls BridgeCredentialStore::revoke(bridge_id)",
        "Scope selection is mode-aware: Relay mode uses read-only scopes, Puppet mode uses read+write scopes",
        "Unit tests verify PKCE generation produces valid S256 challenge",
        "Unit tests verify authorization URL contains all required parameters",
        "Integration tests (with mock HTTP server) verify code exchange, token refresh, and revocation"
      ],
      "actionItems": [
        "Define OAuthBridgeFlow struct in bridge/oauth.rs",
        "Implement PKCE S256 generation (SHA-256 hash, base64url-encode)",
        "Implement build_authorization_url with all required OAuth parameters",
        "Implement exchange_code calling token endpoint via reqwest",
        "Implement token storage integration with BridgeCredentialStore",
        "Implement refresh_token with exponential backoff retry logic",
        "Implement revoke_tokens calling RFC 7009 endpoint",
        "Add oauth module declaration to bridge/mod.rs",
        "Add unit tests for PKCE and URL generation",
        "Add integration tests with mock HTTP server for token exchange and refresh"
      ],
      "blockedBy": [
        "SCP-BCH-008"
      ],
      "sources": [
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.11.3 OAuth 2.0 Reference Binding"
        },
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.11.1 Lifecycle Phases"
        },
        {
          "file": ".docs/adrs/phase-5.md",
          "section": "## ADR-023: Bridge Connector Protocol"
        }
      ],
      "details": {}
    },
    {
      "id": "SCP-BCH-010",
      "title": "Generate per-shadow sender key on shadow creation",
      "gate": "gate-bridge-sender-key",
      "priority": "P1",
      "severity": "critical",
      "status": "done",
      "files": [
        "crates/scp-core/src/bridge/shadow.rs",
        "crates/scp-core/src/crypto/sender_keys/mod.rs"
      ],
      "description": "When create_shadow() is called, generate an AES-256-GCM sender key for the new shadow identity and store it in SenderKeyStore keyed by (context_id, shadow_did). This is the foundational key material for the bridge encryption model specified in §12.6.1: shadow identity messages use the sender key layer (§9.16) rather than MLS encryption. Each shadow gets its own sender key so that key compromise of one shadow does not affect others. The generated key must be cryptographically random (32 bytes from the platform CSPRNG).",
      "acceptanceCriteria": [
        "create_shadow() generates a 32-byte AES-256-GCM sender key using the platform CSPRNG",
        "The generated sender key is stored in SenderKeyStore keyed by (context_id, shadow_did)",
        "Each shadow identity receives a unique sender key — no key reuse across shadows",
        "Sender key generation failure causes create_shadow() to return an error (no shadow created without a key)",
        "The sender key is zeroized on drop (Zeroizing wrapper or equivalent) (SenderKey already derives ZeroizeOnDrop — verify it applies to the stored key)",
        "Unit tests verify sender key is stored after shadow creation",
        "Unit tests verify distinct shadows in the same context receive distinct keys",
        "Unit tests verify shadow creation fails gracefully when SenderKeyStore is unavailable"
      ],
      "actionItems": [
        "Add sender key generation logic to create_shadow() in bridge/shadow.rs",
        "Generate 32 random bytes via the platform CSPRNG for the AES-256-GCM key",
        "Store the key in SenderKeyStore with (context_id, shadow_did) as the composite key",
        "Wrap key material in Zeroizing for safe memory handling",
        "Add error variant for sender key generation/storage failure",
        "Add unit tests for key generation, storage, uniqueness, and failure paths"
      ],
      "blockedBy": [],
      "sources": [
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.6.1 Bridge Encryption Model"
        }
      ],
      "details": {}
    },
    {
      "id": "SCP-BCH-011",
      "title": "Distribute shadow sender keys via pull-based protocol",
      "gate": "gate-bridge-sender-key",
      "priority": "P1",
      "severity": "critical",
      "status": "done",
      "files": [
        "crates/scp-core/src/crypto/sender_keys/key_protocol.rs",
        "crates/scp-core/src/bridge/mod.rs"
      ],
      "description": "Native members request shadow sender keys via SenderKeyRequest. The bridge responds with the shadow's sender key wrapped via HPKE. This reuses the existing pull-based sender key distribution protocol specified in §9.16.2: SenderKeyRequest from native member, SenderKeyResponse from bridge containing the HPKE-wrapped key. The bridge operator's identity key is the HPKE sender; the requesting member's identity key is the HPKE recipient. Domain separation label for HPKE must use \"scp-sender-key-v1\" per the existing protocol. New members joining a bridged context automatically request sender keys for all active shadows.",
      "acceptanceCriteria": [
        "Native members can send SenderKeyRequest for a shadow's sender key by (context_id, shadow_did)",
        "Bridge handles SenderKeyRequest by retrieving the shadow's sender key from SenderKeyStore and wrapping it via HPKE",
        "HPKE wrapping uses the bridge operator's identity key as sender and the requesting member's identity key as recipient",
        "HPKE domain separation label is \"scp-sender-key-v1\" per §9.16.2",
        "SenderKeyResponse contains the HPKE-wrapped sender key and is routed to the requesting member",
        "Members joining a bridged context request sender keys for all active shadows",
        "SenderKeyRequest for a non-existent shadow returns an error (not a silent failure)",
        "Unit tests verify SenderKeyRequest/Response roundtrip with HPKE wrapping and unwrapping",
        "Unit tests verify domain separation label is correctly applied"
      ],
      "actionItems": [
        "Extend key_protocol.rs to handle SenderKeyRequest for shadow DIDs routed to the bridge operator",
        "Implement bridge-side handler that retrieves the shadow's sender key and wraps via HPKE",
        "Ensure HPKE uses \"scp-sender-key-v1\" domain separation label",
        "Add logic for new-member-join to request all active shadow sender keys",
        "Add error handling for missing shadow or unavailable key",
        "Add unit tests for request/response roundtrip, domain separation, and error paths"
      ],
      "blockedBy": [
        "SCP-BCH-010"
      ],
      "sources": [
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.6.1 Bridge Encryption Model"
        },
        {
          "file": ".docs/specs/09-security-model.md",
          "section": "### 9.16.2 Key Distribution (Pull-Based)"
        }
      ],
      "details": {}
    },
    {
      "id": "SCP-BCH-012",
      "title": "Construct sender-key-encrypted envelopes for shadow messages",
      "gate": "gate-bridge-sender-key",
      "priority": "P1",
      "severity": "critical",
      "status": "done",
      "files": [
        "crates/scp-core/src/bridge/mod.rs",
        "crates/scp-core/src/envelope/inner.rs"
      ],
      "description": "POST /v1/scp/bridge/message constructs a sender-key-encrypted envelope (not MLS) for shadow messages. AES-256-GCM encryption with the shadow's sender key, random 12-byte nonce per message, BridgeProvenance attached per §12.5. The envelope is distinguishable from MLS envelopes by structure: sender-key-encrypted envelopes contain a sender key ciphertext with the shadow's DID in the sender field, while MLS-encrypted envelopes contain an MLS ciphertext payload (§12.6.1). The receiver uses the envelope structure to select the correct decryption path — MLS for native members, sender key for shadow identities. Both paths already exist in the protocol.",
      "acceptanceCriteria": [
        "POST /v1/scp/bridge/message constructs a sender-key-encrypted envelope using the shadow's AES-256-GCM sender key",
        "Each envelope uses a unique random 12-byte nonce generated from the platform CSPRNG",
        "BridgeProvenance is attached to the envelope per §12.5 via mark_bridge_provenance()",
        "The envelope's sender field contains the shadow's DID",
        "Sender-key-encrypted envelopes are structurally distinguishable from MLS-encrypted envelopes",
        "Receivers can select the correct decryption path (sender key vs MLS) based on envelope structure alone",
        "Nonce reuse is prevented — each message gets a fresh random nonce",
        "Encryption failure (e.g., missing sender key) returns an appropriate error to the bridge endpoint",
        "Unit tests verify envelope construction with correct AES-256-GCM ciphertext",
        "Unit tests verify envelope is decryptable with the shadow's sender key",
        "Unit tests verify BridgeProvenance is present on constructed envelopes",
        "Unit tests verify sender-key envelopes are distinguishable from MLS envelopes"
      ],
      "actionItems": [
        "Implement sender-key envelope construction in the bridge message handler",
        "Retrieve the shadow's sender key from SenderKeyStore by (context_id, shadow_did)",
        "Encrypt message content with AES-256-GCM using the sender key and a random 12-byte nonce",
        "Attach BridgeProvenance via mark_bridge_provenance()",
        "Set the envelope sender field to the shadow's DID",
        "Ensure envelope structure is distinguishable from MLS envelopes per §12.6.1",
        "Add unit tests for encryption, decryption roundtrip, provenance attachment, and envelope type discrimination"
      ],
      "blockedBy": [
        "SCP-BCH-010",
        "SCP-BCH-003"
      ],
      "sources": [
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.6.1 Bridge Encryption Model"
        }
      ],
      "details": {}
    },
    {
      "id": "SCP-BCH-013",
      "title": "Add bridge_operator_did to context metadata on bridge registration",
      "gate": "gate-bridge-sender-key",
      "priority": "P1",
      "severity": "critical",
      "status": "done",
      "files": [
        "crates/scp-core/src/bridge/registration.rs",
        "crates/scp-core/src/context/params.rs"
      ],
      "description": "When register_bridge() succeeds, add the operator's DID to context metadata (§5.7) so that context members can see bridge presence and evaluate trust accordingly. Per §12.6.1: \"Context metadata (§5.7) MUST include bridge_operator_did when a bridge is registered.\" This is a MUST-level requirement. The bridge_operator_did field is added to context metadata and is visible to all members, including in pre-join context previews. If multiple bridges are registered, all operator DIDs are listed. On bridge revocation, the operator's DID is removed from context metadata.",
      "acceptanceCriteria": [
        "register_bridge() adds bridge_operator_did to context metadata (§5.7) on successful registration",
        "bridge_operator_did is visible to all context members",
        "bridge_operator_did is visible in pre-join context previews",
        "Multiple bridge registrations result in all operator DIDs listed in context metadata",
        "Bridge revocation removes the operator's DID from context metadata",
        "Duplicate bridge registration by the same operator does not create duplicate entries in metadata",
        "Context metadata update is recorded as a context event",
        "Unit tests verify bridge_operator_did appears in context metadata after registration",
        "Unit tests verify bridge_operator_did is removed from context metadata after revocation",
        "Unit tests verify multiple operator DIDs are correctly listed for multiple bridges"
      ],
      "actionItems": [
        "Add bridge_operator_did field (or list) to context metadata struct in context/metadata.rs",
        "Update register_bridge() in bridge/registration.rs to write operator DID to context metadata",
        "Update revoke_bridge() to remove operator DID from context metadata",
        "Handle deduplication for same-operator re-registration",
        "Ensure context metadata change emits a context event",
        "Add unit tests for registration, revocation, multiple bridges, and deduplication"
      ],
      "blockedBy": [],
      "sources": [
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.6.1 Bridge Encryption Model"
        }
      ],
      "details": {}
    },
    {
      "id": "SCP-BCH-014",
      "title": "Decide bridge admission in scp-node from the context event log, not from the node-local bridge registry",
      "gate": "gate-bridge-http-binding",
      "priority": "P0",
      "severity": "critical",
      "status": "pending",
      "files": [
        "crates/scp-node/src/bridge_auth.rs",
        "crates/scp-node/src/bridge_handlers.rs",
        "crates/scp-node/src/http.rs",
        "crates/scp-node/tests/bridge_integration.rs"
      ],
      "description": "Spec §12.10.6 step 1 states that a bridge node MUST admit a bridge only on a governance approval that the node itself verifies, and names the paths the rule refuses: 'a file the node's operator writes, a request body, and a leaf that any party hands to the node, the node's own operator included'. The shipped node admits bridges from exactly the first of those paths. StorageBridgeLookup::register_bridge (crates/scp-node/src/bridge_auth.rs) writes a caller-supplied BridgeConnector under the bridge/registry/ storage prefix with no event-log check, find_bridge reads it back, and four sites branch on the status field of that stored record: bridge_auth_middleware and its type-erased twin bridge_auth_middleware_dyn in crates/scp-node/src/bridge_auth.rs, and status_handler in crates/scp-node/src/bridge_handlers.rs, which renders that field into the GET /v1/scp/bridge/status response. The router that a deployed node builds in crates/scp-node/src/http.rs mounts only the _dyn forms — bridge_auth_middleware_dyn on the JWT-authenticated bridge routes and webhook_auth_middleware_dyn on the webhook route — so a change that rewrites only the generic forms leaves every shipped route admitting bridges from the node-local record. Every acceptance criterion below that names a function names both forms, and the integration tests drive the router that http.rs builds rather than a middleware function directly. The webhook path consults no bridge at all: WebhookRequest (crates/scp-node/src/bridge_handlers.rs) carries event_type, event_id, timestamp, and payload and no bridge identifier, and webhook_handler answers accepted: true after an event-type whitelist and a dedup check, so a suspended or revoked bridge's deliveries still enter SCP. This story replaces the node's admission input with the criterion of §12.10.6 step 1 over the context event log the node holds as the operator-member, and resolves a webhook delivery to a bridge through the X-SCP-Platform-Key-Id identifier that §12.10.2 step 6 defines: the SHA-256 of the 64 ASCII bytes of the bridge's lowercase hex bridge_id followed by the 32 registered platform_key bytes. The identifier covers the bridge_id because one platform publishes one signing key and one node may run one bridge into each of two contexts against that platform, so a digest over the key bytes alone names two bridges and therefore two contexts. The shipped webhook path also answers the wrong shape: webhook_auth_middleware_dyn verifies the signature before webhook_handler runs and returns 401 BRIDGE_NOT_AUTHORIZED when find_webhook_key resolves no key, so an unauthenticated caller reads a node's key set off the response code; §12.10.6 step 1 requires the route to answer every failure of the criterion with the one rejected webhook response. Spec 12.10.6 step 1 also bounds two inputs that a first implementation gets wrong: the suspension deadline takes its lower bound from the node's own commit-execution instant rather than from the leaf timestamp alone, because spec 9.8.2(c) bounds an envelope created_at only in the future direction, and spec 12.2.2 makes that instant a value the node records durably once per leaf, because a node that re-derived it on every restart would push a bounded suspension past every deadline it ever computes; and the node answers every failure of the criterion with one webhook response, because it verifies no signature before it answers and an unauthenticated caller must not read a context's governance state off the response. Scope boundary: the four bridge lifecycle EventType variants and their BridgeRegistrationEvent payload already exist in scp-event-log and scp-protocol; appending those leaves from the governance execution path is SCP-BCH-015, not this story. Two inputs of the criterion are node-local rather than log-carried, and each one needs a durable record: the commit-execution instant that bounds a suspension deadline below (spec 12.2.2), and the log position that an import produced, which spec 12.10.6 step 1 makes the node record durably and read on every later evaluation, because a node holding that position in memory alone reads every imported leaf as one it executed after the next restart. One dependency this story does not own: spec 12.10.6 step 1 records that ADR-030 log pruning removes the BridgeRegistered payload that clause 1 reads, and that a node whose log pruned it refuses a bridge governance never suspended and never revoked. ADR-030 owns that question. This story asserts that refusal is the behaviour a node ships, so an implementer reads the consequence off a test rather than off a deployment.",
      "acceptanceCriteria": [
        "StorageBridgeLookup::register_bridge is removed, or its every caller is removed, so no scp-node code path writes a BridgeConnector to the bridge/registry/ storage prefix from a caller-supplied value",
        "bridge_auth_middleware and bridge_auth_middleware_dyn each decide admission by reading the bridge lifecycle leaves of the context event log the node holds as the operator-member, and neither reads BridgeConnector::status; a source search for \"bridge.status\" over crates/scp-node/src/ returns no match",
        "A bridge whose lifecycle leaves hold no BridgeRegistered leaf with action Approved is refused: the middleware returns the BRIDGE_FORBIDDEN error of spec 12.10.3",
        "A bridge whose bearer token verifies and whose lifecycle leaves fail the criterion on a clause other than the suspension clause is answered HTTP 403 with code BRIDGE_FORBIDDEN, and is answered neither HTTP 401 with code BRIDGE_NOT_AUTHORIZED nor code BRIDGE_SUSPENDED, asserted through the router that http.rs builds for a revoked bridge and for a bridge with no BridgeRegistered leaf (spec 12.10.6 step 1, which assigns the code for every failure of the criterion)",
        "A bridge any of whose lifecycle leaves is BridgeRevoked is refused with BRIDGE_FORBIDDEN, and a test appends BridgeRevoked followed by BridgeReactivated and asserts the bridge stays refused",
        "A bridge whose last lifecycle leaf is a BridgeSuspended leaf with duration None is refused with BRIDGE_SUSPENDED",
        "A bridge whose last lifecycle leaf is a BridgeSuspended leaf with duration Some(d) is refused with BRIDGE_SUSPENDED until the suspension deadline of spec 12.2.2 passes and admitted after it, where that deadline is the later of leaf.timestamp saturating_add(d) and the node's commit-execution instant for that leaf saturating_add(d), asserted by two calls with an injected clock",
        "A BridgeSuspended leaf whose timestamp the committing member backdated to more than d seconds before the node executed its commit is refused for d seconds measured from that execution instant, asserted by a test whose leaf timestamp is (now - 2d) and whose injected clock advances by d",
        "A BridgeSuspended leaf with duration Some(u64::MAX) refuses the bridge at every clock value the test advances to, and the admission function neither panics nor wraps, asserted on a debug build",
        "A bridge whose BridgeRegistered payload operator_did is not the DID under which the node holds its context membership is refused with BRIDGE_FORBIDDEN",
        "In a Broadcast context, a bridge whose lifecycle leaves carry two distinct authors is refused with BRIDGE_FORBIDDEN (spec 12.10.6 step 1, which leaf is the last one)",
        "WebhookRequest gains no bridge field; the webhook route resolves the bridge from the X-SCP-Platform-Key-Id header, which carries the lowercase hex SHA-256 of the 64 ASCII bytes of the bridge's lowercase hex bridge_id followed by the 32 registered platform_key bytes (spec 12.10.2 step 6), asserted by a test whose header is that digest and by a test whose header is the digest of the platform_key bytes alone, which resolves to no bridge",
        "the webhook route answers a delivery whose X-SCP-Platform-Key-Id matches the key identifier of no stored (bridge, key) pair with HTTP 200 and accepted: false, verifies no signature for it, and returns no 401 and no 403; the test drives the router that http.rs builds, so it covers webhook_auth_middleware_dyn and not the generic form alone",
        "the webhook route answers a delivery claiming a bridge whose last lifecycle leaf is an unexpired BridgeSuspended leaf with HTTP 200, accepted: false, and the fixed reason string \"Bridge not admitted\", and verifies no signature for it; a test sends four deliveries that carry one event_id — one claiming that suspended bridge, one claiming a revoked bridge, one claiming an unregistered bridge, and one whose X-SCP-Platform-Key-Id matches the key identifier of no stored pair — and asserts the four status codes are equal and the four response bodies are byte-identical (spec 12.10.6 step 1: one response for every failure of the criterion, carrying one pinned reason string), asserted through the router that http.rs builds",
        "the webhook route answers a delivery claiming a bridge with no BridgeRegistered leaf, and a delivery claiming a bridge any of whose lifecycle leaves is BridgeRevoked, with HTTP 200 and accepted: false, and verifies no signature for either",
        "An integration test in crates/scp-node/tests/bridge_integration.rs writes a BridgeConnector directly into the bridge/registry/ storage prefix, appends no bridge lifecycle leaf, and asserts every bridge endpoint refuses that bridge",
        "A context whose event log the node populated through import_context admits no bridge from a lifecycle leaf at or below the imported log position: a test imports a ContextExport carrying a BridgeRegistered leaf with action Approved and the node's own operator_did, and asserts every bridge endpoint refuses that bridge (spec 12.10.6 step 1, the log the criterion reads is a log the node executed)",
        "GET /v1/scp/bridge/status reports a status the 12.10.6 step 1 criterion decided over the context event log, and crates/scp-node/src/bridge_handlers.rs matches on no BridgeConnector::status field, asserted by a test that revokes a bridge by appending BridgeRevoked and asserts the endpoint stops reporting Active",
        "Every bridge route a deployed node serves enforces the criterion: a test builds the router through the same function crates/scp-node/src/http.rs calls, with the same _dyn middleware, and asserts that a bridge whose lifecycle leaves hold a BridgeRevoked leaf is refused on every JWT-authenticated bridge route and on the webhook route",
        "Two bridges in two contexts that registered the same 32 platform_key bytes carry two distinct X-SCP-Platform-Key-Id identifiers, and a delivery stamped with one of them resolves to that bridge's context only, asserted by a test that revokes the first bridge and asserts a delivery stamped with the second bridge's identifier still enters the second context while a delivery stamped with the first bridge's identifier is answered accepted: false",
        "A delivery whose X-SCP-Platform-Key-Id matches stored (bridge, key) pairs of two different bridges is answered accepted: false and verifies no signature, and a delivery whose identifier matches two pairs of one bridge, which is what a rotation to the already-registered key bytes produces, resolves to that bridge and is admitted (spec 12.10.2 step 6)",
        "The node records its commit-execution instant for each bridge lifecycle leaf durably, and a restart recomputes no deadline: a test appends a BridgeSuspended leaf with duration Some(d), drops and rebuilds the admission state from the same storage twice with the injected clock advanced by less than d on each rebuild, and asserts the bridge is admitted at the same clock value the pre-restart deadline named (spec 12.2.2)",
        "A node that finds no recorded commit-execution instant for a BridgeSuspended leaf substitutes its first-read instant once and records it: a test builds the admission state over storage that carries the leaf and no recorded instant, rebuilds it three times, and asserts the deadline the third rebuild computes equals the deadline the first one computed (spec 12.2.2)",
        "A bridge whose lifecycle leaves sit at or below an imported log position is refused even though the node stores that bridge's platform key and delegated credentials, and a later approved RegisterBridge proposal for the same operator, platform and context admits the bridge that approval's own bridge_id names while the imported bridge stays refused and no endpoint serves it (spec 12.10.6 step 1, the log the criterion reads is a log the node executed)",
        "The node records the log position that an import produced durably, and a restart refuses the same imported leaves: a test imports a ContextExport carrying a BridgeRegistered leaf with action Approved and the node's own operator_did, drops and rebuilds the admission state from the same storage twice, and asserts every bridge endpoint refuses that bridge after each rebuild (spec 12.10.6 step 1, the log the criterion reads is a log the node executed)",
        "A bridge whose BridgeRegistered leaf the log retains with its payload pruned is refused with BRIDGE_FORBIDDEN and its webhook deliveries are answered accepted: false: a test appends a BridgeRegistered leaf with action Approved, admits the bridge, prunes the payload through the ADR-030 pruning path, and asserts the bridge is refused on every bridge route (spec 12.10.6 step 1, an unresolved dependency: retention); the test fails if the node admits a bridge whose registration payload its log no longer holds"
      ],
      "actionItems": [
        "Give BridgeState a handle to the context event log the node holds as the operator-member, injected through the constructor (no singleton)",
        "Write the 12.10.6 step 1 criterion as one function over the bridge lifecycle leaves of one context log, returning a typed admission verdict (Admitted / Suspended / Forbidden) rather than a bool",
        "Call that function from bridge_auth_middleware, from bridge_auth_middleware_dyn, and from status_handler, in place of every BridgeConnector::status branch, and verify by searching crates/scp-node/src/ for \"bridge.status\" that no branch remains",
        "Delete StorageBridgeLookup::register_bridge and the BRIDGE_REGISTRY_PREFIX write path, and delete or re-point every caller",
        "Resolve a webhook delivery to a bridge by matching the X-SCP-Platform-Key-Id header against the SHA-256 of each stored bridge_id-plus-platform_key pair, then apply the same criterion before verifying the signature; move the resolution and the criterion ahead of verify_webhook_signature in webhook_auth_middleware and webhook_auth_middleware_dyn so the route answers accepted: false rather than 401 when resolution or the criterion fails",
        "Extend crates/scp-node/tests/bridge_integration.rs with one test per acceptance criterion above",
        "Record the node's commit-execution instant for each bridge lifecycle leaf in durable storage when the node executes the commit, read the recorded value on every deadline evaluation, and write the first-read substitute once when no recorded value exists (spec 12.2.2)",
        "Write the fixed reason string \"Bridge not admitted\" into every rejected webhook response that a failure of the 12.10.6 step 1 criterion or an unresolved X-SCP-Platform-Key-Id produces, and write no per-failure text into that field, so the free-form reason carries none of the disclosure the single status code removes (spec 12.10.4, POST /v1/scp/bridge/webhook)",
        "Drive every new integration test through the router that crates/scp-node/src/http.rs builds, so the tests cover the _dyn middleware the deployed node mounts",
        "Record the log position that import_context produced for a context in durable storage, and read the recorded value on every evaluation of the criterion, so a restart refuses the same imported leaves the pre-restart node refused"
      ],
      "blockedBy": [],
      "sources": [
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.10.6 Bridge Node Lifecycle"
        },
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.10.2 Authentication"
        },
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.2.1 Bridge Registration Protocol"
        },
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.2.2 Bridge Removal Protocol"
        },
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.10.4 Endpoints"
        },
        {
          "file": ".docs/adrs/phase-5.md",
          "section": "## ADR-023: Bridge Connector Protocol"
        }
      ],
      "details": {}
    },
    {
      "id": "SCP-BCH-015",
      "title": "Define the four bridge governance actions and append their lifecycle leaves from the governance execution path",
      "gate": "gate-bridge-http-binding",
      "priority": "P0",
      "severity": "critical",
      "status": "pending",
      "files": [
        "crates/scp-protocol/src/bridge/registration.rs",
        "crates/scp-protocol/src/context/governance/mod.rs",
        "crates/scp-runtime/src/context/governance_helpers.rs",
        "crates/scp-runtime/src/context/actor/handlers/governance.rs",
        "crates/scp-runtime/tests/governance_integration.rs"
      ],
      "description": "Spec §12.2.1 step 3 and §12.2.2 make every approved RegisterBridge, SuspendBridge, ReactivateBridge, and RevokeBridge action append a bridge lifecycle leaf carrying a BridgeRegistrationEvent payload, and §12.10.6 step 1 makes those leaves the only admission input a bridge node reads. GovernanceAction (crates/scp-protocol/src/context/governance/mod.rs) declares none of those four actions, and spec §12.10.2 step 5 names a fifth, UpdateBridgePlatformKey, that the enum also lacks, so no proposal in the shipped system carries any bridge action and no execution path can append any bridge leaf. The EventType variants (BridgeRegistered, BridgeSuspended, BridgeReactivated, BridgeRevoked) and the BridgeRegistrationAction variants they carry exist in scp-event-log and scp-protocol, and crates/scp-protocol/src/bridge/registration.rs carries one constructor per decision action for the two older ones (approve_registration produces Approved, revoke_bridge produces Revoked) and none for the Suspended and Reactivated variants, so those two variants are variants no code can construct. This story declares the five governance actions, adds the two missing registry constructors beside their twins, and wires the governance execution path to append the four leaves, so a node implementing SCP-BCH-014 has leaves to read. An elapsed SuspendBridge duration appends NO leaf (§12.2.2), because that expiry records no fact: the bridge operator's own node is the only node that evaluates admission, and it computes the deadline from the BridgeSuspended leaf it already holds. §7.3.1 does admit leaves that a member-local timer triggers (TTL expiry and close, governance-freeze expiry, deferred economic-policy application) and keeps each convergent by stamping it with the pre-computed deadline that convergent context state already holds, so this story forbids the BridgeReactivated append on a deadline for the first reason and not for the second.",
      "acceptanceCriteria": [
        "GovernanceAction declares RegisterBridge, SuspendBridge, ReactivateBridge, RevokeBridge, and UpdateBridgePlatformKey with the payload fields spec 12.2.1, 12.2.2 and 12.10.2 step 5 give each one, and every wildcard-free match over GovernanceAction in the workspace compiles with an arm for each",
        "crates/scp-protocol/src/bridge/registration.rs declares suspend_bridge and reactivate_bridge beside approve_registration and revoke_bridge, each returning a BridgeRegistrationEvent whose action is BridgeRegistrationAction::Suspended { reason, duration } and BridgeRegistrationAction::Reactivated, and each moving BridgeConnector::status to Suspended and back to Active",
        "A unit test calls suspend_bridge and reactivate_bridge on a registry and asserts the returned events and the resulting BridgeStatus, so neither Suspended nor Reactivated remains a variant no code constructs",
        "An approved RegisterBridge action appends one BridgeRegistered leaf whose payload is a BridgeRegistrationEvent with action Approved, asserted by reading the context event log after the approval",
        "An approved SuspendBridge action appends one BridgeSuspended leaf whose payload action is Suspended with the proposal's reason and duration",
        "An approved ReactivateBridge action appends one BridgeReactivated leaf whose payload action is Reactivated",
        "An approved RevokeBridge action appends one BridgeRevoked leaf whose payload action is Revoked",
        "A rejected RegisterBridge proposal appends no bridge lifecycle leaf; the test asserts the log gains only GovernanceProposalResolved",
        "A test advances a clock past the deadline of a BridgeSuspended leaf with duration Some(d) and asserts the context event log gained no BridgeReactivated leaf and no event at all (spec 12.2.2)",
        "The appended leaf payload carries bridge_id, operator_did, governance_did, context_id, and timestamp, each asserted against the approving proposal",
        "An approved RegisterBridge action assigns the bridge_id spec 12.2.1 step 3 defines, hashing the leaf's own timestamp and no member's local clock reading, asserted by a test in which two members execute one approval and derive one identifier",
        "Two members of the same context hold byte-identical leaves for the same approval, asserted by comparing Merkle roots after both apply the commit",
        "An approved SuspendBridge action appends a BridgeSuspended leaf whose timestamp is the committer-assigned created_at of the commit envelope (spec 7.3.1) and not the appending member's local now(), asserted by comparing the leaf timestamp against the envelope created_at"
      ],
      "actionItems": [
        "Declare RegisterBridge, SuspendBridge, ReactivateBridge, RevokeBridge and UpdateBridgePlatformKey on GovernanceAction, and add an arm for each to every match over that enum",
        "Add suspend_bridge and reactivate_bridge to crates/scp-protocol/src/bridge/registration.rs beside approve_registration and revoke_bridge, following the same signature shape and the same BridgeStatus transitions",
        "Add a bridge-lifecycle append to the governance action execution path for the four approved bridge actions",
        "Derive the assigned bridge_id from the approval leaf's own timestamp (spec 12.2.1 step 3), so every member executing one approval derives one identifier",
        "Serialize BridgeRegistrationEvent as MessagePack into EventPayload::data per spec 12.12.2",
        "Assert in a test that no code path appends a BridgeReactivated leaf on a suspension deadline",
        "Add a pipeline_wiring.rs assertion for the new append step"
      ],
      "blockedBy": [],
      "sources": [
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.2.1 Bridge Registration Protocol"
        },
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.2.2 Bridge Removal Protocol"
        },
        {
          "file": ".docs/adrs/phase-2.md",
          "section": "## ADR-011: Verifiable Event Log (Merkle Tree)"
        },
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.10.2 Authentication"
        }
      ],
      "details": {}
    },
    {
      "id": "SCP-BCH-016",
      "title": "Derive bridge_id from the approval leaf's timestamp on every SDK path, and remove the local-clock helper",
      "gate": "gate-bridge-http-binding",
      "priority": "P0",
      "severity": "critical",
      "status": "pending",
      "files": [
        "crates/scp-ffi/common/src/bridge_id.rs",
        "crates/scp-ffi/src/bridge_connector.rs",
        "crates/scp-ffi/napi/src/bridge_connector.rs",
        "crates/scp-ffi/uniffi/src/bridge.rs"
      ],
      "description": "Spec §12.2.1 step 3 makes the timestamp component of a bridge_id the approval leaf's own timestamp and no member's local clock reading, so every member executing one approval derives one identifier and the bridge lifecycle leaves that spec §12.10.6 step 1 reads all name one bridge. scp_ffi_common::generate_bridge_id (crates/scp-ffi/common/src/bridge_id.rs) hashes scp_clock::SystemClock.now_secs() instead, and its own doc comment calls that non-determinism intentional and cites the same spec section as its authority. Three registration paths call it: crates/scp-ffi/src/bridge_connector.rs, crates/scp-ffi/napi/src/bridge_connector.rs, and crates/scp-ffi/uniffi/src/bridge.rs. Two members executing one approval through any of those three paths therefore derive two 64-character identifiers for one bridge, and a node applying the §12.10.6 step 1 criterion finds that bridge's lifecycle leaves filed under an identifier no other member agrees on. The same three paths then call approve_registration with a caller-supplied approver and a hardcoded timestamp of 0 and discard the returned event, so an SDK caller reads status active for a registration no governance model decided and no leaf records. This story removes the local-clock derivation, routes every SDK registration and approval through the governance execution path that SCP-BCH-015 builds, and derives the identifier from the leaf that path appends.",
      "acceptanceCriteria": [
        "crates/scp-ffi/common/src/bridge_id.rs reads no clock: generate_bridge_id either takes the approval leaf's timestamp as a parameter or is deleted, and a source search for \"now_secs\" over crates/scp-ffi/common/src/ returns no match",
        "The bridge_id a registration assigns is the lowercase hex SHA-256 of context_id || operator_did || platform || timestamp where timestamp is the approval leaf's own timestamp, asserted by a test that derives the identifier from one leaf on two members' state and compares the two strings",
        "A test calls the identifier derivation twice with one leaf timestamp and asserts the two results are equal, and the doc comment claiming non-determinism by design is deleted rather than reworded",
        "No FFI registration path calls approve_registration with a caller-supplied approver and a hardcoded timestamp: each of crates/scp-ffi/src/bridge_connector.rs, crates/scp-ffi/napi/src/bridge_connector.rs and crates/scp-ffi/uniffi/src/bridge.rs submits a RegisterBridge governance proposal and returns the identifier the executed approval assigned, or returns a typed error naming the governance decision it lacks",
        "A test on each of the three bridges asserts that a registration call for a context whose governance approved nothing returns no active bridge and assigns no identifier",
        "A cross-bridge test asserts the three bridges derive one identifier from one (context_id, operator_did, platform, leaf timestamp) tuple, so the PyO3, napi and UniFFI paths agree byte for byte"
      ],
      "actionItems": [
        "Take the approval leaf timestamp as a parameter of the identifier derivation, or delete generate_bridge_id and derive the identifier where the leaf is appended",
        "Re-point crates/scp-ffi/src/bridge_connector.rs, crates/scp-ffi/napi/src/bridge_connector.rs and crates/scp-ffi/uniffi/src/bridge.rs at the governance execution path, and delete the approve_registration call with the hardcoded timestamp from each",
        "Delete the non-determinism-by-design doc comment rather than rewording it, because spec 12.2.1 step 3 requires the opposite property",
        "Add a cross-bridge parity test that derives one identifier from one tuple on all three bridges"
      ],
      "blockedBy": [
        "SCP-BCH-015"
      ],
      "sources": [
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.2.1 Bridge Registration Protocol"
        },
        {
          "file": ".docs/specs/12-platform-bridge-connectors.md",
          "section": "### 12.10.6 Bridge Node Lifecycle"
        }
      ],
      "details": {}
    }
  ]
}
