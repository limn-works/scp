package works.limn.scp

import org.junit.jupiter.api.Test
import kotlin.test.assertTrue

class SmokeTest {
    @Test
    fun `kotlin test infrastructure is operational`() {
        assertTrue(true, "Kotlin test harness is working")
    }
}
