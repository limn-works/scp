{
  "name": "@limn-works/scp-ts-wasm",
  "version": "0.1.0",
  "description": "In-browser Shared Context Protocol participant SDK — the full MLS protocol runs in-tab over scp-client-wasm, keys on-device (ADR-057). The wasm-mechanism tier of @limn-works/scp-ts: a capability subset (no governance/economy/saga/media/DHT/broadcast hosting — the scp-runtime scope fence). Self-contained: bundles its own shared core.",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "sideEffects": false,
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build:wasm": "bun scripts/build-wasm.ts",
    "build:wasm:test": "bun scripts/build-wasm.ts --test",
    "build": "bun run build:wasm && tsup",
    "check": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "lint": "biome check src/ tests/ scripts/ examples/",
    "format": "biome format --write src/ tests/ scripts/ examples/",
    "guard:release-only": "bun scripts/check-release-only.ts",
    "guard:node-free": "bun scripts/check-node-free.ts",
    "test": "bun run build:wasm:test && bun test"
  },
  "engines": {
    "node": ">=22",
    "bun": ">=1.0"
  },
  "dependencies": {},
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@limn-works/scp-ts": "workspace:*",
    "@types/bun": "^1.3.10",
    "@types/node": "^25.3.2",
    "fake-indexeddb": "^6.0.0",
    "tsup": "latest",
    "typescript": "^5.7.0"
  }
}
