{
  "name": "@limn-works/scp-ts-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "Bun workspace root for the SCP TypeScript SDK family (ADR-057 Slice 3). Not published — it only links the two published sibling packages so `@limn-works/scp-ts-wasm` can take a workspace-source dependency on `@limn-works/scp-ts` for the single-sourced shared core, which is bundled into each published package at build (ADR-057 Amendment 2026-07-15, D1).",
  "workspaces": [
    "typescript",
    "typescript-wasm"
  ]
}
