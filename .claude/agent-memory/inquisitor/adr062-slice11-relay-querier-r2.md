---
name: adr062-slice11-relay-querier-r2
description: ADR-062 Slice 11 (SCP-CAPINJECT-011) relay READ+WRITE round-2 — write half ships production-INERT; story resilience premise + action-item-5 unreconciled
metadata:
  type: project
---

# ADR-062 Slice 11 / SCP-CAPINJECT-011 — relay querier READ+WRITE, round-2 (@04c666220, branch feat/adr062-slice11-relay-querier)

Round-1 charges RESOLVED: phantom "11b" write-half deferral gone (write half — TransportRelayPublisher + InMemory default severance on `RepublishManager<D,R>` + republish-loop SCPR-frame fix + `scpr::encode/decode_did_record` — now IN this story, built); Model-A blocker gone (spec §9.10.12 + `publish_raw`/`query_raw` raw-blob path distinct from OuterEnvelope, transport impls present).

**TOP round-2 finding (premise/coherence; artifact-vs-code divergence, root=ADR-062 Decision 6):** the WRITE half ships **production-INERT** while the READ half is production-live — the exact "empty half that FALSELY claims restored §3.10.8 resilience" the story's own description forbids.
- READ live: `ensure_did_resolver_initialized_on` (scp-ffi/src/identity.rs:130, UN-gated) builds `RealMultiRelayQuerier<TransportRelayQuerier>` into the production resolver at all 3 bridges (pyo3:150, napi:202, uniffi:9342).
- WRITE inert: the only production-code publisher entry `publish_did_record_to_relay` (scp-ffi/common/src/resolvers.rs:47) is called ONLY from `#[cfg(feature="testing")]` fns `publish_to_resolver_dht_for`/`publish_to_shared_dht_for` (pyo3:218, napi, uniffi:~470) — each doc'd "Only reached from testing-gated create paths (production create fails closed)". NO production `RepublishManager` anywhere: `with_relay_publisher`/`start_republishing`/`relay_republish_loop` = zero non-test hits; scp-node zero. So in a shipped (non-testing) artifact, relays receive ZERO DID frames; story action-item #5 ("wire a production RepublishManager at node/runtime construction") NOT done; §3.10.5 step-4a 6-day relay republish cycle unwired (even a published record dies at 7-day TTL, no renewal).
- Mitigation/root: FORCED by Decision 6 (production identity CREATE fails closed → no production DID to publish/republish yet). DHT write is SYMMETRICALLY testing-gated (same `publish_to_resolver_dht_for`, seeds in-memory resolver client seq=1; no production DHT RepublishManager either). So relay write = same production reachability as DHT write.
- Fix per one-way flow: amend story/ADR — write half lands as built-and-tested-but-production-inert (symmetric to DHT), production activation (RepublishManager relay loop + create ceremony) blocked by Decision 6 and lands with that slice. Leaving action-item #5 + "both halves LIVE restore resilience" unamended = phantom provenance (test-based ACs pass while central premise is test-only). AC11 `disabled_node_resolves_self_and_peer_via_relay` hand-wires `TransportRelayPublisher` directly — proves capability, not production wiring.

Sub-questions (a)-(e) all SOUND: (a) `RelayQuerier::query→Vec` all-candidates + verify-in-composer = security-correct (defeats intra-relay shadow-suppression, 38d0e2f7; decode=framing-no-authority in transport, BEP44-verify in composer; resolver re-verifies as DiD, justified by uniform relay+DHT path + trait doesn't promise verified). (b) `RealMultiRelayQuerier<Q>` generic = the mechanism keeping scp-identity off scp-transport (circular-dep avoidance, §3.10.12), not accidental. (c) deleting `relay_resolve`/`RelayResolveResult` (main: zero prod callers, test-only standalone) + centralizing on `verify_relay_record` SOUND — resolver owns seq-freshness/rollback (Step5 cached_seq) + cache (Step7); verify_relay_record=BEP44+JSON+self-cert used for BOTH relay+DHT per §3.10.5 byte-identical mandate (minor: name applies to DHT too). (e) 4 near-duplicate triples (scpr::DidRecord wire / RelayQueryRecord querier-trait DTO / RelayRecord composer+relay_url provenance for healing / scp_dht::DhtRecord) = mild drift smell but each seam justified. SCPR decoder spec-conformant (version→kind→bound-check-widened-u64→exact-length).
