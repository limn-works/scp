---
name: outlet-adr061-unary-reversal-frontier-delete
description: ADR-061 unary-not-a-stream reversal + tautological Frontier deletion + §5.4.5 spec reconciliation on feat/outlet-xctx-streaming-saga (43bf88b5f, db89f9f24)
metadata:
  type: project
---

Interrogation of 2 commits on feat/outlet-xctx-streaming-saga (HEAD 0b665ae4f → 43bf88b5f spec-reconcile, db89f9f24 Frontier-delete). Verdict: all 4 decisions SOUND; one MED provenance finding.

**Why:** mid-session a Plan agent REVERSED an earlier conclusion that the classic unary invoke_outlet path violated 035-AC5 by emitting stream_chunk_count:0 and needed a 2-chunk manifest.

**How to apply:**
- Q1 (reject unary-2-chunk-manifest) SOUND + ADR-mandated. ADR-061 Supersedes clause KILLS "every call is a stream"; unary=`output_hash`, streaming=`stream_manifest_hash`, unary explicitly NOT a degenerate 1-chunk stream. §5.4.5:347 preamble agrees. Unary event (invoke.rs:733) correctly leaves stream fields at sentinels (stream_chunk_count:0, chunks_billed:0). 035-AC5 is about a "non-progressive executor run through the STREAMING runtime" (test invoke.rs:5853), NOT the unary verb — earlier conclusion mis-read the AC surface. Setting chunks_billed=1 on unary would FABRICATE a billing record (unary settles via `cost` receipt) = double-count. 034/035 done-flip defensible: those ACs are all streaming-surface; 033/034/035 = same-context slice (f96079706).
- Q4 (delete Frontier arm) SOUND. Deleted check compared event fields vs a StreamManifestCommitment built from the SAME dispatch summary → both operands from one source → equality structural/unfireable (simplifier BLOCKER correct). NO adversary catches anything it missed. Retained checks are STRICTLY STRONGER: inline AuditAnomaly::ChunksBilledSelfMismatch (dispatch.rs:3288) compares escrow-ledger `summary.billed_count` vs Merkle-frontier `summary.manifest_billed` = two INDEPENDENT accumulators (genuine, non-tautological); + event-local `<=` backstop at append (event_log.rs:715 verify_outlet_invoked_event_local, any-source). Residual: a future build_streaming_outlet_event field-mapping bug is better guarded by single-source construction than a runtime tautology — no real guarantee lost.
- Q3 (spec rewrite 43bf88b5f) SOUND artifact-flow. Old §5.4.5:610 "non-streaming invocation is a stream that emits two chunks" was residue of the SUPERSEDED framing, already CONTRADICTED §5.4.5:347. ADR-061→spec is flow-DOWN (permitted); code merely already matched. Reworded text faithful (distinct modes, shared OutletInvokedEvent shape). 035-AC5 PRD tweak also flow-down.
- Q2 (keep Sequence primitive verify_chunks_billed/verify_outlet_invoked_event_manifest/append_outlet_invoked_verified with ONLY #[cfg(test)] callers) LEGITIMATE not YAGNI — BUT MED provenance finding. Prod same-context caller correctly REMOVED (tautological there: same-context event's chunks_billed is BY CONSTRUCTION the manifest-derived value → AC22 mismatch condition structurally unreachable same-context; `<=` backstop covers any-source insert). Full re-derivation is non-vacuous ONLY where event+chunks have INDEPENDENT provenance = the xctx recording boundary. THE FINDING: the "keep it" provenance cites `slice3-plan.md W4` which is a SCRATCHPAD-ONLY ephemeral file (NOT git-tracked; "artifacts are system of record — if an agent can't find it, it doesn't exist"). The findable durable consumer is SCP-OUT-036 (pending, labeled W3 not W4) AC7 "each context's event log records its own OutletInvoked with identical stream_manifest_hash" — architecturally the right home, but NO pending AC mechanically names append_outlet_invoked_verified/verify_outlet_invoked_event_manifest as the mechanism. So the primitive is tested-on-architectural-faith with no mechanical guard against slice-3 wiring the recording differently → rot risk. Honest fix: pin a durable AC in SCP-OUT-036/045/046 committing the receiving context to record via append_outlet_invoked_verified. Also "034 AC21/22 are test criteria" is imprecise: positional AC23 IS a test, but positional AC22 ("wire-rejected at log-insert") is a PRODUCTION guarantee (met same-context by backstop+construction).
