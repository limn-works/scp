---
name: pr2218-kea-epoch-advance-counter
description: PR#2218 (#1847) rotate_all_author_keys returns epoch advances + KEA leaves + GovernanceDeadlockRecovery companion — checkpoint-counter drift BLOCKER + best-effort-vs-mandatory KEA spec tension
metadata:
  type: project
---

PR #2218 `fix/rotate-all-author-keys-epoch-advance` (#1847) @84be6115b.

**BLOCKER (self-incoherent):** `execute_reconfigure_governance` (governance_helpers.rs ~3341-3383) appends TWO durable `?`-propagated leaves (`GovernanceReconfigured` + new `GovernanceDeadlockRecovery`) but bumps `*checkpoint_events_since_mut() += 1` (line ~3384). The PR's OWN sibling KEA fix (execute_revoke line 1031, execute_rotate_content_keys) uses `+= 1 + kea_success_count` and documents the invariant "counter must track true durable-leaf count to prevent §9.9.3 checkpoint-position drift" (governance_logic.rs:156-158). Same PR obeys the invariant for KEA, violates it for the deadlock leaf it adds. Fix = `+= 2`.

**Why:** `checkpoint_events_since` gates §9.9.3 checkpoint positioning; undercount drifts checkpoints.
**How to apply:** any PR adding an event-log append must bump the counter to match; grep every `append_context_event*` against its nearest `checkpoint_events_since_mut() +=`.

**MEDIUM — best-effort KEA vs spec "mandatory":** spec §5.14 (05-contexts.md:2012/2019) "All authors MUST rotate keys after a governance ban (mandatory KeyEpochAdvance per author)." Code makes the crypto ROTATION fail-closed durable (rides Class-S ContextSnapshot) but the event-log KEA LEAF best-effort (warn-continue) in ALL four broadcast paths. Comment "same pattern as MemberBlocked" is a TRUE status-quo appeal — broadcast_helpers.rs:723 (block) + :258 (unsubscribe) already do best-effort KEA while the MemberBlocked/AccessRevoked leaf itself is fail-closed `?`. So PR is COHERENT with existing code, but neither this PR nor the precedent amended the spec to say "mandatory" binds the rotation, not the leaf. Structural reason leaf can't be atomic w/ rotation: async append can't ride sync Class-S closure (ADR-049 Decision 7) → best-effort-after-durable is defensible, but per artifact-flow the spec should be clarified. Root = pre-existing broadcast_helpers best-effort KEA, not this PR.

**MEDIUM — non-atomic companion leaf (Q3):** GovernanceReconfigured + GovernanceDeadlockRecovery = two separate awaits; crash between = reconfig-with-no-justification (the exact thing the companion prevents). Root = justification modeled as SEPARATE leaf, not a field of GovernanceReconfigured's payload. Function is exclusively deadlock-resolution (its own INVARIANT doc) → folding justification into the reconfig leaf payload = atomic by construction, eliminates window.

**LOW-MED — old_epoch = new_epoch.saturating_sub(1) (Q4) + dead timestamp (Q2):** `BroadcastKeyEpochAdvance{author_did,new_epoch,timestamp}` shaped for the relay-notification consumer (per-author block path uses timestamp), pressed into event-log-leaf service where timestamp is dead AND old_epoch is missing (reconstructed via -1). Producer (`rotate_all_author_keys`: `author.epoch += 1`) HAS the pre-increment old_epoch, discards it, forces caller to re-derive under a smuggled "+1 always" premise. Sound today (all broadcast advances are +1) but data model carries what this path doesn't need + omits what it does. Fix = carry old_epoch on the advance struct.

**PR#2234 PASS-3 FINAL @2fd43bee9 (origin/fix/rotate-content-keys-review-followup, 7 commits, 603 ins/10 files) — VERDICT: SOUND, no blocker.** TRAP RE-CONFIRMED: local working tree ≠ pushed branch (working tree still has BEST-EFFORT execute_revoke + phantom `TODO(spec) §2033`; pushed branch is FAIL-CLOSED, no §2033). ALWAYS `git show origin/branch:file`, never `sed` the checkout. DECISION SHIFT since @39a19e90c: execute_revoke + execute_rotate_content_keys FLIPPED best-effort→FAIL-CLOSED (map_err→`?` per leaf) + per-leaf inline `checkpoint_events_since += 1` after each durable `?` append. JUSTIFIED: (1) convergent-trigger→convergent-leaf (ADR-011 phase-2.md:~900 "derived record convergent iff trigger convergent"); execute_governance_action (gov_helpers:5596) is replay-guarded (executed_proposals) + processed per-member on commit → governance-ban/RotateContentKeys leaves ARE convergent → fail-closed prevents SILENT per-member §9.9.3 root divergence; (2) COHERENCE — restores KEA to match its SIBLING leaves AccessRevoked/ContentKeysRotated/GovernanceReconfigured already appended fail-closed `?` 10 lines above; prior best-effort KEA was the odd-one-out. ASYMMETRY (unsubscribe + block KEPT best-effort) is PRINCIPLED not drift: unsubscribe = subscriber-unilateral, §5.14.8 authorization-UPWARD-safe/coalesced, NOT MLS-commit-ordered → NON-convergent trigger → best-effort defensible; block = single-author unilateral, non-convergent. Q4 COUNTER SETTLED SOUND: checkpoint_events_since is ONLY a cadence threshold (queries_helpers.rs:716 `events_since>=50` OR `>0 && 600s`, reset to 0 on emit); checkpoint.event_count + merkle_root come from AUTHORITATIVE log length (build_checkpoint:784 `event_log_entries().len()`), NEVER from the counter → over-count=early checkpoint, under-count=late, both BENIGN cadence drift, NOT a false-equivocation vector. Per-leaf inline bump is EXACT under mid-loop failure (strictly better than end-of-loop `+= 1+count` which skips on early-return). RESIDUAL (MEDIUM, pre-existing, artifact-directed, NOT blocker): fail-closed does NOT achieve atomicity/convergence — ban(Class-S)+AccessRevoked(`?`) already durable before KEA loop; on KEA failure the op returns ERROR for an already-succeeded security operation (user already locked out via durable rotation), caller cannot distinguish "ban failed" from "ban ok, audit leaf failed", and executed_proposals replay-marker FORECLOSES retry → member permanently divergent + no recovery. Author ALREADY caught + softened the retry-overpromise (commit 29e816264: "surfaces error" not "will cause retry" — honest). This non-atomicity is a PRE-EXISTING property of the WHOLE governance-leaf model (AccessRevoked has identical shape), structurally forced by ADR-049 Decision 7 (async leaf can't ride sync Class-S closure); PR extends the established pattern, doesn't introduce it. Root-cause fix = ADR note on convergent-leaf-append atomicity, out of PR scope. SORT SOUND: sort_unstable_by author_did centralized at PROTOCOL layer (rotate_all_author_keys + governance_ban_subscriber + unsubscribe all identical); §9.9.3 (09-security-model.md:793) mandates CONVERGENCE (root-equality at equal count), NOT a specific order → lexicographic is a free uniform choice; DID-keyed HashMap uniqueness makes sort total+deterministic; sorting can only IMPROVE determinism for any consumer (HashMap iter was already randomized). Seed seam (seed_broadcast_author: class_s.rs + commands.rs + handlers/broadcast.rs + supervisor.rs) fully `#[cfg(feature="testing")]`, custody-free router returns ContextNotRegistered, mirrors SeedPeerPseudonym precedent — never in prod. Multi-author test (governance_integration.rs:3244) drives REAL propose→auto-execute RotateContentKeys w/ 2 authors, asserts 1 CKR + 2 KEA leaves + action_specific_delta==1+num_authors against LOG LENGTH (validates counter-mirrors-leaves invariant, not self-referential). LOW obs (pre-existing): block_broadcast_subscriber MemberBlocked fail-closed while its KEA best-effort — same unilateral trigger, mild internal asymmetry, not changed by PR. Spec §5.14.8 edits + new RotateContentKeys clause add "fail-closed per ADR-011 (convergent governance trigger→convergent leaf)" — correct provenance, artifact updated BEFORE relying on it in code (one-way flow honored).

**FAST-FOLLOW PR#2234 @39a19e90c (fix/rotate-content-keys-review-followup):** single commit, 148 ins/5 files. NOTE stale-base trap — local `main` ref pinned at merge-base; correct diff = `origin/main...branch` (1 commit), NOT `main...branch` (41 already-merged commits). RESOLVES the BLOCKER: `execute_reconfigure_governance` now bumps `checkpoint_events_since += 1` immediately after EACH durable leaf (`?`-guarded), one after GovernanceReconfigured + one after GovernanceDeadlockRecovery — stronger than `+= 2` because a 2nd-append `?`-failure leaves the counter accurate. RESOLVES the ban-path sort (mirrors rotate_all_author_keys; SOUND — authors is `HashMap<String,AuthorState>` DID-keyed → uniqueness structural → sort_unstable correct). Provenance repoints §2008/§2015 + §9.17 → §5.14.8/§5.14.10 all VERIFIED-REAL; ADR-011 (Verifiable Event Log, phase-2.md) REAL. STILL-OPEN after 2234: (1) NEW phantom ref `§2033` in 2 shipped TODO(spec) comments (gov_helpers:986,3204) = a LINE NUMBER dressed as §; root = the new RotateContentKeys mandatory-KEA MUST-clause was placed in UNNUMBERED prose ("#### Threat model and operational contracts", ~line 2031) so it has no citable id — fix flows to artifact: give it a numbered subsection (or house in §5.14.10) then cite. (2) Atomicity gap (orig Q3) NOT closed — DEFENDED by a new comment that only covers error-returns, not the crash window between the 2 appends; root fix STILL clean+available (fold justification into a GovernanceReconfigured payload → exclusively-deadlock path, single caller :4700, justification always-present, leaf currently payload-less; one leaf ⇒ counter question also vanishes). (3) old_epoch=-1 + dead-timestamp RESOLVED-BY-RUSTDOC not by carrying old_epoch — honest but perpetuates the 2-consumer data-model coupling. (4) new spec clause says KEA "MUST advance — mandatory" but leaf is best-effort; clause doesn't distinguish "rotation mandatory/fail-closed vs leaf best-effort audit metadata" — TODO honestly flags. execute_add_member cfg! NOT in this diff (out of scope). Verdict: counter+sort SOUND & correctly fix the BLOCKER; residual = 1 phantom-ref (fixable now) + defended-not-closed atomicity root.

**SOUND (Q5):** protocol-layer sort-by-author_did in rotate_all_author_keys = defensible misuse-resistance (HashMap iter randomized per-process → unsorted return would make every caller responsible for Merkle determinism, forgetting = silent consensus break). Framing nit only: doc justifies via "event-log leaf ordering" (runtime concern) when honest reason is "canonical order for a HashMap-derived Vec."

**PASS 4 @9b8282ec2 (review-branch DIVERGED from 2234 @39a19e90c — neither is ancestor of the other; this branch does NOT carry 2234's superior fix).** MEDIUM: `execute_reconfigure_governance` on THIS branch uses flat `+= 2` after two independent fail-closed `?` appends (gov_helpers:3416). Residual partial-failure undercount: if GovernanceReconfigured append succeeds but GovernanceDeadlockRecovery append fails (`?`→Err, early return), 1 leaf is durable but counter is +0 (the `+= 2` is never reached). Violates the file's own documented invariant ("counter incremented only when a durable leaf is actually appended" governance_logic.rs:153-156). Canonical file idiom = bump `+= 1` IMMEDIATELY after each successful `?` append (governance_logic.rs:387/429/467/721 ConsequenceTriggered/EnforcementFailed/Enforced; propose freeze-loop gov_helpers:3383) — and 2234 already applied exactly this two-immediate-bump form on the parallel branch, my prior note calling it "stronger than `+= 2`". So the finding is self-confirmed: this branch's `+= 2` is the weaker of two known forms; reconcile with 2234 or split into two immediate bumps. Real-world impact BOUNDED: checkpoint_events_since is per-node-local, gates local checkpoint CADENCE only; checkpoint.event_count is authoritative (true log length) → undercount causes at most a permanent 1-offset cadence → transient Behind/Ahead catch-up, NOT false equivocation. LOW: MLS-attribution parenthetical (gov_helpers:3247) "Welcome distributes the resulting epoch state to new members" is general-MLS-true but tangential/misleading for RotateContentKeys — a rekey Commit adds NO members → produces NO Welcome; existing members advance via the broadcast Commit itself (try_broadcast_commit:3213), which the comment omits. Everything else re-verified SOUND: sort determinism (String cmp byte-wise, DID-keyed uniqueness), best-effort `+ kea_success_count` counts local durable successes correctly (cross-node kea_success_count divergence is BY-DESIGN best-effort, counter stays locally accurate, no false equivocation), all-or-nothing pre-validate rotation, only prod caller updated (gov_helpers:3147), banned-user locked out by durable rotation regardless of audit-leaf fate. Spec naming: step-5 "ReadAccessRevoked event emitted to event log" — durable leaf is actually EventType::AccessRevoked{scope} (no ReadAccessRevoked EventType exists; ReadAccessRevoked is only a receive-buffer ContextEvent) — pre-existing prose imprecision, code comment "1 for AccessRevoked" correctly matches durable type.
