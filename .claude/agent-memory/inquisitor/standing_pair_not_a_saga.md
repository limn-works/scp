---
name: standing-pair-not-a-saga
description: Interrogation verdict (SOUND) on reclassifying standing-pair creation from a cross-context saga to single-context async creation; the reusable tests applied
metadata:
  type: project
---

Branch `spec/standing-pair-not-a-saga-v2` @ fd3b2fd2a (docs-only) reclassified §5.15.8 standing-pair creation from a 3rd cross-context saga to **single-context async creation**. Interrogated 2026-06-24; verdict **SOUND** on all decisions.

**Why:** Reverses a genuine DOA miscategorization (a two-phase-commit saga that was never needed) rather than rationalizing sunk cost. Also reverses an earlier colon-join `derived_context_id` derivation in favor of §9.5.1 length-prefix (adopted 2026-06-24), retiring a prose/reviewer-contingent "method-admission gate."

**How to apply (reusable tests for future saga/context-classification interrogations):**
- **The distinct-context test the spec states:** "a saga coordinates atomicity across 2+ *distinct* contexts that share no sync protocol." A 2-member MLS group is ONE context (both parties derive the identical id, hold replicas synced by MLS + event-log) → fails the test → not a saga. The two genuine sagas each demonstrably name two distinct contexts: §6.2.4 = caller-context + target-context; §5.14.13 = host-context + broadcast-context. "Exactly two" is DERIVED from the invariant, not an accidental count.
- **The "did the reframe eliminate or just relocate the hard part?" test:** the genuine hard problem (two independent nodes simultaneous-create race) is confronted in *Concurrent-creation collision resolution* via a DETERMINISTIC `did_lo`-survivor tie-break + LOCAL per-actor mutex/generation/init-key-single-use atomicity + observation-free reaper — NOT a vote/quorum/journal. Proportionate, not relocated consensus.
- **Live-divergence verification (do this, don't trust the doc):** spec claimed "no live divergence to reconcile." Verified: `derive_standing_context_digest`/`generate_standing_context_id` colon-join TODAY (crates/scp-runtime/src/context/standing_helpers.rs:63), BUT the live `standing_context` get-or-create (supervisor.rs:5377) is NOT wired to any FFI bridge (zero hits in crates/scp-ffi/ + bindings/), and `initiate_standing_pair_create` is a deferred NotImplemented stub. One in-core impl, no cross-impl agreement requirement until wiring → claim accurate.
- **Apparent §3.8.1↔§5.15.8 method-admission-gate "contradiction" is not one:** §3.8.1 keeps a gate for canonical *agreement* (reject un-normalizable DIDs); §5.15.8 retired a separate gate for *injectivity* (colon-freedom). Two different gates; spec distinguishes them explicitly.
- Code-side cleanup (`SagaInput::StandingPairCreate` + `creation_receipt.rs` removal) correctly sequenced as a SEPARATE downstream PR — honors one-way artifact flow.
- Only non-blocking observation: §5.15.8 documentation DENSITY (~30 normative paras, some restatement) — legibility risk for the implementing agent, not a soundness defect.

**v2 revision (HEAD 536c6d192, 2026-06-24) — honesty/precision edits on top, re-interrogated; verdict still SOUND, no premise concerns.**
- **(a0) mismatch-guard re-framed honestly:** now states it catches only HONEST canonicalization divergence, NOT adversarial split-brain — because `InvitationBundle.context_id` (§5.12.3.1) is a creator-SIGNED but DID-pair-UNBOUND String label (verified: line 808/824 — creator signs the asserted id, nothing binds it to the pair). Framing is FACTUALLY CORRECT, not a concession-to-hide-weakness. The guard still earns its place: it is a free local equality check on an input B already derives, and it converts honest did:web divergence (silent split-brain) into clean rejection. Adversarial agreement rests on §9.7.1 bound-creator + MLS membership (correctly relocated, not deleted). Could the guard be STRENGTHENED instead of disclosed-around? No — there is no extra binding available cheaply (the id is not derivable into the signature without a protocol change to InvitationBundle, and §9.7.1+MLS already cover the adversarial case). Disclose-not-fix is correct here.
- **Availability dual (§3.8.1):** the same guard turns adversarial did:web divergence into an UNDIAGNOSABLE pairing-denial (no-oracle requirement vs diagnosability). Accepted explicitly + bounded (did:web fallback-only; did:dht airtight). Sound — a real tradeoff named honestly, bounded to a non-production method.
- **FIX 3 discovery_context self-clear (§5.12.2):** closing the self-registration self-clear on discovery_context/known_did (not just shared_context) is the RIGHT call and grounded — §9.3 line 227 "(not self-created)" discriminator is real and load-bearing. Deeper question "should discovery_context EVER clear a first-contact bar?": the not-self-created qualifier makes it equivalent to shared_context (third-party-attested trust), so it is defensible; residual is the operator's trust in the discovery tools, which §5.12.2 owns. Not a papered-over gap.
- **Drop-filter SHOULD-strength + anti-spam amplifier (FIX 4/5):** these are HONEST DISCLOSURES of Phase-2E interim residuals, NOT design-debt-by-disclosure. Root cause = Welcome-joiners cannot SEND until Phase-2E (no sender-key rotation → no cryptographic severance); the drop-filter is the only in-power interim mitigation and is correctly a SHOULD (a send-gated node genuinely cannot do more). The anti-spam amplifier (publicly-computable derived_context_id ⇒ any party can force 1 DID-resolve+1 sig-verify per forged convergence Welcome) is BOUNDED (no amplification, no join, no state change) and is the honest cost of the gate-decidable convergence carve-out. Both are real-residual-accepted, not disclosing-around-a-fixable-design.
- **Coherence after v2:** section still coheres. The residuals all trace to ONE root (Phase-2E send-gating not yet shipped), which is a sequenced-future capability, not rot. The disclosures are converging (precision edits), not accumulating new spellings of a bypass. No non-convergence signal.
