---
name: out031-pr1-invalid-grant-class
description: SCP-OUT-031 PR-1 — protocol.invalid-grant class assignment is cargo-culted; §5.4.4 definition + estimate-exceeds-bound precedent put it in Input, not Protocol
metadata:
  type: project
---

SCP-OUT-031 PR-1 (`feat/outlet-031-pr1-fixtures-reconciliation`, commit e44055576) — interrogated two DOA-permanent premises.

**P1 — UNSOUND / REVERSE.** `protocol.invalid-grant` (SDK `Credit(0)`/out-of-`u32` guard) was mapped to Protocol/`SCP-OUTLET-6100`, justified as "matches shipped Python `InvalidGrant(ProtocolError)` + AC's `OutletProtocolError` parent." That is status-quo inertia, not merit. §5.4.4 (05-contexts.md:305-312): Protocol = "registration/validation/classification/session-lifecycle"; Input = "schema, size, type, **range** violations." A zero/out-of-u32 `Credit` is a **range violation on a caller-supplied scalar** = Input's literal charter. Clincher: the taxonomy already classifies the near-identical case as Input — §5.4.5:374 `estimated_chunk_count` out-of-range → `OutletErrorClass::Input::EstimateExceedsBound` (6120). `grant` is the same shape (invoker-supplied numeric stream flow-control scalar, not `input_schema` payload). Placing its sibling in Protocol contradicts a decision the taxonomy already made = drift. Extra tell: §5.4.4:338 slug leading-segment IS the class, so `protocol.invalid-grant` bakes in Protocol; correct name if Input = `input.invalid-grant`. Also: it's raised at `Credit` construction BEFORE signing (outlets.py:365) so NEVER crosses the wire as an OutletError — its (code,slug,class) is pure client-side ergonomics, and Input ("you passed a bad value") is the honest developer signal. Fix now (cheap): §5.4.4 → AC[13] → error_codes.rs SLUG+slug_to_class arm → 1 fixture (outlet_error_fixtures.json:165) → SDK parent InputError. Residual weak Protocol steelman (grant-is-protocol-flow-control, cf. stream-already-open) — but stream-already-open is a STATE violation, not scalar-range. Human should ratify the flip.

**P2 — genuine-open-question, low severity, NOT a blocker.** Prose placement of the slug is the established pattern (all non-default slugs live in §5.4.4 prose, table is default-slug-only by design intent line 297). Root cause of "spec/code can diverge": §5.4.4:299 provenance note says the spec DOCUMENTS error_codes.rs = artifact-flow INVERSION; code is de-facto slug authority. Fixtures⊇registry is gated (outlet_error_conformance.rs); spec-prose-slugs↔registry is NOT. Follow-up: cross-check gate or promote module-doc table into §5.4.4 + reverse inversion.

**P3 — SOUND.** AC17 (IkmCommitment) strip into 042b is cleanly severable. 042b AC[0-3,9] fully own struct+derive_accept_time+sign+verify+swap-regression-test; 031 remaining ACs have zero IkmCommitment dependency.

**RESOLVED @ commit ed4bb5353 (2026-08-03) — P1 applied, zero findings, premise sound.** Reclassified to Input/6120/`input.invalid-grant`/parent OutletInputError across spec §5.4.4 (+§5.4.5 estimate-exceeds-bound precedent cited), error_codes.rs (SLUG_INPUT_INVALID_GRANT under CODE_INPUT_VIOLATION, slug_to_class Input arm), AC[13], fixture. No residual Protocol pairing (negative test asserts slug_to_class("protocol.invalid-grant")==None). KEY CORROBORATION: errors.rs:319 `Input|Output => DetailKind::FieldViolation {field_path,violation}` vs Protocol `Rule {rule}` — old Protocol pairing forced a FABRICATED `rule` string; Input lets the typed detail be self-describing/true (`field_path:"/grant", violation:"range"`). The taxonomy's own typed-detail schema independently confirms bad-grant = FieldViolation (Input), not Rule (Protocol). My P2 slug-drift finding PARTIALLY CLOSED: commit added enumerable ALL_SLUGS[69] + 2 source-parse gates (new SLUG_* absent from ALL_SLUGS fails build; fixture coverage set-equated vs ALL_SLUGS). Out-of-scope but noted: subclass naming promoted to uniform Outlet-prefix on all 8 (user-ratified) — strict consistency improvement, no concern.
