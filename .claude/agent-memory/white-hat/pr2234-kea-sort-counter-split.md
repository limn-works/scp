---
name: pr2234-kea-sort-counter-split
description: PR #2234 defensive review — KEA Merkle-determinism sort in ban path + reconfigure checkpoint-counter split; scoping + findings
metadata:
  type: project
---

# PR #2234 `fix/rotate-content-keys-review-followup`

## FINAL PASS (round 3, 2026-08-02, HEAD 2fd43bee9, 7 commits, 10 files) — VERDICT PASS
Branch grew 1→7 commits since round-1; the new commits RESOLVE my prior findings. Delta now fail-closes the KEA loops (best-effort→`.map_err(EventLogFailed)?`) in execute_revoke (governance_helpers.rs:~995) + execute_rotate_content_keys (:~3187), citing ADR-011 convergent-trigger→convergent-leaf. **Reviewed the REMOTE branch via `git show origin/...` because local working tree has DIVERGENT uncommitted `M governance_helpers.rs` (Read returned STALE best-effort code + a TODO(spec) that is GONE on remote) — always git-show the remote for this PR.**
Durability model (both fns identical, SOUND): Class-S enforcement (ban / author-key rotation) committed durably FIRST (commit_class_s_* `.await?`), THEN primary leaf (AccessRevoked/ContentKeysRotated) fail-closed, `+=1`, THEN per-author KEA loop fail-closed with inline `+=1` after EACH durable append. On any mid-loop abort: enforcement durable, counter == durable-leaf-count (NO §9.9.3 drift — matches governance_logic.rs:153-155 invariant "counter incremented only when durable leaf actually appended"), function returns Err. This partial-durable-but-fail-SAFE window is PRE-EXISTING (AccessRevoked was always `.await?` after the durable ban) — the PR merely EXTENDS the same semantics to KEA; NOT a new inconsistency class. Enforcement always consistent (banned member can't read, keys rotated); only casualty = incomplete KEA provenance + no clean auto-retry (execute_* NOT idempotent — retry re-rotates + dup leaves). Fail-closed is CORRECT here: a silently-dropped canonical KEA leaf = security-relevant provenance gap (verifier keeps trusting rotated-away key); loud error dominates best-effort. Softened retry-overpromise comment (29e816264) correctly removed "will cause caller to retry" (no retry guarantee).
**H7 sender-key-rotation-after-KEA-loop is NOT a regression (verified nullified):** rotated_authors non-empty ⟺ broadcast_context.is_some() (mod.rs:902/925); needs_sender_key_rotation requires broadcast_context.is_none() (:962) → MUTUALLY EXCLUSIVE. When KEA loop can abort (broadcast) H7 wouldn't run; when H7 runs (non-broadcast) KEA loop is empty. No skipped-enforcement path.
Reconfigure counter split COMPLETE: `+=1` after GovernanceReconfigured (:3315) AND after GovernanceDeadlockRecovery (:3352), each strictly post-`.await?`. Bug2 RESOLVED. Comment now HONESTLY documents effect-before-justification tradeoff (my prior LOW/P2 addressed at doc level; full fix = emit justification first = larger scope).
Sorts: all 3 KEA-vec producers now sorted-at-source by author_did — governance_ban_subscriber (:1653), rotate_all_author_keys (pre-existing :1697 documented), unsubscribe key_rotations (:844, my prior LOW/latent RESOLVED). 3 new unit tests assert sorted order WITHOUT re-sorting (catch a deleted sort — my prior Bug1 test-quality concern RESOLVED). TODO(spec) §2033 phantom ref REMOVED. timestamp rustdoc clarified (unused-in-prod). Spec §5.14.8 + new RotateContentKeys ¶ add fail-closed qualifier + leaf ordering.
Test-only seam seed_broadcast_author (class_s/commands/handlers-broadcast/supervisor, all `#[cfg(feature="testing")]`, non-actor path returns ContextNotRegistered, unreachable from FFI) — faithfully mirrors add_author prod semantics. No prod attack surface.
PERSISTENT findings (all LOW, non-blocking): (1) **counter-VALUE still not directly tested** — all 3 integ tests assert LEAF COUNTS (happy-path), where inline `+=1` and old `+=1+len` are IDENTICAL; the actual mid-loop-failure counter-correctness fix is UNVERIFIED. A revert of the reconfigure split to single `+=1` still passes. Recommend fault-injection test (failing event-log on 2nd append → assert counter==durable leaves) or a read-seam for checkpoint_events_since. (2) provenance-precision: "fail-closed per ADR-011" — ADR-011 (in phase-2.md, canonical EventType log) establishes convergent/divergent distinction but does NOT verbatim state "governance KEA leaf MUST be fail-closed"; inference is sound (KEA is convergent → canonical → durable) but spec §5.14.8 edit introduces a normative rule; confirm ADR-011 classifies it or point citation at where it's made. (3) reconfigure effect-before-justification carryover (now documented).

## ROUND 1 (2026-08-02, HEAD 39a19e90c) [superseded above]
Fast-follow for #2218. **Actual delta vs origin/main = ONE commit (39a19e90c), 5 files.** `git log main..branch` shows 40 commits but those are already-merged PRs (local main stale); diff `origin/main..branch` = single commit. Task's broader "key changes" list (execute_add_member deps.mls, UniFFI feature-gate allow_in_memory_custody→testing, no_pre_rotation_backend) are NOT in this delta — already on origin/main, out of scope for this PR.

**Why:** reviewing PR #2234 defensively. **How to apply:** when re-reviewing, diff `origin/main..branch`, not stale local `main`.

Two bugs fixed:
- Bug1 (HIGH): `governance_ban_subscriber` (broadcast/mod.rs:1654) built `rotated_authors` from HashMap iteration → non-deterministic Merkle root on replay. Fix adds `rotated_authors.sort_unstable_by(author_did)` mirroring `rotate_all_author_keys` (already sorted @1729 from #2218). VERIFIED SOUND: exactly TWO KEA event-log emission sites runtime-wide (governance_helpers.rs:1001 execute_revoke consuming ban's rotated_authors; :3219 execute_rotate_content_keys consuming rotate_all_author_keys advances) — BOTH now consume sorted producers. Per-author block path emits single MemberBlocked leaf (no vec ordering). Sort covers ALL active KEA-leaf paths.
- Bug2 (MEDIUM): `execute_reconfigure_governance` (governance_helpers.rs:3352,3391) emitted 2 durable leaves (GovernanceReconfigured + GovernanceDeadlockRecovery) but bumped checkpoint_events_since once. Split into two `+= 1`, one after each `.await?`. VERIFIED SOUND: each += strictly after successful append; encode_payload failure between → counter at +1 (correct, 1 durable leaf); no over-count. execute_revoke counter `+= 1 + kea_success_count` (:1032) also sound — `1` for AccessRevoked (guarded by prior `.await?`), kea_success_count only counts appends that succeeded.

Findings (none blocking; PR fundamentally sound):
- (LOW/MED) Bug2 counter split has NO direct regression test. Added test `rotate_content_keys_broadcast_kea_governance_integration` exercises execute_rotate_content_keys + asserts LEAF COUNTS, not `checkpoint_events_since` value, and NOT the reconfigure path. Test comment misattributes "verifies Bug 2." A revert to single `+=1` on reconfigure passes all tests. No test in scp-runtime/tests asserts the counter value for any of the 3 bumped paths (revoke/rotate/reconfigure). Recommend fault-injection test: second-append-fails → counter==1.
- (LOW/P2) GovernanceDeadlockRecovery is fail-closed but effect-before-justification: state mutation (signer removal) + GovernanceReconfigured leaf commit BEFORE the justification leaf. Failure/crash at justification append → durable unaudited governance OVERRIDE + only ephemeral error = fail-OPEN for provenance despite fail-closed error surface. PR comment overstates fail-closed safety (state already torn). Hardening: emit justification BEFORE effect (needs moving ahead of mutate closure — larger than PR scope).
- (LOW) Two `TODO(spec): §2033 vs §5.14.10/ADR-011` comments (governance_helpers.rs:986,3204) are FIRST TODO(spec) in repo, lack story/issue ID (CLAUDE.md stub policy). Rust `// TODO` not CI-gated (only clippy todo! macro), so no CI break, but policy wants a tracking story. They correctly flag companion-leaf durability asymmetry: KEA leaves best-effort vs reconfigure recovery fail-closed.
- (LOW/latent) `unsubscribe` (broadcast/mod.rs:817) builds `key_rotations` from `authors.keys()` UNSORTED — the one rotation-vec producer not sorted-at-source. Currently benign: both callers (broadcast_helpers.rs:160, lifecycle_helpers.rs:316) discard it / emit only MemberLeft, NOT per-author KEA leaves. If future wiring emits KEA leaves from unsubscribe-rotation (the §5.14.10/ADR-011 convergence the TODOs anticipate), Bug1 returns. Recommend sort-at-producer or a non-leaf-bearing comment.
