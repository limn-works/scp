---
name: standing-pair-not-a-saga-v2
description: Standing-Pair NOT-a-Saga reframe v2 — confirming passes at 4dab1f296 (round 1) and bfd82ee47 (round 2/independent), branch spec/standing-pair-not-a-saga-v2, both ALIGNED 0 findings
metadata:
  type: project
---

Standing-Pair NOT-a-Saga reframe v2 — ALIGNED, ship across FOUR confirming passes (pass 4 has 1 LOW informational, non-blocking).

**Pass 4 @ `a0e02ab3b` (worktree agent-a22990a7afe431575; REBASED onto NEW origin/main `f37372b25` — clean ancestor, BUT `a6a2c3ceb` from pass 3 is NOT an ancestor, so full re-verify required) — ALIGNED, 0 blocking, 1 LOW.** Same 5 files, +109/-70. HEAD adds rounds past pass-3: collision consent-gate reject-set tightening, block-list time-precision (point-in-time at gate-read; unobserved-then-observed block self-heals via §3.7.1 post-join sever), reaper TTL provenance (welcome_ttl = §5.12.3.3 InvitationBundle 7-day relay TTL, verified at 05:863), per-`TrustRequirement`-arm not-self-created qualifier (shared_context/discovery_context/known_did each with disclosed inherent residual), MLS-layer defense-in-depth note. ALL anchors re-verified on COMMITTED tree (`git show HEAD:`): §9.3 "(not self-created)" 09:227 verbatim; §3.7.1 is_globally_blocked 03:545 real op; §9.7.1 MLS-to-SCP heading; §5.12.3.3 "TTL 7 days (default)" 05:863; §5.12.5 latency hint; §6.2.4 + §5.14.13 saga headings; ADR-049 §10 auto-revive + §Follow-ups #1 spawn-from-Welcome 396. Pure docs (0 non-.docs). Code symbols (StandingPairCreate, creation_receipt.rs) + register_standing_context + derived_context_id PRESERVED in code, correctly untouched (deletion deferred downstream — artifact-flow correct). Historical framing ACCURATE not fabricated: origin/main §5.15.8 genuinely WAS "Standing-Pair Creation Saga" w/ Prepare-A/B (git log `726868936 feat(saga): standing-pair saga foundation types §5.15.8 #1819`); all corrections honestly marked superseded/historical-provenance-only, original problem-statement bodies retained verbatim under markers. Saga count coherent everywhere (2 live: §6.2.4+§5.14.13). SCP-SAGA band 13000-13999 IS registered (sdk-common:45) + check-error-codes.sh:73 validates → §3a "IS registered" claim factually correct.
**LOW (informational, non-blocking):** the ONLY new `#NNNN` added by the diff is `#1793` in 05-contexts.md:1824 §5.15.8 Provenance note ("authored in PR #1793"). `feedback_no_issue_refs_in_code` targets SOURCE code (comments/doc-comments/tests), not spec provenance prose — PR refs in correction/provenance notes are a legitimate different category. But project convention leans toward §-refs over PR-numbers in durable artifacts; could drop "in PR #1793" without losing meaning. Judgment-call nit, not a violation.

Standing-Pair NOT-a-Saga reframe v2 — ALIGNED, ship, 0 findings across THREE confirming passes.

**Pass 3 @ `a6a2c3ceb` (descendant of bfd82ee47; origin/main `4dc3a5013` is ancestor; +1 commit `docs(saga): gate collision-rejoin on consent...`) — ALIGNED 0 findings.** Incremental delta = 05-contexts.md ONLY (+6/-4). FOUR refinements, all sound + internally consistent: (1) consent-gate added as FIRST element of collision atomic brace `{consent-gate (block-list mandatory) + confirm-creator + fresh-join + destroy}` + new normative ¶ — actor-local on Welcome receipt (per-context mutex + §3.7.1 is_globally_blocked real op at 03-identity.md:545); stranger default-deny satisfied implicitly (did_hi self-initiated → did_lo not a stranger); no cross-context await. (2) Reaper relay-set scope CORRECTED: max over "every relay A emitted to (orig + re-drives), NOT merely relays currently in set" — a dropped relay still retains until its own TTL; excluding closes window early → reaps a replica offline-B could still join. A-local observation-free, dumb-pipe model intact. (3) Ok-return latency clarified: verified member MAY observe found-vs-create latency for OWN pair (§5.12.5 ~0ms/~200ms), only the TYPED discriminant + non-member observation foreclosed — resolves a prior internal contradiction with the AlreadyExists clause (1880). (4) Residual DISCLOSED: did_hi self-only re-drive before did_lo's Welcome consumes one of did_lo's single-use KPs for a Welcome did_lo ignores + did_hi destroys — bounded (1/race-then-redrive, per-DID cooldown), folds into general MLS KP-pool concern, not a correctness break. Docs-only (code deletion still deferred). Saga taxonomy still 2 (§6.2.4+§5.14.13); all "three saga" hits historical/correction-framed only; all anchors resolve (§9.7.1/§5.12.3.3/§5.12.5/§5.12.6/§3.7.1/ADR-049 §10/§Follow-ups); no new #NNNN.

**Pass 2 @ `bfd82ee47` (on origin/main `4dc3a5013`, 2026-06-24) — independent confirming pass, ALIGNED 0 findings.** `git merge-base --is-ancestor origin/main bfd82ee47` = YES (clean fast-forward base). DOCS-ONLY 5 files: ADR-049 §3/§3a, DEFERRED-commit-11, 05-contexts §5.12.2/§5.15.4/§5.15.8 (full rewrite)/example, 09-security-model §9.4.3, sdk-common.md reserved-band label.

**Core classification (SETTLED):** standing-pair creation = single-context async (1 MLS group, 2 members, symmetric `derived_context_id`; replica sync = MLS epoch-Commits+Welcome + event-log RFC-6962 consistency layer, NOT a saga journal). Genuine sagas = EXACTLY TWO: §6.2.4 cross-context tool invoke + §5.14.13 broadcast-hosting handshake. Collision-resolution stays in ACTOR MODEL: per-context actor mutex + fused-join single-use (ADR-049 §9) + cryptographically-BOUND creator-credential check (§9.7.1 KeyPackage-sig/DID-VM binding, not self-asserted string) + Welcome-freshness replay binding (destroy gated on same single-use init-key as join). No saga/cross-actor await reintroduced.

**Verification done (reusable checklist for any re-review):**
- `git grep -iE "three saga|all three saga|3 live saga"` corpus-wide = EMPTY.
- `git grep -i "standing.pair.*saga"` hits = only "standing-pair Welcomes" (anti-spam) + "no saga export" (correct non-saga usages).
- All anchors resolve: §5.14.13, §6.2.4, §9.4.3, §9.7.1, §5.12.3.3, §5.12.5, §3.7.1, §9.3, ADR-049 §10/§Follow-ups #1.
- §9.3 "(not self-created)" qualifier the new stranger-bar imports EXISTS verbatim (09-security-model.md:227).
- SCP-SAGA band 13000-13999 GENUINELY registered in sdk-common.md AND validated by check-error-codes.sh (line 71-73) → ADR-049 §3a's updated "IS registered" claim is factually correct (corrects prior stale "not yet registered").
- v1 LOW finding (stale "standing-pair handshake" example in sdk-common.md reserved-band) = NOW FIXED → 0 occurrences corpus-wide. Carried-findings = 0.

**NAME-COLLISION lesson REPEAT (v1 + v2):** `CreationReceipt` survives in phase-2.md:105-108/287 + prds/main.json:1448-1484 — this is the GENERIC single-context `create_context` two-phase builder receipt (fields: mls_group/sender_key/event_log/published), a DIFFERENT live struct from the standing-pair saga `creation_receipt.rs` apparatus being removed. v2 correctly leaves it untouched. Do NOT flag phase-2/PRD CreationReceipt as a stale standing-pair ref.

**Spec-only / code-deletion deferred (artifact-flow correct):** SagaInput::StandingPairCreate / StandingPairCreatePrepared variants + creation_receipt.rs scaffolding removal explicitly deferred to a separate code-correctness PR. derived_context_id + register_standing_context PRESERVED (surviving §6.2.4/§5.14.13/06-xctx cross-refs valid; register_standing_context now pinned as internal-only, never an FFI export, mirroring §3a's no-saga-export pin).

LESSON: re-reviewing an evolved branch → FIRST `git merge-base --is-ancestor`; grep COMMITTED tree (`git show <sha>:` / `git grep <sha>`), never the diff worktree (worktree is a different branch here).

See [[standing-pair-not-a-saga-reframe]] for the superseded v1 pass (3a161e640) + original NAME-COLLISION lesson.
