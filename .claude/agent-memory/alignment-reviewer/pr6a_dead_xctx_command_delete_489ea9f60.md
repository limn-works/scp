---
name: pr6a-dead-xctx-command-delete-489ea9f60
description: Review of 489ea9f60 (chore/105-pr6a) deleting dead ToolsCommand::InitiateCrossContextToolInvocation NotImplemented variant + phantom-provenance scrub — ALIGNED, 0 findings
metadata:
  type: project
---

# PR-6a delete dead xctx tool command @ `489ea9f60` (branch chore/105-pr6a-delete-dead-xctx-command, worktree agent-ab98dbae24687e41d) — ALIGNED, ship, 0 findings; RESOLVES the prior-sibling MINOR

THIRD reworked SIBLING of [[xctx-dead-command-delete-49adc0be9]] + [[dead-xctx-command-delete-3dc875afb]] (`git merge-base --is-ancestor` = NEITHER is an ancestor of 489ea9f60 — all three are siblings off different bases). **The MINOR carried unfixed across BOTH prior siblings IS NOW FIXED here:** the `dispatch_tools_command` doc at supervisor.rs:~4648 — which both prior passes flagged as byte-identical to origin/main and STILL containing phantom "saga-initiator variant returns NotImplemented during the commit-11 window — see DEFERRED-commit-11" — has in 489ea9f60 been rewritten to the accurate "saga produced directly by start_cross_context_tool_invocation_saga, not via mailbox; borrowed non-`'static` SagaSigningKeys keep it off the mailbox; invoke_tool_with_economy off-mailbox for the DISTINCT non-`Send`-closure reason" (visible as the `@@ -4645,12 +4645,17 @@` hunk). So this rework swept the one site the prior two missed → 0 findings.

Part of the #105 saga-FFI-export track (see [[project-saga-phase2c-d-sequencing]] / sole live saga = §6.2.4). Deletes the dead `ToolsCommand::InitiateCrossContextToolInvocation` mailbox variant (was a `NotImplemented` stub with NO live producer) + its 3 dead handler sites (handlers/tools.rs::dispatch arm, actor/mod.rs ack arm, supervisor.rs reply_tools_not_registered arm) + `reply_saga_deferred` helper. Scrubs phantom-provenance "deferred"/"Current placeholder" doc-comments. Adds RESOLVED banner to Gap 2 of DEFERRED-commit-11-saga-use-cases.md.

**Why ALIGNED / what I verified:**
- Deleted variant fully dead: `git grep InitiateCrossContextToolInvocation 489ea9f60 -- crates/ bindings/` EMPTY. `reply_saga_deferred` EMPTY. Only 2 surviving string hits = new Gap-2 RESOLVED banner (DEFERRED:83) + new Resolution text (DEFERRED:109), both documenting the deletion (= the only acceptable hits).
- Doc-comment technical claims EXACT vs code: `SagaSigningKeys<'a>` (supervisor.rs:889) holds two `&'a ed25519_dalek::SigningKey` = borrowed non-`'static`; `start_cross_context_tool_invocation_saga` (supervisor.rs:5309) bounds executor `F: FnOnce(Value)->Fut + Send + 'static`. So comments' distinction is RIGHT: saga kept off mailbox by the BORROWED SagaSigningKeys, NOT the executor (which IS Send+'static); `invoke_tool_with_economy` off-mailbox for the DISTINCT reason = non-`Send` generic closure. Two different obstructions, correctly separated.
- FFI export deferral PRESERVED not scrubbed: comments uniformly say export "remains deferred, pending per-participant-set saga gating (ADR-049 §3a)"; cross-checked ADR-049 §3a (:71/:85/:94) = FFI surface lands "with the wiring PR, not before," gated on per-set gating + channel-auth caller_did. §6.2.4 (06-xctx:240) confirms saga produced supervisor-side, co-resident seam today, cross-node FFI deferred.
- Gap-2 banner mirrors Gap-3/4 convention: leading blockquote closing "...retained for historical provenance only" — identical shape to Gap-1 Superseded (:53) + Gap-4 RESOLVED-AS-WITHDRAWN (:138). Stale `**Current placeholder.**` subheading RENAMED to `**Resolution.**` (kills present-tense live-placeholder claim).
- Phantom §5.16 citation GONE (grep empty in commands.rs + handlers/tools.rs); replacement cites §19 (econ gov) + §6.2.0.2 (hard rate limits, exists in 06-xctx) — accurate for the consume/refund/reserve/settle helpers the `Tools` variant actually carries.
- NO scope bleed: Gap 1 standing + Gap 4 ContextMigration bodies untouched. Gap-3 broadcast body untouched (the `## Gap 3` line in diff grep was a CONTEXT line, not +/-). Exit-criteria item-2 edit only drops `tools.rs` from reply_saga_deferred list. "one live saga"/"Gaps 3-4 withdrawn" wording ALREADY on origin/main (broadcast §5.14.13 cut earlier — see [[bcast-hosting-saga-cut-385a35c5b]]; §5.14.13 absent from spec, BroadcastHostingHandshake zero live code at HEAD) → this commit inherits, doesn't introduce.
- BONUS positive: `tools_command_context_id` helper (supervisor.rs:10604) catch-all `_ => None` → explicit `ToolsCommand::Placeholder { .. } => None` = strict improvement (now exhaustive, compiler-checked vs future variants, no silent swallow).
- reply_broadcast_not_registered edit = comment-ONLY (removes now-inaccurate "saga-initiator variant" clause; BroadcastCommand match body untouched).

LESSON: dead-NotImplemented-variant deletion + doc scrub → (1) grep crates/+bindings/ for variant name = MUST be empty except the new banner/resolution text; (2) verify EACH rewritten "why it's off the mailbox" claim against the actual struct lifetime + fn trait bounds (here two DIFFERENT obstructions — borrowed-key vs non-Send-closure — were correctly NOT conflated); (3) confirm a deferral the task says to PRESERVE is still stated (don't let a "scrub deferred language" PR also delete a still-valid forward deferral — distinguish "phantom present-tense deferral" from "genuine future-work deferral"); (4) a reclassification in exit-criteria text (Gap3 live→withdrawn) is NOT scope bleed if origin/main already records it — diff origin/main first; (5) catch-all `_` → explicit last-variant after a deletion is a GOOD exhaustiveness hardening, call it out as positive.
