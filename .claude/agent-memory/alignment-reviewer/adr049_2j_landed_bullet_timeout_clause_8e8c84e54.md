---
name: adr049-2j-landed-bullet-timeout-clause-8e8c84e54
description: ADR-049 Phase-2J §9 Follow-ups #1 "Landed" bullet gains the LIFECYCLE_TIMEOUT clause — resolves my round-5 LOW, but the new clause misattributes it to §10 (panic recovery)
metadata:
  type: project
---

Doc-only commit `8e8c84e54` on `feat/adr049-2j-spawn-from-welcome` (successor to `1e16634ad` code + round-5 review). Adds ONE clause to `.docs/adrs/ADR-049-actor-per-context.md:409` (§9 Follow-ups #1 "Landed (core slice)" bullet) stating the join's post-precheck locked region (ConfirmConsume → install_joined_group → durability-check → persist → spawn) is wrapped in `tokio::time::timeout(LIFECYCLE_TIMEOUT, …)`, returns `TransportTimeout` on elapse with idempotent installed-group + durable-snapshot rollback, so a slow/crafted Welcome can't pin `bootstrap_spawn_lock`. This RESOLVES the round-5 LOW (Landed bullet omitted the timeout bound).

Substantive claim VERIFIED accurate vs supervisor.rs `spawn_actor_from_welcome`: timeout wrap @10521 (`Self::LIFECYCLE_TIMEOUT`, Box::pin); elapse arm @10651-10670 destroys group (`destroy_mls_group`) + deletes snapshot (`delete_context`), both idempotent `let _ =`; TransportTimeout @10666; prechecks A-D outside the bound @<10502; finalization outside the bound @10672+ (actor registered only at step-5, elapse arm reached only with NO live actor — load-bearing invariant from round-5 holds). Non-duplicative (line-154 generic is Decision 7 handler-call timeout, different scope) and completes the crash-safety enumeration. Down-flow (code landed first @1e16634ad; doc describes it).

FINDING (LOW, doc-accuracy / mis-pointer): the clause cites "(§10)" for "the create/import/restore/respawn peer-bootstrap timeout discipline." §10 is "Actor panic recovery" (watchdog / panic detection / respawn budget / poison) — grep confirms NEITHER `LIFECYCLE_TIMEOUT` NOR `TransportTimeout` appears anywhere in §10. The generic timeout principle lives in **Decision 7** (line 154: "Every transport and storage call inside a handler wraps timeout(30s)"), and the bootstrap-dispatch-arm LIFECYCLE_TIMEOUT wrap is a CODE convention (dispatch arms @2647/2779/2852 + restore_context @4450), not a numbered ADR decision. §10 documents respawn-from-snapshot (one of the four bootstraps) but not its timeout wrap. So "(§10)" over-attributes — a reader following it finds no timeout discipline. Fix: drop the "(§10)" pointer, or re-point to Decision 7 for the generic principle. My round-5 shorthand "line-154+§10 generic discipline" conflated the two; strict reading shows §10 has zero timeout prose.
