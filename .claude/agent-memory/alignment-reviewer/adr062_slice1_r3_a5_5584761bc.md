---
name: adr062-slice1-r3-a5-5584761bc
description: ADR-062 Slice 1 (SCP-CAPINJECT-001) Round-3 alignment — R2-4 A5 single-activation-path nullifier gating RESOLVED at 5584761bc; APPROVE
metadata:
  type: project
---

# ADR-062 Slice 1 / SCP-CAPINJECT-001 — Round-3 A5 verification @ 5584761bc (feat/adr062-slice1-dht-e1, worktree agent-a84a486b70f195952, 2026-07-16) — APPROVE

Fix delta e54de4fae..5584761bc. Round-2 MED R2-4 (nullifier arms used `#[cfg(any(test, feature="testing"))]`, a 2nd G1-invisible activation path) RESOLVED.

**A5 (ADR:102)** mandates every nullifier in-memory enum arm gated `#[cfg(feature="testing")]` ONLY — single uniform path so feature-absence ≡ type-absence for G1's feature-graph check. Bare `test` disjunct = 2nd path that (a) doesn't cross crates, (b) invisible to G1.

**R2-4 RESOLVED** — all nullifier arms now `feature="testing"` only:
- config.rs DhtMode::Memory variant
- lib.rs publish_did_document_for_mode Memory arm (:3232)
- self_host.rs dispatch_hosted_site_by_dht_mode Memory arm (:1349)
- scp-dht/src/dht_client/mod.rs InMemoryDhtClient type+StoredItem+impls+HashMap/Mutex imports
- scp-dht/src/lib.rs re-export
Grep: zero `any(test` disjuncts remain on any nullifier arm. Residual `any(test,feature=testing)` sites (scp-node self_host.rs:41 import, :1952 build_memory_did_method, lib.rs:1492 ApplicationNode::dev) are CONSUMER test-helpers, NOT type defs/enum arms — compile fine because scp-node dev-dep + testing feature carry `scp-dht/testing` (Cargo.toml:86,104). Not A5 violations.

**scp-dht self dev-dep SOUND** — `scp-dht = {path=".", features=["testing"]}` in dev-deps (Cargo.lock +1 self-edge). `testing=[]` empty marker. Sanctioned A5 mechanism (ADR:67 "each consumer constructing InMemoryDhtClient in own tests lists scp-dht/testing in dev-deps"); here scp-dht's OWN `#[cfg(test)]` unit tests get it. Dev-deps never enter shipped graph → G1 soundness preserved. NOT a new violation.

**R2-8 / finding E** — ADR §A2↔A4 self-DID-regression framing PRESENT+INTACT (ADR:166,168 — honestly names co-located governance self-resolution regression Slice1→Slice11). Delta didn't touch ADRs. construction.md M2 (:63/:194) Domain+Disabled legitimate & NEVER rejected — the R2-2 Domain fix routes build_domain_inner publish through publish_did_document_for_mode (Disabled SKIPS, no new rejection). Stale-doc fixes landed (HostSiteConfig.dht Memory→Disabled self_host.rs:918/940; NodeConfig.dht advisory→load-bearing config.rs:342 + D/dht independence note).

**R2-3 IDENT_1058** — added to error_codes.rs (artifact of record) as SCP-IDENT-1058, sequential after 1057, family-consistent, distinct from generic IDENT_1001. Wired all 3 native bridges (uniffi bridge.rs:265, napi identity.rs:83, pyo3 identity.rs:80).

**R2-5 ci.yml + #2153** — ci.yml adds `cargo test -p scp-ffi-common` default-features lane so `#[cfg(not(feature="testing"))]` absence tripwires actually execute (workspace nextest enables scp-ffi-common/testing, compiling them out). Comment honestly notes ci.yml not an enforcement file, additive positive lane. #2153 filed OPEN ("Wire NodeConfig.dht_gateways end-to-end + unified validated DHT-gateway config"); config.rs comment references it. No silent artifact divergence.

Bonus (round-2 GATEWAYS decision-a): new `scp_dht::validate_gateway_url` = ONE shared contract, called by BOTH FfiDhtClient ClientDhtConfig::into_client (common/dht.rs:148) AND node build_pkarr_client (self_host.rs:2027) — doc claim verified real, not phantom. Fail-closed on malformed http/https URL.

Zero new misalignments. VERDICT: APPROVE.
