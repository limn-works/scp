---
name: pr6c-py-saga-wrapper-review
description: PR-6c Python SDK wrapper for tool_invoke_cross_context_saga (#1939 slice PY) — ALIGNED, 0 findings; re-verified at HEAD 4307a8f2b (prior c7149be15/defabd5cf)
metadata:
  type: project
---

PR-6c Python slice wrapping native PyO3 `tool_invoke_cross_context_saga` (native = PR-6b #116 [[pr6b-ffi-saga-116-review]]). Reviewed at `c7149be15`, re-verified at `defabd5cf`, and AGAIN at `4307a8f2b`. All axes PASS, ALIGNED, ship.

**HEAD 4307a8f2b delta vs prior c7149be15 review (range-diff `origin/main c7149be15 HEAD`):** commits 1-5 are BYTE-IDENTICAL (rebased onto new main, all `=`) → core wrapper carries 0-findings forward. NEW = commits 6-9: (6) test retry bool-guard + Saga* ToolError taxonomy; (7) test code-guard/empty-args/cause-chain; (8) FIX `len(args) > 0` not falsy `if args` in `_saga_terminal_from_bridge` message guard (CI "falsy-optional gate"); (9) docs align empty-args test docstring with the new guard. All benign/correct. Test count 49→54.
- **errors.py:197 `len(args) > 0` fix:** `exc.args` is ALWAYS a tuple → `len(args) > 0` is semantically IDENTICAL to falsy `if args` for the message line (empty tuple is the only fallback case for both; `("",)` truthy in both). Net improvement: now UNIFORM with the sibling `len(args) > 1` (code) / `len(args) > 2` (datum) guards on lines 198-199. Empty-args path: `_BridgeSagaAborted()` → args=() → message=`str(exc)`(=""), code=None→default 13067, datum=None→retry_after_ms None. No IndexError. CORRECT.
- **Test docstring match (test_tools.py:584-591):** quotes `str(args[0]) if len(args) > 0 else str(exc)` VERBATIM, explains dropping the guard raises IndexError. Matches errors.py:197 EXACTLY. Mock `_BridgeSagaAborted = type("SagaAbortedError",(Exception,),{})` → zero-arg ctor valid + `__name__` matches dispatch. ✓
- New tests also pin: bool-datum NOT coerced to retry (bool⊂int guard), non-string args[1] code-guard (2-tuple `(msg,1500)`→code falls back to 13067 not 1500), `raise translated from exc` cause-chain (`__cause__ is bridge_exc`), Saga* are ToolError subclasses.

**Verified against the MERGED native bridge (not just the .pyi):** native `SCP::tool_invoke_cross_context_saga` 9 params; wrapper forwards all 9 positionally in exact order. error.rs:362 `Saga*Error::new_err((formatted,code,datum))` → args=(message,code,datum); SDK reads args[2] STRUCTURALLY, dispatch on `type(exc).__name__`. retry_after_ms `Option<u64>` → None→Python None NEVER 0. Default codes Aborted=13067(generic, NOT 13050 phantom-authz), NeedsRepair=13065, Busy=13066; native decompose (saga_errors.rs:105) always supplies explicit code, `_default_code` only fires on code-omitted (defensive). block-until-terminal=`asyncio.to_thread` over native `block_on`. saga_id supervisor-minted: request struct (tools.rs:1202) has NO saga_id field; wrapper reads `output.saga_id.0` (out-only). SagaResult frozen dataclass pass-through, receipt/output None verbatim. chain_depth 0-255/timestamp_ms≥0 reject bool+float, SCP-VALID-7002, fail-fast BEFORE saga, mirrors sync sibling wrapper.

**Scope:** matrix flip python-only (`python:false→true`, python exemption removed since now live; ts/kotlin/swift STAY false + KEEP exemptions; notes updated "Python SDK wrapper is live"). check-sdk-coverage alias adds 4-lang row (sibling-row convention); ts/kotlin/swift inert name-maps gated by matrix false — NOT scope violation. 54 saga tests pass, ruff clean, 0 NEW `#NNNN` in py source/tests (pre-existing #1549/#1690 refs in untouched docstrings; matrix #1939 in JSON data = legit).

OBS: name-based dispatch (`type(exc).__name__`) couples to bridge class names with no structural tie (same pattern as BRIDGE_ERROR_MAP; tests use same-named synthetic mocks so a bridge rename wouldn't be caught by these unit tests).

LESSON: re-review of a rebased+extended PR → range-diff vs prior-reviewed SHA FIRST; `=` commits carry findings forward, focus only on the NEW delta. A `if collection:`→`len(collection) > 0` swap on a guaranteed-tuple (`exc.args`) is semantically a no-op for the empty-vs-nonempty split — verify it's identical for the falsy-but-present case (`("",)`), confirm it satisfies the CI gate, and check the docstring was updated to quote the NEW form. SDK-wrapper-over-merged-native: .pyi NOT authoritative; verify vs real `pub fn` sig + `new_err` args-tuple + decompose codes; default codes generic-not-specific; datum positional + None-never-0; matrix single-lang flip keeps other langs exempt.
