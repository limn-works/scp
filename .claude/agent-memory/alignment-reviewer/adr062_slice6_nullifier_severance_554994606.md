---
name: adr062-slice6-nullifier-severance
description: ADR-062 Slice 6 / SCP-CAPINJECT-006 nullifier-severance review @ 554994606 — APPROVE-WITH-CHANGES; 1 MEDIUM matrix/code asymmetry (pyo3 verify)
metadata:
  type: project
---

# ADR-062 Slice 6 / SCP-CAPINJECT-006 @ 554994606 (feat/adr062-slice6-nullifier-severance, base 2adb7dd36)

Severs all four in-memory nullifiers (custody/attestation/DHT/pre-rotation) to `#[cfg(feature="testing")]`; deletes `allow_in_memory_custody`; adds G1 (`scripts/check-shipped-feature-graph.sh`). Verdict: **APPROVE-WITH-CHANGES**.

**Why:** overall faithful to ADR-062 §Decision 6 + §17.17 + §9.7.4.1 (mandatory pre-rotation commitment) + §9:187 (attestation absence expected/non-penalizing). Pre-rotation create FAILS CLOSED (IDENT_1059 / `IdentityError::NoPreRotationBackend`) across all bridges + scp-identity config.rs create_inner + scp-node lib.rs. G1 is a sound CLOSED ⊆-allowlist (zero nullifiers); confirmed scp-ffi transitively pulls scp-node/scp-mcp so G1 covers scp-node's graph too (allowlist entries scp-node/*, scp-mcp/*, scp-relay-client/* are LIVE, not dead). AC1 clean (allow_in_memory_custody grep=0 everywhere). AC2 reword is a LEGITIMATE correction of an over-broad grep (kept testing-gated in_memory custody accept arm + not-testing REJECT arm; scoped to not flag durability-only STORAGE `{"type":"in_memory"}` selector) — intent preserved+strengthened, not code-driven weakening. AC4 all InMemoryPreRotationCustody refs testing/test-gated (struct fields uniffi bridge.rs:2624 + napi runtime.rs:1430 both `#[cfg(feature="testing")]`).

**How to apply:** MEDIUM finding to fix — sdk-capability-matrix.json:131 (python verify exemption) claims "on a shipped PyO3 build identity_verify_device_attestation DECLINES with a typed SCP-IDENT-1016 error." FALSE: the pyo3 module-level `#[pyfunction] identity_verify_device_attestation` (scp-ffi/src/identity.rs:920) is `#[cfg(feature="testing")]`-gated ONLY; the inner `#[cfg(not(feature="testing"))]` IDENT_1016 block (939-951) is DEAD (the KNOWN dead-pyo3-block). Shipped PyO3 bridge OMITS verify → Python SDK shim `hasattr` fallback (scp.py:953) raises IDENT-1016 at SDK level, not the bridge. Contrast: pyo3 attest_device is a METHOD with a real shipped `#[cfg(not(testing))]` arm (identity.rs:2294→IDENT_1015), so its matrix claim IS accurate; NAPI is symmetric (scp.rs:4522 not-testing block returns both 1015/1016). error_codes.rs IDENT_1016 doc CORRECTLY says "shipped NAPI method (and Python SDK shim)" — deliberately NOT PyO3 — so matrix:131 self-contradicts error_codes.rs. Root cause: attest made symmetric, verify not. Fix = split pyo3 verify free-fn into two cfg-gated module fns (mirror attest/NAPI, delete dead block) — preferred; else correct matrix:131 to describe SDK-shim hasattr decline. LOW: story files[] over-lists scp-node/src/config.rs + self_host.rs (neither modified; nullifier use there is #[cfg(test)]/absent). OBS: #2171 issue ref in error_codes/shim/docs unverified offline (ADR-025 is the real anchor).
