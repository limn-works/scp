---
name: crypto22-slice2-verify-attestation
description: CRYPTO-22 Slice 2 pure verify_attestation (§9.7.1 checks 3-13) — ALIGNED, zero findings; dropped same-context check is spec-MANDATED not a scope cut
metadata:
  type: project
---

# CRYPTO-22 SLICE 2 `verify_attestation` @ 24de49690 (crypto22-s2-verify-attestation, part of #2187) — ALIGNED, zero findings

Second CODE slice after [[crypto22-slice1-attestation-type]]. Diff = ONE file, +669/-3, `crates/scp-mls/src/keypackage_attestation.rs`. Artifact-flow CLEAN: `git diff origin/main...branch -- .docs scripts '*.json'` EMPTY (code-only, downstream of merged §9.7.1 spec).

**Intent match — exemplary.** `pub fn verify_attestation(att, ctx) -> Result<(), AttestationVerifyError>` implements §9.7.1 "the checks" 3-13 exactly, one typed error variant per failing check. Checks 1-2 (DID resolution + `MAX_ATTESTATION_KEY_RESOLUTION_STALENESS` freshness) correctly DELEGATED to caller via `AttestationVerificationContext::resolved_current_vm_pubkey` doc-contract (Add=fail-closed, Update=last-known-good grace on transient failure but reject on rotated-key resolution-success — matches §9.7.1 "Resolution failure policy"). Extra `MissingKeyPackageInitKey` variant = honest caller-contract guard (Add MUST carry KeyPackage, RFC 9420 §7.1), documented as "not a numbered §9.7.1 check" — no spec conflict, correct fail-closed.

**Dropped `processing_context_id` = spec-MANDATED, NOT a silent scope cut (the key question).** §9.5.2:101 (origin/main 09-security-model.md) states verbatim: attestation is "**deliberately context-agnostic**: it carries no `context_id`. A KeyPackage is a per-identity, pre-published pre-key bundle that must be mintable offline — before any group it will be added to is known — so a context scope is unknowable at mint time; group-scope binding is provided separately and redundantly by the `scp_context_params` GroupContext extension (`0xFF02`, §5.13.3)." §9.7.1's 13-check list contains ZERO same-context check. So a `processing_context_id`/same-context check in this pure verifier would FABRICATE a guarantee the attestation does not make and CONTRADICT §9.5.2. Code's own doc-comment ("There is no context_id field — by design") cites §9.5.2/§9.7.1/§5.13.3 accurately. If the earlier plan/sketch carried `processing_context_id`, that was the PLAN's error; code correctly follows spec over stale plan (artifact-flow: specs govern plans). §5.13.3:1676-1708 confirms 0xFF02 is the group-binding locus, enforced at Welcome/import, not by this verifier.

**Check 10 persona-agnostic (confirmed).** Pure equality `attestation.signing_key_id != ctx.leaf_credential_signing_key_id` (SigningKeyId 2-variant enum Active/Agent). No persona hardcoded; `signing_key_id_mismatch` test flips Active→Agent. Matches §9.7.1 check 10 "credential and attestation MUST name the same verification method (explicit equality)." Persona-selection is a separate track; this slice only checks the two ids agree.

**Scope = pure-verifier boundary, no over-reach.** Deterministic, side-effect-free, wasm-safe: no DID resolution, no network, no clock I/O, no wiring (no ContextManager/FFI), no signer (sign_in_place is test-only). `AttestationVerificationContext` = flat named-field struct (agent-first API standard). Under-scoped in the intended way (1-2 = later wiring slice S4/S6/S7).

**OBSERVATIONS (non-blocking, all aligned):** (1) internal reorder — check 12 lifetime-cap evaluated after 13a (`expires<=issued`) to keep `expires_at - issued_at` subtraction underflow-safe, then 13b/13c; all MUST-reject so accept/reject outcome unchanged; commented. (2) `signing_preimage` visibility TIGHTENED pub(crate)→private with honest doc updates ("Crate-internal"→"Module-private"); ZERO external consumers (only `signing_hash` + in-file `mod tests`; the other repo `signing_preimage` hits are unrelated `CrossContextOutletStreamReceipt`) — genuine API-surface tightening consistent with module invariant "signing_hash is the only external signable output," no scar-tissue. (3) freshness 13b inclusive (`now==expires_at` valid, not_after inclusive) consistent w/ check 11 tying expires_at to Lifetime.not_after. Tests: 1 positive + 1 negative per check + boundary (lifetime exactly max, skew boundary inclusive) + explicit anti-trap (Update with init_key==encryption_key accepted, asserts fixture exhibits the equality).
