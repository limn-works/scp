---
name: class-s-gate-relabel-defense-in-depth-11fe5ecba
description: Wave-25 relabel of check-class-s-fail-closed.sh from "exhaustive" to honest best-effort defense-in-depth (autoref CLASS-A residual) + fixture-73 falsifiability — ALIGNED, zero findings
metadata:
  type: project
---

Worktree xctx-saga, HEAD 11fe5ecba (gate-script only, +52/-32, 1 file). Branch chore/fuzz-pin-nightly review request. ALIGNED, ZERO findings.

**What it does:** Relabels `scripts/check-class-s-fail-closed.sh`'s self-description (comment header) from claiming the marker set EXHAUSTS the finite field-mut-acquisition grammar / is CONVERGED → to an honest "BEST-EFFORT DEFENSE-IN-DEPTH backstop, NOT exhaustive." Names the real evasion (CLASS-A autoref method-name): Class-S fields are HashMap/HashSet (verified state.rs:554 `pub executed_proposals: HashSet`, :894 `pub saga_pending: HashMap`, :969 xctx_caller_reservations, :999 xctx_nonce_dedup), and a `&mut self` mutating method (`.entry().or_insert_with()`, `.get_mut()`, `.drain()`, `.extend()`, `.clear()`, `.remove_entry()`, IndexMut `map[k]=v`, `.iter_mut()`, `.values_mut()`, `.and_modify()`) acquires its borrow via AUTOREF — no `&mut`/`ref mut` token on the call line. Verified NONE of those method names appear as markers in the MUTATORS block (only the comment word "entry" = allowlist entry). So forms (2)/(3) syntax is finite but form-(1) method grammar is OPEN-ENDED → a source-text gate cannot close it (one-more-name = growing denylist). Names the convergent fix: TYPE-SYSTEM GUARD (private Class-S fields + Deref-no-DerefMut + `#[must_use]` commit-guard persisting fail-closed → mutation-without-persist = COMPILE error, alias+method-name proof) as the committed next PR (construction.md / ADR-052 direction — both VERIFIED to exist: `.docs/standards/construction.md` 27KB, ADR-052 in phase-2.md).

**Fixture 73 fix:** `byvalue_destructure_control` persist swapped `persist_state_fail_closed`→`persist_state_best_effort` (now matches sibling controls 74/75). Rationale CORRECT: a fail-closed persist line is itself a HIT-suppressor, so a control using it could never HIT regardless of marker behavior → its MUST-NOT-HIT assertion (line 3665) was VACUOUS. Best-effort makes it FALSIFIABLE (if normalize_refmut wrongly fired on a by-value bind, fixture WOULD HIT → assertion catches). Assertion kept MUST-NOT-HIT, unchanged → strictly stronger now.

**Verification:** self-test EXIT=0; real scan EXIT=0 PASSED; only non-comment change is the single fixture-73 persist swap (NO marker added/removed — marker set byte-identical; NO assertion weakened). Saga code itself verified correct elsewhere — all CURRENT Class-S mutations persist fail-closed; the gap is FUTURE-regression detection only.

**Why ALIGNED on anti-deferral (CLAUDE.md "no deferral"):** This is NOT deferring required feature work. It is (a) correcting a false self-description — a provenance/honesty FIX that the "enforce mechanically, not documentation" + "guard against non-convergent enforcement" (CLAUDE.md:189) tenets demand; (b) the relabel itself is the convergence-stop the simplifier-class mandate requires (>3 passes spelling-chasing = reframe, don't grind). The owner's "ship-now relabel + type-guard-next" sequencing is legitimate because the gate currently OVERSTATES safety (a live correctness-of-documentation bug) and the relabel removes the overstatement immediately; the type-guard is net-NEW sound enforcement, not a patch to incomplete current behavior. Anti-deferral forbids shipping incomplete FEATURES and calling gaps "v2"; it does not forbid landing an honesty fix ahead of a separately-scoped superior enforcement mechanism when current runtime behavior is already correct.

**LESSON:** When a mechanical gate relabels itself from "exhaustive/sound" to "best-effort/defense-in-depth," verify the demotion is HONEST not a cover for weakening: (1) confirm the named evasion is real (grep the evasion method names are genuinely absent from the marker set + confirm field types make autoref reachable); (2) confirm NO marker/assertion was removed or weakened in the same diff (a real demotion keeps all coverage, only corrects the CLAIM); (3) confirm the named "convergent fix" artifacts (ADR/standard) actually exist — else it's phantom provenance; (4) for a "make assertion falsifiable" change, confirm the OLD form was actually vacuous (here: fail-closed persist suppresses HIT unconditionally) and the NEW form can fail.
