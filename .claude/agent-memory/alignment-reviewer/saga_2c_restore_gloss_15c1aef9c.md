---
name: saga-2c-restore-gloss-15c1aef9c
description: §17.16.4 restore-then-replay branch saga-2c HEAD 15c1aef9c — single comment-gloss fix (restore-entry names) + whole-feature sanity-check; ALIGNED zero findings
metadata:
  type: project
---

Worktree saga-2c, branch HEAD **15c1aef9c** on top of 418886d77 (2026-06-23). Reviewed `git show 15c1aef9c` (single comment-line fix) + sanity-checked whole feature `git diff origin/main...15c1aef9c`. VERDICT: **ALIGNED, zero findings.**

**The target commit (15c1aef9c) — comment-gloss accuracy fix, ALIGNED.** pipeline_wiring.rs gate comment (lines 919-922) previously claimed each FFI bridge "exports its own `context_restore_all`-shaped entry point." FALSE for PyO3/UniFFI. Verified actual exports: PyO3 `restore_all_contexts` (src/context.rs:4038), UniFFI `restore_all_contexts` (uniffi/src/bridge.rs:10260), napi per-instance `context_restore_all_on` (napi/src/context.rs:3800; public method `context_restore_all` scp.rs:2605 delegates to it). New gloss names them precisely and now matches the test body's own enumeration (lines 963-968: PYO3/UNIFFI fn `restore_all_contexts`, NAPI fn `context_restore_all_on`). Comment-only; no assertion / test body / production logic touched, exactly as commit msg states.

**Whole-feature sanity check — IN SCOPE, no artifact contradicted.** Branch = 9 commits, TWO grounded workstreams:
1. §17.16.4 restore-then-replay ordering (a784ca50d..15c1aef9c): type-witness `RestoredContexts` (private `ids`, module-private `new`, sealed) gates `replay_unresolved_sagas(&RestoredContexts)`; `restore_on_startup` (supervisor.rs:8088) calls `restore_all_contexts().await?` THEN `replay_unresolved_sagas(&restored)` — restore strictly before replay, type-enforced (compile_fail doctest). This is the same machinery I reviewed at d0c57bd75 (see [saga_2c_restore_then_replay_d0c57bd75.md]). Spec §17.16.4 (17:960+) + §6.2.4 (06:294) reworded in lockstep: reconciliation runs ONCE per process start after restore, NO within-restart post-restore sweep, "the only later sweep is a subsequent restart." ADR-049 note added: ordering enforced by construction BUT crash-recovery INERT in production today because `with_providers` hardcodes `NoopSagaJournal` (supervisor.rs:1301/1371/1381); goes live when bridges switch to `with_providers_and_journal` + durable `ProtocolRepositorySagaJournal` (later phase). Verified inert claim true.
2. §5.14.13 broadcast-hosting handshake saga (b001f49a6, bottom commit): new 1201-line `crates/scp-protocol/src/context/broadcast/hosting_handshake.rs` — BroadcastHostConfig/Request/Grant/AcceptedHostSnapshotEntry, §9.5.1 field-enumerated signing, domain seps SCP-BCAST-HOST-REQ-V1:/GRANT-V1:. Fully grounded in §5.14.13 (05-contexts.md:1587). sdk-common.md error band 13100-13999 *(reserved)* carved → 13100-13199 broadcast-hosting (codes 13100/13101/13102 registered) + 13200-13999 reserved. Additive, aligned. This is a SEPARATE saga family riding the same branch — NOT scope creep INTO §17.16.4; it has its own spec section. Task framed scope as "§17.16.4" but the branch legitimately bundles the broadcast-saga protocol-types commit at its base; both are grounded.

LESSON: when asked to sanity-check "stays within §X scope," a feature branch may legitimately bundle a second grounded workstream (here a whole saga family in the base commit) — don't flag it as out-of-scope if it traces to its OWN spec section + registers its own error codes additively. The scope question is "does anything CONTRADICT an artifact," not "is everything literally §X." LESSON: a one-liner comment-gloss fix is verifiable in full — read the corrected comment AND the test body it claims to mirror AND the three real bridge export sites; the fix is right iff comment names == test-body names == on-disk fn names.
