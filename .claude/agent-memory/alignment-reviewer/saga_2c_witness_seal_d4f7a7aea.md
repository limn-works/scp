---
name: saga-2c-witness-seal-d4f7a7aea
description: §17.16.4 RestoredContexts witness-seal + behavioral bridge-bootstrap test + saga-witness-test-mint feature review (branch feat/2c-saga-dispatch, HEAD d4f7a7aea) — ALIGNED zero findings
metadata:
  type: project
---

# §17.16.4 RestoredContexts forge-closure + behavioral bridge-bootstrap (d4f7a7aea atop d0c57bd75) — ALIGNED, ZERO findings

Worktree saga-2c, branch feat/2c-saga-dispatch. Delta hardens the type-encoded restore-then-replay ordering witness from prior commit [[saga_2c_restore_then_replay_d0c57bd75]] against forgery + adds behavioral enforcement. Pure downstream code/test/CI hardening of an already-spec'd invariant (§17.16.4:963 normative restore-then-replay) — NO spec/ADR/plan file touched, NONE should be (witness/feature are impl mechanisms, specs are protocol-not-impl).

**Why ALIGNED (all 5 review Qs resolved in favor):**
- **Witness seal:** `RestoredContexts` drops `#[derive(Default,Clone)]`→`#[derive(Debug)]` only. `Default::default()` WAS a public zero-arg forge (any crate mints empty witness → `replay_unresolved_sagas` w/o real restore). Now sealed: private `ids` + module-private `new`, mirrors CrossContextSagaSeal/OwnedIdentityDid. 2 compile_fail doctests pin it (E0599 no-Default, E0451 private-field), each fails for the RIGHT reason.
- **Q2 (durable-journal-in-test vs Noop-in-prod) = LEGITIMATE.** Plan generic-moseying-lightning.md:49 puts prod journal wiring (replace NoopSagaJournal w/ ProtocolRepositorySagaJournal + spawn replay at startup = 2D) in **PR-7, a SEPARATE later PR**. Verified prod still Noop: `with_providers` (supervisor.rs:1379) hardcodes `Arc::new(NoopSagaJournal)`; both bridge-instance ctors (bridge_instance.rs:2876,4260) call with_providers NOT with_providers_and_journal. Test injects ProtocolRepositorySagaJournal because it's the ONLY way to exercise the replay leg (Noop has nothing to reconcile). Correct, not premature.
- **Q3 (saga-witness-test-mint feature + CI) = legit mechanical enforcement.** Feature `=[]` (implies nothing); `testing` (Cargo.toml:17) does NOT pull it; NO crate `[dependencies]` enables it → severs the leak chain `scp-ffi→dep:scp-testing→scp-core{testing}→scp-runtime/testing` that re-opened forge in allow_in_memory_custody builds. Enabled only by 2 actor_saga_* targets' required-features (Cargo.toml:130,134) + 6 CI invocations. for_test genuinely consumed (actor_saga_coordinator.rs:306+) → non-vacuous.
- **Q4 (with_providers_and_journal→pub) = consistent.** `Supervisor::new` (1220) ALREADY pub + takes arbitrary `Arc<dyn SagaJournal>`; `with_providers` (1354) already pub. Widening adds NO new journal-injection surface — combines provider wiring with_providers exposes + journal arg new exposes. Legit cross-crate (scp-testing) impediment fix; rationale documented in-code 1389-1398.
- **Behavioral test** (saga_bridge_bootstrap.rs) drives shared bridge entry `restore_all_persisted_contexts` → routes through restore_on_startup (bridge_instance.rs:1707, same path as all 3 FFI exports context.rs:4046/napi:3803/uniffi:10265), asserts BOTH restore (ctx resident) AND replay (orphaned Initiated saga → terminal). Sound COMPLEMENT to type-witness, not redundant.
- **pipeline_wiring.rs change = DOC-COMMENT ONLY** (verified zero non-comment lines changed). Gate honestly downgraded MUST→best-effort/defense-in-depth, assertions PRESERVED, declines UFCS-spelling chase as "non-convergent denylist" — textbook CLAUDE.md:189 over-engineering guard adherence.
- 6 `ContextManager::restore_*`→`Supervisor::restore_*` doc fixes (commands/state/event_log/provider/persistence) = correct post-actor-refactor cleanup.

**Non-finding noted:** release.yml:107 `clippy --workspace --all-targets` enables NEITHER testing NOR saga-witness-test-mint → 2 actor_saga_* targets skipped/un-linted there. PRE-EXISTING (already required testing, that line never enabled it either), NOT introduced. ci.yml:374 clippy that does lint them WAS updated.

**LESSON:** for a "harden a type-witness against forge + add behavioral enforcement" hardening round — (1) confirm the new seal kills the SPECIFIC forge named (Default=public zero-arg ctor) + compile_fail pins fail for the RIGHT error code; (2) a new test-only cargo feature is sound iff `=[]`, NOT implied by `testing`, in NO crate's [dependencies], enabled only by required-features + CI — grep ALL references to prove the leak chain is severed; (3) test using durable-journal while prod=Noop is correct WHEN the plan puts prod wiring in a separate later PR (read the PR sequence); (4) a `pub` widening adds no surface if a sibling already-pub ctor exposes the same injectable; (5) a gate doc downgraded MUST→best-effort is NOT a weakening if assertion bodies are byte-identical (grep non-comment diff lines) AND a behavioral test now carries the real enforcement.
