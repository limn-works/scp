---
name: ceiling-wellformed-type-invariant-c2503b59a
description: §5.3.1.1 ceiling well-formedness type-level invariant review at HEAD c2503b59a (charset-dedupe + msgpack-reject follow-up commit atop 8caf7fb62) — ALIGNED, zero blocking
metadata:
  type: project
---

§5.3.1.1 ceiling well-formedness → TYPE-LEVEL invariant, REVIEWED AGAIN at HEAD c2503b59a (worktree /private/tmp/scp-ceiling, branch fix/ceiling-wellformed-custom-enforcement). Supersedes the earlier-snapshot review [[ceiling-wellformed-type-invariant-8caf7fb62]] — c2503b59a is two commits later (8bd7499bb type reframe + c2503b59a charset-prelude dedupe & rmp_serde reject test); the cumulative review range was `git diff 8caf7fb62..HEAD` = ONLY 3 files (roles.rs, wasm/manager.rs, lifecycle_helpers.rs), runtime governance_helpers/class_s/supervisor changes were in the BASE 8caf7fb62 (not in this range).

**Verdict: ALIGNED, zero blocking findings.** Two LOW/non-blocking notes only.

Reframe (verified in code): native `CapabilityCeiling` gets `#[serde(try_from = CapabilityCeilingRaw)]` (private waypoint runs validate_entries on deserialize; serialize UNCHANGED via field-level serde_sorted_set → export digest byte-stable; propagates to embedded ContextRoleState/export snapshot via JSON + BOTH msgpack encodings to_vec/to_vec_named — all tested). WASM (ADR-034) gets `ValidatedCeilingStrings` newtype (private inner HashSet<String>, Deref read drop-in, 3 named ctors from_colon_entries/from_capabilities/from_ucan_strings + #[cfg(test)] test_insert). Dead `build_ceiling_strings` removed. Closes BLACK-003 (custom:payments:approve stored custom_payments:approve on WASM vs payments:approve native) + BLACK-005 (import copied raw, no validation). New `validate_ucan_ceiling_string` (UCAN-form, recognizes underscore built-in spellings) + shared `validate_ceiling_entry_charset` (§9.1A length/control/whitespace/HTML prelude shared by colon+UCAN validators) + shared `validate_custom_ceiling_entry` core + compile-pinned BUILTIN_CAPABILITIES (18, exhaustive-match test).

CONVERGENCE CORRECTION (carry forward): "byte-identical ceiling the native bridge stores" in commit/doc is IMPRECISE — native stores Capability ENUMS, WASM stores UCAN STRINGS (different by ADR-034 design). What is actually equal + what the tests assert = the UCAN-NAME PROJECTION (ucan_capability_name). The newtype's own doc + the create-path test use the accurate "identical canonical UCAN form" phrasing. NOT phantom provenance (test asserts the correct narrower property), just loose prose. LOW.

POINT-4 LESSON (re-confirmed): retaining runtime explicit validate_entries() on import/restore is NOT redundant-negative-value. Verified: Supervisor::import_context takes a ContextExport VALUE (lifecycle_helpers.rs:1721, already-deserialized), ContextPersistence::load_context returns a typed ContextSnapshot VALUE (in-memory providers at :3164/:3197 never cross serde). The type's validating Deserialize covers FROM-BYTES only; the value-in path is a REAL gap the type does not cover. Before flagging a runtime validate() as redundant given a type-level serde invariant, check the boundary SIGNATURE: bytes-in (serde runs, runtime check redundant) vs typed-value-in (serde skipped, runtime check load-bearing).

WASM import vocabulary: WasmContextExportSnapshot.ceiling_strings (Vec<String>, :6941/6947) is produced at export from the already-validated UCAN-form context ceiling (:5969); WASM imports from WASM exports → from_ucan_strings expecting UCAN form is internally consistent. Native↔WASM export interop is the signed-export wire format (ADR-050), separate.

Non-finding observation: BUILTIN_CEILING_CATEGORIES (colon-form &[&str], 18) and BUILTIN_CAPABILITIES (enum, 18) are parallel same-set lists for two vocabularies (colon fast-path match vs UCAN-name match) — NOT mergeable without per-entry conversion, not accidental duplication; enum list carries the exhaustiveness pin. ADR-034's named primary risk is DRIFT; routing WASM through shared scp-protocol validators STRENGTHENS ADR-034, doesn't violate it (WASM still stores strings locally). 3-constructor newtype = misuse-resistant (each maps a distinct INPUT vocabulary), not a single-canonical-pattern violation.
