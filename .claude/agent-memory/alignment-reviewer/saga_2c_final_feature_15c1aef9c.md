---
name: saga-2c-final-feature-15c1aef9c
description: Final whole-feature alignment pass (9 commits) for §17.16.4 restore-then-replay + §5.14.13 broadcast-hosting saga types — ALIGNED, ship, zero merge-blockers
metadata:
  type: project
---

# Saga 2C whole-feature final pass — branch saga-2c HEAD 15c1aef9c (2026-06-23) — ALIGNED / SHIP, ZERO merge-blockers

`git diff origin/main...15c1aef9c` = 9 commits, two workstreams. Final confirmation before merge.

**Workstream A — §17.16.4 restore-then-replay ordering** (commits a784ca50d→15c1aef9c). Already reviewed in depth at d0c57bd75 (ALIGNED, zero). Re-confirmed intact at HEAD: `RestoredContexts` witness still sealed (private `ids` field, module-private `const fn new`, `for_test` gated behind `saga-witness-test-mint` feature NO non-dev dep enables — not implied by `testing`); `replay_unresolved_sagas(&self, restored: &RestoredContexts)` still requires the witness → replay-before-restore = compile_fail. `restore_all_contexts` is `pub(crate)`; out-of-crate startup forced through combined `restore_on_startup`. Reaper `caller_context_deleted_from_persistence` (supervisor.rs:6106): `Ok(None)`→reap, `Ok(Some)`→keep, `Err`→conservative keep (warn "treating as STILL PRESENT") — BYTE-faithful to new §6.2.4/§17.16.4 "transient read error treated conservatively as still present, never reaped" + new "Permanently-deleted caller (normative)" para. ADR-049 §A note ACCURATE: prod bridges call `with_providers`→hardcodes `NoopSagaJournal` (supervisor.rs:1371,1381); durable journal arrives via `with_providers_and_journal` later phase → saga crash-recovery INERT in production today, exactly as noted.

**Workstream B — §5.14.13 broadcast-hosting handshake TYPES** (commit b001f49a6). NEW file `crates/scp-protocol/src/context/broadcast/hosting_handshake.rs` (+1201, in scp-protocol = pure sync, wasm-ok, correct crate) + `broadcast.rs`→`broadcast/mod.rs` rename adding `pub mod hosting_handshake`. TYPES-ONLY — NO runtime dispatch (grep of supervisor/actor = nothing new; the `BroadcastHostingHandshakePrepared`/`Snapshot` scaffold in `saga_prepared_state.rs` is PRE-EXISTING on main, untouched, opaque-bytes pending later 2C runtime step). Doc-comments repeatedly say "a later 2C runtime step" — no false completeness claim. Preimage field order BYTE-EXACT to spec §5.14.13: REQ = Fixed32(host_id),Fixed32(bcast_id),VarBytes(subscriber_did),Fixed32(wrapping_pubkey),VarBytes(jcs(requested_config)),OptVarBytes(ucan),RawBytes(nonce16),U64(ts); GRANT = same but U64(current_key_epoch) replaces ucan. `RawBytes(&[u8;16])` = 16 raw bytes no len-prefix = spec `RawBytes16`. `OptVarBytes` absent = `CanonicalField::Absent` = ABSENT_SENTINEL `SHA-256(0x00)` (canonical.rs:24-30) ≠ zero-len VarBytes — byte-exact + non-collision tests present. `verify` takes RESOLVED key (signer-authorization is caller's job, documented). Config: clamp range fields, validate rejects only `expires_at_ms==0` (lifetime ceiling is B Prepare-B decision, correctly NOT in leaf type). Two domain separators `SCP-BCAST-HOST-REQ-V1:`/`-GRANT-V1:` already in §9.18.2 registry on main; distinctness tested.

**Spec/registry lockstep.** §6.2.4 (06:294) + §17.16.4 (17:963-965 + new "Permanently-deleted caller" para) reworded in LOCKSTEP, identical vocabulary, explicit cross-refs; ZERO stale "later recovery sweep — after restore" negation survives (grep = only corrected "once per process start / only later sweep is a subsequent restart"). sdk-common.md error-band re-partitioned: 13000-13009 + 13010-13099 (tool-saga) untouched; 13100-13199 → broadcast-hosting; 13200-13999 reserved. Codes 13100/13101/13102 used in impl ∈ assigned range. `check-error-codes.sh` PASSES (2300 codes). Two newest commits (418886d77, 15c1aef9c) = comment/markdown-only, verified (no non-comment Rust line changed).

**Verdict: ALIGNED — ship.** No merge-blocking misalignment. Grounded in §17.16.4/§6.2.4/§5.14.13; specs amended in lockstep; ADR-049 note accurate; error codes additive; no scope creep (types-only B leg, no half-wired dispatch); no DOA decisions; no artifact contradicted.

LESSON: a "whole-feature final pass" over a multi-commit branch where the core was already reviewed mid-stream (d0c57bd75) = (1) re-confirm the witness seal / pub(crate) / required-arg survived later docs commits at HEAD, (2) prove the trailing docs commits are literally comment-only (grep `^[+-]` minus `//` minus `++/--` = empty), (3) for a NEW types-only leg confirm it's truly types-only by grepping the runtime dispatch surface for the new names + confirming any prepared-state scaffold is pre-existing-on-main, (4) re-run the error-code gate after any band re-partition.
