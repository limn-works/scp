---
name: adr049-3a-saga-gating-pr2-review
description: ADR-049 §3a Phase 2C PR-2 per-participant-context-set saga gating + granularity CI gate review (2026-06-14) — ALIGNED + 1 LOW doc-staleness + 1 plan-hygiene note
metadata:
  type: project
---

# ADR-049 §3a Phase 2C PR-2 Saga Gating Review (2026-06-14) — ALIGNED

Branch `feat/actor-2c-saga-gating`, HEAD b66a67b43 on origin/main ca4f0e012 (6 files, +888/-106). Worktree `.claude/worktrees/saga-gating`. Task #87.

**What it does:** replaces supervisor-wide `AtomicBool` saga guard (`saga_pending_guard` + `SagaGuardReset`) with per-participant-context-set reservation: `reserved_saga_contexts: std::sync::Mutex<HashSet<String>>` + `try_reserve_context_set` (atomic overlap-check-then-insert) + `SagaSetReservation` RAII guard (releases on EVERY terminal incl NeedsRepair AND panic-unwind) + `saga_participant_context_set` extractor (sibling of `saga_input_participants`, dedups via HashSet). Adds `target_context_id: [u8;32]` to `SagaInput::CrossContextToolInvocation`. Adds test-only `SagaInput::TestForceNeedsRepair` (cfg test/testing; Prepare succeeds, Commit always fails → drives real NeedsRepair). New `scripts/check-saga-gating-granularity.sh` (NEGATIVE rename-proof scalar-guard regex + POSITIVE P1/P2/P3 + FFI-ordering arming clause + 3-fixture self-test) wired into ci.yml (self-test FIRST, then real) and added to CLAUDE.md NEVER-WEAKEN list.

**Source artifacts (READ):** ADR-049 §3a `.docs/adrs/ADR-049-actor-per-context.md` lines 63-84 (esp. 71 per-context-pair, 73 ordering hard-prereq, 75-80 mechanical granularity gate keyed on SEMANTICS not type-name, 82 SCP-SAGA taxonomy). Spec §5.15.4 `.docs/specs/05-contexts.md:1772` (per-participant-context-set serialization, non-empty intersection overlap, NeedsRepair RELEASES concurrency reservation; FSM-terminal but not resolved). §6.2.4 `06-cross-context-communication.md:242/310` (call mutates 2 contexts → needs target_context_id; NeedsRepair releases concurrency slot but NOT economic/escrow reservation — out of scope here, correct). §5.15.8 standing id-form. Master plan `~/.claude/plans/generic-moseying-lightning.md`.

**VERDICT: ALIGNED.** Every claim maps exactly to source. Ran real gate + --self-test: both exit 0. Negative regex correctly distinguishes `Mutex<HashSet<..>>` (per-set, allowed) from `Mutex<()>`/`Mutex<bool>` (scalar, banned); `spawn_generation: AtomicU64` doesn't trip (name not saga-prefixed). Only struct-literal construction of CrossContextToolInvocation is the test (sets target_context_id); all others are `{ .. }` destructures or unrelated `ToolsCommand::Initiate*`/`*Prepared` types → field add breaks nothing. `target_context_id: _` in saga_input_participants leaves journal shape unchanged (gating vs provenance split, explicit comment). §9.4.3/replay-rebuild correctly DEFERRED to PR-7/2D with precise forward contract in field doc (rebuild non-terminal unresolved EXCLUDING NeedsRepair). CLAUDE.md edit purely ADDITIVE. std::sync::Mutex choice justified (sync critical section, guard provably never across .await, poison via into_inner) — §5.15.4 allows any primitive delivering the properties.

**FINDINGS:**
- LOW doc-staleness: `crates/scp-runtime/tests/shuttle_actor.rs:54` — `ignore`d doc-code block still references deleted `saga_pending_guard` field + obsolete instance-wide admit-one semantics (`saga_guard_admits_one`). Never compiles (/// + ```ignore) but seeds future shuttle test against old model. Update to per-set shape.
- Plan-hygiene (not code): master plan line 14 Track-B 3-gap list + line 3 "2C investigated-not-built" header + line 71 now partially superseded — per-set gating (the §3a mechanical prereq, plan lines 12/342) is now BUILT+gated, unblocking start_*_saga FFI on granularity (still gated on Prepare/Commit dispatch + SCP-SAGA registry). Plan has NO current owner (drift caveat in header) → surfaced, not assumed.

**LESSON (saga-gating gate review):** a granularity gate keyed on SEMANTICS must be checked two ways — (1) run --self-test to prove the negative/positive/FFI-ordering fixtures actually fail/pass as claimed (a dead gate passes silently); (2) prove the negative regex distinguishes the LEGITIMATE per-set store (`Mutex<HashSet>`) from the BANNED scalar forms — a too-broad `Mutex` ban would false-positive the correct fix. When a struct field is added to an enum variant, grep ALL construction sites and classify each as struct-literal (must update) vs pattern-destructure `{ .. }` (unaffected) — don't assume "N matches = N broken callers." For "guard replaced by RAII reservation," grep the OLD field/type name repo-wide for stragglers (found one in an ignored doc-block).
