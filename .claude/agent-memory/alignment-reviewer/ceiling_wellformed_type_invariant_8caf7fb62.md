---
name: ceiling-wellformed-type-invariant-8caf7fb62
description: §5.3.1.1 ceiling well-formedness reframed to type-level construction+deserialization invariant (native #[serde(try_from)] + WASM ValidatedCeilingStrings newtype); worktree /private/tmp/scp-ceiling, branch fix/ceiling-wellformed-custom-enforcement, HEAD 8caf7fb62 — ALIGNED, zero findings
metadata:
  type: project
---

Read-only review of UNCOMMITTED diff in worktree /private/tmp/scp-ceiling (branch fix/ceiling-wellformed-custom-enforcement, HEAD 8caf7fb62). 3 files, +759/-101: scp-protocol/src/context/roles.rs, scp-ffi/wasm/src/manager.rs, scp-runtime/src/context/lifecycle_helpers.rs. Sits on top of #1882 ceiling-grammar + #1879 UCAN step-8 [[pr1879_ucan_all_att_step8_structured_eval]].

**The reframe.** Ceiling well-formedness (spec §5.3.1.1) moved from per-caller validation to a TYPE-LEVEL invariant.
- NATIVE: `CapabilityCeiling` gets `#[serde(try_from = "CapabilityCeilingRaw")]`. `CapabilityCeilingRaw` is PRIVATE, the sole deserialize waypoint; `TryFrom` runs `validate_entries()` and fails the whole deserialize on first malformed entry. Serialize UNCHANGED (serde_sorted_set) → signed-export digest byte-stable. Because `ContextRoleState` embeds `CapabilityCeiling`, the guard propagates to `rmp_serde::from_slice` of export snapshots (proven by new test). New `validate_ucan_ceiling_string()` = UCAN-form (underscore) counterpart to colon-form `validate_ceiling_entry()`; both delegate custom core to new shared `validate_custom_ceiling_entry()`. `BUILTIN_CAPABILITIES` const + `builtin_capabilities_list_is_exhaustive` test = compile-error-on-drift positive enumeration.
- WASM (ADR-034): new `ValidatedCeilingStrings` newtype (private inner HashSet<String>, Deref to &HashSet<String> = drop-in). Three named constructors: from_colon_entries (create), from_capabilities (ModifyCeiling), from_ucan_strings (import). Deletes old `build_ceiling_strings` (which stored from RAW string).

**Why correct / what it closes.** Create previously formatted `custom:payments:approve`→`custom_payments:approve` on WASM but native stores `payments:approve` (BLACK-003). Fix: `Capability::new` strips `custom:` prefix (roles.rs:150-153) → `Custom("payments:approve")` → `ucan_capability_name()` → `payments:approve`. Import previously copied raw ceiling_strings w/ NO validation (BLACK-005) → now from_ucan_strings validates+stores canonical. All 3 WASM paths converge byte-identical to native (test pins create==modify==import). This STRENGTHENS §9.9.3 cross-bridge convergence (every canonical leaf field must be byte-convergent across honest members) by removing a latent equal-count/different-root false-positive. Import REJECTS non-canonical colon-form built-ins (`tool:invoke:*`) — from_ucan_strings rule 3 calls validate_custom_ceiling_entry directly (NOT validate_ceiling_entry) to exclude colon-form built-ins so stored vocabulary stays strictly canonical UCAN.

**VERDICT: ALIGNED, zero blocking/MED/HIGH.** Spec §5.3.1.1 verified verbatim (single-colon, explicit-`:*`-only wildcard, kebab charset, §9.1A sanitization + 256-byte cap mirrored in validate_ucan_ceiling_string). Provenance clean (§5.3.1.1, ADR-034, §5.4.1 tool_id [a-z0-9_-] max128, §9.9.3 all backed). API: 3 constructors NOT a single-canonical violation — 3 genuinely distinct input vocabularies (user colon / parsed enums / untrusted UCAN strings), each validates differently; pub(crate) internal type not SDK-facing. `#[serde(try_from)]` invisible-but-correct (Err on malformed = expected for validated domain type, serialize unchanged).

LOW observations (none actionable):
1. Runtime `validate_entries()` calls RETAINED at import_context/restore_context — author rationale ACCURATE: `Supervisor::import_context` accepts already-deserialized `ContextExport` VALUE, `ContextPersistence::load_context` hands back already-typed `ContextSnapshot` (in-memory provider may never cross serde) → those in-memory boundaries bypass the validating Deserialize. So this is belt-and-suspenders at a REAL gap the type genuinely can't reach, NOT redundant re-check of a type-enforced property (type only enforces on from-bytes path). Does NOT muddy the reframe. Passes CLAUDE.md over-engineering guard.
2. `capability_to_ucan_format` survives (manager.rs:3676/3716) but ONLY on `suspended_capabilities` field (SuspendCapability/RestoreAccess), NOT ceiling_strings — correctly out of scope, not a regression.

LESSON: a "type is the guarantee" reframe does NOT make every runtime validate() call redundant — verify whether the IN-MEMORY/value-constructed entry points cross serde at all. If a boundary accepts an already-deserialized VALUE (not bytes), the validating Deserialize never runs there → an explicit re-validate is defense at a real gap, not negative-value redundancy. Check the actual boundary signature (does it take bytes or a typed value?) before flagging the retained call as muddying.
LESSON: 3 constructors on a validated newtype is misuse-RESISTANT, not a single-canonical-pattern violation, when each maps to a distinct INPUT vocabulary that must validate differently; collapsing them would force callers to pre-normalize = the bug being fixed.
