---
name: saga-2c-final-comment-accuracy-54f937e0f
description: Final alignment pass on saga-2c feature — two comment-only accuracy fixes (for_test feature-gate + pipeline_wiring restore-entry gloss) atop the restore-then-replay + broadcast-handshake feature; ALIGNED, ship
metadata:
  type: project
---

Worktree `/Users/alec/Developer/limn/scp/.claude/worktrees/saga-2c`, branch HEAD `54f937e0f` (parent `15c1aef9c`). Final read-only alignment pass over two comment-only commits + whole feature (`git diff origin/main...54f937e0f`, 33 files +3220/-168). VERDICT: ALIGNED, ship — zero findings. Same feature family as my [[saga_2c_restore_then_replay_d0c57bd75]] review (ALIGNED, zero); these two commits are downstream comment-accuracy corrections, no code/assertion/visibility change.

**Why:** This is the §17.16.4 restore-then-replay TYPE-ENCODING + §5.14.13 broadcast-hosting-handshake saga feature (Phase 2C saga work). Final pass requested before merge.

**How to apply:** If re-reviewing saga-2c, the type-witness ordering + spec lockstep are already verified in [[saga_2c_restore_then_replay_d0c57bd75]]; focus any new pass only on the delta.

VERIFIED FACTS:
- **Commit 54f937e0f** (`actor_saga_crash_recovery.rs:149` comment): corrected "`testing`-gated `for_test`" → "`saga-witness-test-mint`-gated `for_test`". CONFIRMED at supervisor.rs:162 `#[cfg(feature = "saga-witness-test-mint")] pub const fn for_test`. Cargo.toml line 17 `testing = ["allow_unencrypted_storage","scp-protocol/testing","scp-platform/testing"]` does NOT include saga-witness-test-mint; line 32 `saga-witness-test-mint = []` is empty standalone, enabled ONLY by `required-features` of two test targets (actor_saga_coordinator:130, actor_saga_crash_recovery:134 = `["testing","saga-witness-test-mint"]`). So the load-bearing claim — no production/`allow_in_memory_custody` dep edge reaches the witness minter, so a prod caller cannot forge RestoredContexts and the compile-time restore-then-replay ordering holds — is EXACTLY correct. The old comment was factually wrong AND contradicted the invariant.
- **Commit 15c1aef9c** (`pipeline_wiring.rs:917` gate gloss): corrected "each exports its own `context_restore_all`-shaped entry" → names real per-instance entries `restore_all_contexts` on PyO3/UniFFI, `context_restore_all_on` on napi. CONFIRMED: PyO3 method scp-ffi/src/context.rs:4038 `restore_all_contexts`; UniFFI bridge.rs:10260 `restore_all_contexts`; napi free fn napi/context.rs:3800 `context_restore_all_on`. Matches the test body's own enumeration at pipeline_wiring.rs:965-967 verbatim. Old gloss was wrong (only napi has `context_restore_all`-shape, and even that per-instance entry is `_on`).
- No surviving `testing`-gated mischaracterization of RestoredContexts::for_test (2 other `testing`-gated grep hits = unrelated: actor_saga_concurrent test-path gloss + key_package_actor panic-ban gate).
- Spec lockstep intact: §17.16.4 (17:963) "SINGLE startup pass, restore-then-reconcile, NO separate within-restart post-restore sweep, only later sweep = subsequent restart"; §6.2.4 (06:294) reworded identically ("NEXT process start", "once per process start"). No stale "later recovery sweep — after restore" survives. ADR-049 addition is HONEST about production-inert state: prod bridges use `with_providers` → hardcoded `NoopSagaJournal`, crash-recovery becomes live only when switched to `with_providers_and_journal` (later phase) — correctly disclosed, not phantom.
- Broadcast handshake §5.14.13 fully spec-grounded in 05-contexts.md:1587+ (detailed normative section: A=host/B=broadcast, fixed A-before-B order, BroadcastHostingHandshakePrepared all-public-fields, grant nonce/timestamp/config staging for Commit-replay byte-identity). New file lives at `crates/scp-protocol/src/context/broadcast/hosting_handshake.rs` (pure sync types crate — correct home; diff-stat path-wrap made it LOOK like scp-runtime, it is NOT).

LESSON: a diff `--stat` truncates/wraps long paths — a file that appears under `scp-runtime/src/context/broadcast/` may actually be `scp-protocol/src/context/broadcast/`; confirm with `git ls-tree -r <sha> --name-only | grep` before concluding a file is missing or mis-placed. LESSON: to verify a "X-gated not Y-gated" comment-accuracy fix, read BOTH the `#[cfg(feature=...)]` attr on the item AND the Cargo.toml `[features]` graph (does Y's feature list transitively include X's?) AND the test target `required-features` — all three must agree the gate is the dedicated standalone feature, not the broad `testing` one.
