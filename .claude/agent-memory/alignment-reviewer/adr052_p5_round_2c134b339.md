---
name: adr052-p5-round-2c134b339
description: ADR-052 Phase B-P5 downstream-docs round @ 2c134b339 — NEEDS DISCUSSION, 1 finding (line 396 missed invalid-pseudo-code fix)
metadata:
  type: project
---

# ADR-052 Phase B-P5 Round @ `2c134b339` (branch `feat/p5-downstream-updates`, 2026-06-17) — NEEDS DISCUSSION, 1 finding

HEAD `2c134b339`, 1 commit past `aac9851dc` (which was 1 past FINAL `4fb6a1d74`). Latest commit `2c134b339` = "docs(guides): fix stale line-pins and invalid pseudo-code in self-hosting guide" — ONLY touches `.docs/guides/self-hosting-a-website-on-scp.md`. Other 5 files byte-identical to verified prior rounds.

**Why the commit exists / what it fixed (all verified correct):**
- §3: replaced stale `lib.rs:1485`/`:1640` pins with grep-able `impl NatStrategy for DefaultNatStrategy` (symbol EXISTS at lib.rs:1842); fixed `lib.rs:3526` (unrelated stub) → `lib.rs:3193` (the real comment "NAT strategy needs the public HTTP port, not the internal relay port (#641)" — VERIFIED at 3193).
- §3 gap#2 (line 191) + §5 Phase 1 (line 289): replaced INVALID Rust pseudo-code `host_site_until(HostSiteConfig::defaults(Reach::NatTraversal) { … })` (you cannot put a struct-literal `{…}` after a fn call) with accurate `HostSiteConfig { reach, tls, dht, … }` + `host_site_until` form. VERIFIED against main.rs `run_self_host` (:719 builds `HostSiteConfig{reach,tls,dht,…}`, :736 calls `host_site_until`) — exact match.

**FINDING (NEEDS DISCUSSION, informational doc-precision):** The commit fixed 2 of 3 occurrences of the invalid construct. Line 396 (dated 2026-06-16 running-log entry, §7) STILL contains `host_site_until(HostSiteConfig::defaults(Reach::NatTraversal) { … })` — the identical invalid syntax the commit was created to eradicate. Before commit: 3 occurrences (191, 289, 396); after: only 396 remains. The commit message even scopes "Running-log §6.2" as in-scope (fixed one running-log entry) but missed the §7 running-log entry at 396. Unlike the genuinely-historical 2026-06-13/06-14 entries (which describe the *former typestate builder* — correctly preserved as history), line 396 makes a PRESENT-TENSE claim about the CURRENT API ("now calls … directly") using the invalid syntax. Recommended fix: line 396 → `host_site_until(HostSiteConfig { reach: Reach::NatTraversal, … }, shutdown)` or similar valid form.

**Everything else verified ALIGNED:** website.rs COMPILES (`cargo check -p scp-node --example website`) + clippy clean; struct/defaults parity (HostSiteConfig :701, defaults :785, on_ready FnOnce :760, HostSiteReady.plaintext :659/.port :654); on_ready fires post-deploy/pre-listener with same plaintext+port the listener uses (:1228-1236) — banner can't lie. README local-demo prose matches example. deploying-guide all 3 recipes use valid `..HostSiteConfig::defaults(Reach::X)` spread, all fields exist (reach/tls/dht/site_dir/storage_path/port), InvalidConfig caveats match lower_host_site_reach_tls (:914-958: Domain/Acme/Terminated/Custom→InvalidConfig). main.rs "by default"/`SCP_NODE_DHT_MODE=memory` docs accurate (production default :658). No stale `HostSiteOptions` anywhere; remaining `skip_nat:`/`plaintext:` tokens are internal lowering-struct fields; `dht_mode` hits are local vars/test names; `no_domain` hits all in historical running-log.

REUSABLE: when a commit's STATED PURPOSE is "remove invalid pseudo-code X" or "fix stale pattern X", grep for ALL occurrences of X across the file (`git show COMMIT^:file | grep -n PATTERN` vs HEAD) — a fix-this-defect commit that leaves one instance behind is an incomplete fix even if the survivor is in a "historical" log, because a present-tense claim in a dated entry is still asserting current behavior.
