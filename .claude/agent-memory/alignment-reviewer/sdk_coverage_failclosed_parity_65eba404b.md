---
name: sdk-coverage-failclosed-parity-65eba404b
description: PR #1867 fix/sdk-coverage-fail-closed-and-parity @ 65eba404b — ALIGNED, 0 findings; att[0]-only revert resolves prior 8909092eb BLOCKING; final-state review
metadata:
  type: project
---

PR #1867 `fix/sdk-coverage-fail-closed-and-parity` @ 65eba404b (2026-06-23). Base 1f1ea7cd2 (merge-base==base, clean). 91 files. ALIGNED, ZERO findings.

**Att[0] saga resolved.** Commit chain: 8909092eb (apply multi-att AND-intersect TS+Py) → 205966ced (REVERT to att[0]-only, both langs) → 65eba404b (docs fix stale att[i] refs). att[0]-only is FINAL state. Prior review @8909092eb found BLOCKING (Python `evaluate_trust` docstring stale at att[0] while code did multi-att) — NOW MOOT, code reverted to att[0]-only and docs match.

**Revert rationale is technically correct (verified):** calling `ucanValidate(ctx, token, cap_uri)` N times for N att entries consumes the nonce on att[0] → step9 (nonce uniqueness §9.5) fails NonceReused on att[1..N] → AND-intersect → false verdict. Bridge op takes ONE cap_uri; multi-att-in-SDK impossible without a new bridge op that validates all att against ceiling consuming nonce once. Documented in lesson `ucan-validate-needs-real-capability-uri.md §Multi-att limitation` INCLUDING the security gap disclosure (out-of-ceiling att[1] + in-ceiling att[0] → withinCeiling:true). Honest, documented limitation, not a silent gap.

**Spec alignment:** §7.2.1 11-step pipeline (07-spec:71-83) is the Tier-1 ENFORCEMENT path run by the bridge per single cap_uri. SDK `evaluateLayer1` is a self-consistency trust SIGNAL (token's own att[0] vs itself), NOT the enforcement boundary. att[0]-only is a weaker trust signal, not a weakened enforcement gate. Defensible.

**Q-by-Q:** (1) gate fail-closed CONFIRMED: total_ops==0 floor-guard returns 1 (check-sdk-coverage.py:1646), missing-SDK-key errors, unmatched-true errors, all-exempted errors; RAN gate = 223 ops EXIT PASS 0 errors + 13/13 self-tests; ci.yml:145 now runs self-tests BEFORE gate; gate ADDED to CLAUDE.md enforcement-files list (additive hardening). (2) att[0]-only documented gap, lesson discloses security implication — ALIGNED. (3) UcanPermissionError: sdk-common.md:13 NOW makes it canonical across BOTH Py+TS (was "Python: UcanPermissionError to avoid shadowing"); errors.ts:81 class + index.ts:247 export, Py __init__:91/262; no PermissionError alias remains; prds/main.json all refs updated (resolves prior stale-doc item). (4) ADR-053 (renamed from ADR-051 collision; ADR-051=causal-dag now distinct file) Status:Proposed, full format (Context/Decision/Cross-lang/Consequences/Alternatives/Open-questions), grounded §9.7.4.1 §3-5 + ADR-003 §4b; ZERO impl leaked (grep PreRotationCustodyProvider/CallbackPreRotationCustody/import_seed_bytes = none in source) — correct artifact-flow. (5) TS/Py parity strong: both att[0]-only, both fail-closed-on-malformed, both fail-fast; PERM-3030 re-raise (Py by exc-type+startswith guard, TS by PERM-3001 closed-regex outside catch) = documented existing design, not a gap. (6) nothing half-done: WASM ucan.rs routes ALL failure branches through ucan_error_code → [SCP-PERM-3001] cross-bridge parity (was hardcoded PERM_3000 + None drop); doc-comment corrected; mls/provider.rs = doc-only (ContextManager→per-context-actor ADR-049); sketch.md CapabilityValidation 4→6 fields (nonceValid+timeBoundsValid) matches struct; all "stub/placeholder" diff hits are test scaffolding.

OBS (pre-existing, NOT this PR): wasm.ts:1409 passes handle.creatorDid as expected_aud_did to ucan_validate (aud-binding checks against creatorDid not subject) — untouched, separate follow-up. `__extractAllCapabilityUris` returns ALL URIs but evaluateLayer1 uses [0] — awkward name but honestly documented + is the seam for future multi-att.

LESSON: an apply→revert→docs-fix commit triad means review the FINAL state, not the intermediate — my own prior BLOCKING (multi-att stale docstring) was self-resolved by the revert. When an SDK "validation" looks like it weakens a spec pipeline, check whether it's the ENFORCEMENT path (bridge Tier-1) or a self-consistency SIGNAL (SDK layer) — the spec's 11-step ceiling check runs in the bridge per-cap_uri, so SDK att[0]-only doesn't weaken enforcement.
