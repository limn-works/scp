---
name: classs-fin-trunk-compile-boundary-review
description: ADR-049 §9 Class-S fail-closed — retire 4354-line awk gate, replace with compile-time ClassSCell boundary + bounded whitelist tripwire; ALIGNED zero findings (branch classs-fin-trunk, HEAD beddb1c70, 2026-06-23)
metadata:
  type: project
---

# Class-S §9 compile-time-boundary finalization — ALIGNED, ZERO findings

Branch classs-fin-trunk, HEAD beddb1c70 on origin/main a632c731a (merge-base == origin/main, clean two-dot, 31 commits, +11757/-8412 across 45 files). Reviewed worktree, compiles clean (scp-protocol+scp-runtime), 49/49 class_s tests green.

**What it does (ADR-049 §9):** retires the source-text gate `scripts/check-class-s-fail-closed.sh` (4354 lines, non-convergent denylist) + its CI job, replaces with a TYPE-SYSTEM boundary + bounded positive-allowlist tripwire. Closes the §9 structural residual + 3 review findings (RED-CS3 behavioral, BLACK-CS-01/02/03 structural).

**Verified ALIGNED on every axis:**
- Deleted gate is NOT in CLAUDE.md enforcement-files list (lines 108-124) → deletion is sanctioned, not a forbidden-file weakening. ADR documents the replacement as a STRONGER compiler boundary. Lesson `.docs/lessons/rust/compile-time-boundary-over-source-text-denylist.md` consistent.
- **Compile boundary real:** `ClassSCell` has NO `DerefMut`, NO `state_mut`/`role_state_mut` (deleted; grep confirms zero non-doc callers). `assert_not_impl_any!(ClassSCell: DerefMut)` + `(SharedClassS<'static>: DerefMut)` + `(ClassSCommitToken: Clone,Copy)` static guards present (class_s.rs:3240/3250/3294).
- **Best-effort view airtight by construction:** `ClassCMut::new` (class_s.rs:1834) destructures `&mut PerContextState` field-granular; `class_s` bound is consumed by `SharedClassS::new(class_s)` (param `&'a ClassSState`, reborrows shared) → field `class_s: SharedClassS<'a>` (class_s.rs:574), no `&mut` to Class-S. Three privatized fields = `PerContextState.class_s`, `GovernanceState.class_s`, `GovernanceState.revoked_spending_ucan_cids` → `pub(in crate::context)`.
- **Two-view GROW split (BLACK-CS-03):** `RoleStateClassCMut` (best-effort, no GROW — only SHRINK prune + shared read + shared ceiling) vs `ConsequenceRoleStateMut` (GROW `suspend_capabilities`/`suspend_all`, consequence-only, caller persists fail-closed). `ClassCSplit.role_state` typed `RoleStateClassCMut` → best-effort caller structurally cannot GROW. Witness `assert_role_view_has_no_grow` (class_s.rs:3263) = positive fn-item references (weaker than assert_not_impl — adding GROW to RoleStateClassCMut would NOT trip it; doc at :3258 honestly says so).
- **roles.rs privatization matches ADR disclosure EXACTLY:** `ceiling`/`suspended_capabilities` → `pub(crate)` (:868/:903), `CapabilityCeiling.capabilities` → `pub(crate)` (:392). Disclosed residual `assignments`/`members`/`member_capabilities`/`role_definitions` STILL `pub` (:870-886) — ADR line 26 says "consciously deferred, NOT claimed closed." Honest.
- **Cross-crate seam:** `ContextRoleState::class_c_parts()` (:1210) hands disjoint refs WITHOUT naming private fields (ceiling shared `&`). Whole-ceiling write = named `set_ceiling` (:1113) behind whole `&mut`, only inside `commit_class_s_keep`. NOTE: `ContextRoleClassCParts.suspended_capabilities` is a bare `pub &mut HashMap` — a runtime caller holding the raw parts could GROW directly; but the ONLY 3 runtime callers (class_s.rs:1162/1797/2263) immediately wrap into `*::from_parts` which re-confines. A bounded "by-convention" seam at the protocol-crate boundary; not exploited.
- **Whitelist tripwire (BLACK-CS-02) sound+convergent:** `class_s_no_persist_mutator_whitelist_is_bounded` = CLOSED positive allowlist of 6 (`into_inner, class_c_view, commit_class_c_best_effort, clear_committed_reservation_idempotent, set_generation_for_test, restore_class_s`). Brace-depth-aware `find_inherent_impl_blocks` (catches inline/multi-space/newline `impl ClassSCell{`), parser-drift count cross-check, macro-invocation fail-loud, block-comment fail-loud. `persist_state_best_effort` DELIBERATELY not a persist marker (best-effort ≠ §9 for Class-S). Module doc explicitly frames it as non-adversarial review speed-bump w/ type system as real boundary — matches CLAUDE.md over-engineering tenet.
- **Behavioral closure (RED-CS3) real:** `enforce_triggered_consequences` returns downward-auth bool (governance_logic.rs:178), threaded as `&mut bool` sink through send/receive/tool-settle/periodic-sweep; cell-holding caller persists fail-closed. Test `consequence_suspension_persists_fail_closed_and_keeps_suspension` proves FailPersistence surfaces §9 PersistenceFailed (not silent ack) + KEEP-direction retains suspension.
- **In-scope supporting changes:** `NonceDedup::is_replayed` split into pure `is_replayed_read` + `evict_expired` (read off `&mut`, exact-behavior-preserving) — supports shared-`&` Class-S view. FFI runtime.rs `.capabilities` → `.iter()`/`intersect()` (ceiling privatization fallout). `create_checkpoint_if_due`→`_view` rename (checkpoint counter migration). metrics `scp_class_s_token_dropped_uncommitted_total` = release-mode token-drop backstop (lesson corollary). New `#[allow]`s all test-only or justified (needless_pass_by_ref_mut keeps spawned future Send since ClassSCell !Sync).
- No regressions to actor model; sets up cleanly for Phase 2C/2D saga work.

**LESSON:** for a "retire text-gate → compile boundary" review: (1) confirm the deleted gate is NOT in the protected-enforcement list (else forbidden weakening); (2) prove the boundary is real by reading the VIEW CONSTRUCTORS' destructures (perimeter is the destructure, per the lesson) + the static `assert_not_impl_any` guards + grep zero stale escape-hatch callers; (3) for a two-view GROW split, confirm the best-effort type LACKS the GROW method AND the split struct carries the no-GROW type; (4) check the cross-crate seam doesn't leak a raw `pub &mut` to privatized state to a lingering holder (here: re-confined at every call site); (5) confirm the ADR DISCLOSES the deferred residual rather than over-claiming "closed."
