---
name: sdk-coverage-failclosed-parity-3ea854ff3
description: Alignment review of PR #1867 @ 3ea854ff3 — VALID-* absorb-as-all-false in evaluateLayer1; ALIGNED 0 findings
metadata:
  type: project
---

# PR #1867 fix/sdk-coverage-fail-closed-and-parity @ 3ea854ff3 (2026-06-23) — ALIGNED, 0 findings

Range 1f1ea7cd2..HEAD (88 commits, 92 files). Top commit 3ea854ff3 = the substantive new change vs prior reviews (att[0] saga, ucan-validate, etc covered below in [[sdk-coverage-failclosed-parity-65eba404b]] family). HEAD verified `git rev-parse` = 3ea854ff3; base IS ancestor (no phantom-delete trap).

**3ea854ff3 = VALID-* bridge-boundary absorption.** When att[0].with is a non-empty-but-malformed URI (control chars, HTML-special `<`/`>`), the bridge's pre-flight validate_capability_uri rejects with `[SCP-VALID-NNNN]` BEFORE reaching the UCAN pipeline. Previously evaluateTrust/evaluate_trust THREW; now both return all-false — consistent with the null-capUri path (absent att[0].with → all-false, trust.ts:556, trust.py:827). No privilege gain was possible from the throw (a throw cannot produce true), but a caller catching throws with a "trusted" default would have been exposed; inconsistency removed.

**Q1 (title match): YES.** All three title claims delivered: (a) fail-closed coverage gate — check-sdk-coverage.py increments `errors` on unmatched-true/missing-exemption/blank-exemption, `total_ops==0` guard returns 1, `errors>0` returns 1 (verified lines ~1533/1591/1646/1674); (b) TS/Py trust parity — both SDKs structurally parallel; (c) VALID-* consistency fix = this commit.

**Q2 (spec intent for Layer-1): ALIGNED.** sketch.md:803 documents Layer-1 capabilityValidation as "Protocol Enforcement (mechanical, pass/fail)"; trust.ts:524 + trust.py:758 frame it as a "token self-consistency" check ("is this token structurally valid/signed/in-ceiling/unexpired" — NOT "does it authorize action X"). A malformed URI → all-false is the canonical pass/fail fail-closed verdict and matches the pre-existing null-capUri path. VALID-* absorption is consistent with documented intent, not a spec deviation. (Note: §7.2.1 11-step is the bridge Tier-1 ENFORCEMENT path per-cap_uri; evaluateLayer1 is the SDK self-consistency SIGNAL, not the gate — absorbing a boundary-validation rejection into all-false weakens neither.)

**Q3 (evaluate_trust Python root export): COMPLETE.** __init__.py:166 exports `evaluate_trust` + full trust surface (CapabilityValidation/BehavioralRecord/TrustEvaluation/Attestation/Endorsement/etc) + __all__:277. Mirrors TS index.ts:88 `export { evaluateTrust }`. Bridge-provenance evaluator disambiguated as `bridge_evaluate_trust` both langs (Py __init__:51/268, TS index.ts:142). All claimed parity additions present: discover_contexts (discovery.py:177), economy_verify_payment_receipts (economy.py), BridgeTrustLevel, TrustLevelDict/ResolutionPathDict (discovery.py:23/39).

**Q4 (ADR-053): genuine design decision, NOT premature lock.** Now a STANDALONE file `.docs/adrs/ADR-053-pre-rotation-custody-substrate-isolation.md`, `Status: Proposed`. Documents the pre-rotation key custody substrate-isolation gap at the FFI callback-custody boundary (separate subsystem from trust/coverage — appropriately decoupled from the PR's main thrust). Design-only: interface signatures (consume/generate/public_key, CallbackPreRotationCustody adapter, single-use handle invariant) describe the proposed design; NO implementation code leaked. Open questions §115 incl "does §9.7.4.1 need a sub-clause" = honest, artifact-flow-respecting (spec change lands before code if needed). Correct ADR posture.

**Q5 (TS/Py VALID-* parity): IDENTICAL behavior, parallel tests.** Absorb/propagate boundary equivalent: TS absorbs PERM-3001 (classified verdict) + VALID-* (all-false), throws all else incl PERM-3030/PERM-3000 (trust.ts:473/484/493). Py catches UcanError by TYPE → re-raises PERM-3030, classifies rest (== PERM-3001 class); separate `except Exception` absorbs VALID-* (startswith), re-raises rest (trust.py:839/855/866/881). Both re-raise PERM-3030; both absorb VALID-* + normal-UCAN-failure; both propagate genuine faults (CTX-2001). Documented asymmetry rationale in-code (trust.py:841-854: Python type-dispatch needs the startswith guard inside the UcanError handler; TS uses a closed regex allowlist outside). PERM-3000 is WASM-manager-only, never reaches Python native path — benign. Tests parallel: TS trust.test.ts:605/627/655 ↔ Py test_trust.py:720/754/788(non_valid_non_ucan_propagates). Each asserts all 6 cv fields false + (invalid-uri case) bridge WAS called with the exact URI (extractable from payload).

**Q6 (half-done): NONE.** att[0]-only limitation is the SAME pre-existing bound (multi-att needs a bridge op that validates all att[i] consuming nonce once — does not exist), honestly documented identically in both langs (trust.ts:500-505, trust.py:761-766). Not introduced/worsened by this commit. No stubs.

**Issue-ref hygiene (checked, NOT a finding):** source-only (excl tests/.md) issue refs NET-REDUCED: added 4 (#1549×1, #632×3), removed 16 (#1294×1,#1531×5,#1549×6,#632×4). The 4 added are reflows of pre-existing comments shifted onto changed lines (line-wrap), e.g. trust.py:770 "Phase 4 PR 5 Agent B+C (#1549)". Pre-existing residual #1549/#632 refs remain in tests/comments — pre-existing debt this PR already net-improves; a full sweep would be ideal but is out of this PR's introduced scope.

LESSON: when a PR "absorbs a boundary error as all-false," verify (a) the absorbed code is a CLOSED allowlist (PERM-3001 + VALID-* only; everything else propagates) in BOTH langs; (b) it mirrors an EXISTING fail-closed path (null-capUri) so it's a consistency fix not a new policy; (c) the documented Layer-1 model is "pass/fail self-consistency" (sketch.md) so all-false is canonical not a weakening; (d) absorb/propagate boundary is behaviorally equal across the type-dispatch (Py) vs regex-allowlist (TS) idioms; (e) net issue-ref delta in SOURCE (exclude tests/.md/reflows) before flagging no-issue-refs.
