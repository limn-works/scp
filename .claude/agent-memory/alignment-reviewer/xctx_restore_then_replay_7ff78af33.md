---
name: xctx-restore-then-replay-7ff78af33
description: §17.16.4 restore-then-replay recovery-ordering fix review (branch feat/2c-saga-dispatch, HEAD 7ff78af33) — NEEDS DISCUSSION, 1 MED §6.2.4 spec straggler + 2 LOW
metadata:
  type: project
---

# §17.16.4 Restore-Then-Replay Recovery-Ordering Fix — Review (HEAD 7ff78af33, branch feat/2c-saga-dispatch, worktree saga-2c, 2026-06-23)

VERDICT: NEEDS DISCUSSION. Re-review of the fix commit for a prior review's two HIGHs (spec↔code recovery-ordering contradiction + error-code-registry violation). Both HIGHs CLOSED; 1 MEDIUM spec-internal straggler + 2 LOW.

**What the fix does (10 files +554/-155):** amends §17.16.4 (17-persistence-and-storage.md:963-974) FIRST to restore-then-replay (single startup pass: restore contexts → reconcile sagas; non-terminal-leave narrowed to genuinely-undeliverable; "the only later sweep is the NEXT process start"; permanently-deleted caller reaped), THEN reorders `Supervisor::restore_on_startup` (supervisor.rs:7886-7891 = `restore_all_contexts().await?` → `replay_unresolved_sagas().await?` → `Ok(restored)`). Artifact-flow HONORED (git blame: spec+code same commit, spec governs). Error codes `SCP-SAGA-13003/4/5` → `13100/13101/13102` (hosting_handshake.rs:75/79/87) moved out of reserved 13000-13009 sub-block into a NEW carved+registered `13100-13199` broadcast-hosting band (sdk-common.md:62 + rows 115-117); band split contiguous no-gap/overlap; check-error-codes.sh PASSES. FFI doc-comments (bridge_instance.rs + pyo3/napi/uniffi) flipped to restore-then-replay; debug→warn split correct (NotInitialized/PersistenceFailed→debug, else→warn). Gate pipeline_wiring.rs:504 flipped to assert `restore_pos < replay_pos`; `extract_fn_body` hardened to strip `//`-text + string CONTENTS order-preservingly (legit coverage EXPANSION). 3 new tests (refund-delivered+terminal over real persistence; deleted-caller reaped; fail-closed restore-leg-error). §5.14.13 unchanged + still aligned; new "Verification scope" doc-comments on Broadcast*::verify accurate (sig-only; caller validates expires_at_ms>0; B applies min(requested,granted_at_ms+max_grant_lifetime_ms) ceiling at Prepare-B). RawBytes16→CanonicalField::RawBytes doc fix names real variant.

**FINDINGS:**
- MEDIUM (artifact-flow / spec-internal contradiction): `.docs/specs/06-cross-context-communication.md:294` — §6.2.4 crash-recovery prose STILL reads "leaves the journal NON-terminal (at PreparingB) so a later recovery sweep — AFTER THE CALLER CONTEXT IS RESTORED — re-drives the reversal." This is the SAME disproven within-restart "later sweep after restore" model that the amended §17.16.4 (which §6.2.4 cites as normative authority) explicitly repudiates ("NO separate post-restore sweep within a single process start ... the only later sweep is a subsequent RESTART"). 06 was NOT in the commit (git show --stat); line predates fix (blame: dabf133649 2026-06-20). The §17.16.4↔§6.2.4 copy of the contradiction wasn't closed in lockstep. FIX: re-word 06:294 to restore-then-replay (non-terminal only when genuinely undeliverable; re-driven by NEXT process start's pass).
- LOW (commit-msg over-claim + stale inline comments): commit msg claims it "fixed the stale 'later sweep' comments in supervisor.rs" but only fixed the restore_on_startup (7860-7918) + recover_preparing_b_entry (5573-5594) DOC-comments. Residual "later sweep / after a respawn / after the caller respawns" at supervisor.rs:5509, 5667, 16716, 16771, 18585-18588. Behavior still correct (genuinely-undeliverable→non-terminal→next-restart); doc-precision only — but "after a respawn" is exactly the within-runtime mental model the fix retired. metrics.rs:18 "stays non-terminal for the §17.16.4 sweep" = acceptable shorthand, NOT a finding.
- LOW (stale gate/test name): pipeline_wiring.rs:504 `fn restore_on_startup_runs_replay_before_restore` now asserts the INVERSE (restore-before-replay); misnomer (also ref'd actor_saga_crash_recovery.rs:221). Cosmetic (comment-strip makes name non-load-bearing). Rename to `_runs_restore_before_replay`.

**LESSON:** when a fix amends a normative § to flip a model, the SAME contradiction often lives in a SIBLING spec section that cross-references it (here §6.2.4 in 06-… cited §17.16.4 in 17-… as authority) AND in many inline code comments beyond the 1-2 the commit msg names. Grep the WHOLE repo for the retired phrase ("later sweep", "after the caller restores/respawns", "replay-before-restore") and classify each: (a) live contradiction in a cross-ref'd spec section = MED; (b) stale code doc-comment = LOW; (c) generic shorthand still true under new model = non-finding; (d) "describes what the WRONG order WOULD do" = correct, non-finding. A commit msg saying "fixed the stale X comments" is a verifiable claim — grep to confirm it scrubbed ALL of them, not just the headline ones.
