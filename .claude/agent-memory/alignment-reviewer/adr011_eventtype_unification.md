---
name: adr011-eventtype-unification
description: ADR-011 amendment (native↔WASM event-log unification) — APPROVED at commit c6bccc7d5 (re-review); prior CHANGES-NEEDED at 63723abf7 now resolved. Closed-set 76-variant EventType + RFC 6962 export binding.
metadata:
  type: project
---

# ADR-011 EventType Unification Amendment — APPROVED at `c6bccc7d5` (2026-06-17 re-review)

**Re-review note:** Prior review at `63723abf7` was CHANGES-NEEDED — the exclusion passage claimed `MessageReceived`+`EquivocationDetected` as the only-two exclusions as a *current* property, but both had LIVE production append sites the passage didn't acknowledge. Commit `c6bccc7d5` REWROTE the exclusion passage to explicitly state "This is not yet a pre-existing property: the scp-runtime provider CURRENTLY appends both... The unification therefore REMOVES those two append sites, in the same spirit as the name-string defect corrections below; once removed, these are the **only** two exclusions." Blocking finding RESOLVED. Independent re-verification below confirms APPROVE.

**Re-verification at c6bccc7d5 (fresh enumeration):** 59 distinct production appended names resolved (incl. dynamic: `governance_event_label()`→8 Governance* variants, `apply_commit_retry_outcomes` event_log_writes→CommitBroadcast{Succeeded,Pending,Failed}, `deliver_plaintext_or_announcement`→Some("PseudonymAnnounced")/Some("MessageReceived"), consequence helper &'static str→4 Consequence* names, JSON-blob payload.to_string()→SpendApproved/TTLExtended/TTLExtensionRejected, format! defects→AppBound:/AppUnbound:/ContextTombstoned:/ContextMigrationCancelled:). `comm -23 appended enum` residual = EXACTLY {EquivocationDetected, MessageReceived} = the 2 documented exclusions. No uncovered event. Enum=76 variants (code currently 36, ends ProvenanceReceived — spec-leads-code, correct flow). Event struct=7 fields, no signing_key_id (scp-event-log has none; doc-correction direction right). generate_checkpoint retains signing_key_id (checkpoint-signing arg, disambiguated). tree.rs leaf=rmp_serde+0x00 prefix matches amendment's SHA-256(0x00‖rmp_serde(Event)); export sig uses JCS(ContextSnapshot) — two separate paths, no JSON/msgpack contradiction. All cited §§ exist in governing files (5.11A.5/5.10→05, 19.6.1→19, 7.3.7→07, 9.9.4/9.12→09, 8→08). No brittle file:line coords in EventType section. ADR-050/§23.16.8/§23.16.1 export-binding coherent. MemberReset matches phase-6.md:1404.

# (Original review) ADR-011 EventType Unification Amendment at `63723abf7`

Docs-only spec amendment (4 files: ADR-011 in `.docs/adrs/phase-2.md`, `ADR-050-signed-context-export.md`, spec `23-sync-and-offline-strategy.md`, `25-test-vectors.md`). Upstream prerequisite for unifying scp-runtime ContextManager's free-form-string hash-CHAIN event log onto the canonical `scp_event_log::EventLog` RFC 6962 tree.

**Why:** runtime logged free-form event-name strings (incl. `format!`-baked params + JSON-blob tags) to a hash-chain; ADR-011/FFI/WASM use typed `EventType` leaves on the RFC 6962 tree. Divergence broke native↔WASM equivocation (latent #1540) + blocked #1535. Alec chose FULL unification 2026-06-17. See user-memory `finding_runtime_eventlog_not_rfc6962.md`.

**How to apply / verified facts:**
- `EventType` declared CLOSED (76 variants), no `Other(String)` catch-all (rejected-alternative documented). Code enum at `crates/scp-event-log/src/lib.rs:102` currently ends at `ProvenanceReceived` (~57 variants) — the trailing ~19 lifecycle/consequence/commit-broadcast/app-sandbox/recovery variants are ADDED BY this amendment, to land in code when unification ships. Expected (artifact flow: spec leads code).
- Independent enumeration of ALL production runtime Merkle-log append sites (`append_context_event[_with_payload]`, `append_event`, resolving every var/`format!`/`payload.to_string()`/JSON-tag): **64 distinct event names. 62 map to a variant; the only 2 unmapped are `EquivocationDetected` + `MessageReceived` = exactly the 2 documented exclusions.** No uncovered event.
- Key resolved dynamic sites: `governance_event_label()` (governance_helpers.rs:153) → 8 Governance* variants; `apply_commit_retry_outcomes` → CommitBroadcast{Succeeded,Pending,Failed}; `deliver_plaintext_or_announcement` (messaging_helpers.rs:351) → `Some("PseudonymAnnounced")`/`Some("MessageReceived")`; `format_bind_event`/`format_unbind_event` (app_sandbox.rs:868/887) → `"AppBound:{...}"`/`"AppUnbound:{did}"` (defect format! names → typed AppBound/AppUnbound). `recovery/epoch_advanced` (trust_recovery_helpers.rs:258, actor_did="system:recovery", §9.12 step2 MLS Update+self-Commit) → `RecoveryEpochAdvanced`, correctly distinct from ADR-007 sender-key `KeyEpochAdvance`. TTL JSON tags in code are `"TTLExtended"`/`"TTLExtensionRejected"` (all-caps) → amendment correctly renames to PascalCase-acronym `TtlExtended`/`TtlExtensionRejected` (matches `KeyEpochAdvance`/`SpendingUcanGranted` convention).
- Exclusions sound: `MessageReceived` (per-recipient non-convergent; canonical=sender `MessageSent`) + `EquivocationDetected` (local alert §23.16.6) are correct & ONLY two. Both currently ARE appended in code (messaging_helpers.rs:604/2537, queries_helpers.rs:926) — that's the defect the unification removes.
- `signing_key_id` removal coherent: code `Event` (lib.rs:244) already has 7 fields, NO `signing_key_id` (ADR was ahead of code; amendment corrects doc→code). criterion-8 `generate_checkpoint(...signing_key_id)` is a CHECKPOINT-signing arg, retained, explicitly disambiguated.
- Export binding coherent across ADR-050 (replaced "pruning-tolerant chain head / planned cold-start fix" limitation with "RFC 6962 tree::root = full-history completeness; truncation forgery closed by construction") + spec §23.16.8 + ADR-011 §23.16.1 checkpoint root. tree.rs confirms leaf=SHA-256(0x00‖ser), interior=SHA-256(0x01‖L‖R) → root binds full ordered set, claim technically true. `25-test-vectors.md` adds a typed-leaf-KAT placeholder note (byte values impl-derived, added when code lands — acceptable, not a stub since spec defines construction not values).
- All cited §§ verified to exist: §5.11A.5 tombstone, §5.10 TTL, §19.6.1 economic events, §7.3.7 consequence, §9.9.3/4 commit-broadcast/equivocation, §9.12 step2, §8 app-binding-visible-in-log. No contradiction w/ ADR-031/049/029 (MemberReset↔ResetMember phase-6.md:1404 ✓).

**Reusable pattern:** to verify a closed-set-completeness claim, enumerate the ACTUAL producer call sites and resolve EVERY dynamic name (vars, format!, to_string, JSON tags, helper-fn returns) to concrete strings, then `comm -23` the producer set against the declared enum — the residual must equal exactly the documented exclusions. Over-coverage (enum variants nothing currently appends) is NOT a gap for a closed taxonomy.
