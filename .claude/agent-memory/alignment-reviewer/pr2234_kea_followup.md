---
name: pr2234-kea-followup
description: PR #2234 fast-follow to #2218 — sort determinism + reconfigure counter split (both ALIGNED); adds §5.14.8 spec clause + TODO(spec) that DOCUMENT but do not RESOLVE the #2218 best-effort-vs-convergent KEA HIGH
metadata:
  type: project
---

# PR #2234 `fix/rotate-content-keys-review-followup` @ 39a19e90c — NEEDS DISCUSSION

Fast-follow to #2218 (which is now MERGED into origin/main 4e8297b8c). **The real PR is ONE commit `39a19e90c`, 5 files.** GOTCHA: `git diff main...branch` locally shows ~250 files + spurious deletions (relay_querier.rs -519, keypackage_attestation.rs -796, resolution.rs, uniffi/bridge.rs) — that is the STALE-BASE TRAP: local `main` (3c168…) is behind `origin/main` (4e8297…), so two/three-dot vs local main folds in main's post-fork work as inverse deltas. Review by `git show 39a19e90c`. Merge-base(origin/main,branch)=363028aaa; branch = that +1 commit.

**Task's 8-item list conflates #2218 (merged) with #2234.** Items 5-8 (execute_add_member ADR-049 §15, UniFFI allow_in_memory_custody→testing ADR-062, build_ffi_dht_client/no_pre_rotation_backend/rotation_publish_client/publish_to_resolver_dht_for helpers, resolution.rs) are NOT in this PR — they are origin/main's independent work surfaced only by the stale two-dot diff. Out of scope for #2234.

**ACTUAL #2234 delta = 5 files:** 05-contexts.md (+2, the §5.14.8 RotateContentKeys clause), broadcast/mod.rs (sort in governance_ban_subscriber + doc repoint + 2 tests), sender_keys/broadcast.rs (timestamp rustdoc), governance_helpers.rs (§ citations + 2 TODO(spec) + reconfigure counter split), governance_integration.rs (1 integration test).

## ALIGNED (verified sound)
- **Bug 1 sort** `governance_ban_subscriber`: `rotated_authors.sort_unstable_by(author_did)` mirrors the #2218 sort in `rotate_all_author_keys`. author_did is unique per author in the rotation set → no sort_unstable tie nondeterminism. §9.9.3 Merkle determinism. Two good no-re-sort tests.
- **Bug 2 reconfigure counter split**: end-state is TWO `*checkpoint_events_since_mut() += 1` (one after each `.await?` append of GovernanceReconfigured, then GovernanceDeadlockRecovery). Both leaves fail-closed → second-append failure short-circuits before its bump → counter = +1 (matches the 1 leaf that durably landed); both-succeed = +2. Fixes a genuine UNDER-count (pre-fix was a single +1 at end covering both leaves). Clean, fully §9.9.3-aligned. (NOTE: my #2218 memo said reconfigure was `+= 2`; the merged state was actually a single `+= 1` at end — memo was imprecise. Current branch is correct.)
- Doc repoints accurate: `§9.17 → §5.14.8/§5.14.10` (§9.17 = historical-content suppression, wrong for epoch-advance) and `§2008/§2015 → §5.14.8/§5.14.10`. timestamp rustdoc ("consumed only on per-author block relay path; governance/event-log path uses timestamp_secs, this field ignored") is accurate — emit sites use the `timestamp_secs` param.

## UNRESOLVED / findings
- **HIGH (carried from #2218, NOT fixed — only flagged):** governance-triggered KEA leaves (execute_revoke ban path + execute_rotate_content_keys) are still BEST-EFFORT (warn+continue). ADR-011 (phase-2.md:900-912): canonical Merkle log MUST contain ONLY convergent events; "a derived record is convergent iff its trigger is convergent." RotateContentKeys/governance-ban = convergent MLS-ordered trigger → KEA leaf is a convergent canonical leaf → every honest member MUST append it → best-effort silent-skip forks tree::root = the exact §9.9.3 non-convergence the exclusion taxonomy guards. Bug 1/Bug 2 fixes harden leaf ORDER + COUNT but PRESENCE stays non-convergent. Sibling leaves (WriteAccessRevoked/ReadAccessRevoked, GovernanceReconfigured, GovernanceDeadlockRecovery) ARE fail-closed `.await?`.
- **MEDIUM spec-code tension (self-flagged, unresolved):** new §5.14.8 clause says "all authors MUST advance … a mandatory `KeyEpochAdvance` per author … emitted to the event log after the durable `ContentKeysRotated` leaf." "durable" attaches ONLY to ContentKeysRotated, never to the KEA leaves — a deliberate-looking asymmetry that leaves the KEA leaf's persistence tier unstated. Read naively ("mandatory KeyEpochAdvance … emitted to event log") a reader concludes the leaf is mandatory/durable, which the best-effort code contradicts. §5.14.10 lists KeyEpochAdvance as a NEW shared event type but states NO tier. The spec addition DOCUMENTS ordering + mandatory-rotation but does NOT decide fail-closed-leaf vs relay-notification-only — the actual #2218 question. Per artifact-flow invariant the tier decision belongs in the spec BEFORE code, not a TODO.
- **LOW citation:** TODO(spec) comments cite "§2033" — there is NO §2033 section (grep: "2033" appears nowhere in 05-contexts.md text; sections are §5.14.x). It's a LINE number wearing a § sigil (points at the §5.14.8 relay-message framing / governance-ban lifecycle list). My own #2218 memo inherited the same "§2033" sloppiness. Should read §5.14.8 (relay-msg framing) vs §5.14.10 (Event Log) / ADR-011. Imprecise pointer weakens the TODO's value for the eventual spec-fixer.
- **OBSERVATION (artifact-flow):** §5.14.8 spec paragraph lands one PR AFTER the code it describes (#2218 merged first). Not phantom provenance (behavior traces to #1847 audit + pre-existing "mandatory KeyEpochAdvance per author" language in the §5.14.8 governance-ban lifecycle), but the correct order is spec-decides-tier → code-matches; instead code shipped best-effort and the catch-up spec para documents it ambiguously.

**Verdict:** the two mechanical fixes (sort, counter) are correct, aligned, and independently mergeable. But the PR presents a spec clause that documents rather than resolves the #2218 HIGH; best-effort KEA still contradicts ADR-011 convergence doctrine. Resolve spec-first: decide (a) convergent canonical leaf → fail-closed `.await?` (drop the `1+kea_success_count` best-effort counter), or (b) relay-notification-only → not a counted durable leaf.

## SECOND-PASS @ 6a76492ee (fix commit on top of 39a19e90c) — chose option (a) fail-closed. HIGH resolved; NEW MEDIUM (false atomicity)
Coordinator clarified: KEA MUST be fail-closed (RotateContentKeys/RevokeAccess are convergent MLS-ordered triggers per ADR-011). Fix makes execute_revoke + execute_rotate_content_keys KEA loops fail-closed (`.await.map_err(EventLogFailed)?`), removes both §2033 TODOs, spec §5.14.8:2031 now says "KeyEpochAdvance event-log leaf emitted fail-closed per ADR-011 (convergent governance trigger → convergent leaf)", unsubscribe stays best-effort (spec §5.14.8:2035 durability clause: "voluntary unsubscribe … authorization-UPWARD-safe … intentionally coalesced (best-effort) … intentionally NOT part of the fail-closed revocation path").
**VERIFIED SOUND:** ADR-011 citation correct — phase-2.md:907 lists "access"/"membership" in the convergent MLS-commit-ordered stream, :911 "derived record convergent iff trigger convergent". Unsubscribe `+= kea_success_count` (exact under partial failure — tracks only durable appends). Sort test asserts impl not post-hoc. §2033 phantom line-ref (my prior LOW) GONE.
**NEW MEDIUM (governance_helpers.rs execute_revoke ~L1015 + execute_rotate_content_keys ~L3205):** comment "All-or-nothing (fail-closed) so the count is exact" OVERSTATES. Counter bumped ONCE after the loop (`+= 1 + rotated_authors.len()`); event log is append-only, no transaction, rollback FORBIDDEN (§5.14.8:2035). On mid-loop append-IO failure the `?` returns early leaving AccessRevoked/ContentKeysRotated + partial KEA leaves DURABLY appended but counter UN-bumped → checkpoint_events_since under-counts + canonical log partially written. No append dedup exists (verified scp-event-log — lib.rs:827/857 dedup are unrelated tag/serialize sorts). So the comment's "caller retries" recovery re-appends AccessRevoked + prior KEA = DUPLICATE leaves; if NOT retried → permanently-partial KEA set = the §9.9.3 inconsistency fail-closed meant to prevent, relocated to partial-append. REVERSES round-3 #2218 conclusion ("N-append loop cannot be made atomically fail-closed without a transaction") without adding a transaction. IRONY: regresses counter-exactness vs the OLD best-effort code (always exact via kea_success_count) AND vs the unsubscribe path IN THIS SAME PR (keeps kea_success_count). FIX: bump checkpoint_events_since incrementally after EACH successful `.await` (early return then leaves it exact) + pre-encode all payloads before any append (removes encode-failure mid-loop) + drop the "all-or-nothing/exact" comment.
**LOW:** governance-ban KEA fail-closed stated only transitively — 2019 "mandatory KeyEpochAdvance per author", 2031 back-ref "identical to the governance-ban rotation above, and fail-closed"; ban lifecycle step 4 lacks the explicit "fail-closed per ADR-011" that RotateContentKeys got. Add for symmetry.
**LOW/OBS:** unsubscribe best-effort JUSTIFICATION axis — code comment leads with "authorization-UPWARD-safe" (crash-safety/persist-direction), orthogonal to why a LEAF may be omitted (convergence/mandatory-ness). Sound only because spec 2035 SEPARATELY declares KEA "intentionally NOT part of the fail-closed path" (the correct axis). Tension: same unsubscribe path's MemberLeft leaf IS fail-closed w/ §9.9.3-convergence cite (bh.rs). Prefer citing "spec-declared non-mandatory/coalesced leaf" over authorization-direction. Verdict: HIGH resolved correctly (fail-closed IS right for convergence); ship after fixing the atomicity comment + counter-exactness regression.
