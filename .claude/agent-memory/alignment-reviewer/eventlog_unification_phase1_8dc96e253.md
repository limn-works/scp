---
name: eventlog-unification-phase1-8dc96e253
description: Phase 1 event-log unification re-review at HEAD 8dc96e253 (5 commits past 15f674e3) — APPROVE; KAT renumbered to Vectors 32/33; 116 did:key test failures are pre-existing on main, out of scope
metadata:
  type: project
---

# Event-Log Unification Phase 1 re-review @8dc96e253 (2026-06-17) — APPROVE

Supersedes [eventlog_unification_phase1.md](eventlog_unification_phase1.md) (was @15f674e3). HEAD `8dc96e253` is 5 commits past, on `feat/eventlog-unification-phase1`. Diff vs origin/main = +1552/-404 across 12 files; the 6 scp-event-log files are +1305/-47.

**Verdict: APPROVE, 0 blocking, 0 material, 2 informational.** Realizes the *already-merged* ADR-011 native↔WASM amendment (phase-2.md:671-915). ADR untouched by branch (two-dot diff of .docs/adrs/ empty = correct artifact-flow). Only downstream `.docs/specs/25-test-vectors.md` modified.

**Verified faithful:**
1. Variant fidelity: extracted ADR enum (phase-2.md:742-839) vs code (lib.rs:109-389) → IDENTICAL SETS, both exactly 76, `diff` clean. No `Other`/catch-all (the only `(String)` are error variants). Casing `TtlExtended`/`TtlExtensionRejected` correct (lib.rs:306,311).
2. Payload-shape: 8 per-variant positional-rmp structs (payload.rs:82-166). `ContextTombstonedPayload.destination_id` (line 84), NO `destination_context_id`. Positional `rmp_serde::to_vec` (NOT to_vec_named) per doc lines 24-33 — field order is wire contract.
3. Classification (pruning.rs:394 `is_structural_event`): exhaustive match no `_`; 44 structural + 32 operational = 76. Base set matches ADR-030 §2c (phase-6.md:1850-1865): structural base = ContextCreated/MemberJoined/MemberLeft/RoleAssigned/GovernanceAction/ContextClosing/ContextClosed/ContextExpired/MemberBlocked/ConsistencyCheckpoint. NOTE §2c is internally contradictory (lists "Checkpoint" structural AND "ConsistencyCheckpoint" operational); code classifies ConsistencyCheckpoint structural — this is PRE-EXISTING on main (origin/main pruning.rs:384), unchanged by branch. ContentKeysRotated/RecoveryEpochAdvanced=structural, KeyEpochAdvance=operational (deliberate, doc'd lines 384-392). Pinned table test `structural_events_classified_correctly` (pruning.rs:1135) is `[(EventType,bool);76]` + `assert len==76` — PASSES.
4. §25.8 KAT: renumbered to Vectors 32/33 (commit 658e1392e fixed collision with §25.9 pseudonym Vectors 30/31). RAN test_vectors — 8/8 pass; 7 leaf hashes + root byte-match spec (root 0x39e50b87...). vector_33 asserts checkpoint.merkle_root == tree::root (RFC 6962, NOT hash-chain head) + §23.16.1 canonical-hash sig verifies. Runtime-derived then pinned — honest.
5. Two leaf-preimage constructions correctly DISTINGUISHED: (a) Merkle LEAF = SHA-256(0x00 ‖ rmp_serde(Event)) via serialize_event_for_hashing (tree.rs:285) — matches amendment; (b) SIGNATURE canonical = SHA-256("SCP-EVENT-V1:" ‖ event_type_tag_u16 ‖ ...) (tree.rs:363). The u16 tag (tree.rs:390) is in the SIGNATURE preimage only. Tags contiguous 0..=75 (33=EconomicPolicyApplied declared out-of-order but present; base 0-35 preserved, new 36-75). WASM bijection test `canonical_event_type_tag_is_a_closed_bijection` (wasm_conformance.rs:1493) asserts 76 distinct tags onto 0..=75 by calling canonical `event_type_tag` DIRECTLY (verified WASM manager.rs:38 imports `scp_event_log::tree::append_unsigned_event`, Cargo dep line 63 — parity BY CONSTRUCTION, comment accurate).
6. Integration tests (economy/phase2/phase5, -174 net) DELETE hand-mirrored event_type_tag copies, now call `tree::compute_event_canonical_hash`+`tree::append` — the dedup the amendment intends, NOT dropped coverage.
7. payload.rs forward-doc (lines 14-18 "Phase 1 establishes types; production callers land later") ACCURATE: scp-runtime/src has 0 uses of new payload encoders, still emits free-string SCP-EXPORT-ENTRY: (event_log.rs, 5 occ) — runtime cutover + MessageReceived/EquivocationDetected append-site removal correctly DEFERRED to later phase.
8. Cargo.toml: 4 deps (ed25519-dalek/z-base-32/rmp-serde/async-trait) added PURELY to [dev-dependencies] (two-dot diff confirms). Test-only. Clean.

**Informational (non-blocking):**
- INFO-1: `// ...See issue #137.` on a `+` line (wasm_conformance.rs:72) violates feedback_no_issue_refs_in_code. BUT it's a PRE-EXISTING ref (count=1 on origin/main) carried through a reworded comment, INSIDE the `wasm_registry_mirror` block which the review prompt explicitly scoped OUT (Phase-5/6 cleanup, slated for retirement). Branch re-typed it rather than dropping. Minor. (The `{marker:#04x}` match at payload.rs:525 is a hex format specifier — false positive, not an issue ref.)
- INFO-2 (out of scope, NOT a finding against branch): scp-event-log has 116 PRE-EXISTING test failures on main (`unsupported DID format: did:key:...`). Root cause: shared `test_helpers::did_from_pubkey` emits did:key:<hex> which `extract_public_key_from_did` (did:dht-only) rejects. PROVEN pre-existing: ran `cargo test -p scp-event-log --lib` on main repo (695f295ac) = identical 116 failures; test_helpers.rs + extract fn byte-identical to origin/main (two-dot diff empty). Branch adds +13 passing tests (81 pass on branch vs 68 on main), introduces ZERO new failures. Repo should fix the stale helper separately.

**METHODOLOGY LESSON (critical):** `cd /Users/alec/Developer/limn/scp` in a Bash command JUMPS to the MAIN repo (on `main`, different HEAD), NOT the worktree. The Bash tool's DEFAULT cwd is the worktree. Running `cargo test` after `cd <main>` silently tested main's code and hid branch-only tests (payload::tests showed 0). ALWAYS run cargo from the worktree cwd (no `cd`), or use `--manifest-path <worktree>/Cargo.toml`. Caught it when payload round-trip tests (branch-only) didn't appear in --list.
