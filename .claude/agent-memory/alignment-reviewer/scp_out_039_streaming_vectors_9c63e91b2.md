---
name: scp-out-039-streaming-vectors-9c63e91b2
description: SCP-OUT-039 C12 outlet-streaming conformance vectors + SDK receiver gap-detection review (feat/outlet-streaming-ffi, 2026-07-14) — ALIGNED, D3 sound, 2 LOW + 1 obs
metadata:
  type: project
---

SCP-OUT-039 C12 final review @ 9c63e91b2 (feat/outlet-streaming-ffi, base b3a1dd3dc). 7 streaming conformance vectors + gap-detection in all 4 SDK InvocationHandle drains. VERDICT ALIGNED.

**ROUND-2 RE-REVIEW @ b523535c4 ("C12 review fixes") — ALL 3 PRIOR FINDINGS RESOLVED, 0 new misalignment.** (1) §25.21 error_terminal billing note corrected: now "Data(seq0) then terminal Error{terminal:true} at seq1 ... a billable Data PRECEDES the terminal ... NOT the full-refund case" — matches vector exactly (verified: seq0 data, seq1 error 6130). (2) Progress coverage: `{"@type":"progress","pct":50}` interleaved at seq5 in EXISTING multi_chunk (still 7 vectors; 10 Data+1 Progress+1 End=12 descriptors, chunk-count asserts bumped 29→30). Forwarded+cursor asserted at BOTH runtime tiers (conformance.rs multi_chunk_replays_data_progress_then_end + through_open_path multi_chunk_through_open_path, both via assert_transcript_matches renumber-contiguous check) AND all 4 SDK smoke tests (added `any(kind==progress)` + last==end). Billing semantics VERIFIED CORRECT: Progress consumes a sequence slot (renumber contiguous) AND a credit/flow-control slot (try_consume covers Data+Progress, stream.rs:427; credit_window=32 accommodates) but is NOT billed (compute_chunks_billed_ref is Data-only, stream.rs:1440; directly tested by pre-existing chunks_billed_ref_counts_data_only asserting 8 Data+1 Progress+1 End bills 8). SDK non-billing not directly asserted (billing is runtime-only concept) but forwarding+cursor proven (multi_chunk closes Ok ⇒ drain saw contiguous seq across Progress). (3) D3 locus elevated to spec §5.4.5 05-contexts.md:515 receiver-locus paragraph ("receiver bound by this MUST is the invoker-side SDK framework ... InvocationHandle drain"; cross-ctx reassembly reconciled as DiD not replacement) + story references it. credit_exhaustion→credit_stall rename SPEC-FAITHFUL: CODE_EXECUTION_CREDIT_STALL=6133/execution.credit-stall (dedicated, WithBackoff retry) vs CODE_EXECUTION_CREDIT=6131/execution.credit-exhausted (shared w/ stream-gap+stream-cap, Immediate retry) — error_codes.rs:149-155; §25.21 correctly notes distinct 6131 credit-exhausted terminal is uncovered by this set. BONUS clean-ups (not misalignment, consistency IMPROVEMENTS): Swift OutletError.sequenceGap→streamGap aligns to py/ts/kt StreamGap naming + reclassifies case Protocol→Execution-class 6131 (doc fix); GapOutcome{Continue,Cancelled{code}} enum shape harmonized across all 4 receiver trackers (napi was last holdout on Option<&str>) — comment "shared shape" ACCURATE (common.rs pub enum + 3 bridge copies all match). OBS (minor): Progress forwarding rides AC2 "exact chunk sequence" not a standalone AC; non-billing tested by unit tests not the vector itself — both sound, no gap.

**D3 (gap detection in SDK drain now) = RIGHT call.** §5.4.5 "Ordering and gaps" (05-contexts.md:513) "a receiver that observes a gap MUST cancel with Execution::StreamGap" — the SDK drain IS a receiver, spec-faithful. Mirrors slice-2 revocation-recheck posture, VERIFIED in dispatch.rs:456-504: runtime pump = authoritative revocation locus, SDK = defense-in-depth ("Authoritative locus: runtime; SDK is DiD"). Gap follows it, with nuance: same-context CANNOT gap (lossless ordered channel), so no same-context authoritative locus needed; authoritative cross-context detector legitimately lands with slice-3 transport. All 4 SDK drains (py/ts/swift/kotlin) track expected_sequence, raise typed StreamGap/6131, cancel via SAME bridge path as public cancel — REAL detection path, not scripted cancel-ack (satisfies AC6 non-gaming). +1 contiguity assumption is spec-correct (line 513 "missing sequence" = gap; contiguity is the contract). Composes cleanly with slice-3 (idempotent; if transport cancels first SDK never sees gap). NOT premature/misplaced.

**Artifact-flow BOTH legit downstream corrections (NOT code-driving-spec):**
- a357c94f1 §25.2 pubkey d75a...daa3f4a18446b0b8d183f8e3 → d75a...daa62325af021a68f707511a. OLD value was WRONG — new value is the canonical RFC 8032 §7.1 TV1 pubkey (spec cites RFC as authority; old value did NOT derive from stated seed 0x9d61...7f60). Fix flows from RFC (upstream) down to spec. Old value fully purged (0 occurrences repo-wide); §25.13 key-continuity root_key #0 cascade also updated. Legit.
- b3a1dd3dc SCP-OUT-039 story: WASM path crates/scp-ffi/wasm/src/manager.rs (FICTIONAL crate) → crates/scp-client-wasm/src/lib.rs; expanded file list; added machine-verifiable ACs for SDK-drain gap detection + WASM wire-integrity. Operationalizes spec MUSTs (§5.4.5:513) + ADR-034/057 constraint. Legit story maintenance before impl.

**Error codes verified** (error_codes.rs): 6131 CODE_EXECUTION_CREDIT (shared: credit-exhausted + stream-gap + stream-cap-exhausted); 6133 CODE_EXECUTION_CREDIT_STALL (dedicated, round-4 split). credit_exhaustion→6133 NOT 6131 trap correct; sequence_gap→6131 correct. 6134 slot vacated/reserved (round-5 consolidation). monotonic_seq:1 as first grant is CORRECT (cursor starts 0, first grant must exceed). non_streaming vector = §5.4.5:605 "Non-streaming invocation" (Data+End 2-chunk degenerate stream) — spec-faithful, reconciled w/ ADR-061 "unary≠degenerate stream" (605: wire contract always streaming form).

**Findings:** (LOW) §25.21 error_terminal row parenthetical "terminal Error before any billable Data refunds full escrow" INACCURATE — vector emits Data(seq0) THEN Error(seq1 terminal) on an action outlet, so a billable Data precedes the error (contradicts §5.4.5:553 full-refund precondition "before any Data chunk"). Vector's asserted fields (end_status=Error/6130) are correct; only the doc billing note overclaims. (LOW/MED) Progress chunk COVERAGE GAP — no vector exercises `{"@type":"progress"}` ChunkPayload variant (non-billable, non-terminal, consumes sequence) despite story AC "every streaming edge case per §5.4.5". (OBS) D3 locus decision (SDK-drain gap DiD + authoritative detector deferred to slice-3) recorded only in story prose + code comments, not spec/ADR — slice-3 obligation ("production detector replaces the tracker") tracked only by prose. Roadmap-continuity note.
