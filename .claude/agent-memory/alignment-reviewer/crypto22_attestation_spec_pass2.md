---
name: crypto22-attestation-spec-pass2
description: CRYPTO-22 #2187 ephemeral-MLS-leaf + KeyPackage-attestation spec amendment, 2nd-pass alignment re-review @ 20f7a036b — ALIGNED with 1 residual LOW
metadata:
  type: project
---

# CRYPTO-22 / #2187 attestation spec amendment — PASS 2 @ `20f7a036b` (branch crypto-22-attestation-spec, 2026-08-01) — ALIGNED (1 residual LOW)

Amendment replaces the never-implemented **leaf==DID** rule with **ephemeral, context-scoped MLS leaf `signature_key` + separate KeyPackage attestation** (`{did, leaf_sig_key, signing_key_id, issued_at, expires_at}` signed by `#active`/`#agent`, carried as `0xFF03` `scp_keypackage_attestation` LeafNode extension). Human-approved. 8 files: ADR-057 (amendment record), phase-1.md, 00-open-questions, 05-contexts, 09-security-model (normative: §9.7.1 rewrite, §9.5.2 struct, §9.18.2/7 registry, §9.7.4 key-lifecycle), 10-infra, 23-sync, 25-test-vectors (§25.22 Vector 37 KAT).

**Prior verdict was NEEDS-DISCUSSION.** All flagged items CONFIRMED-RESOLVED:
1. **§9.7.4 gap fixed.** §9.7.4 "Key Lifecycle" now has a dedicated **"MLS leaf signature key (Ed25519)"** bullet declaring it ephemeral/context-scoped/`SignatureKeyPair::new()`, DISTINCT from `#0`/`#active`/`#agent`, never a DID VM. Active-Signing-Key entry (:666-667) reworded "Used for MLS credentials" → "Used for MLS KeyPackage attestations (§9.7.1)... does NOT sign the leaf/credential directly." → makes the ADR-057:252 citation "SCP's own key hierarchy (§9.7.4) declares the MLS leaf signature key ephemeral" now TRUE (was overclaim).
2. **3 LOW residues fixed:** §9.12 step 4 → "KeyPackage attestation rotation" (:1229); phase-1.md:106/125 (generate_key_package + credential.rs row) → attestation-signer wording; phase-1.md:459 doc-comment → "MLS KeyPackage attestations".
3. **Faithful, no leaf==DID reintroduced** — repo-wide sweep found the phrase only in explicit negations. No scope creep: Vector 37 / §9.5.2 struct / registry entries are the amendment's declared normative surface (KAT required by completeness baseline), all attestation-related.
4. **context_id removal creates NO downstream friction.** Attestation is deliberately context-agnostic (KeyPackage pre-published offline, scope unknowable at mint); group-scope bound redundantly by `0xFF02` `scp_context_params`. Standing-pair bound-creator check (05-contexts:2186) verifies the attestation-over-leaf against DID doc — does NOT need context_id in attestation. Sweep for "context_id IN attestation" = NONE.

**RESIDUAL LOW (new, not in prior list):** stale doc-comment **"Used for MLS credentials, inner envelope signatures, UCAN issuance"** persists in the ScpIdentity `active_signing_key` doc-comment at TWO downstream mirrors the pass did not sweep: `.docs/prds/main.json:648` (story **SCP-006** `details.scpIdentityStruct`, status=**done**) and production code `crates/scp-identity/src/lib.rs:88-89`. Same text as phase-1.md:459 which the pass DID correct → triplicated comment now internally inconsistent (upstream fixed, downstream story + code stale). Code copy is legitimately deferrable to the #2187 implementation PR (decision record scopes code to follow-on); the main.json story copy should ideally sweep now (artifact-flow: story is downstream of the corrected ADR). Non-blocking.

VERDICT: **ALIGNED** — faithful Option-C (ephemeral-leaf+attestation) encoding, prior gaps closed, one cosmetic downstream doc-comment residue.

## PASS 3 (HARDENED) @ `49ace8b5` (2026-08-02) — CONFIRMED-ALIGNED; same residual LOW persists
Branch reset to single squashed spec-only commit `49ace8b5` (8 files, no crates touched) on top of newer main-tip. Adds BLACK-C22-1 security-fix pass on top of pass-2 model. All 5 asked axes CONFIRMED:
1. **6-field binding** — §9.5.2 table now 6 fields (adds field 3 `leaf_encryption_key`, raw X25519); verifier check 4 enforces `leaf_encryption_key == leaf.encryption_key` (closes stolen-sig-key + attacker-chosen-enc-key HPKE-substitution). **Tier-1 revocation via #active/#agent rotation** = §9.12 new "Leaf-key / MLS-state compromise" scenario; **NO DID-doc watermark** — explicitly "no `attestations_valid_after` watermark or other new DID-document field... tier-2 rejected as over-engineering." No leaf==DID reintroduced (all surviving mentions are negations/rejected-alts in ADR-057). Normative §9.7.1 "KeyPackage signing key" para (leaf sig==#active/#agent) fully REPLACED by attestation model.
2. **§9.12 recovery reuses not forks rotation** — new scenario says "existing operation, not a new mechanism... the same rotation used for active/agent-key compromise above" (rotate_active_key, ADR-003 §4a / ADR-039). Revocation lever = verifiers resolve CURRENT #active/#agent only (check 1), retired key stops resolving → all attestations fail everywhere. Consistent w/ ADR-003/ADR-039 key authority.
3. **84-day cap tied to EXISTING constant, leaf Lifetime untouched** — `MAX_KEYPACKAGE_ATTESTATION_LIFETIME` = 7,261,200s = `KEY_PACKAGE_LIFETIME_MARGIN_SECS`(3600) + `KEY_PACKAGE_LIFETIME_SECS`(7,257,600). VERIFIED real code constant at `crates/scp-mls/src/lifetime.rs:78/85/98` (ADR-057 Prereq-1, mirrors openmls `MAX_LEAF_NODE_LIFETIME_RANGE_SECONDS`). Spec cites it faithfully; check 7 pins attestation window to leaf Lifetime so cap MUST equal that range. Did NOT change leaf Lifetime.
4. **ADR-057 amendment consistent** — 6-field, enc-key binding, current-key-only, positive-whitelist (did:dht:z* AND did:web:* both in scope, testing-gated did:test:/did:key: sole carve-out), resolution-reject, lifetime cap, distinct domain sep (NOT trust-layer SCP-ATTESTATION-V1:). Amendment's 9-item verifier list matches §9.7.1's 9 MUST-checks. Provenance chain ADR-057→§9.7.1→§9.5.2/§9.18.2/§9.18.7→§25.22/§9.12/phase-1/05-contexts coherent. KAT arithmetic self-consistent (preimage 30+26+32+32+11+8+8 = 147B; ext body 26+32+32+11+8+8+64 = 181B). Proper artifact flow: ADR→spec→(future) code; commit records code as follow-on impl PR closing #2187.
5. **No downstream friction** — 05-contexts standing-pair bound-creator check (§2186) rewritten to verify attestation-over-leaf (was "creator-leaf MLS sig key resolves to a VM"), consistent. FFI/join-completion untouched (spec-only); resolve_signing_key production call-site deferred to impl PR, recorded, no contradicting downstream artifact.
**RESIDUAL LOW (UNCHANGED from pass-2, NOT swept):** stale "Used for MLS credentials, inner envelope signatures, UCAN..." doc-comment survives in TWO downstream mirrors — `.docs/prds/main.json:648` (SCP-006 story `scpIdentityStruct` snippet) + `crates/scp-identity/src/lib.rs:89`. phase-1.md:160 upstream WAS corrected this pass (→ "Used for MLS KeyPackage attestations, inner-envelope signatures, UCAN issuance") so triplicated comment now upstream-fixed / 2× downstream-stale. Code copy legit-deferred (commit deliberately spec-only, "no crates touched"). main.json story is a docs artifact downstream of corrected ADR/spec → ideally sweepable in a spec-only pass, but non-normative reference snippet, non-blocking. Same as pass-2 finding; carried, not regressed.

## PASS 4 (8-FIELD, read-as-victim fix) @ `c7773cad` (2026-08-02) — CONFIRMED-ALIGNED; 1 NEW residual LOW + prior residual carried
Branch reset to single squashed spec-only commit `c7773cad` (8 files: ADR-057, phase-1, 00-open-questions, 05-contexts, 09-security-model, 10-infra, 23-sync, 25-test-vectors; NO crates, NO main.json — the crate churn in `git diff origin/main` is base divergence, not this commit). Adds BLACK-C22-HIGH read-as-victim fix on top of the 6-field pass-3 model: attestation now binds **all four** leaf public keys (8 fields: did, leaf_signature_key, leaf_encryption_key, **init_key**, **wrapping_key**, signing_key_id, issued_at, expires_at). All 5 asked axes CONFIRMED:
1. **Faithful.** init_key binding closes read-as-victim-at-join (Welcome's EncryptedGroupSecrets HPKE-sealed to KeyPackage init_key, RFC 9420 §7.1, `init_key != encryption_key`); wrapping_key completes the leaf binding (0xFF01 §9.16 sender-key). No scope creep, no leaf==DID reintro (all mentions negations/rejected-alts), NO DID-doc watermark (tier-2 explicitly rejected §9.12:1252), 84-day cap UNCHANGED (`MAX_KEYPACKAGE_ATTESTATION_LIFETIME` still 7,261,200s).
2. **De-conflation correct per RFC 9420.** §9.7.4 key-lifecycle now enumerates THREE distinct X25519 HPKE roles: ratchet-tree `encryption_key` (§7.2, path secrets), Welcome-seal `init_key` (§7.1, consumed at join), `0xFF01` `wrapping_key` (§9.16 sender-key). Consistent w/ SCP hierarchy + as-built.
3. **Creator-leaf handling honest.** On bare creator/PCS-Update leaf (no KeyPackage) sole HPKE key = encryption_key, so attestation `init_key` field == `leaf_encryption_key` and check 6 (init_key) N/A — documented at §9.5.2 field-4, §9.7.1 three-mint-sites prose, check-6 "critical asymmetry". init_key checked **Add/Welcome time ONLY**, correctly NOT on Commit/Proposal (no init_key on ratchet-tree leaf; attack lands at join). Faithful consequence, not ad-hoc special case.
4. **ADR-057 consistent w/ 8-field; provenance coherent.** Amendment Decision/Verifier/Preimage all 8-field; verifier prose matches §9.7.1's 11-check MUST-list; chain ADR-057→§9.7.1→§9.5.2/§9.18.7/§9.18.2→§25.22/§9.12/phase-1/05-contexts intact.
5. **No new downstream friction.** 05-contexts bound-creator check verifies attestation-over-leaf; join-completion/FFI spec-only; code obligation (#2187 production resolve_signing_key call-site + mint/verify) honestly scoped to follow-on impl PR (ADR-057 amendment last line).
**KAT INDEPENDENTLY VERIFIED byte-exact** (python cryptography): all 4 pubkeys (leaf_sig=RFC8032-tv2 pub, leaf_enc=X25519(sec seed), init_key=X25519(0x11×32), wrapping_key=X25519(0x22×32)), 211-byte preimage, SHA-256 `50cf61db…8957`, Ed25519 sig `fcf01ea5…3509`, 245-byte 0xFF03 body — ALL reproduce exactly. Preimage arithmetic 30+26+32+32+32+32+11+8+8=211 ✓.
**NEW residual LOW:** §9.18.7 constant table (09:1830) has TWO stale check-number cross-refs introduced by THIS pass — inserting wrapping_key (check 5) + init_key (check 6) shifted the window-pin and cap checks +2, but the table still says "(§9.7.1 verifier check 8)" for the lifetime cap (now **check 10**) and "check 7 pins the attestation window" (now **check 9**). Prose-only, verifier-invisible, non-blocking; fix in same PR. **Prior residual LOW CARRIED unchanged** (scp-identity/src/lib.rs:89 + main.json:648 "Used for MLS credentials" — code legit-deferred, main.json spec-downstream snippet untouched by spec-only commit).
