---
name: classs-finalize-adr049-s9-honesty-review
description: ADR-049 §9 Class-S compile-boundary FINALIZE doc-honesty review (branch classs-finalize, HEAD a512ee8e7) — ALIGNED, faithful/non-over-claiming record
metadata:
  type: project
---

# ADR-049 §9 Class-S Finalize — Doc-Honesty Review (2026-06-23, branch classs-finalize HEAD a512ee8e7) — ALIGNED, ZERO findings

Worktree `.claude/worktrees/classs-finalize`. Staged diff = 9 files: ADR-049 §9 prose (+14/-?), ci.yml (-8, removes `class-s-fail-closed` job), class_s.rs (+886), mod/state/context state edits, **DELETES** `scripts/check-class-s-fail-closed.sh` (-4354). This is the FINALIZE round that retires the non-convergent source-text Class-S gate in favor of a `ClassSCell` compile-time boundary + a closed positive-allowlist tripwire test.

**The honesty fix under review:** a prior review found `ClassCMut::role_state_mut()` (class_s.rs:1508) hands out a whole `&mut ContextRoleState` — a DUAL-USE struct carrying the §9 Class-S downward-auth fields `ceiling`/`suspended_capabilities` — reachable from the best-effort path with NO fail-closed persist, contradicting earlier "migration COMPLETE / only &mut to Class-S" claims. Fix SCOPES the compile-time guarantee to the **3 privatized fields** (`PerContextState.class_s`, `GovernanceState.class_s`, `GovernanceState.revoked_spending_ucan_cids` — all `pub(in crate::context)` per state.rs:1250/1267 diff) and DISCLOSES the `ContextRoleState` residual.

**Verified faithful (all 4 task axes):**
1. ADR "Compile-time mutation boundary" bullet (md:165) + "Known residual" sub-bullet (md:166) accurate: boundary IN FORCE for the 3 fields; NOT for ceiling/suspended_capabilities; `role_state_mut` still live; `role_state_class_c_mut` (class_s.rs:1520, returns `RoleStateClassCMut` exposing `ceiling`/`suspended_capabilities` as shared `&` read-only, rest `&mut` — class_s.rs:963/965/975/977) ALREADY EXISTS; "follow-on" deletion. Caller count EXACT: **8 production callers across 4 files** (lifecycle_helpers ×3, tools_helpers ×1, queries_helpers ×3, governance_helpers ×1) — matches ADR "~8 production callers today."
2. Consistent with existing §9: `ClassCMut` holds `class_s: &'a ClassSState` (shared read, class_s.rs:469) + `governance: GovernanceClassCMut` (no class_s reach) + `role_state: &'a mut ContextRoleState` (the whole dual-use struct, class_s.rs:402) — destructure leaves the 3 Class-S fields to coerced shared `&`, role_state stays `&mut`. Inline `role_state_mut` doc (class_s.rs:1497-1510: "Slated for deletion", "DUAL-USE", "§9 bypass", "do NOT add new callers") AGREES with ADR disclosure. Module doc has dedicated "Known residual" section (class_s.rs:161-180, 194-197) — does NOT contradict.
3. NO remaining over-claim. The module-doc "now COMPLETE" (class_s.rs:36) + "ONLY way to mutate Class-S state" (class_s.rs:39) self-scope IN THE SAME SENTENCE to "the Class-S fields are privatized (`PerContextState.class_s` and `GovernanceState.class_s` are `pub(in crate::context)`)" — anchored to the 3 fields, NOT unqualified. "airtight BY CONSTRUCTION" claims (class_s.rs:81-104) are about ClassCMut holding no `&mut` to the 3 Class-S SUB-STRUCTS — true. No-persist-arms para reworded off the deleted gate's `fc=0 be=0 mut=0` vocabulary to "mutate NO Class-S field." Governance-leaf para drops the `CLASS_C_GOVERNANCE_LEAVES` allowlist (now compile-boundary-enforced).
4. Artifact-flow OK: ADR records END STATE honestly (done=3 fields, pending=ContextRoleState pair). Scoping `role_state_mut` deletion as follow-on is LEGITIMATE — task scope was privatize-3-fields + retire-gate; the ContextRoleState dual-use migration is a DISTINCT subsystem touching 8 callers in 4 helper files, and the code ALREADY documented it as pending BEFORE this change (not a new deferral invented to dodge work). The replacement view (`role_state_class_c_mut`/`RoleStateClassCMut`) is BUILT and tested, so this is a wiring-migration remainder, not a stub.

Allowlist test `class_s_no_persist_mutator_whitelist_is_bounded` (class_s.rs:3171) set = `{into_inner, class_c_view, commit_class_c_best_effort, clear_committed_reservation_idempotent, set_generation_for_test, restore_class_s}` matches ADR byte-for-byte; `persist_state_best_effort` correctly EXCLUDED (best-effort ≠ Class-S persist). Gate fully deleted; only surviving `check-class-s-fail-closed` ref is the ADR's own "RETIRED (deleted)" disclosure. `static_assertions::assert_not_impl_any!(ClassSCell: DerefMut)` (class_s.rs:2713) + crate `#![forbid(unsafe_code)]` backstop present.

**LESSON:** a "scope the guarantee honestly" doc-finalize review = (a) cross-check the residual's named code path end-to-end (role_state_mut def + the dual-use struct's field bindings + the restricted replacement's read-vs-mut field split + caller count) — the ADR's "~8 callers / 4 files" must MATCH a real grep; (b) hunt the over-claim by checking whether each "COMPLETE/airtight/ONLY" claim SELF-SCOPES in its own sentence to the privatized set vs. reads unqualified — here every one anchors to the 3 named fields, so no contradiction; (c) confirm the inline code doc and the ADR disclosure use the SAME residual vocabulary ("§9 bypass", "slated for deletion", "dual-use"); (d) for a "retire a gate" finalize, confirm the script is DELETED, its CI job removed, and the only surviving name-reference is the ADR's own retirement note.
