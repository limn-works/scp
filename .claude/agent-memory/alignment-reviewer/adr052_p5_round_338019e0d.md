---
name: adr052-p5-round-338019e0d
description: ADR-052 Phase B-P5 downstream docs review at HEAD 338019e0d (3 docs-only commits past aac9851dc) — ALIGNED, 1 informational citation-precision finding
metadata:
  type: project
---

ADR-052 Phase B-P5 review at HEAD `338019e0d` (branch `feat/p5-downstream-updates`, 2026-06-17). 3 commits past prior verified `aac9851dc`; ALL THREE are guides-only docs commits (HEAD~3..HEAD touches ONLY `.docs/guides/deploying-an-scp-website.md` + `self-hosting-a-website-on-scp.md`). website.rs/main.rs/self_host.rs/README.md BYTE-IDENTICAL to already-verified `aac9851dc` (empty diff) — no re-review of compiled artifacts needed.

**Why:** Round fixes two doc-defect classes: (1) invalid Rust pseudo-code `host_site_until(HostSiteConfig::defaults(Reach::NatTraversal) { … })` — you cannot append a struct-literal body to a fn call — rewritten to honest prose "builds `HostSiteConfig { reach, tls, dht, … }` and calls `host_site_until`" (matches main.rs:719 construction + :736 call EXACTLY); (2) stale line-pins replaced with symbol references.

**Verified against current source (all resolve):**
- `run_self_host` main.rs:719 builds `HostSiteConfig { reach, tls, dht, ... }`, :736 `host_site_until(config,...)` ✓
- `broadcast_routing_id` context/mod.rs:122 ✓; `open_broadcast` + `open_broadcast_trusted` at crypto/sender_keys/broadcast.rs:615/:665 (NOT scp-runtime — short-form "broadcast.rs" = the crypto one) ✓; `broadcast_subscribe` FFI context.rs:4052 ✓; `subscribe_broadcast` broadcast_helpers.rs:58 ✓; `deliver_incoming` messaging_helpers.rs:1002 ✓; pkarr publish :271/resolve :303 ✓
- de-pinned symbols all exist: `fn site_handler` projection.rs:2146, `fn probe_reachability` nat/mod.rs, `fn map_port` nat/upnp.rs, `impl NatStrategy for DefaultNatStrategy` lib.rs:1842, `fn try_tier1_upnp` lib.rs:1782
- new `lib.rs:3193` comment-pin EXACT ("NAT strategy needs the public HTTP port, not the internal relay port")
- Tunnel.public_url deferral note (deploying guide) matches config.rs:136 `// P1: public_url not yet threaded` + `warn_tunnel_public_url_deferred` :684 one-time tracing::warn! called at :769
- validation caveats match `lower_host_site_reach_tls` self_host.rs:914-955 (Acme/Terminated/Custom + Reach::Domain → InvalidConfig) EXACTLY
- "not released on failure" claim TRUE: try_tier1_upnp loop (lib.rs:1800-1834) has no unmap on `reachable==false`, just warns + falls through
- `no_domain()` hits in self-hosting guide (:189/:376/:395) all in dated historical/strikethrough running-log — preserved record, not stale current-API
- `plaintext`/`skip_nat`/`dht_mode` tokens: main.rs `dht_mode` = local var reading SCP_NODE_DHT_MODE env; self_host.rs:659 `plaintext` = field on HostSiteReady (readiness struct) NOT HostSiteConfig; others internal lowered bools/test helpers

**Verdict ALIGNED, 0 blocking, 1 informational:** guide cites `lib.rs:1865` comment as backing "NAT-PMP TCP mapping not released on failure" — but line 1865 is a UDP-probe-socket comment ("the NAT mapping is preserved", about reusing the UDP socket for the self-test). The real evidence for "not released" is the ABSENCE of an unmap call in try_tier1_upnp's loop, not a comment at 1865. Claim is true; citation is imprecise. Also: commit `2c134b339` msg said "replace stale line-pins with symbol references" yet INTRODUCED two fresh line-pins (lib.rs:3193 correct, lib.rs:1865 imprecise) — fresh line-pins re-incur the same drift risk the round set out to eliminate.

**How to apply:** This branch is merge-ready; the lone finding is cosmetic citation precision (swap `lib.rs:1865` for a symbol ref like `fn try_tier1_upnp` or note "absence of unmap in the self-test loop"). REUSABLE: when a docs round's stated goal is "de-pin line numbers," any newly-introduced `file.rs:NNNN` pin is a regression against the round's own intent — flag it even if the line currently resolves.
