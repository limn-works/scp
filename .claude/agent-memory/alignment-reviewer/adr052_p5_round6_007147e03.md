---
name: adr052-p5-round6-007147e03
description: ADR-052 Phase B-P5 downstream docs/examples HostSiteOptions→HostSiteConfig round-6 review at HEAD 007147e03 — ALIGNED, pure prose/comment trim, 0 findings
metadata:
  type: project
---

# ADR-052 Phase B-P5 Round-6 Review — ALIGNED (HEAD 007147e03, 2026-06-16)

Branch `feat/p5-downstream-updates`. Two commits past round-3 base `d5b64fdad`:
- `063ec8a22` docs(examples): trim duplicate inline comments from website.rs
- `007147e03` docs(examples): collapse local-demo knob bullets to one-liner

**Why:** P5 = downstream doc/example migration for the merged P3c `HostSiteOptions`→`HostSiteConfig` rename (#1818). Rounds 1-5 fixed all prose findings; rounds 6's two commits are pure prose/comment trimming (README local-demo section collapsed to a one-liner that defers to the source doc-comment; website.rs inline comments deduplicated). Net +4/-17 LOC, zero semantic change.

**How to apply:** This branch is ready to merge after double-zero. Verification done this round:
- Stale-shape grep (`HostSiteOptions|skip_nat|plaintext:|dht_mode|.plaintext|publish_dht|self_signed:`) across all 4 target files = ZERO.
- Repo-wide `HostSiteOptions` only survives in upstream artifacts (phase-2.md ADR-052 decision text — legitimate historical "fold X into Y") + agent-memory; zero in live downstream code/docs.
- website.rs: 5 imports (`DhtMode, HostSiteConfig, Reach, TlsMode, host_site`) all confirmed public re-exports (lib.rs:49-59). `cargo clippy -p scp-node --example website --features upnp -- -D warnings` = clean.
- Struct/`defaults(Reach)` parity vs self_host.rs:701/:785; all recipe/example field usage matches real fields. Validation caveats (Reach: only NatTraversal/Local/Tunnel valid, Domain=InvalidConfig; TlsMode: only SelfSigned/Plaintext valid, Acme/Terminated/Custom=InvalidConfig) match lower_host_site_reach_tls (self_host.rs:914-958).
- construction.md §179-196 host_site shape conforms.
- README's new "(The source doc-comment explains each choice.)" deferral is accurate — website.rs doc-comment lines 7-15 do explain each of tls/reach/dht plus the 0.0.0.0/LAN nuance and the public-hosting switch.
- All cross-ref links resolve (README→website-site/, →2 guides; deploying guide→example, →background guide).
- README turnkey block `scp-node --self-host --site-dir ./my-site`: both flags valid (main.rs:103, :114).

**Carried-forward informational note (UNCHANGED, NOT a finding):** `Reach::Tunnel{public_url}` is a documented P1 deferral — builder publishes loopback relay URL, ignores public_url, emits one-time warn (config.rs:136, :684-689, :768). Recipes 2/3 stay operationally correct: both forward the operator's tunnel/proxy to the node's loopback listener (Cloudflare `service: https://localhost:8443`; nginx/Caddy `proxy_pass …127.0.0.1:8443`), `dht: DhtMode::Memory`, guide says "the tunnel hostname is the address" — never claims the node publishes public_url. The deferral warning's own guidance ("forward to that loopback listener") = exactly what the recipes do.

Verdict: ALIGNED, 0 blocking, 0 material, 0 new findings. No required fixes.
