---
name: scpout048-pass2-option-a-cancel-removal
description: SCP-OUT-048 round-2 (Option-A browser-cancel removal @ 8ac118069) — ALIGNED; round-3 @ 755ee122c ALL 3 findings RESOLVED, artifact-flow holds
metadata:
  type: project
---

# ROUND-3 CONFIRMING REVIEW @ 755ee122c (delta 8ac118069..755ee122c) — ALIGNED, all round-2 items RESOLVED

Chain 755ee122c→9bed13e8d→8ac118069 confirmed. Delta = 4 files (outlet.json, outlet-stream-session.ts, tests, error_codes.rs); NO .docs/specs (git diff -- .docs/specs/ empty — §5.4.5:515 clarification correctly deferred to #2204 not bundled).
- **[MED] 7027→7029 RESOLVED.** Drain guard now throws SCP-VALID-7029 (outlet-stream-session.ts:427-429). Zero stray 7027 in TS-wasm. 7027 stays governance-only (used solely by napi/discovery.rs:228 = "Governance action validation error" error_codes.rs:809). VALID_7029 registered w/ distinct meaning (re-entrant drain). Coder ALSO registered 7025 (client-not-initialized, client.ts:108), 7026 (managed-transport-to-create, client.ts:254), 7028 (second-live-session, outlet-stream-session.ts:244) — each doc-comment MATCHES its TS usage exactly. check-error-codes.sh PASSES (4249 occ, exit 0). Tests pin 7028 vs 7029 distinctly.
- **[LOW] Citation RESOLVED.** outlet-stream-session.ts:455 now cites 05-contexts.md:485 = the "Credit-based backpressure" para that literally defines stream_credit_stall_secs (default 30) + CreditStall cancel. No stale :530 anywhere. (lib.rs never carried this comment; prompt's lib.rs mention moot, not a gap.)
- **[OBS] Tracking RESOLVED.** outlet.json description + AC[2] now cite #2203 (Option-B cross-context browser cancel, OPEN) + #2204 (§5.4.5:515 remote-receiver carve-out, OPEN). Both issues exist/OPEN. Deferral now system-of-record.
- **New OBS (pre-existing, NOT in delta, non-blocking):** VALID_7010 doc-comment says "UCAN token validation error" but 7010 is the repo-wide GENERIC input/arg-validation code (invalid DID, empty name, malformed bytes, wrong-length seeds, codec errs — used identically across Python/TS/Kotlin/Swift + all Rust bridges + scp-client-wasm). TS-wasm chunk-seq/byte-len uses match de-facto meaning + sibling lib.rs — NOT a genuine same-number/diff-semantics collision like 7027 was. Doc imprecision predates 048, whole-repo scope; not 048's to fix.
VERDICT: ALIGNED. Artifact flow holds (outlet.json downstream annotation only, no upstream reshape). Round-2 MEDIUM+2 LOW all resolved, no regression.

# SCP-OUT-048 PASS-2 — Option-A cancel removal @ 8ac118069 (feat/outlet-xctx-048-wasm-session, 2026-08-02)

**VERDICT: ALIGNED — the Option-A resolution is artifact-flow-correct.** Round-1 (4cdc78a89) I ruled ALIGNED w/ 1 MEDIUM (gap handler surfaced StreamGap/6131 but SKIPPED the §5.4.5:515 signed OutletCancel while a comment FALSELY claimed it cancelled). A DOA-class contradiction was then found: story title/AC[2]/AC[5] required a BROWSER OutletCancel signing predicate, but §5.4.5:547 requires cancel `next_seq` = runtime's live emission cursor ("never a value supplied by the caller") — unreadable by a remote browser — and PRD CRITICAL#3 (outlet.json:2283) + CRITICAL note (:2282) already say cancel is "N/A for WASM". Resolution = **Option A: remove the browser cancel surface entirely, amend the story down to match §5.4.5:547 / ADR-057 / CRITICAL#3.**

## Why the amendment is a legit DOWNSTREAM correction (not code reshaping upstream)
- Story had DRIFTED above its governing artifacts (demanded a browser cancel predicate that §5.4.5:547 makes impossible). Fixing the story to match spec/ADR/PRD-critical is one-way-flow-correct (fix flows DOWN).
- **NO spec file edited** — `git diff main...8ac118069 -- .docs/specs/` is EMPTY. §5.4.5:547 "runtime-derived next_seq" was already written by an earlier slice (SCP-OUT-037). Confirmed point-4.
- **No OTHER AC weakened.** Only cancel-related items changed (title, description, AC[2], AC[5], files, status pending→done, actionItems). AC[0] fence / AC[1] open / AC[3] chunk-sig-verify / AC[4] no-saga-coordination / AC[6] 10-chunk round-trip / AC[7] bun+cargo — UNCHANGED. AC[2] actually STRENGTHENED (adds negative grep: `outlet_stream_sign_cancel|compute_cancel_preimage` must return NOTHING).
- Code deletes: `outlet_stream_sign_cancel` + `outlet_stream_compute_cancel_preimage` wasm fns, TS `outletStreamSignCancel`/`ComputeCancelPreimage`, `session.cancel()`, `NodeStreamCoordinator.cancel` port, `OutletStreamCancel` type, matrix rows + check-sdk-coverage aliases. Matrix-row/alias removal is NOT enforcement-weakening: those rows/aliases were added earlier IN THIS BRANCH (0 occurrences on main) → within-branch correction, never shipped.
- ADR-057:58 fence confirmed verbatim: browser "*participates* (signs its own steps) but does not *coordinate*". Cancel needs the runtime emission cursor (coordination-state only the always-on node holds) → node-delegated is fence-correct.
- **Round-1 MEDIUM RESOLVED:** gap handler (outlet-stream-session.ts:~417 + lib.rs) now HONESTLY states it does NOT sign a cancel, cites §5.4.5:515 + :547 + ADR-057, relies on node-side credit-stall/timeout. No residual false-cancel claim.
- Gap path throws SCP-OUTLET-6131, matching spec:515 exactly (spec:515 literally maps StreamGap→SCP-OUTLET-6131; 6131 is spec-overloaded across credit-exhausted/stream-cap-exhausted/stream-gap — pre-existing, not introduced here).

## FINDINGS
**MEDIUM (newly INTRODUCED by this fix) — SCP-VALID-7027 code-namespace collision.** The concurrent-drain re-entrancy guard was moved from `SCP-OUTLET-6100` → `ValidationError("SCP-VALID-7027")` (outlet-stream-session.ts:387-389). Point-3's CLASS choice (VALID-class for SDK-local re-entrancy, avoiding a spec/§5.4.4-enum edit — OUTLET-6xxx are protocol codes needing spec entries) is CORRECT & one-way-flow-friendly. BUT the NUMBER 7027 is ALREADY canonically assigned in `crates/scp-ffi/common/src/error_codes.rs:809` = "Governance action validation error" (and napi/discovery.rs:228 uses it for discovery-query-format). Same code, unrelated purpose → defeats machine-filterable stable codes. `check-error-codes.sh` Phase-2 does NOT catch it (documented KNOWN LIMITATION: SDK-wrapper TS literals are NOT scanned; "SDK literals must be reviewed manually against error_codes.rs"). Fix: renumber drain guard to a FREE VALID slot — **7029 is free** (7023/7024/7025/7026 used; 7028 = sibling second-live-session guard in same file, clean/unused-in-registry but non-colliding). Register the new number.

**LOW — credit-stall citation line pointer wrong.** Code (outlet-stream-session.ts gap comment + lib.rs) cites reclamation "§5.4.5 `stream_credit_stall_secs`, 05-contexts.md:530". Line 530 is "Cancellation and billing boundary" (cancel-ack timer `stream_cancel_ack_secs`). `stream_credit_stall_secs` is defined at **line 485** ("Credit-based backpressure"). Param name is CORRECT & real; only the line pointer is ~45 off. Repoint to :485.

**LOW/OBS — untracked deferral.** "future cross-context-cancel slice" appears only in prose (description + AC[2]); no story ID exists (grep of prds = 0). Per "Artifacts are system of record" mint a tracking story.

**OBS (point-2) — §5.4.5:515 remote-receiver spec-clarity gap.** :515 literal says gap-receiver "cancels via the signed OutletCancel path AND surfaces StreamGap". A REMOTE browser satisfies only the "surfaces StreamGap" half (can't read runtime cursor per :547). The resolution reads this correctly via :547 + ADR-057 + the spec's own dual-locus reconciliation clause (:515 "becomes load-bearing only ... over a lossy transport ... reconciled ... as defense-in-depth"). A tracked spec-clarification note on :515 (remote receiver surfaces StreamGap + relies on node reclamation; signed-OutletCancel clause is co-located-native-receiver behavior) would remove the apparent divergence. MUST be a deliberate upstream spec edit (separate item) — correctly NOT bundled into this story (one-way flow preserved).

Cross-target KAT strengthened: creditPreimageHex golden pinned in Rust (out048_ts_invoker_fixture_kat.rs) + asserted in TS → closes "sign & compute share one builder so both drift together" blind spot. Credit param objects flattened to named-field options (agent-first, anti-swap-footgun). New SCP-VALID-7028 second-live-session guard (WeakMap liveStreamConsumers) is sound.
