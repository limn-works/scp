---
name: pr2141-postmerge-adr057-e78795e90
description: PR #2141 post-merge (origin/main ADR-057 typed ucan_evaluate/CapabilityValidation superseded the Display-string trust facade) — goals still delivered; 1 SHOULD-FIX sketch.md stale att[0] note
metadata:
  type: project
---

# PR #2141 post-merge HEAD e78795e90 (fix/sdk-coverage-fail-closed-and-parity, /tmp/scp-review-r25, 2026-07-15) — NEEDS DISCUSSION (1 SHOULD-FIX)

Merge 3e8a29707 pulled origin/main (bc4464566: ADR-055 WASM removal + ADR-057 typed `ucan_evaluate`/`CapabilityValidation` trust). The branch's original Display-string trust facade (prefix-classification `_*_PREFIXES`, `_PIPELINE_ABSORBED_CODES`, att[0]-only `ucan_validate`) was FULLY SUPERSEDED and discarded — trust.py + trust.ts are now byte-identical to origin/main (`git diff bc4464566..e78795e90 -- trust.py trust.ts` = empty).

**Goals still delivered:**
- Goal 1 (fail-closed coverage gate): DELIVERED. `python3.12 scripts/check-sdk-coverage.py` PASS 235 ops / 0 errors; self-test test_check_sdk_coverage.py 23 pass. Surviving contribution = private-symbol exclusion in `_extract_python_*` (names starting `_` excluded so ALIASES can't reference private helpers — fail-closed hardening, parity with TS export-keyword requirement). Verified zero python ALIASES value starts with `_` (safe). Fail-closed logic intact (ucan.rs gate: missing-SDK-key→ERROR, empty-matrix→FAIL, unmatched-true→ERROR).
- Bridge/register fix (final commit e78795e90): CORRECT. matrix TS cell false→true + ALIASES `typescript:["bridgeRegister"]`; bridgeRegister IS exported src/index.ts:131 ← src/bridge.ts:79. Prior false-exemption comment was wrong.
- Orphaned test deletion (test_ts_prefix_parity.py): COHERENT. Imported 6 `_*_PREFIXES` constants (`_CAPABILITY_CEILING/_EXPIRY/_NONCE/_REVOCATION/_SIGNATURE_CHAIN/_TOKEN_PARSE_PREFIXES`) deleted by ADR-057 → ImportError at pytest collection. String-prefix parity it guarded no longer exists; typed CapabilityValidation struct enforces parity structurally. Grep confirms zero dangling refs to those constants anywhere.
- Goal 2 (parity) surviving pieces: TS `identity.ts` gains `rotationEventJson` getter (real parity fix — Py/Kotlin/Swift already had it; wired to BridgeIdentityHandle.rotationEventJson bridge.ts:747 ← NAPI native.ts:1661); Python scp.py gains `economy_verify_payment_receipts` (economy parity, delegates to `_scp_core`).
- Spec citation correction `§3.2.1 step 4b` → `§9.12, ADR-003 §4b` across all 4 SDKs + uniffi bridge.rs: CORRECT + IMPORTANT. §3.2.1 = "Key Custody Migration Protocol" (custody swap, NO DID change, NO DidRotationEvent, has no "step 4b"). §9.12 "Compromise Recovery" + ADR-003 §4b governs `migrate_identity` DID-change + DidRotationEvent (spec 09:620 "agent MUST send a DidRotationEvent in every active context"; 09:1168). Old cite was broken provenance; fix accurate.

**SHOULD-FIX (spec-adjacent drift introduced by merge resolution):** `.docs/sketch.md:813-821` `withinCeiling` field carries the branch's stale att[0]-only comment ("Evaluated against att[0] only... treat true as 'att[0] is within ceiling'... dedicated bridge op that does not yet exist... Tracked as SCP-302... See __extractFirstCapabilityUri / _extract_first_capability_uri in trust.py/trust.ts"). Three falsehoods post-merge: (a) references helpers `_extract_first_capability_uri`/`__extractFirstCapabilityUri` that NO LONGER EXIST (grep=0, deleted with the att[0] approach); (b) att[0]-only claim is FALSE vs merged reality — pyo3 bridge ucan.rs:185 authoritatively: "Step 8: **every** granted capability is within the context's ceiling"; trust.py:145-175 caveat is now about intrinsic-mode (capability=None) grant-match SKIP, NOT att[0]-vs-multi-att; (c) SCP-302 is main.json status=**done**, repurposed to "Rebuild trust-signal SDK consumption on structured ucan_evaluate" — NOT a missing multi-att bridge op. origin/main sketch.md:813 is clean bare `withinCeiling: Bool,` — main (correctly) never had this note; merge re-introduced the branch's stale survivor. Fix: drop the att[0] comment block (revert to bare `withinCeiling: Bool,`); KEEP the branch's adjacent additions of `nonceValid`/`timeBoundsValid` (those CORRECTLY expand main's stale 4-field sketch to the real 6-field CapabilityValidation struct). Harm: misinforms future SDK authors into believing withinCeiling is att[0]-narrowed → phantom provenance in the API-surface system-of-record.

No BLOCKER. Merge resolution otherwise coherent with ADR-057.
