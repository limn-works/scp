---
name: pr6c-py-saga-sdk-wrapper-1939
description: Alignment review of PR-6c Python slice (#1939) — SCP.tool_invoke_cross_context_saga SDK wrapper over the PR-6b PyO3 bridge export; ALIGNED, 0 blocking, 1 non-blocking
metadata:
  type: project
---

# PR-6c slice PY (#1939) Python SDK wrapper for §6.2.4 xctx-tool saga @ HEAD `a66f68f13` (base origin/main, branch feat/1939-pr6c-saga-sdk-python) — ALIGNED, 0 blocking, 1 NON-BLOCKING observation

**Fact:** Reviewed the Python SDK wrapper `SCP.tool_invoke_cross_context_saga` (async, scp.py:2042) wrapping the PR-6b PyO3 bridge export `tool_invoke_cross_context_saga` (#1950, landed on main @050b05ba7). Scope = python-only (8 files; ZERO crates/ touched).

**Why aligned (verified against the bridge source, not the matrix prose):**
- Param order/names EXACT match to bridge signature `crates/scp-ffi/src/tools.rs:1929-1953` (caller_context_id, target_context_id, caller_did, tool_registration_id, input, asserted_nonce_hex, timestamp_ms, chain_depth, ucan_proof_id=None). Wrapper forwards positionally in that order (test `test_native_called_with_forwarded_arguments` locks it).
- `SagaResult` (tools.py frozen dataclass + .pyi PySagaResult) = faithful pass-through of bridge `PySagaResult{saga_id:String, receipt:Option<Vec<u8>>, output:Option<Vec<u8>>}` (tools.rs:163-178). receipt/output `None` surfaced verbatim, never synthesized (test asserts).
- Typed terminal translation `_saga_terminal_from_bridge` (errors.py) dispatches on bridge exception CLASS NAME (so a mocked bridge works) + reads datum STRUCTURALLY off `exc.args[2]` (args[0]=msg, args[1]=code, args[2]=datum) — EXACTLY the bridge's pack order `crates/scp-ffi/src/error.rs:362-374` (`SagaAbortedError::new_err((formatted, code, retry_after_ms))` etc). NEVER message-parses. 3 SDK classes SagaAbortedError/SagaNeedsRepairError/SagaBusyError subclass ToolError (errors.py); bridge classes subclass ScpError — same name, different identity, bridged by name-dispatch. Re-raises SDK class `from exc`.
- retry None-never-0: bridge maps Option None→Python None (error.rs:359 comment); wrapper `datum if isinstance(int) and not isinstance(bool) else None`; test `test_abort_without_backoff_preserves_none` asserts None survives.
- SagaId output-only: wrapper has NO saga_id input param; only on SagaResult/NeedsRepair output. Block-until-terminal: bridge call is synchronous-blocking, wrapper wraps in `asyncio.to_thread`. Caller-principal binding is the bridge's (enforce_caller_principal_binding) — SDK just forwards, no auth logic. ALL 5 contract axes hold.
- chain_depth(0-255 u8)/timestamp_ms(non-neg u64) validation MIRRORS the sync `tool_invoke_cross_context` wrapper (scp.py:1999, identical SCP-VALID-7002 + bool/float reject) — consistent, idiomatic, matches bridge u8/u64 boundaries. Non-saga bridge exceptions re-raised unchanged (test `test_unrelated_exception_reraised` asserts `is sentinel`).

**Per-slice matrix approach CORRECT (not false completion):** flips ONLY `Tools/invoke_cross_context_saga` python false→true + REMOVES the python exemption; ts/kotlin/swift stay false WITH exemptions still citing #1939. Notes honestly say "Python SDK wrapper is live (PR-6c slice PY); the remaining per-SDK wrapper methods are tracked by #1939". #1939's acceptance ("all 4 cells flip") is NOT claimed met — commit msg uses `(#1939 PR-6c)` reference, NOT a closing keyword. check-sdk-coverage.py alias added for all 4 SDKs (inert for the 3 false+exempt cells). Matches [[lesson_capability_matrix_exemptions_required]] (false cells carry ADR/§/SCP exemptions) + [[feedback_deferred_items_file_issue_or_do_it]] (filed OPEN issue tracks remaining slices).

**No phantom provenance / no scope creep / no issue-leaks:** zero `#NNNN` in python/script SOURCE (grep clean); #1939 only in matrix notes+exemptions (permitted) and commit msg. No Rust touched. Tests: 19 new (happy/null-passthrough/forward-args/3 terminal translations/passthrough/chain_depth±/timestamp±), asyncio_mode=auto, all module imports present.

**NON-BLOCKING OBSERVATION:** `SagaAbortedError._default_code = "SCP-SAGA-13050"` (errors.py). 13050 is the SPECIFIC caller-membership-reject code; SagaAborted is polymorphic across codes (13050 Rejected, 13067 generic, rate-limit). The default is only reached if the bridge omits a code (unreachable today — bridge always supplies args[1]), but defaulting an unknown-code abort to the specific membership-reject 13050 fabricates a specific authz meaning — the SAME phantom-authz anti-pattern that [[saga-error-13067-generic-abort-14e6128e]] introduced 13067 to avoid. Recommend default 13067 (generic terminal abort) instead. NeedsRepair(13065)/Busy(13066) defaults are correct (those classes are monomorphic in code). Latent/cosmetic — not blocking.

**LESSON:** SDK-wrapper-over-native-bridge slice review → verify against the BRIDGE SOURCE not the matrix prose: param order EXACT (read the `#[pyo3(signature=...)]`), result type field-for-field, typed-error translation reads datum off the bridge's `new_err((msg,code,datum))` pack-order STRUCTURALLY (args[2]) never message-parse, retry None-never-0 both sides, validation mirrors the sibling sync wrapper. Per-slice matrix flip is HONEST when it flips ONE cell, removes ONLY that exemption, keeps the rest false+exempt citing the SAME open issue, and the commit does NOT use a closing keyword. Polymorphic typed-error default-code = check it isn't a specific code masquerading as the catch-all (13067 lesson recurs at the SDK layer).
