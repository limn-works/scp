---
name: adr055-cut-wasm-slice5-strayrefs
description: ADR-055 Cut-WASM Slice 5 (stray code-level refs) review @ 23653e2b5+d9d8b8504 on chore/cut-wasm-stray-refs — GO-WITH-CHANGES, 1 residual-scrub finding
metadata:
  type: project
---

ADR-055 Cut-WASM Slice 5 (stray code refs) review, worktree cut-wasm-5, branch chore/cut-wasm-stray-refs, 2 commits (23653e2b5 stray refs + d9d8b8504 dead WasmLocalRetention variant). 28 files +113/-751.

**Verdict: GO-WITH-CHANGES (ALIGNED on declared scope; 1 LOW residual-scrub finding).** Same Slice-4 lesson recurs (see [[adr055_cut_wasm_slice4_docs_bed2ea08]]): a "scrub X everywhere" final slice must re-grep the WHOLE corpus.

**Convergence invariant SURVIVED (CRITICAL check 1 = PASS).** All 12 doc-comment reframes in scp-event-log/{payload,system_actors,tree}, scp-protocol/{trust/consequence, context/export}, scp-runtime/{export_import, lifecycle_logic, providers/event_log} KEEP the byte-identical-leaf / §9.9.3 equivocation-detection invariant and only swap "native + WASM bridges produce byte-identical leaves across bridges" framing → "all honest members / native runtime". NOT gutted. Factually CORRECT: spec §07:126 confirms "All honest members therefore hold byte-identical leaf preimages for the same event" — the architecture genuinely has multiple honest members minting leaves (not a single producer), so "all honest members" reframe is accurate, not a false single-producer simplification. system_actors.rs correctly dropped the ADR-034 shared-dep-list paragraph (ADR-034 superseded by ADR-055). consequence.rs ConsequenceDispatcher trait reframe correctly drops the WASM impl bullet but keeps the trait (native still implements it).

**4 must-remain refs ALL INTACT (check 4 = PASS):** scp-protocol wasm32 compat (Cargo.toml cfg target_arch), webtransport-wasm transport (scp-transport intact), check-call-invariants.py:115 historical comment (present, correctly frames removed wasm extern-C files as historical/empty), and zero new stray crate-path refs introduced.

**WasmLocalRetention deletion CLEAN (check 5 = PASS):** zero consumers outside wasm/ (grep), zero refs in .docs/ (no spec/ADR requires it), only PreRotationCustodyKind:: match sites use ::InMemory (no exhaustive-match break). cargo check -p scp-platform/-p scp-event-log/-p scp-protocol all compile clean. §9.7.4.1 spec defines 6 methods + Callback only — WasmLocalRetention was bridge-only degraded mode, correctly gone.

**Gate/fixture changes CORRECT:** check-no-ts-mutable-globals.sh removed _wasmModule/_initPromise whitelist entries (internal/wasm.ts GONE, zero refs) — removing whitelist entries STRENGTHENS the gate (legitimate direction). check-bridge-symmetry.sh only comment 4→3 (script logic already wasm-free in earlier slice; pyo3/uniffi/napi only). 6 fixture dirs: wasm widgets.rs deleted + wasm_required/wasm keys + wasm exemption arrays dropped from bridge-aliases.json. Real (non-fixture) bridge-aliases.json has NO wasm key; script READS no wasm key. Fixture test harness `scripts/tests/bridge-symmetry/run-tests.sh` 6/6 PASS. browser-demo dir GONE, no dangling refs.

**FINDING (LOW, residual-scrub, pre-existing — NOT introduced by this branch):** ~18 files carry stale "all four FFI bridges" / "(PyO3/NAPI/UniFFI/WASM)" prose comments now factually wrong (3 FFI bridges post-cut). Examples: scp-ffi/src/error.rs:581 "all four bridges (PyO3/NAPI/UniFFI/WASM) stay in lockstep"; scp-protocol/src/envelope/inner/mod.rs:479 "FFI bridges (PyO3, NAPI, UniFFI, WASM)"; scp-ffi/common/src/{transport_status,context_id,validate,lib,error_codes,tool_id,ucan_errors}.rs "four FFI bridges"/names wasm-bindgen; scp-runtime/{crypto/mls/production_backend.rs:73, context/queries_helpers.rs:3}; check-no-bridge-globals.sh:8 "four FFI bridges" (BUT its BRIDGES=() array is functionally correct: pyo3/napi/uniffi/common, no wasm); bindings/python/tests/bridge_parity/seed_operations.py several. NO functional impact (all are comments/docstrings; gates+arrays already 3-bridge). Fix: re-grep whole corpus for `four.*bridge|PyO3.*WASM|UniFFI.*wasm-bindgen` and reword to three. This is the declared-scope boundary: Slice 5 targeted crate-PATH refs (`scp-ffi-wasm`/`crates/scp-ffi/wasm`) which it scrubbed 100% — these generic "four bridges" prose refs are a separate residual class.

LESSON (reinforces Slice-4): "cut X" multi-slice work leaves TWO residual classes — (1) crate/path refs (caught here), (2) generic count-prose ("four bridges", "all four"). The path-grep that drives the slice misses class (2). Final scrub MUST grep the count-noun too, not just the deleted symbol/path.
