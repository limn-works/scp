---
name: crypto22-slice1-attestation-type
description: CRYPTO-22 slice 1 (scp-mls KeyPackageAttestation type+serialization+Vector 37) alignment review — ALIGNED, code faithfully implements merged spec
metadata:
  type: project
---

# CRYPTO-22 slice 1 — scp-mls KeyPackageAttestation type @ caeafe724 (crypto22-s1-attestation-type) — ALIGNED

First CODE slice implementing the CRYPTO-22 attestation SPEC I reviewed across rounds 1-5 + merge-gate (see [crypto22_attestation_spec_pass2.md] and MEMORY entries). Spec merged to main as #2213/d5de8b153. Slice 1 of ~8 for GH #2187.

**Why:** closes leaf==DID impersonation gap; SCP MLS leaf sig-key is ephemeral/context-scoped (§9.7.4), so a `#active`/`#agent`-signed KeyPackageAttestation binds {did, leaf_signature_key, leaf_encryption_key, init_key, wrapping_key, signing_key_id, issued_at, expires_at} = 8 fields (+64B sig in ext body) to the DID.

**How to apply (verified facts for later slices):**
- Diff = 4 files only (Cargo.toml +sha2, lib.rs re-exports, keypackage_attestation.rs 702L, Cargo.lock). ZERO .docs/scripts/json edits → artifact-flow CLEAN (code implements already-merged spec, does not reshape it).
- KAT: signing_preimage (211B), signing_hash (SHA256 = 50cf61db…), ext body (245B) ALL reproduce §25.23 Vector 37 (25-test-vectors.md:973) byte-for-byte. did len 22→BE32(0x16); issued 0x6553f100; expires 0x65554280; domain "SCP-KEYPACKAGE-ATTESTATION-V1:" = 30B no-prefix. Verified independently.
- Field order/encoding matches §9.5.2 table exactly (4B-BE len-prefix on did+signing_key_id; 4×raw-32 keys no prefix; 2×BE-u64 ts). Domain in preimage only, absent from ext body per §9.5.2.
- **MAX_KEYPACKAGE_ATTESTATION_LIFETIME = alias of lifetime::KEY_PACKAGE_LIFETIME_MAX_RANGE_SECS (7,261,200 = 3600 margin + 7,257,600 84d) is NOT an over-reading.** §9.18.7 (09-security-model.md:1910) EXPLICITLY mandates "the attestation's max range MUST equal that leaf-Lifetime max range" and names the very constant. Single-source alias + `const _: assert!(==7_261_200)` compile-time drift guard is the correct, exemplary encoding. §9.7.1 check 11/12 confirm.
- Scope discipline CORRECT: stops at type+serialization. NO signer (sig stored opaque [u8;64]), NO verifier (checks 1-13 unimplemented), NO wiring (scp_capabilities_with_keypackage_attestation() is additive, docstring says does NOT change call sites). AttestationTrigger enum + MAX_ATTESTATION_KEY_RESOLUTION_STALENESS(300) are forward-looking pub decls transcribed straight from §9.7.1 trigger / §9.18.7 — co-located, not stubs, don't foreclose later verifier design.
- Strict parser rejects trunc/trailing/oversized-len/non-UTF8/non-{#active,#agent} via typed MlsError (no panic). SigningKeyId is 2-variant (Active/Agent) so parse-to-enum enforces spec field-6 "never #0" at type level — right layer, not a premature verifier check.
- Only OBSERVATIONS (non-blocking): scp_capabilities_with_keypackage_attestation() defined+tested but uncalled until a wiring slice (pub, so no dead_code lint). Forward-looking helpers/constants unused in slice 1 = acceptable co-location.

## Pass A re-review after fix pass @ d39636213 — CONFIRMED ALIGNED, no drift
Fix diff caeafe724..d39636213 = single file (keypackage_attestation.rs, +138/-44). Artifact-flow STILL clean (`git diff origin/main...d39636213 -- .docs scripts '*.json'` EMPTY). THE FIX: (1) bespoke `write_canonical_fields` hand-encoder REPLACED by shared `scp_protocol::crypto::canonical::canonical_hash_bytes(domain, &canonical_fields())` — arguably MORE spec-faithful: canonical.rs docstring is literally "Every signed structure in the protocol uses this construction (§9.5.1)"; slice now uses the shared §9.5.1 builder instead of a reimplementation. BYTE-EQUIVALENCE by construction verified field-by-field: did/skid→VarBytes(4B-BE len+bytes)≡old; 4 keys→Fixed32(raw32)≡old; 2 ts→U64(8B-BE)≡old; signing_preimage passes DOMAIN, to_extension_body passes b"" (prepends nothing) then appends sig ≡ old. All 3 Vector-37 KATs INTACT & unchanged (211B preimage, hash 50cf61db…, 245B body) — pin exact hex, fail on any divergence. (2) signing_preimage pub→pub(crate): SAFE narrowing (grep: zero external callers of KeyPackageAttestation::signing_preimage; the cross_context_saga.rs hits are a DIFFERENT type's method). Tightens public surface to only signing_hash (still pub), matching new doc "only signable output is signing_hash". write_canonical_fields fully removed (grep=0). (3) +3 negative parser tests (non-UTF8 did, non-UTF8 skid, oversized skid len-prefix) via build_body helper = additive no-panic robustness. Removed trivial `attestation_trigger_variants_distinct` test (asserted a compiler-guaranteed tautology; AttestationTrigger enum itself preserved line 117) = zero alignment impact. Scope STILL held — no signer/verifier/wiring crept in. unwrap_or_default() on the builder is total for these bounded fields (documented honestly). ZERO findings; Pass A of double-zero clean.
