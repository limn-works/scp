---
name: saga-error-okarm-double-charge-b6a7b49d0
description: PR-6b-0 typed SagaError @ b6a7b49d0 — SIBLING of 408c3079b (same base e406c15c5), adds a SECOND symmetric Ok-arm double-charge fix; ALIGNED 0 findings
metadata:
  type: project
---

# Typed SagaError + Ok-arm double-charge fix @ `b6a7b49d0` (PR-6b-0; base e406c15c5; SIBLING of 408c3079b, NOT ancestor) — ALIGNED, ship, 0 findings, 1 OBSERVATION

range-diff: b6a7b49d0 does NOT pair with prior-reviewed [[saga-error-escrow-ordering-408c3079b]] (range-diff shows `<`/no-`=`). Direct `git diff 408c3079b b6a7b49d0` = ONE file (supervisor.rs +141/-11). The full typed-SagaError change (variant set, RateLimited Option<u64>, 13065/66/67 rows, Err-arm escrow-ordering fix) is IDENTICAL to the lineage; **b6a7b49d0 ADDS a SECOND, SYMMETRIC fix the prior siblings lacked.**

**THE NEW DELTA = Ok-arm double-charge fix.** New helper `resolve_committed_or_needs_repair` (supervisor.rs:6727, called from run_saga_fsm Ok arm @6870; old inline mark_resolved at the Ok arm DELETED). Scenario: dual-commit FULLY succeeded (B executed+charged, A settled, escrow already consumed → `ctx.prepared_a`==None) but the TERMINAL `mark_resolved(Committed)` journal write FAILS. Pre-fix: Ok arm propagated bare Err with `reached_needs_repair==false` → lift → `Aborted{Rejected,13067}` (prefix-less InvalidState "saga journal mark_resolved:" → 13067 fallback). `Aborted`'s contract = "NEITHER side committed" → contract-honoring caller RETRIES with fresh SagaId → tool EXECUTES+CHARGES a SECOND time. Fix: helper sets `ctx.reached_needs_repair=true` BEFORE returning Err → lift short-circuits to `NeedsRepair{saga_id}` ("do NOT blindly retry"). +`record_saga_repair_needed()` metric (§17.16.4). Regression test `lift_run_saga_error_mark_resolved_failure_is_needs_repair_not_aborted` @16782 asserts BOTH directions (needs_repair=false→Aborted{Rejected,13067}; true→NeedsRepair{saga_id}) over the EXACT message — real `lift_run_saga_error` calls, not string-game.

**SELF-HEAL VERIFIED (the load-bearing alignment fact).** On mark_resolved-fail the journal is left at `Committing` (append_journal(Committing)@6858 ran BEFORE commit; mark_resolved is what would make it terminal — `Committing.is_terminal()==false` @saga_journal.rs:1376). Spec §6.2.4:289 "Commit-in-progress journal ⇒ re-send Commit to B and A, idempotent by SagaId, never re-invoking the tool". `recover_committing_entry` EXISTS @supervisor.rs:6294, dispatched from `SagaState::Committing` recovery arm @6035, reconciles to Committed (redrive_xctx_commit_in_progress→mark_resolved(Committed)) or NeedsRepair. Rustdoc ref @6711/16777 is LIVE not dead. So NeedsRepair (conservative terminal handing back saga_id) is exactly correct: durable journal self-heals on next open. lift precedence CORRECT: `needs_repair` checked FIRST (5605) before abort-code classification → flag wins over the 13067 fallback.

**SPEC GROUNDING:** §6.2.4:319 Committed⇒both records/Aborted⇒neither; :337 "auto-voiding a possibly-partially-committed escrow on NeedsRepair would itself be a free-execution exploit" (quoted in Err-arm comment); :335 NeedsRepair escrow NOT auto-voided (held for operator repair); :289 Committing recovery. ADR-049 §3a:90 terminal map (Committed⇒Ok, Aborted{RateLimited/Rejected}, NeedsRepair carries SagaId distinct from Aborted) 1:1. §3a:81 FFI export sequenced AFTER per-set gating (landed) → core-only prereq slice is correct ordering.

**COMPLETENESS:** SagaError derives Debug,Clone,PartialEq,Eq,thiserror::Error (350). All 6 prod RateLimited ctor sites populate retry_after_ms (saga.rs:698/713/808=Some(secs*1000); lifecycle/messaging/tools=None) + 2 test sites; destructures use `..` or read field (non-breaking). NO RateLimited ctor in FFI/SDK layers (field addition contained in protocol+runtime, no cross-layer break). NO FFI/bindings files touched (correct — #120/#121/#122 own the export/wrappers, legitimately separable). NO #NNNN in source. check-error-codes.sh covers 13000-13999 (line 73); 13050/65/66/67 all in band. Producer `start_cross_context_tool_invocation_saga` only called from `mod tests` (no prod caller yet, ADR-049:65). OBSERVATION: full live two-actor dual-commit + journal-fault E2E test deferred (same prohibitive infra as the Err-arm test) — honest unreachability, recovery has dedicated tests; consistent with project rule preferring honest deferral over a faked harness.

**LESSON:** when a resubmit is a SIBLING (not successor) of an already-reviewed commit, `git diff <prior> <head>` directly — range-diff `<`/no-`=` means materially different. A "terminal marker write fails AFTER both sides committed" bug is the SYMMETRIC twin of the commit-exhausted Err-arm bug: BOTH must classify as NeedsRepair-not-Aborted to block a double-charging retry, BOTH set the flag before the fallible journal op, BOTH rely on `Committing`-recovery self-heal. Verify the recovery fn the rustdoc names ACTUALLY exists + is dispatched from the matching non-terminal state arm — that's what makes "NeedsRepair, journal self-heals" true rather than wishful.
