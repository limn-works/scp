---
name: pr2144-error-code-reconciliation
description: #2144 wasm-vs-native error-code reconciliation (fix/2144-error-code-reconcile) — ledger-first artifact-flow correct, but browser 7018/7019 collide with Swift governance CSP codes; VALID band has an un-ledgered SDK-side validator namespace
metadata:
  type: project
---

# #2144 SCP error-code reconciliation review (fix/2144-error-code-reconcile, 3 commits, base d173160cc)

Reconciles browser participant (`crates/scp-client-wasm/src/error.rs`) error codes vs the native FFI-common registry (`crates/scp-ffi/common/src/error_codes.rs`) into a new sdk-common.md ledger section "Registered SCP-CTX-/CRYPTO-/TRANS-/VALID- codes". Browser moved OFF native-colliding codes: Mls(_) 4010→4041, Codec 7010→7018, ChannelContentMismatch 7011→7019, UnknownContext 2001→2077, UnsupportedMembershipChange 2003→2078, Driver 2004→2079, NoPendingJoinMaterial 2005→2080, Transport 5010→5005; KEPT two intentional shared-meaning reuses ContextAlreadyExists→CTX-2003 and PseudonymRegistryEmpty→CTX-2095 (both genuinely semantically identical to native — verified error_codes.rs:281/438).

**ALIGNED dimensions:** commit order ledger(46c5dbf07)→code(3241ef2e0)→docs(d222e27df) = artifact-flow correct, ledger treated as source of truth. New section mirrors STORAGE 8010-8013 pattern exactly (Code|Owner|Condition, multi-owner col, "documentation not enforcement" disclaimer). Treating sdk-common.md canonical vs error_codes.rs=native-materialization is consistent (STORAGE 8010-8013 confirmed ABSENT from error_codes.rs, browser-owned ledger-only). wasm-fence rationale factually correct: scp-client-wasm Cargo.toml has NO scp_ffi_common dep (ADR-057/ADR-034 fence holds). Exhaustive no-wildcard allowlist test in error.rs = sound bounded positive whitelist (good, not a denylist).

**MATERIAL FINDING (under-scoped surface enumeration):** browser's newly-chosen SCP-VALID-7018 and 7019 are ALREADY assigned by the Swift SDK — `bindings/swift/Sources/SCP/Governance.swift:1161` (7018 = CSP "data:" source) and :1166 (7019 = CSP "blob:" source). The reconciliation only checked wasm-vs-error_codes.rs (where 7018/7019 are free — native VALID jumps 7017→7020), never the hand-written SDK-side governance-validation VALID band. That band (Swift Governance.swift 7010-7024, Kotlin Types.kt 7010-7012, TS-native context.ts/outlets.ts 7010-7012) is a THIRD un-ledgered code-assigner overlapping the native UCAN band (7010 UCAN-token vs "outlet name empty"; 7016/7017/7020/7021/7022 all pre-existing native-vs-SDK collisions). So the ledger's universal claim "one meaning across ALL surfaces" + STORAGE precedent "never reuse ... even across languages" is FALSIFIED on arrival by Governance.swift 7018/7019 = the exact #2144 defect class relocated, not eliminated. Collision-free fix exists: 7028/7029 are free across native+Swift+Kotlin+ts-wasm.

**SECONDARY:** ledger VALID rows for 7010/7011 label them "native FFI-common registry ... NOT emitted by scp-client-wasm" but omit that TS-native/Swift/Kotlin SDK validators emit 7010/7011/7012 with input-validation meaning (outlet name / mime / deploy_id) — within the ledger's OWN "feeds TS ScpError hierarchy" scope (bindings/typescript/src/outlets.ts:59, context.ts:184). Per-code registration incomplete even in-scope.

Note: ledger is documentation-not-enforcement, and nothing mechanically checks cross-surface collisions, so the 7018/7019 Swift collision would not be caught by CI. Verdict given: NEEDS DISCUSSION.

## ROUND-2 @ 457487275 (rebased onto origin/main, merge-base 7e05ed15e) — ALIGNED (Round-1 MATERIAL fully resolved)
Coder recomputed the full 5-surface `SCP-<band>-<n>` union and reassigned every browser code to a genuinely-free number. VERIFIED by my own greps on the rebased tree:
- New browser codes CTX-2082/2083/2084/2085/2086, CRYPTO-4020/4030/4040/4041, TRANS-5005, VALID-7028/7029, and SDK-wrapper VALID-7025/7026 appear ONLY in the wasm surfaces (crates/scp-client-wasm, crates/scp-client, bindings/typescript-wasm). Repo-wide rg (all langs, excl markdown) = ZERO occurrences on native error_codes.rs / bindings/swift / bindings/kotlin / bindings/typescript/src / python. No collision anywhere.
- 7018/7019 (the Round-1 Swift CSP collision) fully vacated — Codec now 7028, ChannelContentMismatch now 7029.
- CTX-2003 reuse correctly DROPPED: confirmed 3-way overloaded (native error_codes.rs:281 "already exists", Swift Context.swift:506/520 "message stream already active", Kotlin ContextConformanceTest.kt:145 "not a member"); ContextAlreadyExists minted fresh at CTX-2083. error.rs test asserts `!= 2003`.
- CTX-2095 is the ONLY reuse, genuinely same-meaning (pseudonym-registry-empty) on all 5 surfaces (error_codes.rs:438 + Swift/Kotlin/ts-native + scp-client-wasm PseudonymRegistryEmpty→2095). Pinned by positive-allowlist test INTENTIONAL_CROSS_SURFACE_REUSES=["SCP-CTX-2095"] (bounded, exhaustive vs reconciled_code).
- 2086 rationale honest: 2080/2081 taken by Kotlin (ServerTest.kt:118/142), so NoPendingJoinMaterial sits at 2086.
- Ledger HONEST: explicitly says "NOT a claim that every SCP code has one meaning across all surfaces"; documents pre-existing overlaps 7010/7011/7012 (verified: native UCAN + Swift mimetype/deploy_id Governance.swift + Kotlin Types.kt + ts-native outlets.ts/context.ts) and CTX-2003 as out-of-#2144-scope. No false invariant.
- Artifact-flow ledger-first: commit order 444aec407(docs/ledger)→b27321e98(code)→457487275(docs/tests). Scope clean: native/swift/kotlin/ts-native UNTOUCHED (no attempt at the pre-existing overlaps). scp-client/error.rs change = doc-comment corrections of stale colliding codes (7011→7029, 5010→5005, 2040→2095) to match reconciled mapping.
- LOW (non-blocking): task said a discussion #2208 tracks the systemic cross-language reconciliation; the ledger defers it as "a separate, unaddressed concern" but does NOT cite #2208 (absent from ledger + all commit msgs). Honesty holds (load-bearing requirement met), but per "artifacts are the system of record," adding the #2208 pointer would close provenance on the deferred work.
