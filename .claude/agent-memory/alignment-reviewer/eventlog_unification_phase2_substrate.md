---
name: eventlog-unification-phase2-substrate
description: ADR-011-amendment/ADR-050 event-log unification Phase-2 substrate review at dc18f5899 — APPROVE w/ 2 minor; stale chain-head comment + #710 issue-refs in source
metadata:
  type: project
---

Reviewed branch `feat/eventlog-unification-phase2-substrate` @ `dc18f5899` (2-dot diff vs Phase-1 base `1c0ccbc7d`, +2113/-1777 across 46 files). Governing artifacts: ADR-011 Amendment (native↔WASM event-log unification, `.docs/adrs/phase-2.md:862-920`), ADR-050 (`.docs/adrs/ADR-050-signed-context-export.md:65,77`).

**Verdict: APPROVE** — 6/6 fidelity items pass; 2 minor non-blocking findings.

**Why:** The work fully migrates `scp-runtime` off the free-form-string `"SCP-EXPORT-ENTRY:"` hash-CHAIN onto the canonical RFC 6962 `scp_event_log::tree` substrate shared with WASM/FFI. `git grep -c "SCP-EXPORT-ENTRY\|compute_entry_hash\|EventLogEntry" crates/scp-runtime/src` == 0 (chain gone). Runtime provider (`providers/event_log.rs`) now delegates append→`tree::append_unsigned_event`, root→`tree::root`; export binding `event_log_merkle_root` = `tree::root` (export_import.rs:625/280). Exactly the 2 ADR exclusions (`MessageReceived`, `EquivocationDetected`) removed from the Merkle log — both are `ContextEvent` receive-buffer signals only (messaging_helpers.rs:374-385 returns `None` to suppress append while preserving buffer push; queries_helpers.rs:915 same). Round-1 fix: governance consequence eval (governance_logic.rs:751-758) maps buffered `MessageReceived`→synthetic `EventType::MessageSent` for WarningCount bookkeeping — synthetic local `Vec<Event>` (prev_hash [0;32], empty sig), NEVER persisted. §7.3.7 consequence payload decode (consequence.rs `payload_target_did`/`rmp_array_first_string`) now reads typed positional MessagePack first-array-element + JSON `target_did`; dropped legacy null-terminated + `custom_key` paths (pre-release no-backcompat — correct). New typed payloads `AccessRevokedPayload`/`GovernanceActionExecutedPayload` (payload.rs) round-trip-tested incl empty-target. Bridge common layer (common/src/event_log.rs) fully off `EventLogEntry`→`scp_event_log::Event` with single `event_type_label` SoT for surfaced string. Item 6: ZERO `.docs/` modifications — artifact flow clean.

**Finding 1 (minor, doc-correctness):** export_import.rs:628-645 inline comment block is STALE pre-substrate narrative. It still says "the signed root is the event-log hash-CHAIN HEAD, not a Merkle-tree commitment over all entries", "verify_merkle_chain is pruning-tolerant (it does not validate entry[0].prev_hash)", and "front-truncation is not authoritative-state-neutral / planned hardening". ALL THREE now FALSE: the verifier (export_import.rs:444-506) uses `tree::append_unsigned_event` which enforces `sequence`-from-0 + `prev_hash==GENESIS_PREV_HASH` for entry[0] (tree.rs:56-77), so prefix/front-truncation is REJECTED outright. The rustdoc 25 lines above (453-465) AND ADR-050:65 both correctly say prefix-truncation is "closed, not merely detected." The 628-645 comment contradicts the new code, its own rustdoc, and the merged ADR → phantom provenance. Fix: rewrite/delete the comment to match `tree::root`-over-all-entries semantics.

**Finding 2 (minor, convention):** Two genuinely-new added lines carry `#710` issue-refs in SOURCE comments — `providers/event_log.rs:124` (`/// # Per-event storage (#710)`) and `:706` (`// O(1) persist: only the newly appended event (#710).`). Violates project rule `feedback_no_issue_refs_in_code` (issue numbers only in PR/commit msgs). Strip both.

Reusable pattern: in big-rewrite "substrate swap" PRs, the *code* and the *adjacent rustdoc* get updated but a SECOND inline comment block elsewhere in the same fn often preserves the old mental model verbatim — grep the diff for the old domain nouns ("chain head", "pruning-tolerant", "not ... completeness") even after confirming the code is correct.

**Round 8 @ `1abce261b` (2026-06-18) — APPROVE.** Round 7 (not recorded separately) had already cleared all findings (0 findings). Round-8 sole delta = commit `1abce261b` (docs-only, governance_logic.rs, +13/-13): consolidates the `event_log_entries_for_consequences` doc-comment into one coherent block AND strips the forbidden inline issue-refs `#1530`/`#1531`/`#1594` (was `(ADR-017, #1530, #1531, #1594)`, `actor_did (#1594)`, `**Known limitation (#1594):**`, plus an orphaned `(ADR-017, #1531)` trailing paragraph now folded into the body). Verified: NO `#NNNN` on any added line; `#1530/#1531/#1594` not reintroduced anywhere in the file across the full branch diff; doc claims (EventType projection onto coarse trigger buckets, payload-bytes-passthrough, dedup-prefer-event-log, 1000-event buffer cap, borrowed ReceiveBuffer per ADR-049 §Decision 1) all match the function body (governance_logic.rs:642-740); zero `.docs/` upstream artifacts touched across the whole branch (artifact-flow intact). Fidelity to ADR-011-amendment/ADR-050/§9.9.3/§23.16.8 unchanged — comments-only. Out-of-scope-by-instruction (not raised): pre-existing `#636` refs elsewhere, dead `app_sandbox` AppBound formatters.
