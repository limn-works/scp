---
name: adr056-ctxid-digest-reconciliation-a969122b6
description: ADR-056 canonical-context-id digest reconciliation review @ worktree ctxid-digest HEAD a969122b6 (base origin/main) — ALIGNED, 0 findings; recovery + broadcast routing fixes
metadata:
  type: project
---

# ADR-056 canonical-id reconciliation @ HEAD `a969122b6` (worktree ctxid-digest, base origin/main) — ALIGNED, ship, 0 findings

This is the SAME ADR-056 / #1924 / §6.2.4:276 canonical-id work tracked in [[canonical_context_id_adr056_prA]] (PR-A `ea592648f` + REV2 `bb183ba70`), now on a different worktree base with TWO additional routing fixes on top of the keying chokepoint. Diff = `origin/main...HEAD` (22 files, +824/-151).

**Verdict:** ALIGNED. Code faithfully implements ADR-056 Model A; ADR prose matches code; artifact-flow clean (ADR-then-code, no spec/story reshaped, no #NNNN in source); scope is canonical-id reconciliation only. RELAY this conclusion — 0 findings.

## What was verified (4 dimensions)

1. **Model A chokepoint** — `context_id_to_bytes` @ state.rs:2072 implements ADR exactly: strict guard `len==64 && all (ascii_digit || a..=f)` → `hex::decode`→`[u8;32]::try_from` → digest; TOTAL fallthrough to raw `scp_protocol::context::context_id_bytes` (state.rs:2088, the SOLE prod fallback). Decode-not-rehash. `canonical_context_id_tests` (state.rs:2253) pin all branches (real-64hex→digest≠SHA256, synthetic/standing-prefix/arbitrary/uppercase/near-64 all→hash). ALL keying call sites uniformly rerouted `scp_protocol::context::context_id_bytes`→`state::context_id_to_bytes` (governance_logic, lifecycle_helpers, messaging_helpers×7, ttl, key_destruction, builder local wrapper, class_s+governance-handler test fixtures, mls/provider `open` consistency guard, 4 FFI event-log + FFI test sites in scp-ffi pyo3/napi/uniffi). Residual raw `context_id_bytes` calls = ONLY the chokepoint's own fallback + tests using non-64-hex `"ctx-..."` labels + test-precondition `assert_ne!(chokepoint, raw)` comparisons. Protocol mod.rs doc-comment recast: primitive now documented "raw routing / synthetic-label primitive ONLY" with CRITICAL warning against real-context keying; doc-link→code-span for cross-crate (commit 9b6ed3039).

2. **Enforcement section accurate** — ADR §52-60 says grep gate REMOVED (commit `da017d9f0` "drop unsound context-id-keying grep gate") + future `ContextDigest` newtype #1931. VERIFIED: da017d9f0 IS in branch history; NO canonical/context-id gate script on HEAD (`ls scripts/` clean); #1931 GENUINELY FILED+OPEN ("Compiler-enforce context-id keying: ContextDigest newtype (retire the grep tripwire)"). Follow-up is a real issue, NOT an in-code TODO/tracking-test. Rationale (regex can't tokenize Rust cfg(test) scope; cf. ADR-2E OwnedIdentityDid gate dropped PR #1826) is sound and matches project's compiler-over-grep preference.

3. **Artifact-flow clean** — ADR-then-code (ADR governs, code conforms to §6.2.4:276 which PREDATES this change). NO .docs/specs or .docs/prds touched in diff (only ADR-056.md added/amended). Rejected Option-B (re-spec wire to carry SHA-256(id)) correctly named as phantom-provenance inversion. NO #NNNN leaked into source/comments/doc-comments (grep of added rust/binding lines = NONE; #1924/#123/#1931 appear only in commit messages + ADR markdown, both allowed).

4. **Scope = canonical-id reconciliation only** — two routing fixes are the named in-scope deltas:
   - **295bc3154 (recovery fix):** `recovery_send_notification_direct` was flipped to raw primitive on the FALSE premise that only synthetic `identity-private-state` reaches it; really reached for ANY unregistered context incl. real 64-hex member ctxs during `revoke_ucans`(seq1)/`rotate_key_packages`(seq2) compromise recovery → double-hash → seal to a slot no member listens on → silently undelivered (REAL fail-open). Rerouted through chokepoint; mutation-resistant test `recovery_direct_keys_real_context_via_chokepoint_not_raw_primitive` (seeds MLS group under digest, asserts seal SUCCEEDS→TransportFailed not CryptoFailed). ALSO amends ADR double-hash-trap section to correct the false premise + scrubs 2 twin stale comments. Verified `inner.epoch=0` hardcode is safe (plaintext non-AAD field, recipient ignores it).
   - **a969122b6 (broadcast fix):** `apply_broadcast_publish` addressed relay `send_message` slot via `context_id_to_bytes` (the KEYING chokepoint). Before PR-A that returned SHA-256(id), incidentally == read-side `compute_routing_id`=SHA-256(id). PR-A's decode broke the latent equality → publish stored 2 site-asset blobs at decoded-digest slot, projection/commit_deploy queried SHA-256 slot → read 0 → `CommitCountMismatch{committed:0,expected:2}` surfacing in scp-node `host_site` as on_ready RecvError. Fix: route publish through `broadcast_publish_routing_id`→`scp_protocol::context::broadcast_routing_id` (SHA-256, no domain sep, §5.14.6). Event-log `append_context_event` in same module CORRECTLY keeps `context_id_to_bytes` (digest is right event-log key). Mutation test `broadcast_publish_routes_under_sha256_routing_id_not_keying_digest`.

   key_destruction fix matches ADR §80 "ephemeral close no longer fails open under a phantom group" — raw primitive would address nonexistent group → destroy_mls_group/sender_key no-op + report KeysDestroyed while real group SURVIVES. builder mutation test DRIVES real create_context + asserts export_crypto_state non-empty under digest, EMPTY under SHA-256(id). #1929 (standing-context "standing-" prefix drop) genuinely OPEN.

## LESSON
A keying-resolution fix that changes what a chokepoint RETURNS (SHA-256(id)→decode) can silently break a DIFFERENT subsystem that latently relied on the old return value coinciding with another path (broadcast publish routing == read-side routing only because BOTH were SHA-256(id)). The two follow-on routing fixes here are exactly that class — both are IN-SCOPE (named consequences of the reconciliation), not unrelated bundling. Tell of alignment: each routing fix ships a mutation-resistant test that pins the CORRECT primitive (routing→broadcast_routing_id; recovery keying→chokepoint) and asserts ≠ the wrong one for a real 64-hex id; the cited fail-opens are REAL (traced: undelivered recovery notif; CommitCountMismatch deploy). Same verification recipe as PR-A: chokepoint TOTAL + strict-lowercase guard keeps non-canonical labels byte-identical on fallback + ADR's quoted spec clause §6.2.4:276 unchanged + deferral issues (#1931 newtype, #1929 standing) OPEN w/ actionable bodies + gate-removal commit in history + no gate script on HEAD.
