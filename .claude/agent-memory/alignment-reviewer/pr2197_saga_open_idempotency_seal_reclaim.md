---
name: pr2197-saga-open-idempotency-seal-reclaim
description: #2197 spec commit 9067782d3 (saga-open idempotency + universal seal-phase reclamation for streaming saga) — NEEDS DISCUSSION, 1 HIGH default-invariant contradiction + stale base vs merged #2209
metadata:
  type: project
---

#2197 "lazy-open cancellation orphan / double-reserve" spec-first change, commit 9067782d3 on worktree /Users/alec/Developer/limn/scp-wt-2197-spec. Files: 05-contexts.md §5.4.5 (universal seal-phase reclamation), 06-cross-context-communication.md §6.2.4 (nonce as saga-open idempotency key + durable index), 09-security-model.md §9.18.B (2 ContextParams rows), ADR-061 dated addendum (2026-08-02).

**Design is SOUND and solves #2197 at root cause** (better than the issue's suggested SDK-side sagaId-binding): (a) atomic lookup-or-create index (nonce→SagaId) written BEFORE Prepare-A escrow debit = register-before-reserve, kills double-reserve + orphan; (b) same-principal channel-authenticated resume (NEVER caller-supplied caller_did, R1) reattaches same SagaId/reserve; (c) universal seal-phase reclamation (idle deadline 6134 + max-duration cap 6136) force-settles SERVER-SIDE — correctly reasons a caller-side cancel cannot drop a detached UniFFI runtime().spawn().await future, so fix MUST be server-side. All cross-refs verified real (§17.16.4, ADR-049 §3a settle_at_close, CallerReservationRecord, Durably-staged caller deduction, §6.2.5).

**FINDINGS:**
- **HIGH (must-fix, self-contradiction):** default `stream_seal_idle_secs`=30s (09 §9.18.B:2050, 05 §5.4.5:571) is NOT strictly greater than default `stream_credit_stall_secs`=30s (09:2044, 05:490), yet §5.4.5:571 says "An implementation MUST reject a ContextParams configuration with stream_seal_idle_secs <= stream_credit_stall_secs." 30<=30 → DEFAULT config is self-rejecting, or if defaults bypass validation the deterministic-terminal-code guarantee (credit-stall 6133 fires before idle 6134) is void at default (tie). Raise default seal-idle (e.g. 45/60s).
- **HIGH/MEDIUM (stale base, must rebase before PR):** merge-base = this commit's parent 0cdeb635f; origin/main is 3 ahead incl. 34184398b (#2238 closes #2209) which ALREADY merged 6132 (execution.stream-cap-exhausted, WithBackoff) + moved node-pump-ceiling 6131→6132 + count word "fourteen"→"fifteen". This branch cut pre-#2209: count "fourteen"→"sixteen" (should be seventeen post-merge: 14+#2209's 1+#2197's 2), and its registry table LACKS 6132 → real 3-way conflict on §5.4.5:314 count line + table region; merge must preserve main's 6132 row. NO code-number collision though (6134/6136 disjoint from 6131/6132/6133 — task Q5 answered: confirmed no collision).
- **MEDIUM (cross-artifact consistency):** ADR-049 §3a:84 still enumerates seal-phase bounds as "timeout_ms, effective_max_billable_chunks, credit-stall timers — NOT the 30s saga per-phase timeout" — exactly the incomplete set #2197 proves insufficient (timeout_ms==0 + wedged forward channel + no credit-stall = blocks forever). ADR-061 addendum CLAIMS to strengthen ADR-049 §3a clause 2 to "bounded, always-reached" but does NOT edit ADR-049 §3a → stale enumeration misleads a reader landing there. Add cross-ref to §5.4.5 universal seal-phase reclamation.
- **LOW:** #2197 issue body explicitly names same-context InvocationHandle + Python reference as sharing the systemic pattern; spec framed solely around streaming-saga seal-phase. Add scope sentence (same-context money-safety now covered by timeout_ms==0 universally backed by stream_max_duration_secs, OR explicitly out-of-scope).
- **LOW:** 05 §6.2.5 streaming-saga mode line 387 still says seal phase "bounded by the invoker's credit/escrow envelope — NOT the 30 s saga phase timeout" w/o cross-ref to new deadlines; wedge is exactly where credit/escrow does NOT bound.
- **OBS:** addendum says "6132 reserved there" — 6132 is allocated/live in #2209, not a reserved gap (imprecision). Spec text implementation-flavored (inner_rx.recv, UniFFI/Kotlin mechanics) but consistent w/ existing §5.4.5 register. 6134/6136 retry=Never coherent (re-acquire via idempotent-resume, not auto-retry).

**Artifact-flow: CLEAN.** ADR-061 addendum defers normative authority to §6.2.4/§5.4.5 ("Normative in..."); additive/strengthening not contradicting; spec-first (no code, runtime is follow-up). Legibility-before-opt-in OK (both knobs in §9.18.B ContextParams, concrete defaults+ranges visible). max_duration=900s clearly marked "proposed default, product call" in both 09 + 05; effective=min(timeout_ms,cap) coherent w/ OutletStreamOpen.timeout_ms comment. Verdict: NEEDS DISCUSSION — design ALIGNED, two must-fixes (default invariant + rebase) mechanical.
