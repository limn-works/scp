# ADR-062 Capability Injection — security review (branch docs/adr-062-capability-injection, 2026-07-13)

Scope: DESIGN review of ADR-062 + PRD (4 stories SCP-CAPINJECT-000..003) + spec §17.17 (new upstream) + §9.7.4.1/ADR-054 (pre-rotation). Code diffs on branch (state.rs del, provider.rs, sequence.rs) are just branch-behind-origin/main noise, NOT ADR-062.

Reframe verdict: SOUND end state. Nullifiers (plaintext custody, always-pass attestation, in-memory DHT) → test-harness-only (`#[cfg(any(test,feature="testing"))]`), gated behind {scp-platform/testing, scp-dht/testing, scp-testing}, G1 proves absent per shipped artifact. Durability-only storage stays a runtime option. `allow_in_memory_custody` switch deleted. Security reduces to: (a) every nullifier behind one of the 3 checked features, (b) G1 proves them absent. Reduction holds.

Findings:
- MED (Q1, the "other welded type" hunt): `server.rs` under the shipped `server` feature carries UNCONDITIONAL (non-switch, non-test) nullifier refs — `use scp_platform::testing::{InMemoryKeyCustody, InMemoryStorage}` (server.rs:22), `IdentitySource::<InMemoryKeyCustody, ConcreteDidMethod>` phantom annotations (322/435), `InMemoryDhtClient::new()` (455). scp-ffi-common Cargo.toml:30 `server = [... "scp-platform/testing" ...]` pulls testing unconditionally. Slice 3(e) SAYS sever "scp-ffi-common server" but its file list omits server.rs → the InMemoryKeyCustody annotations must be retyped or sever won't compile. Slice 0 moves InMemoryStorage→in_memory but omits server.rs + common/Cargo.toml. Risk = CLAUDE.md-warned workaround (keep testing edge, ship all nullifiers). G1 backstops fail-closed. Same class as the missed pre-rotation welded field.
- MED (Q3): encrypted-offline pre-rotation backend (AES-256-GCM + Argon2id 64MiB/3/4, ≥128-bit) is an adequate v1 floor, BUT substrate isolation §9.7.4.1§3 reduces to WHERE the passphrase (AES key material) lives — neither ADR-054§2 nor spec pins it. Auto-generated passphrase co-persisted in operational KeyCustody satisfies the letter while nullifying §3. Conformance-test AC checks seam (separate provider), NOT passphrase colocation → wouldn't catch it.
- LOW (Q3): ADR mandates Zeroizing on seed bytes, not explicitly on the Argon2id-derived AES key.
- LOW/MED (Q4): E2 credentials (ADR-rated HIGH, InMemoryCredentialStore welded via impl Default, shipped napi/pyo3) deferred to Slice 4; blob `impl Default for BlobStorageBackend → in_memory()` (storage.rs:561) is a live SCP-CAPSEL-8011 never-default violation until Slice 5. Both disclosed/sliced; G1 tightens per slice.
- OBS (Q2): SECURE. `git grep attestation` in scp-runtime/scp-protocol = EMPTY — NO protocol/runtime check consumes device attestation. uniffi already ships `#[cfg(not(...))] identity_attest_device_impl → Err(ScpError)` fail-closed. Removing always-pass in-memory attestation removes a false signal; absence returns typed error, not silent pass. Nothing depends on attestation succeeding.
- OBS (positive): branch REVERTED the prior "backend-pending-residue" spec-MUST weakening of SCP-CAPSEL-8012 (commits b4831f1ba/46aa0751c) — un-did a scar rather than institutionalizing it.
- OBS: G1 checks 3 features; sound only because every nullifier impl traces to one of them. ADR should state G1 must also ensure bridge-level `testing` features define no nullifier code themselves.
