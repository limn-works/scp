---
name: review-classs-fin-trunk-beddb1c70
description: Security review of classs-fin-trunk Class-S §9 structural-residual close (RoleStateClassCMut vs ConsequenceRoleStateMut split, cross-crate ceiling/suspended privatization, AssignRole demotion FC) — CLEAN, zero findings
metadata:
  type: project
---

SECURITY REVIEW — SCP ADR-049 §9 Class-S FINALIZED structural residual (branch classs-fin-trunk HEAD beddb1c70, 31 commits vs origin/main). VERDICT: CLEAN, zero CRITICAL/HIGH/MEDIUM. Supersedes the role_state_mut residual flagged at 272c4d079 — that residual is now CLOSED.

**Why:** This is the terminal Class-S §9 PR that closes the ContextRoleState dual-use ceiling/suspended_capabilities residual (the prior "documented residual, future regression gap") via cross-crate privatization + a two-view split. Plus it FIXES a real prior auth-downgrade window (AssignRole demotion).

**How to apply (what was verified, empirically):**
- TWO role-view split (class_s.rs ~1094 / ~1344): general `RoleStateClassCMut` holds ceiling as `&` (read-only), suspended_capabilities as `&mut` but exposes ONLY shrink-only `prune_suspensions_to_role_grants` + read — NO `suspend_capabilities`/`suspend_all`. Consequence-only `ConsequenceRoleStateMut` exposes the GROW. Both hold ceiling as `&` so neither writes it. GROW on best-effort STRUCTURAL view = compile error (method absent).
- CROSS-CRATE PRIVATIZATION (roles.rs): `ceiling`/`suspended_capabilities`/`CapabilityCeiling.capabilities` now `pub(crate)`. Cross-crate reach ONLY via `class_c_parts()` (disjoint-field destructure, ceiling handed `&`, suspended `&mut`), inherent `system_assign_role`, `set_ceiling`, `suspended_for`, `ceiling()`/`iter()`/`intersect()`/`contains()`.
- `#[cfg(any(test, feature="testing"))]` mutators `ceiling_mut`/`capabilities_mut` (roles.rs 1124/472) NOT in production. VERIFIED EMPIRICALLY: scp-protocol/testing is enabled ONLY by scp-runtime's own `testing` feature (Cargo.toml:17) — line 70 enabler is [dev-dependencies] (resolver-2 does NOT leak dev-dep features to normal builds). `cargo tree -p scp-ffi -f "{p} {f}"` shows scp-protocol resolves `default` only. `cargo build -p scp-ffi` (no testing) compiles clean EXIT 0 → no prod path references the gated mutators.
- ALL 5 production consequence GROW sites persist FAIL-CLOSED when `downward_auth_applied`: (1) governance periodic sweep handle_evaluate_periodic_consequences (governance.rs:851), (2) receive deliver handle_deliver_incoming (messaging.rs:368, w/ error-precedence + cause-chaining), (3) send finalize persist_finalized_send (messaging_helpers.rs:2308/2323 — paid=token FC, free=upgraded FC on flag), (4) tool settle dispatch_tool_settle (tools_helpers.rs:1329, BOTH Ok+Err arms), (5) governance action finalize_governance_action via execute_governance_action token.discharge_with (governance_helpers.rs:4914 — token deferred FC persist covers the consequence GROW; `let _ = downward_auth_applied` SOUND because token persists whole post-finalize state FC on every path incl dispatch-error 4896).
- `downward_auth` SINK threading airtight: deliver_incoming(&mut bool) → validate_and_drain_timeouts / buffer_ahead_message / deliver_message_and_drain_buffered / run_buffered_post_delivery all `*sink |=` (4 prod buffered-drain sites 2784/2877/3007/3114; the `let _suspended=` at 3752 is a #[test]). flag accounting in process_one_triggered_consequence rigorous: H10 escalation suspend_all returns true (305), empty-caps SuspendCapability returns false (no GROW→no persist), AssignRole returns true unconditionally (over-persist promotion = safe upward dir).
- ALL production direct protocol GROW/demotion/ceiling-write inside `commit_class_s_keep`/`begin_class_s` FC combinator via `view.rest_mut()`: execute_suspend_member (gh:794), execute_revoke suspend (gh:872), execute_reset_member system_assign_role (gh:1384), transfer-admin demote-then-promote (gh:1818), execute_*-suspend_all (gh:4339), execute_modify_ceiling set_ceiling (gh:476). The only prod `set_ceiling`. All other set_ceiling/ceiling_mut/suspend_* are #[cfg(test)] (export_import:914, lifecycle:2972 restore_reconcile, saga:2464, supervisor:10703 — all cfg(test); lifecycle uses cfg(all(test,feature="testing"))).
- REAL FIX (commit 04859fdc7, not a residual): consequence-engine AssignRole DEMOTION previously returned suspended:false → routed member_capabilities-shrink to best-effort coalesced persist → ≤50ms crash restored pre-demotion HIGHER caps = silent re-grant. Now EnforcementOutcome.suspended→downward_auth, AssignRole arm = downward_auth:true unconditional + regression test consequence_assign_role_demotion_owes_fail_closed_persist. GENUINE prior auth-downgrade rollback window, CLOSED on this branch.
- Enforcement: assert_not_impl_any!(SharedClassS: DerefMut) (3250); positive compile-witness assert_role_view_has_no_grow (3263) names RoleStateClassCMut's downward-auth surface (read+shrink+ceiling-read) + confirms suspend_all CONFINED to ConsequenceRoleStateMut. Honest tripwire per anti-denylist lesson — the real guarantee is the no-GROW-method compile shape + #![forbid(unsafe_code)]. Old 4354-line check-class-s-fail-closed.sh DELETED (not in CLAUDE.md protected list; replaced by compile boundary, covered by workspace clippy/nextest).
- TESTS: `cargo test -p scp-runtime --lib --features testing` = 1914 passed, 0 failed, 1 ignored. Production `cargo build -p scp-ffi` = clean.

ONE SUBTLETY (not a finding): GROW is NOT structurally unreachable from a best-effort cell-holder — `consequence_split()` is a method on ClassCMut (the non-persisting view) so any class_c_view() holder CAN reach the GROW view. The compile-time guarantee is narrower than "GROW unreachable": it's "the DEFAULT/structural role view (role_state_class_c_mut) cannot GROW." Protection against coalesce-loss rests on the runtime discipline (OR-accumulate downward_auth_applied + FC persist), which I verified at all 5 prod consequence_split sites + 2 from_state sites. This is the intended design; documented honestly in the view docs.
