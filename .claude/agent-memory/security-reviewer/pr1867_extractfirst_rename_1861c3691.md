# PR#1867 extractFirst rename + python evaluate_trust root export + WASM comment fixes (1861c3691) -- 2026-06-23 -- CLEAN, 0 findings

Branch fix/sdk-coverage-fail-closed-and-parity, HEAD 1861c3691 (3 tip commits: 1861c3691 rename+py-export, 9e81e5648 + 4ccc4dd49 WASM comment-only). Stale base (1f1ea7cd2..HEAD huge); per-commit diff authoritative.

1. `__extractFirstCapabilityUri` (trust.ts:320): safe. `token.split(".")[1]` undefined-guarded; `Buffer.from(seg,"base64url").toString("utf8")` then `JSON.parse`; `att[0].with` typeof-string-guard; empty->null; try/catch->null. NO injection — unverified payload only SELECTS which URI to feed `ucanValidate`, which re-verifies sig+capability-match independently. Rename from `__extractAll*`: NEW behavior is STRICTER/MORE fail-closed — old skipped att entries lacking `with` (so att[0] no-with + att[1] with-uri validated att[1]); new returns null if att[0] itself lacks valid `with`. TS+Python both make identical change. (Buffer at 324 = Node API; same assumed-global pattern as wasm.ts:620/632 — pre-existing portability assumption, not this PR, not security.)
2. PERM-3001 closed allowlist (validateOneCapUri trust.ts:473): ONLY `/^\[SCP-PERM-3001\]/` absorbed; everything else (3000/3030/future) `throw error`. Intact.
3. Python evaluate_trust: defined trust.py:738 (async def), imported __init__.py:166 from scp_sdk.trust, in __all__ at :277. EXPORTED. Present.
4. ucan_error_code (common/src/ucan_errors.rs:48): const fn, EXHAUSTIVE match over all UcanError variants in 6 buckets, all->PERM_3001, NO wildcard `_=>` (only `_=>` are inside warn comments). `#[allow(match_same_arms)]` deliberate. Compile-error forces classification of any new variant.
5. validate_tool_ucan_wasm (ucan.rs:562) returns Result<(),(String,Option<&str>)>; ALL 3 error branches return Some(ucan_error_code(&e)) = Some. Doc-comment FIXED: old said unwrap_or reached on missing-token `_=>` branch (WRONG — missing-token handled in tools.rs with VALID_7000, never reaches helper); new says "All error branches return Some(code), unwrap_or(PERM_3000) safety net is dead." ACCURATE. 3 tools.rs call sites (tool_invoke:521, cross_context:635, session_invoke:750) `code.unwrap_or(codes::PERM_3000)` — defensive, dead-but-type-permitted-None. Comments now accurate. Comment-only commits, zero behavior change.
6. test-guard.ts: SECURE. _evaluateTestEnv uses Object.hasOwn (anti-prototype-pollution). _ENV_AT_LOAD read ONCE via IIFE at module load; _IS_TEST_ENVIRONMENT frozen const (value not getter — runtime process.env mutation can't flip). assertTestEnvironment gates BOTH test hooks: __setBridgeForTests (bridge.ts:837) + __constructScpWithNativeForTests (scp.ts:3739). Neither hook nor __extract* re-exported from index.ts. package.json exports map exposes ONLY "." — deep-import of internal/test-guard blocked for consumers.

Python PERM-3030 re-raise (trust.py:855 startswith before _classify_ucan_error:856) intact — fail-closed ordering preserved.
