# Slices 1/2/3 — Convergent consequence window / snapshot creation-time / CommitBroadcast demotion (2026-06-22)

Reviewed off main `1f1ea7cd2`. Branches built atop the Phase-2 event-log substrate swap (trait `append_event` now takes explicit committer-assigned `timestamp_secs: EventType + EventPayload + u64`, NOT `SystemTime::now()` — the old main-branch `ContextLog::append` local-clock stamp is GONE on these branches). Verified at builder.rs:158 + providers/event_log.rs append_event (verbatim, no future-bound clamp).

## Slice 3 (#1859, @5f548c0b6) — CLEAN
- evaluate_consequence_rules gained `convergent_now` param. Convergent triggers (WarningCount/Custom, is_convergent_trigger==true) anchor window on `convergent_now` = max(Source-1 durable log ts) captured BEFORE buffer merge; non-convergent (MessageVelocity/ToolRateExceeded) keep local `now`. All ~9 runtime call sites + WASM consequence.rs thread it; participation-only paths correctly discard via `_convergent_now`. WASM mirrors native (max of event_log_events() before merge, now_secs fallback).
- SECURITY doc-comment (governance_logic.rs ~648-665) is ACCURATE & complete: leaf ts are committer-assigned (proposal.created_at, signed but proposer-chosen, NOT future-bounded — confirmed no clamp). Convergent != non-forgeable; residual = malicious committer/quorum future-dates to widen window → admin/quorum-gated, deferred to convergent-wall-clock RFC. NO local-clock ceiling: applying min(convergent_now, now) would reintroduce divergence (anchor becomes member-skewed). Sound. Honest disclosure, no silent gap.
- Convergence tests use scp_event_log API w/ explicit ts (1_700_000_000) — correct, that IS the production substrate now.

## Slice 2 (#1858 + 18d8d5a49, @00d7a3be2) — ONE LOW (legacy-zero divergence)
- ContextSnapshot.creation_timestamp_secs added (#[serde(default)] -> 0). Verbatim consume safe on BOTH bridges: native import_context (lifecycle_helpers.rs:1444) calls validate_export_for_import (sig verify_strict + exporter==creator) @1460 BEFORE consuming @~1512; WASM import_context (manager.rs:5428) calls deserialize_and_verify_envelope FIRST @5429 (mandatory Ed25519) before consuming snap.creation_timestamp_secs @~5942. Field is inside signed JCS preimage (round-trip test confirms). Sole consumer = TTL upper bound (creation+ttl); backdating SHORTENS (fail-safe), future-dating bounded by ttl. Not re-pinned to local now (unlike observed_at) — correct, re-pin would reintroduce divergence.
- LOW: legacy/`creation==0` fallback DIVERGES between bridges. Native convergent_ttl_deadline_secs(0, Some(ttl)) = `0+ttl` (distant past -> immediate expiry, convergent). WASM handle_ttl_expiry match `_ => now()` stamps LOCAL fire-time. So a pre-field snapshot in a mixed native/WASM ctx -> native leaf=`ttl`, WASM leaf=`now()` -> Merkle divergence = the §9.9.3 equivocation the slice closes. Native doc-comment's "0+ttl, fail-safe, consistent" reasoning does NOT describe WASM's actual behavior. Reachability LOW (pre-release no-migration: pre-field snapshot shouldn't exist; create=now, import=verbatim), but both doc-comments treat zero as a live fallback. Make WASM match native (`(_, Some(ttl)) => creation.saturating_add(ttl)` even when creation==0) OR reject creation==0 imports.

## Slice 1 (#1857, @6687ecaae) — CLEAN
- ADR-011 phase-2.md exclusion-taxonomy edit: adds category 3 "per-committer broadcast-retry bookkeeping" (CommitBroadcasted/Pending/Succeeded/Failed) — permanently kept as EventType but NEVER durably appended (per-committer, no convergent order even under ADR-051). Accurate.
- Code: try_broadcast_commit_or_enqueue + apply_commit_retry_outcomes drop all append_context_event(CommitBroadcast*) calls; events now surface as local ContextEvent only; fns become infallible (removed Result). No security record lost: grep confirms NO durable consumer (checkpoint/export/proof/consequence) reads these. pruning.rs classifier branch (returns false) + test_vectors.rs KAT for the variant are substrate-generic over the closed enum, not contradictions (enum NOT narrowed).
