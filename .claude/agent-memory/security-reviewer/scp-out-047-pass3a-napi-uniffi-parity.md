---
name: scp-out-047-pass3a-napi-uniffi-parity
description: SCP-OUT-047 pass 3a NAPI+UniFFI streaming-saga FFI (open/poll/recover) auth-parity review vs PyO3 reference — PASS, 0 findings
metadata:
  type: project
---

# SCP-OUT-047 pass 3a — NAPI + UniFFI streaming-saga FFI (fd33fadf2, scp-wt-047) — 2026-07-17 — AUTH-PARITY, 0 FINDINGS

Both bridges mirror the PyO3 reference (crates/scp-ffi/src/outlet_stream.rs) EXACTLY. Shared bridge-agnostic driver = crates/scp-ffi/common/src/streaming_saga.rs (StreamingSagaEntry, serialize_saga_chunk, drive_recover_truncated_close). Files: napi/src/outlet_stream.rs, napi/src/scp.rs, uniffi/src/outlet_stream.rs, uniffi/src/bridge.rs, both runtime.rs.

**Why PASS (traced, not grepped):**
1. OPEN caller-principal binding: both run `enforce_caller_principal_binding(caller_context_id, caller_did)` at step (b) BEFORE any outlet read / saga drive / receiver handout. caller_context_id/target_context_id derived from HANDLES (source_handle.context_id / target_handle.context_id), NEVER envelope. invoker_did = caller_did everywhere (all runtime-side). NAPI open napi/outlet_stream.rs:1210; UniFFI uniffi/outlet_stream.rs:1206.
2. RECOVER invoker gate (money-moving): both = validate_did → hosted-identity gate (NAPI identity_registry_contains; UniFFI identity_custody_registry.contains_key) → lookup entry pinning invoker_did → `if caller_did != invoker_did → caller_not_invoker_err PERM_3001` BEFORE key resolution/drive → evict on success. Rejection leaves stranger's entry INTACT. NAPI 1565; UniFFI 1521. Pinned invoker_did compared (not "any hosted").
3. CrossContextVerificationDescriptor built RUNTIME-SIDE inside start_cross_context_streaming_outlet_invocation_saga; neither bridge passes operator_pk/operating_context_id/outlet_id/caveats_binding/expected_request_id from caller. (operator_pk refs in both = the pre-existing verify_forwarded_chunk client-side sig-verify helper — public key, verification only, not the open descriptor.)
4. NEW `UniffiKeyCustody::export_ed25519_signing_key` (bridge.rs 947-951): pub(crate) enum-dispatch ANALOGUE of pre-existing per-backend method (InMemory backend def bridge.rs:859, Callback 522/531 — all in fd33fadf2^ parent). Only NEW caller = resolve_context_active_signing_key_by_id (uniffi/outlet_stream.rs:1100) in recover. Resolves key_handle from per-instance identity_custody_registry keyed by TARGET context's creator_did (read from runtime UCAN state for pinned target_context_id — NOT caller-controlled), gated behind BOTH recover auth gates, key used in-process→dropped, never crosses FFI. Sign-only Callback returns Err → fail-closed CTX_2040. NO new key-access path; underlying export capability already existed + already used by resolve_uniffi_signing_key.
5. Handle-affinity: both open exports check_handle BOTH source+target before dispatch (NAPI scp.rs napi_check_handle! + again in _on:1147; UniFFI 1799/1803). poll/recover take saga_id string, no handle — same as PyO3; per-instance registry keyed by saga_id makes cross-instance saga invisible; recover has own auth gates, poll possession-based. Key resolution divergence (PyO3 by context_id, NAPI reads rt.core.creator_did via with_context then resolve_context_signing_key, UniFFI reads creator_did from ucan_state then custody export) all resolve TARGET active signing key via per-instance custody — consistent, no bypass.
6. Registry per-instance (Arc<DashMap> field on NapiBridgeInstance / UniffiBridgeInstance, cleared in bridge_specific_shutdown), never global. Error paths leak only public identifiers (caller_did/invoker_did/saga_id) — no key/receiver/escrow. Test seams (insert_test_streaming_saga_entry) cfg(test/testing/allow_in_memory_custody), NOT #[napi]/#[uniffi::export].

Minor non-blocking: hosted-identity-gate rejection code differs cosmetically (NAPI CTX_2001, UniFFI CTX_2040, PyO3 generic context) — the MONEY-MOVING invoker gate is PERM_3001 in all three (the security-load-bearing one). Not a finding.

## pass-3a CLEANUP (f69650ab8) — 2026-08-01 — VERIFIED, 0 new findings; runtime gap CONFIRMED
Closes the pass-3a minor: adds the active-state guard to the streaming-saga OPEN on all 3 bridges + aligns the cosmetic code. Verified:
- Money-path gap CLOSED all 3: non-active caller→OUTLET_6010 / target→OUTLET_6011, BEFORE caller-principal binding + saga drive + escrow. PyO3 reads AUTHORITATIVE supervisor `read_context_state` (both axes, None→reject fail-closed) — stronger than NAPI/UniFFI which read handle-cached state (source_handle.state / .state.lock). NAPI after napi_check_handle; UniFFI at top before input validation.
- CTX_2040→CTX_2001 (UniFFI resolve_stream_signer ×3, resolve_context_active_signing_key_by_id, recover_truncated_close): ONLY the `code:` field value changed, rejection control-flow (.ok_or_else/.map_err/early-return) unchanged; aligns UniFFI to what NAPI/PyO3 already emit. Invoker gate `caller_did != entry.invoker_did → PERM_3001` intact + untouched all 3.
- Tests genuine: non-active open drives REAL impl, asserts code + registry-is-empty (no saga/receiver). creator_did invariant test pins UCAN-state creator_did == handle.creator_did (recover signing-key source) — DiD defense against future refactor sealing under wrong key.
- Seams safe: PyO3 test helpers in non-#[pymethods] cfg(test/testing/allow_in_memory_custody) impl; NAPI set_state_for_test pub(crate)+#[cfg(allow_in_memory_custody)], fail-soft lock test-only.
- **CONFIRMED RUNTIME GAP (point 6, runtime-level follow-up, NOT 047 scope):** `open_outlet_stream_phase1` (supervisor.rs:12454) → `reserve_outlet_stream_economy` (outlets_helpers.rs:1106) has NO ContextState::Active gate before escrow debit — only clock/coerce/hard-rate/membership(6089)/overflow/funds. So bridge guards are the SOLE barrier. SAME-CONTEXT stream open (`outlet_stream_open` all 3 bridges) SHARES the runtime gap AND is NOT bridge-guarded (only napi_check_handle, no active check) → a non-active same-context can debit escrow. Unary saga (`reserve_outlet_economy`, also no runtime active gate) IS bridge-guarded. Durable fix = active gate in the streaming reserve/phase1 path (covers same-ctx stream + saga uniformly), superseding per-bridge guards.
