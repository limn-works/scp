# Standing-Pair "Not a Saga" §5.12.2/§5.15.8 Round-11 (aaa4e1460, spec/standing-pair-not-a-saga-v2) -- 2026-06-24 -- ZERO FINDINGS / SOUND

HEAD aaa4e1460 = round-10 (536c6d192, zero-findings) + exactly 5 NEW commits (536c6d192 IS ancestor; incremental, not rebased). Merge-base origin/main = f37372b25 (PR#1888). Files changed vs round-10: ONLY 05-contexts.md + sketch.md + sdk-common.md + technical-overview.md. ADR-049/DEFERRED/03-identity/09 = EARLIER rounds, untouched here.

## 5 new commits, all STRONGER or honesty-improving (verified):
1. **Auto-accept narrowed to ALLOWLIST-ONLY.** Round-10 still kept 3 TrustRequirement arms (shared_context/discovery_context/known_did) w/ per-arm self-clear qualifiers; this change DROPS shared_context + discovery_context as auto-accept triggers entirely. §5.12.2 L744/754: known_did(Explicit) sole trigger; co-membership + discoverability explicitly NOT trust signals; no self-clear (candidate can't add self to evaluator allowlist). L755 No-default normative: absent explicit AutoAcceptPolicy => prompt (default-deny). Child-context L1381: §5.13.2 eligibility = crypto-enforced FLOOR on who-can-reach, NOT trust signal; routes through known_did allowlist. step-4(b) L1862: allowlist-or-prompt. NO residual silent-auto-accept / weak-signal path anywhere. §09 L113 "from a known DID" anchor now matches precisely.
2. **PROVENANCE NOTE ACCURATE (both removal sites VERIFIED in code).** Core: crates/scp-protocol/src/context/policy.rs:22 enum TrustRequirement = {Any L24, SharedContext L27, Explicit(Vec<DID>) L29} -- NO KnownDid, NO discovery_context (never implemented = TRUE). satisfies_trust in invitation.rs L115/267. WASM: crates/scp-ffi/wasm/src/context.rs:2559 check_trust reimpl (ADR-034) = Some("Any")=>true L2565 (the silent accept-from-any), Some("SharedContext") L2566, Explicit L2573. Note's warning "or WASM silently retains accept-from-any" is LITERALLY TRUE -- L2565 is exactly that path. Both sites correctly named. (Note: WASM SharedContext arm actually checks trusted_dids_json allowlist not real co-membership -- pre-existing code/spec divergence, downstream PR removes the arm anyway; correctly deferred.)
3. **KeyPackage-no-drain CORRECTNESS FIX (strictly stronger than r10).** r10 said did_hi self-only re-drive "consumes one of did_lo's KeyPackages...did_hi destroys"; NEW (L1887) says did_lo's KP is NEVER consumed -> no pool drain. VERIFIED SOUND vs the project's own single-use model: §09 L652 consumption=owner SDK deletes private key "after a KeyPackage is used in a Welcome message"; ADR-049 §9 L163 init-key single-use enforced at INVITEE's join_from_welcome (RFC9420 §10 HPKE init key). did_lo IGNORES did_hi's Welcome => never joins from it => never consumes its own init key. Relay is dumb pipe, no consume-on-fetch in spec. r10 wording was pessimistic/over-stated; fix is more accurate.
4. **JOIN-THEN-DESTROY reorder EXHAUSTIVELY aligned (strictly stronger).** All restatements (L1846/1849/1851 fresh-join->destroy / 1853 rejoin-then-destroy / 1855 convergence window) now consistently destroy AFTER successful join. Forecloses stale-destroy-then-failed-join DoS on did_hi's legit group BY CONSTRUCTION. Atomic bundle {id-agreement(a0) -> block-list -> confirm-bound-creator -> fresh-join(consumes init key) -> destroy} still spans all under actor mutex + generation check => confused-deputy/replay/forged-creator STILL caught (L1851). Survivor-role clarification added: did_lo (lower DID) = survivor, ignores all inbound (creator is did_hi not did_lo), purely local determination distinct from a0.
5. **Anti-spam reflected-resolution disclosure (honest, no mechanism change).** L1877 adds: DID-resolve is NETWORK op (DHT/HTTPS), off-path party knowing both DIDs (id publicly computable) forces outbound resolution traffic vs 3rd-party DID host = bounded reflected-resolution, 1 resolve/forged-Welcome, no join/state/fan-out. Honesty-only.

TIP commit aaa4e1460 = 1-line attribution FIX: did_lo-ignores bullet "its later destroy" -> "did_hi's later destroy of that orphan" (did_lo=survivor never destroys; correct).

## Load-bearing guarantees CONFIRMED INTACT:
- Existence/decline oracle closure (uniform Ok value+timing; FFI no created:bool/peer_joined enrich; identical shape all bindings) -- L107/108 unchanged context, intact.
- Block gate FIRST in collision bundle; block-privacy reply-oracle closure; published-KeyPackage targeting bit only via stranger-bar bypass which allowlist-default-deny blunts (L1862/1887 wording updated, semantics intact).
- Length-prefix injectivity (§9.5.1) unconditional.
- a0 mismatch guard = honest-canonicalization backstop only (InvitationBundle.context_id creator-asserted label, not pair-bound); agreement vs dishonest creator rests on §9.7.1 bound-creator + MLS membership. Transient resolve=retryable deferral, permanent=reject, never silent join.

## Provenance grved: §09 L113 known-DID; policy.rs:22; context.rs:2559 (WASM); invitation.rs:115; §09 L652 KP-consume; ADR-049 §9 L163 (init-key join), §10 L212 (auto-revive residual cited as "ADR-049 §10").

## NO SIBLING on this branch. HEAD = tip of local+remote spec/standing-pair-not-a-saga-v2; 5 new commits all reviewed. NOTE: origin/HEAD dffbe3edb = SEPARATE merged PR #1889 ("reframe standing-pair single-context async") on MAIN, NOT ancestor of our HEAD -> branch will need merge-time reconciliation w/ #1889, but no unreviewed commit on THIS branch.
