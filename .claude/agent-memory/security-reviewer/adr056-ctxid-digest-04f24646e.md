# ADR-056 Canonical Context Identity = 32-byte digest (branch chore/fuzz-pin-nightly worktree ctxid-digest, top 04f24646e) -- 2026-06-29 -- ZERO FINDINGS

Diff origin/main...HEAD. Fix for #1924 double-hash fail-open. Chokepoint `context_id_to_bytes` (state.rs:2072, now `pub` cross-crate via `scp_core::context::state`): if id is EXACTLY 64 chars AND all `[0-9a-f]` -> `hex::decode` to [u8;32] digest; else `SHA-256(id)` (raw `scp_protocol::context::context_id_bytes`). Total/panic-free (fallthrough on impossible decode failure; clippy denies unwrap/expect). `generate_context_id` (scp-ffi/common/context_id.rs:50) = `hex(32 OsRng bytes)` lowercase, so every real id hits decode branch.

## Verified SAFE on all 5 asked dimensions
1. **Fail-closed**: seal (provider.rs:1581) + open (1675) resolve-consistency guards BOTH now route through `context_id_to_bytes`; BOTH directions have negative tests (`seal_rejects_..._does_not_resolve`, `open_rejects_..._does_not_resolve`). Error strings = "does not resolve to the supplied context_id" — no key/plaintext leak. Recovery-direct send (supervisor.rs:3569 `recovery_send_notification_direct`) rerouted from raw primitive -> chokepoint (was real fail-open: reached for ANY unregistered ctx incl real 64-hex member ctxs during revoke_ucans/rotate_key_packages, not just synthetic identity-private-state). inner.epoch=0 hardcode SAFE (plaintext signed field, NOT AAD-bound; AAD binds sender-key epoch from real crypto state).
2. **Key destruction targets live digest**: key_destruction.rs:91 + ttl.rs (local wrapper -> chokepoint). Real-64-hex regression tests seed group+sender key under DIGEST, drive prod destroy path with STRING, assert export_crypto_state empty under digest after (mutation-resistant; raw-primitive regression => survives => test FAILS). Forward secrecy real on ephemeral close + TTL expiry.
3. **Input handling**: decode branch is bijection on 64-hex domain — no two distinct ids alias; aliasing a victim digest needs 256-bit preimage (== already knowing the id). Synthetic-label SHA-256 colliding a real digest = 256-bit collision (infeasible). No panic/DoS (total fn). Attacker can't force wrong slot.
4. **Error surfaces**: generic "does not resolve" strings, no material.
5. **No auth/UCAN/membership/governance check altered** — pure id->bytes resolution only.

## Keying-vs-routing split (the load-bearing correctness point)
- KEYING (MLS group, sender keys, event log, store, seal/open AAD ctx-bytes): chokepoint `context_id_to_bytes` (digest for real id).
- ROUTING: `context_routing_id` (domain-sep `scp:context-routing:`||id, encrypted ctxs); `broadcast_routing_id` == raw `SHA-256(id)` (§5.14.6, broadcast). Commit a969122b6 fixed `apply_broadcast_publish` (broadcast_helpers.rs) to route `send_message` slot via `broadcast_publish_routing_id` (=SHA-256) NOT the keying digest — read side `scp_node::projection::compute_routing_id`=SHA-256; previously incidentally equal on main, ADR-056 broke equality => host_site CommitCountMismatch{committed:0}. Pinned by `broadcast_publish_routes_under_sha256_routing_id_not_keying_digest`.

## Redirect completeness verified
All prod keying sites in scp-runtime/scp-ffi route through chokepoint: builder.rs (local fn delegates), lifecycle_helpers (create/import/restore/export), messaging_helpers (build_encrypted_envelope/send/deliver/persist/timeouts/buffer), governance_logic (event_log_entries_for_consequences), trust_recovery_helpers, supervisor reconnect publish (9075). FFI: pyo3/napi event_log + testing(join_from_welcome/sync_sender_keys/decrypt), uniffi bridge event_log, scp-testing fullstack/node — all `scp_core::context::state::context_id_to_bytes`. Remaining raw `context_id_bytes(` non-test uses = ONLY chokepoint's own fallback (state.rs:2088) + broadcast routing (correct). export_import raw uses all `#[cfg(test)]` mod (>=912) `ctx-*` labels (chokepoint==raw there). agent_binding_pipeline_tests use `ctx-*` labels (consistent). raw primitive `context_id_bytes` doc-comment in scp-protocol/context/mod.rs rewritten to "raw routing/synthetic-label primitive ONLY; do NOT key a real context".

Mechanical enforcement: source-text grep gate was tried+REMOVED (regex can't tokenize Rust cfg(test) scope, perpetual fail-open). Compile-time `ContextDigest` newtype tracked #1931. Consistent with OwnedIdentityDid AST-gate drop (PR #1826).
