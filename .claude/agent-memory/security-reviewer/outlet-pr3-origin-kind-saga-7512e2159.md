---
name: outlet-pr3-origin-kind-saga-7512e2159
description: Audit of outlet PR-3 delta (f6fd5705a mint origin_kind materialization + saga caveat resolver switch; 4af0af916 review fixes) atop b4e97ff50. ZERO exploitable; 1 MEDIUM completeness; 2 obs.
metadata:
  type: project
---

# Outlet PR-3 origin_kind + saga resolver delta (feat/outlet-report-pr3, b4e97ff50..7512e2159) -- 2026-07-10 -- ZERO exploitable

Two commits atop the prior b4e97ff50 audit: f6fd5705a (mint materializes §7.3.8 origin_kind on delegated/root outlet caps + saga NoCaveat->TokenNb resolver) and 4af0af916 (review fixes: OutletQuery ceiling symmetry, §08 DSL tools->outlets, FFI ceiling error/docs).

**Verified SAFE:**
- **origin_kind CANNOT be forged to a higher-privilege kind (item 1)**: `origin_kind` is NEVER caller-supplied. `build_delegated_caveats` (mint.rs:193) infers it from delegated stems (`infer_origin_kind_from_capabilities`) or inherits from parent, then runs `parent.narrow(child)` (strict-equality on origin_kind, fail-closed child=None). Stems that drive inference are attenuation-checked in delegate_ucan step 2 (`parent.matches(child_cap)`) BEFORE build_delegated_caveats. `CapabilityUri::matches` (capability.rs:175) requires resource EXACT match -> outlet_call parent can't grant outlet_query child, so stem family preserved through delegation. `verify_origin_kind_matches_stem_family` (validate.rs:1870) pins nb.origin_kind to token's OWN stems (OriginKindMismatch if declared!=inferred). Mixed-family rejected unconditionally at mint (try_new_for_root) AND validate (classify_outlet_stem_family). origin_kind IS consumed (stream.rs:1337-1363 session-parity gate) but fed from VALIDATED-NARROWED effective set, so forgery can't survive validation.
- **saga resolver switch (item 2) is fail-closed + security-neutral-to-stricter**: validate_ucan_rebind (saga.rs:1130) required_cap = hardcoded `outlet_call:{outlet_registration_id}` bound to B's own context hex, presenting principal = channel-auth caller_did (confused-deputy binding, AudienceMismatch on wrong aud). It IS an outlet-invocation gate so TokenNbCaveatResolver is CORRECT (matches ffi/outlets.rs:362, napi/outlets.rs:60). OLD NoCaveatResolver would FALSE-REJECT legit delegated outlet chains (None,None arm -> OriginKindUnspecified via attestation-keyed child_is_outlet_edge) -- the bug this fixes. New tests prove: delegated outlet_call/_query materialize+validate; `hand_built_delegated_outlet_token_with_nb_none_is_still_rejected` = the anti-launder guarantee. Fail-closed paths: absent proof_id -> Ok no-op (ungated); resolve_proof err -> 13012; validate_ucan err -> 13013. validate_ucan runs sig-verify (Step 2) + full-chain sig (Step 3) BEFORE trusting nb; Step 7c verify_leaf_outlet_stem_consistency catches forged depth-1 nb. NoopNonceTracker justified (long-lived proof nonce, mirrors broadcast.rs).
- **FFI fail-loud (item 3) all fail-CLOSED + cross-bridge parity**: 4 sites x 3 bridges (pyo3/napi/uniffi media_check + media_initiate + validate_declaration ceiling+role) filter_map->ok_or_else. Ceiling is an ALLOWLIST; erroring on bad entry = denial = fail-closed (old silent-drop only SHRANK allowlist = also fail-closed). Error msgs echo caller cap string + static hint only (no secrets/keys/paths/schema). VALID_7000/PyValueError/NapiError. No accept-path regression.
- **OutletQuery ceiling symmetry (item 4)**: validate_declaration (app_sandbox.rs:773) new arm is strict OR-add: OutletCall matches only OutletCallAll, OutletQuery only OutletQueryAll (disjoint families, still requires wildcard present in ceiling/role). Never query-covers-call, never widens beyond ceiling.
- **§08 DSL (item 5)**: to_capabilities (app_sandbox.rs:141) tools->outlets; `_ => Custom(cat:action)` fallthrough = fail-closed non-authorizing. is_outlet keyed on `parts[len-2]=="outlets"`. No way to emit OutletQuery from DSL (stricter gap, not hole). All output still ceiling-checked in validate_declaration.
- **legacy stem hard-reject intact (roles.rs:220 Capability::new)**: outlet:invoke/outlet_invoke/tool:invoke/tool_invoke/tool:register/tool:interface -> None BEFORE prefix split. Authoritative sites ok_or_else-abort; granted-set filter_map sites silently drop (fail-closed narrow).
- **item 6 check-error-codes.sh UNCHANGED since b4e97ff50** (git diff empty). SCP-TOOL-6114 (caveat mint) in-band 6100-6199, runtime-guarded by validate_outlet_error_code.

**MEDIUM (completeness, NOT a bypass — carry-forward of the prior OBS):**
- `py_check_scoped_capability` (ffi/src/context.rs:1861), `check_scoped_capability_inner` (napi/src/context.rs:5030), `sandbox_check_capability` (uniffi/bridge.rs:8132) — all 3 bridges identical — have the `OutletCallAll ⊇ OutletCall(specific)` shortcut but MISS the `OutletQueryAll ⊇ OutletQuery(specific)` arm that 4af0af916 added to validate_declaration. Fail-CLOSED (a role w/ OutletQueryAll wrongly DENIED a specific query cap here) + cross-bridge parity intact (uniform gap, not divergence), so no security bypass. But it's the SAME asymmetry class the review fix targeted — the fix touched validate_declaration and missed these 3 sibling helpers. Recommend aligning (add the OutletQuery OR-arm to all 3), or better: route all 4 through CapabilityCeiling::contains (roles.rs) which handles both families. Also these 3 keep filter_map(Capability::new) silent-drop on the GRANTED set (fail-closed: narrows authority).

**OBS (non-blocking):**
1. event-log payload.rs:437 test uses stale `"tool:invoke:*"` string in AppBoundPayload.capabilities round-trip (Vec<String> provenance, NOT parsed for authz). Cosmetic/stale, immaterial.
2. Pre-existing test-compile break (unrelated): spawn_from_welcome_tests.rs:3162 `seed_peer_pseudonym` E0599 (pseudonym-routing work); ucan_validate_integration needs --features testing.

**Tests run green:** scp-protocol caveats 114/114; ucan_validate_integration 46/46 (incl 3 new origin_kind tests); app_sandbox 103/103.
