# ADR-049 PR-7 atomic-crypto-move — fix re-review (2a916e9c2..9a365d978) — 2026-07-15

Branch feat/adr049-pr7-atomic-crypto-move. Double-zero pass. ZERO blocking findings; all 5 items SOUND.

1. BLACK-P7-1 identity binding (messaging_helpers.rs:3147): `request.requester_did != sender_did` → Err BEFORE requester_pk resolve / handle_sender_key_request / nonce record. Closes the real bypass: without it, member Bob sets requester_did=Alice and signs with HIS OWN key — that sig verifies against requester_pk (resolved from authenticated sender_did=Bob), so the block/membership gate inside handle_sender_key_request (which keys on request.requester_did) is fooled. Binding forces the gated DID == authenticated DID == signing key. Cannot claim another DID (would need their MLS leaf key). Test `sender_key_pull_receive_path_binds_identity_and_wire_format` asserts reject + pending_distributions empty. Residual: actor block list still empty (deferred #2146) — pre-existing; MLS membership is live Sybil defense; binding makes future block correct.

2. Item-12 context-from-actor: test-only `handle_land_sender_key_response` UNCHANGED in range (comment only) — already correct at base. Real prod path = decrypt_and_dispatch KeyResponse arm (messaging_helpers.rs ~3194): single context_id_bytes threaded through process_incoming_sender_key → check_and_advance_sender_epoch (GATE) → set_unchecked (INSTALL) onto actor-owned cs. Gate-before-install preserved; no split-context confusion (actor only handles its own context; routing guarantees cell==context_id).

3. compile_error guard (scp-ffi/src/lib.rs:58): `all(testing, extension-module)`. Verified feature graph: scp-ffi testing→scp-core/testing→scp-runtime/testing compiles the 3 `#[cfg(feature="testing")]` MessagingCommand handlers + signed_at_override parity seam. extension-module IS the prod Python cdylib marker (maturin passes it; cargo test omits it). Single positive co-enable assertion — bounded/sound, not a denylist. Scoped to PyO3 target only (NAPI/UniFFI use own mechanisms) — acceptable per comment scope.

4. validate_key_package stateless flip (governance_helpers.rs execute_add_member + lifecycle_helpers.rs join_context): provider copy retired (cfg-gated test-only). Now: deps.mls.validate_key_package(bytes,clock) [sig/lifetime/ciphersuite] + KeyPackageIn::tls_deserialize + scp_mls::group::key_package_in_did(kp,Mls10,clock) → compare cred_did==owner_did/member_did else reject. key_package_in_did (scp-mls/group.rs:708) runs FULL KeyPackageIn::validate (leaf-node sig, KP sig, version, Lifetime) + hardened-clock lifetime re-validate THEN reads DID from VERIFIED leaf → cryptographically authenticated DID, "no weaker advisory window" vs add_member. DID binding PRESERVED, no bypass. None-KP fails closed in prod (cfg test/testing only). Minor: double full-validate (harmless belt+suspenders).

5. pub→pub(crate) narrowing (send_checkpoint/send_heartbeat in messaging_helpers, a pub(crate) module) = NO actual visibility change (pub in pub(crate) mod was already crate-only); pure source-text/check-cross-layer clarity. two_party_test_support bob_signing_key/pair_resolver private→pub(crate): module is `#[cfg(test)]` — never production; reached only by cfg(test) mod tests. NO production surface widened; capability reduction intact.

Also verified: heartbeat handler ok_mutated/err_mutated (Class-C MLS send-gen advance, #2149) correct — seal precedes empty-routing no-op so even peerless heartbeat must coalesce; err path also post-seal. BLACK-P7-2 wire-format: actor answer now wrapped in SenderKeyDistributionMessage::KeyResponse (state.rs:2042) matching receiver's from_bytes; fullstack/node.rs + golden test decode the enum.
