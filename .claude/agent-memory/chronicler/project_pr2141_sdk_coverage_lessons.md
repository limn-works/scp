---
name: pr2141-sdk-coverage-lessons
description: PR #2141 (fix/sdk-coverage-fail-closed-and-parity) docs — three UCAN/coverage lessons, sketch withinCeiling att[0] annotation, and the one doc defect found in review
metadata:
  type: project
---

PR #2141 `fix/sdk-coverage-fail-closed-and-parity` added/extended three lessons + a sketch.md annotation. Chronicler R2B3 review (2026-07-15).

**Fact:** Layer-1 trust in Python/TS SDKs validates only `att[0]` of a UCAN. `withinCeiling` is the *uniquely* per-att field (signatures/nonce/revocation/expiry are token-level), so annotating only `withinCeiling` as att[0]-scoped in sketch.md is precise, not an omission. `CapabilityValidation` has 6 fields (tokensValid, signaturesValid, withinCeiling, nonceValid, notRevoked, timeBoundsValid) — pre-existed on main; the sketch was stale at 4 and is now catch-up (not a code→spec flow violation).

**Why:** WASM error routing (`WasmValidateError::Ucan`→PERM-3001 absorbed vs `Context`→CTX-2023 re-thrown) is the parity-critical distinction; the closed allowlist `_PIPELINE_ABSORBED_CODES = {[SCP-PERM-3001]}` is the security boundary.

**How to apply:** The three lessons (typescript-node-only-globals-break-browser, coverage-gates-must-fail-closed, ucan-validate-needs-real-capability-uri) are accurate against implementation. See [[feedback_two_dot_diff_stale_base_trap]] for the review-worktree at /tmp/scp-review-r25.

**R3B1 (2026-07-15) — both R2 residuals FIXED in commit 0ca2b222d, verified correct & complete:** (1) Python snippet now shows `_set_all_false(cap_validation)` before `break` — matches trust.py:915-920. (2) New lesson `python-regex-fullmatch-boundary-validation.md` grounded in trust.py:476/888 (`_CONTEXT_ID_RE.fullmatch`) + regression test test_trust.py:927. All 18 lesson cross-refs resolve; PRD validates (368 stories); PermissionError→UcanPermissionError rename consistent across sdk-common.md/scaffolds/ADRs/main.json/errors.ts (deprecated alias removed, zero stale active-SDK refs). Matrix exemption corrections are factually accurate (rotate_key IS exported by UniFFI bridge.rs:2178 + Swift ScpBindings.swift:1195 — old "does not export" text was wrong; bridgeRegister genuinely internal at bridge.ts:360).

**Sole remaining finding (low-severity SHOULD-FIX, non-blocking):** `.docs/scaffold/go.md` lines 231 & 238 still declare `PermissionError` while the now-canonical sdk-common.md hierarchy names it `UcanPermissionError`. Go is NOT an active binding (project map = python/ts/swift/kotlin) and has no `PermissionError` stdlib-shadowing hazard, so it's a pure downstream-scaffold consistency gap, not a parity/correctness defect. `.docs/standards/go.md` is already clean.

**R6 FINAL (2026-07-15, HEAD 58f6f06b5) — DOCUMENTATION SOUND.** Four post-R4 commits (Swift TrustTests add, Swift+Kotlin insecure participation-twin deletes, scratch-file removal). Verified: (1) zero stale doc/comment refs to deleted twins — every remaining `ParticipationFact/RequireParticipation/ParticipationThreshold/ParticipationProfile` hit is a legit canonical Rust/spec type or generated UniFFI binding, NOT the deleted pure-Swift/Kotlin reimpls; (2) all 4 lessons match code (TS `__decodeBase64UrlToUtf8` trust.ts:333; Python `_CONTEXT_ID_RE.fullmatch` trust.py:889 + `_set_all_false`+break at 920/960; ucan closed-allowlist `_PIPELINE_ABSORBED_CODES` at 935; coverage-gate fail-closed); (3) SCP-302 present, status pending, source anchor §7.2.1 resolves; (4) deletion commits self-document with EXACT non-phantom refs (Swift→`ScpBindings.swift:15097` generated secure path; Kotlin→`scp-kt/.../Scp.kt:1714` delegating to `uniffi.scp.verifyParticipationRequirements`). Scratch file gone, tree clean.

**R9 (2026-07-15, post-merge HEAD e78795e90) — SHOULD-FIX: stale WASM-bridge refs in two net-new lessons.** Branch merged origin/main (ADR-055 WASM-bridge removal + ADR-057 in-browser shared-wasm). Merge-reconcile commit e78795e90 correctly fixed Bridge/register TS matrix cell (bridgeRegister IS public: index.ts:131 re-exports bridge.ts:79; coverage gate PASS, 0 errors) and deleted orphaned test_ts_prefix_parity.py (imports 6 prefix constants now gone from trust.py; typed CapabilityValidation replaced them). BUT missed reconciling its OWN two net-new lessons vs the WASM-bridge deletion it pulled in: (1) `typescript-node-only-globals-break-browser.md` frames whole lesson around "the WASM bridge" + tells authors to reuse helpers in `src/internal/wasm.ts` — DELETED file (TS SDK is napi-only per bridge.ts:784 ADR-055; src/internal/ = bridge/json-utils/native/test-guard, no wasm.ts); (2) `ucan-validate-needs-real-capability-uri.md` "WASM error routing" §(lines 80-103) documents fix pattern in `crates/scp-ffi/wasm/src/ucan.rs` `WasmValidateError`/`validate_tool_ucan_wasm` — DELETED crate + [SCP-PERM-3000] "WASM manager" bullet + cross-ref to wasm-partial-ucan-validation.md. Both lessons' PRINCIPLES survive (structured>prose; feature-detect Node globals under ADR-057 in-browser wasm) — fix = re-ground refs, don't delete. NOTE (non-defect): commit msg e78795e90 mis-cites "ADR-057 (ucan_evaluate typed path)" — that ADR is now ADR-059 (renumbered from 057 after collision w/ in-browser-client ADR-057, see phase-2.md:1964); ALL living docs/comments correctly cite ADR-059 (trust.py:106, sdk-consume-structured lesson:71), so commit-msg-only, immutable, no living-doc defect. Pre-existing wasm-partial-ucan-validation.md (on main, not this PR) likely also ADR-055-stale = separate concern.

**R25 (2026-07-16, HEAD bc3d88eef, post ADR-055/ADR-059 full merge) — SHOULD-FIX (strong): `ucan-validate-needs-real-capability-uri.md` main body describes DELETED code as current.** After the merge, `evaluate_trust` (trust.py:989-998) calls `instance.ucan_evaluate(context_id, token, None, subject_did)` + `structured_to_capability_validation` — passes `None` (not att[0].with, not "*"); the structured diagnostic reads bools and does NOT throw on capability outcomes (comment trust.py:964-967). trust.ts has NO evaluateTrust body (moved to SCP class Phase 4). VERIFIED zero matches across `bindings/` for: `__extractFirstCapabilityUri`, `validateOneCapUri`, `__decodeBase64UrlToUtf8`, `_PIPELINE_ABSORBED_CODES`, `REVOCATION_PREFIXES`, `_set_all_false`, `_extract_first_capability_uri`. The lesson marks ONLY two sub-sections "Historical (superseded by ADR-059)" (_PASSED_BEFORE + multi-att) but leaves the equally-superseded main body current-tense: "Fix" (§17-33, `__extractFirstCapabilityUri`+`ucanValidate(uri)`), "PERM-3001/VALID-* closed allowlists" (§34-77, `validateOneCapUri`/string absorption), "Bridge error routing" (§79-92), "Revocation prefix narrowing" (§158-184, `REVOCATION_PREFIXES`), "Rules"/"Detection" (§94-104). Internally contradictory + will lead a future agent to reintroduce the superseded client-side pattern. This lesson is NET-NEW on this branch (+194). Enduring/accurate parts that survive: title principle "never pass '*' — pass None for intrinsic validity" (code comment trust.py:973 confirms); "What Layer 1 measures: self-consistency NOT authorization" (§106-120, matches comment 969-976); closed-allowlist-over-denylist as a *general* principle. Fix = mark the client-side sections historical or rewrite for the structured `ucan_evaluate(None)` path + correct "What happened". Commits 3de060e97/730e512e5 claimed to reconcile this lesson w/ ADR-059 but did it partially. CLEAN at R25: sketch.md capabilityValidation = 6 fields (tokensValid/signaturesValid/withinCeiling/nonceValid/notRevoked/timeBoundsValid, sketch.md:810-817), att[0] annotation removed; matrix Bridge/register typescript:true consistent (bridgeRegister exported index.ts:131); ADR numbering verified (ADR-055=WASM removal, ADR-057=in-browser-shared-mls, ADR-058=typed trust INPUTS, ADR-059=structured trust OUTPUTS/ucan_evaluate renumbered from 057 per phase-2.md:1964); typescript-node-only-globals lesson OK (Note reframes deleted `__decodeBase64UrlToUtf8` as evergreen principle, cites ADR-055/ADR-057 correctly); check-sdk-coverage.py alias removal (3713b0e1c) removes an INERT redundant TS alias (domain_camel auto-matches bridgeRegister), does NOT weaken coverage. CONSIDER: ucan lesson §193 "See also" still points at wasm-partial-ucan-validation.md (file exists, not a broken link, but describes removed WASM pipeline — pre-existing ADR-055-stale, separate cleanup).

**R26 Pass-1 double-zero (2026-07-16, HEAD 28623a226) — DOCUMENTATION SOUND; prior R25 SHOULD-FIX + CONSIDER both FIXED.** Commit `13aecbbbb` "docs(lessons): restructure ucan-validate lesson for ADR-059 era" resolves the R25 finding: the ucan-validate lesson now cleanly separates "## Current approach (ADR-059)" (structured `ucan_evaluate(context_id, token, None, subject_did)` → 6 booleans, matches trust.py:965 exactly incl. the `None` challenge cap + required non-defaulting `presenting_agent_did`) from "## Historical: the ucanValidate + att[0].with extraction era (pre-ADR-059)" (all past-tense, "now deleted"). No current-tense superseded code remains; internal contradiction gone. TS example `scp.ucanEvaluate(handle, token, subjectDid)` matches the intrinsic-mode call (capability omitted→null; scp.ts:2366 sig is `ucanEvaluate(handle, token, presentingAgentDid, capability?, proofTokens?)`). "See also" no longer points at stale wasm-partial-ucan-validation.md (R25 CONSIDER cleared); both cross-refs (typescript-node-only-globals, delegation-chain-full-validation) resolve. `_coded_bridge_error` docstring (errors.py:315) accurate: BRIDGE_ERROR_MAP lookup by class name defaulting ContextError (326), SCP-CAT-NNNN regex recovery (327-8), already-typed ScpError passthrough (324-5). `scp-typed-errors.test.ts` docstring accurate: getBridge path wrapped by wrapBridgeErrors Proxy (getBridge→createNativeBridge→wrapBridgeErrors, native.ts:2127); explicit try/catch guard path for sync methods; issue #2157 is REAL+OPEN ("wrap this.#native in wrapBridgeErrors at SCP construction"). scp.py 8-method wrapping = pure code, no docstrings. VERDICT LGTM.

**Lessons authored (2026-07-16) from the error-handling fix work (`_coded_bridge_error` / typed-errors):** three new `.docs/lessons/` files — (1) `python-bridge-error-message-strip-double-bracket.md` (strip `[SCP-CAT-NNNN] ` prefix before building ScpError; `__str__` = `f"[{self.code}] {self.message}"` at errors.py:49 re-prepends the code, so raw string → doubled bracket; extract code first, then strip); (2) `test-error-code-fixtures-must-pass-conformance-gate.md` (check-error-codes.sh scans ALL .ts/.py/.rs incl. tests, no test exclusion; GOV=11000-11999, use `SCP-UNKNOWN-*` allowlisted sentinel for placeholder tests); (3) `wrap-error-sibling-methods-together.md` (wrap identity_remove + identity_remove_if_present together — sibling footgun, scp.py:741/750). No lessons index/README exists (flat dir); nothing to update. All three cross-linked to each other.

**R8 FINAL (2026-07-15, HEAD 5d118e1a2) — DOCUMENTATION SOUND.** Two commits since R6 (`7097938f5` scratch-file removal, `5d118e1a2` tightened `_CODES_RETURN_RE` `codes::(\w+)`→`=>\s*\{?\s*codes::(\w+)` in test_ucan_conformance.py); neither touches .docs/. `_CODES_RETURN_RE` referenced by zero lessons/specs/ADRs → regex change introduces no staleness. All 9 lessons accurate at HEAD, SCP-302 pending w/ §7.2.1 anchor resolving, identity-migration lesson cites §9.12 (correct) + §3.2.1 (legit custody-path ref) — both resolve. No stale refs anywhere.
