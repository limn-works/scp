---
name: pr2234-rotatecontentkeys-followup
description: PR #2234 doc/provenance review — fixes #2218 stale §-refs but reintroduces the same line-number-as-§ anti-pattern (§2033) and adds an inaccurate "dead data" note
metadata:
  type: project
---

PR #2234 `fix/rotate-content-keys-review-followup` — cleans up review findings from [[pr2218-keyepochadvance-provenance]]. Real diff is tiny (5 files) once you diff vs `origin/main`, NOT local `main` (local main was ~340 files stale — always fetch+diff origin/main).

Findings (doc/provenance review):
- **§2033 = line-number-as-§ anti-pattern REINTRODUCED** in BOTH new TODO(spec) comments (governance_helpers.rs:986 + :3204): `// TODO(spec): §2033 vs §5.14.10/ADR-011 tension unresolved`. There is no §2033/§20.3.3 section; spec line 2033 is actually the "Sybil resistance" para, not the fail-closed durability para (line 2035 = §5.14.8 "Durability (crash boundary)"). Correct ref = §5.14.8 durability clause / §9.4.2. This is the EXACT defect the PR set out to fix (§2008/§2015→§5.14.8/§5.14.10).
- **"dead data" note inaccurate**: broadcast.rs:132 rustdoc + governance_helpers comment claim `BroadcastKeyEpochAdvance.timestamp` (ms) "is only consumed on the per-author block relay-message path." FALSE — that field is never read in ANY production code (only tests, broadcast/mod.rs:6261). The relay/block path signs `SenderKeyEpochAdvance` (key_protocol.rs:55) which has NO timestamp field. Field is genuinely unconsumed everywhere; note asserts a nonexistent live consumer.
- **spec-vs-code MUST/best-effort**: new §5.14.8 clause says "all authors MUST advance ... a mandatory KeyEpochAdvance per author" but the KEA leaf emission is best-effort (warn+continue). Two distinct things conflated: the epoch-advance state mutation (fail-closed, in rotate_all_author_keys before ContentKeysRotated leaf) vs the KEA event-log LEAF emission (best-effort). Spec's "mandatory" + the durability clause (leaf listed as an observer that must not see effect pre-durability) together imply fail-closed leaf, contradicting best-effort code. TODO honestly flags but doesn't resolve.
- TODO(spec) has no SCP-NNN story id (deferral/no-deferral tenet). Project norm bans bare issue-refs in code, but `#1847` sits on touched test-header lines (pre-existing).

Verified GOOD: §5.14.8 (Blocking, now holds RotateContentKeys clause) + §5.14.10 (Event Log, defines KeyEpochAdvance type) both exist & are correct refs; remaining §9.17 refs (governance_helpers:1616, broadcast:833) are LEGIT (Content Access Key Layer, real §9.17.x) — PR correctly only changed the §9.17 that wrongly labeled broadcast-key rotation; spec "leaves after durable ContentKeysRotated leaf" matches impl; determinism sort-by-author_did + 2 new tests sound (§9.9.3); INVARIANT comment on execute_reconfigure_governance is PRE-EXISTING (not new) and accurate (single non-test call site @4700).

Task-desc drift: task listed a "# Parameters" rustdoc section, an INVARIANT comment as newly-added, and `#[allow(too_many_lines)]` on execute_add_member — none of those are in the actual pushed diff.

**PASS 3 (2026-08-02, branch reworked to 7 commits):** Pass-2 findings RESOLVED. §2033 phantom ref ELIMINATED (git grep §20xx = 0). Spec-vs-code contradiction fixed in the CORRECT direction: coder made governance-path KEA leaf emission FAIL-CLOSED (`.map_err(...)?` in execute_revoke + execute_rotate_content_keys), matching spec §5.14.8 "mandatory/fail-closed per ADR-011". ADR-011 convergence citation is FAITHFUL — phase-2.md:911-912 ("derived record convergent iff trigger convergent") + 977-984 ("every honest member appends the identical consequence leaf"). Counter now bumps +1 inline after each durable leaf (mirrors execute_reconfigure_governance). Sort rustdocs ACCURATE (rotate_all_author_keys body sorts at mod.rs:1740; GovernanceBanResult + unsubscribe sorted; 3 new tests assert sorted WITHOUT re-sorting). execute_revoke comment "AccessRevoked leaf (fail-closed above)" = real EventType::AccessRevoked via `.await?`.
REMAINING (2× MEDIUM): (1) timestamp rustdoc — "currently unused in production" qualifier now ACCURATE (no prod reader anywhere), but clause "Carried for the relay-message consumer on the per-author block path" asserts a consumer that CANNOT exist: block-path wire msg = SenderKeyEpochAdvance (key_protocol_verify.rs:119) has NO timestamp field, and all KEA leaves use KeyEpochAdvancePayload{old,new} (no ts). Field is carried by nothing on any path. (2) Provenance asymmetry: spec §5.14.8 durability clause groups per-author block AND governance ban together as authorization-downward, but PR makes ban-KEA fail-closed while block-KEA (block_broadcast_subscriber) stays best-effort. unsubscribe best-effort IS spec-grounded (authorization-UPWARD, §5.14.8). block-KEA best-effort exemption from the same ADR-011 convergence rule is UNDOCUMENTED. No CRITICAL/HIGH remain.

**PASS 4 (2026-08-02, commit 8818768a9):** Pass-3 finding (2) RESOLVED — block_broadcast_subscriber now carries the ADR-011 rationale (broadcast_helpers.rs ~737: "per-author unilateral block is not a convergent governance trigger (single-origin, not MLS-commit-ordered); companion KEA is a trailing audit leaf, remains best-effort"). Grounded in ADR-011 convergence taxonomy (phase-2.md:911). Accurate + sufficient. Counter logic verified consistent (conditional bump only on KEA success = matches durable leaf count, no §9.9.3 drift). "intended to be co-located" softening = accurate (best-effort KEA can be absent). UnsubscribeResult.key_rotations sort-order rustdoc = exact (ascending sort_unstable_by author_did, test asserts alice<bob<charlie w/o re-sort). field doc "Currently unconsumed — no production wire message or event-log leaf reads this field" = VERIFIED accurate (all 3 paths use timestamp_secs/block_ts, not advance.timestamp; only unit-test read @broadcast.rs:1085).
PASS-4 REMAINING findings: (A) MEDIUM — pass-3 finding (1) only HALF-fixed: the phantom "relay-message consumer on the per-author block path" was killed in the STRUCT FIELD doc (broadcast.rs) but SURVIVES VERBATIM in governance_helpers.rs execute_rotate_content_keys NOTE (~lines 3186-3187 on PR branch): "The ms field is carried by BroadcastKeyEpochAdvance for the relay-message consumer on the per-author block path; it is dead data in this governance path." Now directly SELF-CONTRADICTS the corrected field doc within the same PR. Block path (verified) neither constructs nor reads a BroadcastKeyEpochAdvance. (B) LOW — seed_broadcast_author docstrings inconsistent: class_s.rs:236 correctly says "mirroring the author-publish/add path"; but commands.rs SeedBroadcastAuthor doc + supervisor.rs seed_broadcast_author doc both claim it "populate[s] the author registry the same way a delivered RotateContentKeys governance action would" — RotateContentKeys does NOT add authors (rotate_all_author_keys iterates EXISTING authors; real registration = manager_methods.rs:232 add_author(creator) at creation). Test-only code, mechanism clear from code. NOTE: verify against `git show origin/fix/...:file` NOT local worktree — local governance_helpers.rs is a 3rd uncommitted state (still has stale kea_success_count + §2033 TODO that the PR branch already removed).
