---
name: adr057-t1c-dht-extract
description: ADR-057 T1c extract DHT transport into scp-dht crate — now COMPLETE @427a6c3f8 (was INCOMPLETE @6932d2fbf)
metadata:
  type: project
---

**PASS-3 (908daf5ce, pass-2-polish) VERIFIED — near-COMPLETE, 1 LOW nit.** Range c102f8222..HEAD (908daf5ce atop 427a6c3f8 atop 6932d2fbf). 908daf5ce = doc/test/wrapper polish only; introduced NO regression. Confirmed: (a) inlined `scp_dht::verify_bep44_signature(...)?` in dht.rs:958 preserves fail-closed — resolve_did returns IdentityError, `?` uses exhaustive `impl From<scp_dht::DhtError> for IdentityError` (lib.rs:295, all 3 variants, no wildcard); deleted private wrapper had ZERO other callers (grep empty); scp-identity compiles clean. (b) deleted duplicate test `bep44_signable_format_is_correct` — byte-identical survivor at scp-dht/src/lib.rs:101 (passes; 9/9 scp-dht tests green incl 3 verify_bep44 tamper/roundtrip). (c) .docs/ relabel corpus-consistent: ZERO bare `T1c`, 6×T1c-a + 6×T1c-b in ADR-057. (d) doc paths resolve: architecture.md DID-method row fixed scp-core/src/identity/mod.rs → scp-identity/src/lib.rs (DidMethod trait actually at :401 ✓); self-hosting guide → scp-dht/pkarr_client.rs publish:233/resolve~292 ✓. (e) no residual scp_identity:: refs to moved symbols (only hit = gitignored templates/personal-relay/target/ build cruft, 0 tracked target files; current template main.rs:23 `use scp_dht::PkarrDhtClient`, scaffold main.rs:27 `use scp_dht::InMemoryDhtClient` — both fixed; 3 FFI DidDht refs import InMemoryDhtClient from scp_dht ✓). Full-range re-confirmed: publish ORDER correct (scp-dht:388 BEFORE scp-identity:420/runtime:444/node:484 — prior gap#1 fixed); Cargo.lock+fuzz/Cargo.lock have scp-dht; no-shim gate closed set; ci.yml+docs.yml fuzz filters; production-dht user-agent `concat!("scp-dht/",env!("CARGO_PKG_VERSION"))` compiles+clippy-clean. SOLE FINDING (LOW, non-blocking): commit relabeled T1c→T1c-a "corpus-wide in .docs/" but 5 bare-`T1c` citations remain OUTSIDE .docs/ pointing to a now-nonexistent ADR heading — code: scp-identity/src/dht.rs:3314 (security-parity=actually T1c-b), scp-identity/src/lib.rs:294 (From seam=T1c-a), scp-dht/src/lib.rs:14 (=T1c-a), scp-dht/README.md:18 (=T1c-a); + CLAUDE.md:266 project-map (=T1c-a; NOT an enforcement section, safe to edit). Same nit-class the commit itself targeted; extraction correctness/completeness fully intact.


ADR-057 T1c: extract DHT transport (DhtClient/DhtRecord/InMemory/Pkarr + bep44_signable/verify_bep44_signature + DhtError) into new native `scp-dht` crate. Worktree adr057-t1c, branch feat/adr057-t1c-scp-dht, HEAD 6932d2fbf on base c102f8222.

VERDICT: INCOMPLETE (READ-ONLY review). Core extraction sound but 6 gaps.

**Clean/verified (no findings):** scp-dht in workspace members; all consumer Cargo edges (scp-identity/runtime/node/testing/ffi{common,napi,uniffi} declare scp-dht dep); production-dht forwarding (scp-identity `production-dht=["scp-dht/production-dht"]`; scp-node + uniffi enable scp-dht/production-dht directly; scp-identity has ZERO own production-dht cfg gates left); mainline/reqwest fully removed from scp-identity Cargo; root Cargo.lock has scp-dht committed; no-shim gate (check-no-shim-reexports.sh) closed set + owning_dir + CLAUDE enforcement list all include scp_dht; wasm CI job (ci.yml:338) correctly EXCLUDES scp-dht (tokio, not wasm-safe); all 3 FFI bridges import scp_dht::InMemoryDhtClient; node uses scp_dht::{bep44_signable,PkarrDhtClient}; verify_bep44_signature REMOVED from scp-identity lib.rs pub-use (not repointed=no shim); From<DhtError> for IdentityError maps 3/3 variants; tests traveled (5 InMemoryDhtClient tests in dht_client/mod.rs + 4 NEW bep44 tests in scp-dht/src/lib.rs).

**FINDINGS (INCOMPLETE):**
1. release.yml: scp-dht publishable (no publish=false) but ABSENT from tags array (:167-184), publish-crates steps (:363-486), dry-run summary (:863). ORDERING BREAK: scp-identity(:411)/runtime(:435)/node(:475) declare scp-dht version="=0.1.0-beta.2" (registry) → `cargo publish -p scp-identity` FAILS (scp-dht not on crates.io). scp-dht must publish BEFORE scp-identity.
2. ADR-057 T1c bullet (line 86) scopes TWO deliverables: transport extraction + z-base-32 PARSER CONSOLIDATION ("collapsing DidDht::verify and same-crate scp_identity::dht::extract_public_key onto the single scp-did parser…T1c resolves both before consolidating"). Commit did ONLY extraction — dht.rs:830 extract_public_key unchanged (git diff empty), still Result<_,IdentityError> inline decode; DidDht::verify(dht.rs:2135) still uses native parser not scp_did. Two parsers remain.
3. scaffolds/rust-client/src/main.rs:27 `use scp_identity::dht_client::InMemoryDhtClient` = deleted module. + scaffolds/rust-client/Cargo.toml has 0 scp-dht dep. NOT CI-built (won't fail CI) but shipped broken.
4. templates/personal-relay/src/main.rs:23 `use scp_identity::{…PkarrDhtClient…}` = no longer re-exported. + Cargo.toml 0 scp-dht dep.
5. fuzz/Cargo.lock: committed HEAD has NO scp-dht (grep -c=0) but fuzz→scp-runtime→scp-dht. Fix exists ONLY as uncommitted working-tree change (M fuzz/Cargo.lock). ci.yml:830 fuzz check no --locked → self-heals, but committed lock inconsistent.
6. ci.yml:67-76 fuzz path filter omits crates/scp-dht/** (lists all other transitive fuzz-dep crates). docs.yml:63-72 same minor omission (rustdoc auto-includes via --workspace).
Doc divergence (all .docs/ + CLAUDE.md UNTOUCHED by diff): CLAUDE.md project map no scp-dht; architecture.md:275 map no scp-dht + :706 "the DHT is not separable" now CONTRADICTS T1c + :720 stale scp-core/src/identity/dht_client.rs path; ADR-057:83-86 still "T1c…follow" future tense (T1 marked landed, T1c not).

LESSON: on a crate-EXTRACTION slice, the highest-yield gaps are the non-code layers — release.yml publish-order for the NEW publishable crate (registry-version deps make dependents' publish fail), the committed-vs-working-tree fuzz/Cargo.lock split, CI path filters listing transitive-dep crates, and whether the ADR's slice bullet scoped MORE than one deliverable (T1c = extraction + parser consolidation; only half shipped).

=== RE-VERIFY @427a6c3f8 (delta commit atop 6932d2fbf; base c102f8222) → VERDICT NOW COMPLETE ===
Delta commit `427a6c3f8` "fix(dht): T1c review batch" closes ALL 6 prior gaps + adds hygiene:
1. release.yml FIXED — scp-dht in TAGS(:171), publish step (:388-393) at position 4 right after scp-did BEFORE consumers scp-identity(:420)/runtime(:447)/node(:487), and dry-run summary (:872). Publish order verified topological. All 3 lists now 17 crates identical.
2. Parser consolidation NOW LEGITIMATELY DEFERRED — ADR-057 T1c bullet split into **T1c** (transport extraction, retensed past-tense "landed in this change set") and **T1c-b** (did:dht parser consolidation, "immediate next slice"). T1c-b honestly documents the two SEMANTIC blockers (error-taxonomy Result<_,String> vs Result<_,IdentityError>::ZBase32DecodeError; did:key test-scope gate divergence) — legit forward-only slice split per one-way flow, NOT phantom deferral. Judged against UPDATED artifact = not a gap.
3. scaffolds/rust-client FIXED — main.rs:27 now `use scp_dht::InMemoryDhtClient`; Cargo.toml adds scp-dht path dep.
4. templates/personal-relay FIXED — main.rs:23 `use scp_dht::PkarrDhtClient` + scp_identity import drops PkarrDhtClient; Cargo.toml adds scp-dht path dep w/ production-dht.
5. fuzz/Cargo.lock FIXED — scp-dht now committed (pkg @2509, dep-of two crates @2587/2710).
6. ci.yml FIXED — scp-dht in `fuzz:` path filter (:76, correct location — fuzz→scp-runtime→scp-dht transitive; NOT in any wasm line). docs.yml doc filter :68 adds scp-dht/src.
Doc divergence ALL FIXED: CLAUDE.md map adds scp-dht row; architecture.md adds crate-map row + Layer-ladder entry + fixes "DHT is not separable"→qualified to DID-**method** layer (transport WAS separable, extracted) + fixes stale DhtClient replaceable-subsystem path scp-core/src/identity/dht_client.rs → crates/scp-dht/src/dht_client/.
Delta HYGIENE (new, correct): deleted pointless public DidDht::bep44_signable pass-through (inlined caller+test onto scp_dht::bep44_signable); made z-base-32(crate module `zbase32`)+tracing OPTIONAL under production-dht — VERIFIED airtight: pkarr_client.rs (only user of zbase32::encode/tracing/mainline/reqwest) fully `#[cfg(feature="production-dht")]` gated at mod.rs:180 + lib.rs:23, so default build (bep44 helpers + DhtClient/DhtRecord/InMemoryDhtClient + DhtError) compiles with zero optional deps.
Re-verified clean: workspace member (Cargo.toml:7); no-shim gate scp_dht in closed set(:47)+owning_dir(:55)+header; both locks have scp-dht; residual sweep for scp_identity::{dht_client,DhtClient,DhtRecord,DhtError,bep44_signable,verify_bep44,InMemory/PkarrDhtClient} = ZERO (3 hits were `scp_identity::DidDht::<InMemoryDhtClient>` w/ imported scp_dht type = correct); scp-identity lib re-exports only DID-method symbols (extract_public_key STAYS — that's the T1c-b deferred parser).
ONE NON-BLOCKING OBSERVATION (pre-existing, out of T1c symbol-move scope): architecture.md replaceable-subsystems table DidMethod row still cites `scp-core/src/identity/mod.rs` but trait actually lives scp-identity/src/lib.rs:401 — stale path in the same table whose adjacent DhtClient row the delta correctly fixed; predates T1c (DidMethod not moved by this slice), does not make T1c incomplete.
LESSON-2: a follow-up "review batch" commit that closes an extraction's non-code gaps is the norm; when re-verifying, the parser-consolidation deferral is only legit if the ADR was ACTUALLY split into a named next slice with honest blockers (T1c-b) — a bare "deferred" note would be phantom. Confirm the artifact edit, not just the code.
