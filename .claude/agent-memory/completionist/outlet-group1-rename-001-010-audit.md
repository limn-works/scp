---
name: outlet-group1-rename-001-010-audit
description: Whole-outlet audit Group 1 (SCP-OUT-001..010 tool→outlet rename+foundation) @ origin/main d5de8b153 — 7 COMPLETE, 3 GAP (007/009/010 deliverables never reached main via squashed re-port #2098)
metadata:
  type: project
---

# Outlet Group 1 audit (SCP-OUT-001..010) @ origin/main d5de8b153

Whole-outlet completeness audit of the tool→outlet rename + foundation stories. All 10 marked `done` in `.docs/prds/outlet.json`.

**META-FINDING (root cause of the 3 gaps):** The outlet feature reached main NOT via the per-story commits but via ONE squashed re-port commit `d4998aec3 feat(outlet): complete outlet re-port (#2098)` (ancestor of HEAD). The original per-story commits (`f5236ef6a` SCP-OUT-007, `b5a25f142` SCP-OUT-009, the 010 superseded_by edits) are on the outlet-report/redesign branch line that is **NOT an ancestor of HEAD** (`git merge-base --is-ancestor f5236ef6a HEAD` = NO). The re-port brought protocol/runtime/event-log/FFI/SDK/enforcement cleanly but OMITTED the deliverables of 007, 009, 010 → three stories falsely marked done on main.

## Verdicts
- **001 spec** COMPLETE substance. Specs clean of residual tool; separators `SCP-OUTLET-REGISTRATION-V2` + `SCP-OFFER-ID-V1` consistent code↔spec. AC0/AC1/AC5 grep-ACs are STALE/self-contradictory tripwires: AC0 bans the very separators that legitimately shipped; AC1 `OutletInvoke\b` catches the KEPT wire type `CrossContextOutletInvoke`; AC5 wants `SCP-OUTLET-OFFER-ID-V1` in 09-security but shipped is `SCP-OFFER-ID-V1` in 25-test-vectors. §5.4.4 Outlet Error Taxonomy NOW EXISTS (resolves prior phantom-provenance note).
- **002 protocol** COMPLETE. Named const `OUTLET_REGISTRATION_V2_DOMAIN` exists (hash.rs:62) — AC5 wanted exact name `SCP_OUTLET_REGISTRATION_V2` (stale). roles.rs:5877 negation comment ("does NOT have OutletInvoke") trips AC8 grep (benign). AC6 self-contradictory (bans shipped separator). PaidActionType::OutletCall / per_outlet_call present.
- **003 event-log** COMPLETE substance (0 Tool* variants; OutletRegistered/Updated/Invoked/Verified/InterfaceEstablished/OutletRemoved/CrossContextOutletInvoked present). ACs 1(variant SET)/2/3/4/8 describe a SUPERSEDED manual-discriminant + `OldFormatRejected` + per-type-metrics design; shipped = ADR-011 unified serde `EventType` (closed enum, name-tagged, no discriminant() table, no OldFormatRejected, metrics.rs has no per-type counters). Stale ACs.
- **004 runtime** COMPLETE. tools/ gone, outlets/ present, 0 old fns, InvocationError::Outlet* present. AC2 `manager/outlets.rs` & AC4 8-fn-on-ContextManager list STALE (ops live in context/outlets/ submodule; verify/session in outlets/session.rs).
- **005 FFI** COMPLETE (PyO3/NAPI/UniFFI). ZERO tool residue all bridges. All ops present: outlet_register/invoke/verify/session_create/invoke/close/interface_expose/accept/revoke/invoke_cross_context(_saga). Naming: `outlet_*` (PyO3), `outlet_*_on` (NAPI handle-affinity), bridge.rs (UniFFI); AC3/AC10 `context_outlet_*` spelling STALE. **WASM ACs 12/13/14 N/A**: `crates/scp-ffi/wasm` has NO source — removed per ADR-057; browser = `scp-client-wasm` participant-only, no outlet surface (matches prior "WASM N/A 0 outlet surface").
- **006 SDKs** rename COMPLETE (outlets.py/ts/swift files moved, tool test files deleted) EXCEPT residual (see below). Namespace-consolidation ACs (2/3/4/11/12/17/18/22/23 full 7-verb OutletNamespace + sessions/offers sub-namespaces) UNMET — shipped `ctx.outlets` exposes ONLY `invoke`; other verbs flat on top-level SCP/Context. BUT single-verb design is SANCTIONED (outlet.json:2267 later story cites "SCP-OUT-006 single-verb" as canonical; code comments cite it) → stale ACs. AC33 SessionId newtype ABSENT in ALL 4 SDKs (sessions use raw str/string) — genuine divergence needing reconciliation. Swift ships the AC18-PROHIBITED top-level `Context.invokeOutletCrossContext` (Outlets.swift:216).
- **007 MCP translator GAP** (marked done, deliverables ABSENT on main). No `crates/scp-mcp/src/translator.rs`, no `mcp_to_scp`/`scp_to_mcp` (grep=0), no `crates/scp-mcp/tests/`, no dot-delimiter kind projection. scp-mcp is LIVE (workspace member, consumed by scp-ffi Cargo.toml:70) and still uses SCP-internal tool vocab: `fn invoke_tool(context_id, tool_name, arguments)` (server.rs:158 trait + sse.rs:581/stdio.rs:275/server.rs:925). allowlist.rs doesn't use outlet_id vocab. Built in non-ancestor f5236ef6a; re-port omitted it.
- **008 enforcement** COMPLETE. matrix 0 `tool_`, outlet_economy_wiring.rs exists (tool_economy_wiring gone), ffi_conformance 0 tool_, pipeline_wiring 27 outlet refs. All 4 scripts GREEN (check-sdk-coverage/protocol-sync/cross-layer/protocol-deps exit 0).
- **009 vectors GAP** (marked done, deliverable ABSENT). `tests/conformance/vectors/outlet_registration_v2.json` (12 entries) does NOT exist (only outlet_stream_vectors.json + outlet_streaming_saga_vectors.json present). 25-test-vectors.md doesn't document V2 vectors. Domain separator IS pinned inline (registration.rs:535, registry.rs:1726, hash.rs, uniqueness catalog test_vectors.rs:1303) but the AC0-4 cross-bridge sign-verify conformance vector suite + negative-V1 test is missing. Built in non-ancestor b5a25f142; re-port omitted it.
- **010 main.json GAP** (marked done, provenance markers ABSENT). `details.superseded_by` on SCP-010/013/018/040/041 ALL absent (AC0-4 fail). AC-TEXT cleanup DID land (SCP-018→"outlets (Vec", SCP-040→"py_outlet_register"). AC5 (0 tool-era AC strings)/AC6 (validate-prd 437 stories)/AC7 pass. All 5 stories status=done so no active dangling scope, but the provenance-linking deliverable is missing.

## Residual-tool findings (concrete)
- **bindings/typescript/src/trust.ts:707** — `.filter(e => e.eventType === "ToolInvoked")` but canonical is `OutletInvoked` (core lib.rs:133; other SDKs use OutletInvoked) → `toolInvocations` (field :113) ALWAYS EMPTY. Live functional bug + cross-SDK parity break. Also residual prose/naming :16/:104/:112/:694/:704-706. ONLY trust.ts still emits "ToolInvoked".
- scp-mcp `fn invoke_tool(...tool_name...)` internal vocab (007 gap, above).
- Core crates otherwise CLEAN: only roles.rs:228/233/5698-5702 = intentional SCP-OUT-014 legacy hard-reject of `tool:invoke:`/`tool:register`.

## Overall: INCOMPLETE (3 stories falsely done: 007, 009, 010; + trust.ts residual bug; + AC33 SessionId absent).
LESSON: when a feature lands via a squashed "re-port" commit instead of the per-story commits, per-story `done` status is unreliable — check `git merge-base --is-ancestor <story-commit> HEAD` for each story's original commit; a NO means the re-port may have silently dropped that story's deliverable.
