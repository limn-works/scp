---
name: adr057-t1-primitives-dissolve
description: Completeness review of ADR-057 T1 — dissolve scp-primitives into scp-clock/scp-crypto, extract wasm-safe scp-did; verdict COMPLETE
metadata:
  type: project
---

ADR-057 T1 crate-topology split (branch refactor/dissolve-primitives-split-identity, HEAD 5b35cb9aa — re-verified 2026-07-03; earlier at d2a783a55/280983dd7, range 86519aa6f..HEAD). Verdict: **COMPLETE** — zero findings.

**Latest commit 5b35cb9aa (release-pipeline fix, independently re-verified clean):** dropped `publish=false` scp-client-wasm from release.yml — all 3 lists (TAGS lines 168-183, individual `cargo publish -p` steps 363-486, dry-run summary line 863) now equal EXACTLY the 16 publishable crates = all 19 workspace crates minus the 3 `publish=false` (scp-ffi, scp-testing, scp-client-wasm); publish order is a valid topological order with leaves clock→crypto→did first. Also hardened no-shim gate's `//`-line filter with a `*/` guard (skip a `//` line only when it lacks `*/`, closing a prior-line block-comment evasion — one bounded exception, not open-ended block parsing), and reframed ADR T1c's 2-parser divergence as a browser-path security-parity gap (through T1→T1c the wasm stack — scp-event-log tree.rs + scp-protocol bridge/claiming.rs — reaches ONLY the weaker `scp_did::extract_public_key_from_did` which lacks the z-base-32 canonicality round-trip check; the port LEADS T1c). Gates `check-no-shim-reexports.sh` + `check-protocol-deps.sh` both pass; both wired at ci.yml:186-187; no-shim registered in CLAUDE.md:112 enforcement list.

**Why:** dissolves scp-primitives junk-drawer into three wasm-safe capability leaves; extracts DID data model out of scp-protocol into scp-did; deletes all back-compat shims; retargets every layer. Reviewed for completeness/divergence per completionist mandate.

**How to apply (what I verified, so a re-review traces fast):**
- scp-primitives fully gone: workspace members, ALL crate manifests, root+fuzz Cargo.lock, CI path filters, no stale present-tense doc refs (architecture.md/ADR/READMEs are correctly past-tense).
- Leaf deps match ADR crate table EXACTLY after pass-4: scp-clock = zero-dep (WasmClock lives in scp-client-wasm/src/time.rs impl scp_clock::Clock, NOT in the leaf); scp-crypto = ed25519-dalek only; scp-did = pure leaf, ed25519-dalek + serialization directly, **no scp-crypto edge** (validates via VerifyingKey::from_bytes). ADR table line 109 + architecture.md:667 both state "scp-did on ed25519-dalek directly" — consistent.
- Every declared scp-clock/crypto/did edge is actually used (grepped all consumers; lowest = 1 real usage, none comment-only). No unused deps. z-base-32 imports as `zbase32` (lib≠package name) — used, not dead.
- DidDocumentError→DidError rename complete (zero remnants). DidError in scp-did/src/document.rs:55.
- No forbidden shim re-exports: check-no-shim-reexports.sh passes, registered in CLAUDE.md enforcement list (line 112), wired in ci.yml:187. mls/mod.rs `pub use scp_mls::*` shim deleted; call sites import scp_mls directly; facade merges scp-mls sync + scp-runtime async bridge coherently.
- check-protocol-deps.sh updated + passes; wasm fence expanded (ci.yml:338 now checks the 6-crate browser stack).
- release.yml: scp-primitives publish step removed; scp-clock/crypto/did added as first 3 (leaves-first, valid topo order); publish-steps list == summary line 872 list.
- fuzz Cargo.toml retargeted (scp-clock/did/mls added, scp-crypto correctly omitted — no fuzz target needs verify_ed25519_signature); every fuzz edge used.
- T1c inventory (FUTURE slice, not in this range) file claims accurate: 6 out-of-crate bep44_signable sites, lib.rs:49 verify_bep44_signature re-export, two extract parsers (scp_did::extract_public_key_from_did + scp_identity::dht::extract_public_key). scp-dht crate correctly does NOT exist yet.
- Compile-verified: 3 leaves `cargo check --all-targets` green; full browser stack `cargo check --target wasm32-unknown-unknown` green (fence holds, DID model genuinely wasm-reachable).
- attestation.rs staying in scp-protocol (IdentityLinkAttestation §3.5.1 wire type) is INTENTIONAL and documented in ADR "Scope of the naming principle" — distinct from did_attestation.rs (rotation/migration proofs) which moved to scp-did.

**Re-verified at HEAD 47afa5c4f (range 86519aa6f..HEAD, 2026-07-03) — still COMPLETE, zero blocking findings.** Three commits landed after 5b35cb9aa: c1ef1394d (canonicality port into scp_did::extract_public_key_from_did), cc23e51f6 (uniform canonicality across UCAN resolver + DidDht::verify + app_sandbox prefix-bug repair), 47afa5c4f (local did:key gate in BridgeDidResolver + Python classifier repair + issue-ref scrub). Specific checks:
- **Test count reconciled = 9454 passed / 39 skipped / exit 0** with the exact CI feature set (DYLD + allow_in_memory_custody×3 + scp-core/testing + scp-runtime/testing + scp-runtime/saga-witness-test-mint). The "9440" a prior fix "reported" is a truncated-`tail` PROGRESS INDEX `(9440/9454)`, NOT a total — same run's tail shows 9440/9448/9449/9450 all as `(N/9454)` progress lines. Net +6 test attrs across range, 0 removed. No test lost. LESSON: nextest progress lines look like totals when `tail`-captured mid-run; always read the `Summary [...] N tests run` line.
- **Python classifier repair fully covered (pyo3 only).** trust.py `_SIGNATURE_CHAIN_PREFIXES` reclassified DID-decode strings as "signatures"; all 5 strings emitted by extract_public_key_from_did are covered. test_ucan_conformance.py now brace-matches the fn body, pins the exact 5-string set (`test_did_extraction_prefixes_are_the_expected_set`), and feeds synthetics through the real `_classify_ucan_error` asserting non-"unknown". **No parallel TS classifier exists** — trust.ts/errors.ts do zero string-prefix UCAN-stage bucketing (0 startsWith); the stage classifier is Python-SDK-only, so NAPI/TS needs no repair. Confirmed.
- **did:key gate (check c):** within scp-ffi the ONLY did:key surface is BridgeDidResolver, now gated `#[cfg(not(any(test, feature="testing")))]` against THIS crate's own testing (not scp-did's, which custody enables transitively: allow_in_memory_custody → dep:scp-testing → scp-core/testing → scp-protocol/testing → scp-did/testing — chain verified real). IdentityBackedDidResolver delegates to a production resolve_fn (no did:key). RESIDUAL (informational, NOT a T1 finding): `verify_event_signature` (scp-event-log tree.rs:320) and `verify_claim_signature` (scp-protocol bridge/claiming.rs:210) call the same parser and inherit its custody-enabled did:key branch — but this is PRE-EXISTING (unchanged across this range, not introduced by delegation like BridgeDidResolver was), reaches only the wasm-safe parser, and ADR-057 T1c blocker #2 ("did:key test-scope divergence") explicitly defers the custody-independent gate for the native/consolidation path. app_sandbox is NOT part of this — its did:dht branch delegates but did:key:z is its own base58btc (real W3C method), never reaching the parser's test-hex branch.
- Invariants held: 0 stale scp-primitives refs; no-shim + protocol-deps gates pass; release.yml = exactly 16 `cargo publish -p` (scp-client-wasm correctly absent, publish=false); 0 github-issue-URL refs in scp-did/scp-ffi source (scrubbed); DidDht::verify canonicality delegation has regression test `verify_rejects_non_canonical_spelling_of_matching_key`; scp-did compiles for wasm32.
