---
name: outlet-report-pr3-origin-kind-close
description: PR-3 §7.3.8 origin_kind mint↔validate close verified COMPLETE @7512e2159 — the mint-side fix (f6fd5705a) that resolved the b4e97ff50 asymmetry; value-caveat enforcement HONESTLY deferred; one naming residual (check_outlet_invoke)
metadata:
  type: project
---

Follow-up to [[outlet-authz-report-pr3-mint-validate-asymmetry]]. Branch feat/outlet-report-pr3 @7512e2159 (3 commits past b4e97ff50: f6fd5705a mint fix, 4af0af916 dedup/DSL, 7512e2159 §06 docs). Reference end-state origin/feat/outlet-redesign.

VERDICT: COMPLETE for what it scopes (origin_kind close + capability rename), with 1 must-fix naming residual + the value-caveat enforcement HONESTLY deferred.

**1. mint↔validate CLOSED (prior asymmetry RESOLVED).** f6fd5705a added build_delegated_caveats/build_root_caveats/infer_origin_kind_from_capabilities to mint.rs. Delegate path (mint.rs:810) materializes explicit origin_kind on outlet child: inherit parent's OR infer from stem family (outlet_call→Action, outlet_query→Query), mixed-stem→AttenuationViolation, non-outlet child→nb=None. Root path (mint.rs:547) runs try_new_for_root mixed-family gate, returns nb=None for single-family root (rule 3). validate.rs:1786 "symmetric with the mint" comment now TRUE (cites build_delegated_caveats which exists). Round-trip test GENUINE + PASSES: ucan_validate_integration.rs:2769 mint_delegate_validate_outlet uses TokenNbCaveatResolver (real outlet resolver, NOT NoCaveatResolver), 3 tests all pass — 2 positive (call→Action, query→Query materialize+validate), 1 negative (hand_built nb=None still rejected, rule-4 unchanged). This is EXACTLY the round-trip test the prior review found MISSING. full_pipeline test still narrows AWAY outlet (msgs only) but the dedicated §7.3.8 section covers the outlet edge.

**2. value-caveat enforcement HONESTLY DEFERRED (acceptable, NOT a completeness violation).** Reference has FULL runtime enforcement: check_invocation_local, CaveatCounterStore, IncrementCaveatCounter mutation intents, caveat_post_input_check hook (invoke.rs:3244), CaveatCounterApi::release, + SDK caveats.py/caveats-roundtrip tests×4/ffi common/caveats.rs. PR-3 ships NONE of it — UNIFORMLY absent (not half-shipped). KEY SAFETY PROOF: mint CANNOT emit a value-caveat — DelegateParams/MintParams carry NO caveat params, build_delegated_caveats only copies origin_kind. So nothing under-enforced: a max_calls/amount_max/rate_window token would have to be forged, and validator rejects. has_counter_bearing_caveat/requires_post_input_check (caveats.rs:434/447) are pub-but-UNCALLED forward infra (0 production callers; ref has 3) — compile clean (pub not dead-code-linted), 106 test fns in caveats.rs. §7.3.8 spec (07-...md:918-923) DESCRIBES full pipeline incl "consults CaveatCounterStore" = specs-lead, legitimate. NO code comment claims value-enforcement wired: invoke.rs has zero such claim; caveats.rs:420-432 counter-store doc is a FUTURE-caller contract ("Callers that cannot reach a counter store MUST fail-closed"), not a wired-claim. No ADR/PRD claims PR-3 ships enforcement. Deferral coherent.

**3. §06 spec↔code ALIGNED.** CrossContextOutletReceipt/outlet_registration_id/outlet_invoked_event_id consistent in spec (06-...md) AND code (cross_context_saga.rs:129/143/148). Zero residual tool_registration_id/tool_invoked_event_id/CrossContextToolReceipt in spec. EVENT names ToolInvoked/CrossContextToolInvoked correctly PRESERVED (EventType variants, event-log untouched boundary). No drift.

**4. has_outlet_invoke_capability GONE** → single has_outlet_call_capability (scp-protocol + runtime invoke.rs:1116). RESIDUAL FOUND (MUST-FIX, LOW blast): app_sandbox.rs:551 AppSandboxHandle::check_outlet_invoke — reference renamed it check_outlet_call; PR-3 did NOT. Method name says "invoke" but doc+body reference OutletCall/OutletCallAll (internally inconsistent). NOT FFI/SDK-exported (app_sandbox internal only, 5 test callers). Sole app_sandbox.rs delta vs reference. FYI: FFI op outlet_invoke (outlets.rs:1765) + bridge-alias canonical "outlet_invoke" + parser action verb "invoke" (app_sandbox.rs:154/164→OutletCall) are all CORRECT/reference-identical — the migration renamed the CAPABILITY not the operation-verb/DSL-action; §08 DSL keeps actions ["read","write","invoke","admin"] (08-...md:80/101) intentionally.

**5. Cross-layer/SDK COMPLETE.** outletQuery+outletCall string builders present ALL 4 SDKs (py types.py:256/268, ts types.ts:58/69, swift Types.swift:136/144, kt Types.kt:70/78) + read-vs-mutate docs all 4 (query=read-only/never-billed, call=may-mutate/may-bill). check-bridge-symmetry 0 findings, check-sdk-coverage PASS, check-pure-helpers PASS. §08 DSL outlets/{id} consistent (spec 79/100, parser app_sandbox.rs:148), zero tools/{ or outlet:invoke residual.

**6. No stubs/None-placeholders/dead let _ =.** mint.rs/validate.rs/caveats.rs zero todo!/unimplemented!. "placeholder" mentions all comments/test-data. mint.rs:616 let _ = write!(acc,..) = standard infallible-String-write idiom (pre-existing). No let _ = fn-name enforcement theater for the caveat helpers.

Tests run green: 3 outlet round-trip + full_pipeline + 62 mint unit + clippy scp-protocol/scp-runtime exit 0 (no dead-code warn on pub helpers).

MUST-FIX: rename check_outlet_invoke→check_outlet_call in app_sandbox.rs (+5 test callers) to match reference + its own OutletCall doc/body. JUSTIFIED-DEFERRAL: value-caveat runtime enforcement (safe: mint can't emit the fields).
