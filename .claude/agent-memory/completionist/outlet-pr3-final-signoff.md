---
name: outlet-pr3-final-signoff
description: PR-3 outlet authorization migration FINAL sign-off COMPLETE @baeebdd92 — the culminating end-to-end completeness check across all layers; the last review-fix commit closed the FFI error-string residuals + removed a dead unreachable parser arm
metadata:
  type: project
---

Culminating sign-off for the entire outlet authorization re-port (PR-3), worktree scp-wt-outlet-pr3, branch feat/outlet-report-pr3, tip baeebdd92 (range a65a778c9..baeebdd92, 14 commits). Reference end-state origin/feat/outlet-redesign. Supersedes/completes [[outlet-pr3-review-fixes-close]] (@1ffe476e5) + [[outlet-report-pr3-origin-kind-close]] (@7512e2159). VERDICT: COMPLETE for PR-3 scope (authorization model). Value-caveat runtime enforcement + streaming + cross-context are separately-planned later slices, honestly deferred.

**baeebdd92 (final commit, +13/-13, 7 files) does TWO things, both verified sound:**
(1) Migrates the LAST stale "OutletInvoke" CAPABILITY-NAME references in FFI ERROR STRINGS / doc-comments to "OutletCall" — mcp.rs:794 (pyo3) + bridge.rs:4680 (uniffi) log strings "agent lacks OutletCall capability"; outlets.rs:880/1433 err text "does not have OutletCall capability"; ConsequenceRule.kt doc [OutletCall]; integration.test.ts comment. These were the exact residuals flagged in the prior memory as "human-readable error strings, not enum variants."
(2) Removes a DEAD UNREACHABLE parser arm in roles.rs Capability::new: the exact-match `n == "outlet:invoke:*" || n == "outlet_invoke:*"` arm was unreachable because the PRECEDING `starts_with("outlet:invoke:")` / `starts_with("outlet_invoke:")` hard-break checks (lines 225-227) already return None for those inputs. PROVEN safe: existing test out014_capability_new_returns_none_for_outlet_invoke_hard_break (roles.rs:5688-89) asserts BOTH `outlet:invoke:*` AND `outlet:invoke:foo` → None and still PASSES with the arm gone. Also fixed a misleading test comment in context.rs (deny is via no-matching-Custom-grant, not "malformed").

**All 6 task-specified verification axes CONFIRMED by reading code + running tests:**
1. Capability model complete every layer: enum OutletQuery(id)/OutletQueryAll/OutletCall(id)/OutletCallAll (roles.rs:81-88); CapabilityCeiling::contains (roles.rs:719) single authority, disjoint families (QueryAll⊉Call, CallAll⊉Query); legacy hard-reject roles.rs:225-230 (outlet:invoke:/outlet_invoke:/tool:invoke:/tool_invoke: all→None); 4 SDKs all have outletQuery+outletCall builders + *All consts (py types.py:256/268, ts types.ts:58/69, swift Types.swift:136/144, kt Types.kt:70/78) + Kotlin ceiling helper handles both families symmetrically (Types.kt:306-318). git grep OutletInvoke|ToolInvoke in crates+bindings = every hit is EventType (ToolInvoked/CrossContextToolInvoked lib.rs:133/432 — preserved ADR-011 Merkle boundary), OutletInvokedEvent (event payload type), CrossContextOutletInvoke (saga envelope), FFI op-verb outlet_invoke, Kotlin test-stub field lastOutletInvoke*, or the OUT-014 negation comment. ZERO grantable-cap residual.
2. §7.3.8 origin_kind mint↔validate CLOSED: mint.rs (crates/scp-runtime/src/crypto/ucan/mint.rs — NOT protocol) build_delegated_caveats:193 (called :810) + build_root_caveats:276 (called :547) + infer_origin_kind_from_capabilities:124; delegate materializes explicit origin_kind (inherit-or-infer stem, mixed→AttenuationViolation), root runs try_new_for_root gate→nb=None single-family. Round-trip test ucan_validate_integration.rs (real TokenNbCaveatResolver): delegated_outlet_call_materializes_action... + ..._query... + hand_built_..._nb_none_is_still_rejected — ALL 3 PASS. Value-caveat enforcement HONESTLY deferred: CaveatCounterStore type GENUINELY ABSENT (only doc-comment caveats.rs:791 + §07 spec text), check_invocation_local (caveats.rs:811) ZERO prod callers (all #[cfg(test)] 4192-4300), Mint/DelegateParams carry NO caveat fields (verified field lists) ⇒ mint CANNOT emit value-caveat ⇒ "no live divergence" LITERALLY TRUE. §07 spec:918/923/925 explicitly marks "deferred/NOT YET WIRED/spec leads/no live divergence" (correct one-way-flow specs-lead). enforce_caveat_invocation phantom-ref GONE.
3. Cross-layer consistency: 3 FFI check helpers (pyo3 py_check_scoped_capability context.rs:1881, napi check_scoped_capability_inner context.rs:5051, uniffi sandbox_check_capability bridge.rs:8153) ALL route through CapabilityCeiling::new(granted).contains(&required) — single authority both families. FFI error strings name OutletCall. SDK read/mutate docs present ALL 4 (query=read-only/never-billed, call=may-mutate/may-bill). FFI cross-family fail-closed test scoped_capability_check_honors_both_wildcard_families_fail_closed PASSES.
4. Artifact alignment: §05 full outlet:query/outlet:call model + closed-by-construction Capability::new-as-sole-authority. §06 saga envelope CrossContextOutletInvoke/CrossContextOutletReceipt/CrossContextOutletInvocationPrepared/outlet_registration_id/outlet_invoked_event_id match code; EventType ToolInvoked/CrossContextToolInvoked spec refs match code (preserved boundary). §08 DSL action verb "invoke"→OutletCall (parser) intentionally preserved; resource outlets/{id}. phase-2.md enum+role double-grant+disjoint rules exact; phase-3 PaidActionType::OutletCall. ZERO grantable OutletInvoke/ToolInvoke residual in ADRs/PRDs.
5. No stubs/None/dead-code presented as done: TODOs in diff are pre-existing (napi context.rs:2019 §9.10.4 from #1674) or UniFFI-autogenerated (ScpBindings.swift — PR-3 only bumped a regenerated method checksum). No todo!/unimplemented! against outlet criteria. has_outlet_invoke_capability GONE.
6. Enforcement NOT weakened: ONLY 2 enforcement-adjacent files touched in whole range. (a) check-error-codes.sh (NOT in CLAUDE.md hard-lock list) — b4e97ff50 ADDITIVE port of reference's line-scoped `SCP-CODE-OK:` skip marker (bounded, per-line whitelist, integrity-probed unmarked SCP-TOOL-9999→VIOLATION). (b) ffi_conformance.rs (IS hard-locked) — PURE comment rename tool_invoke_cross_context_saga→outlet_invoke_cross_context_saga; const MIN_PARITY_OPERATIONS=106 UNCHANGED (no threshold weakening). NO pipeline_wiring/matrix/allowlist/bridge-aliases touched (capability-as-STRING migration adds no new bridge op ⇒ matrix correctly untouched; matrix's outlet_invoke refs are the OP-verb, preserved).

**WASM genuinely N/A**: git grep OutletCall|OutletQuery|outlet:call in crates/scp-ffi/wasm = EMPTY. scp-client-wasm participant-path-only, no outlet capability surface (ADR-034).

**ONLY observation (OUT-OF-SCOPE, NOT a PR-3 gap, LOW/non-blocking)**: 25-test-vectors.md:375 + test_vectors.rs:385 Vector 32 KAT has AppBoundPayload{capabilities:["tool:invoke:*"]} pinned into leaf hash e0c0691d... . BUT: (a) AppBoundPayload.capabilities is Vec<String> OPAQUE event-log field, does NOT flow through Capability::new; (b) introduced by 1c0ccbc7d (#1827) — PRE-DATES + independent of PR-3 range; (c) PR-3 did NOT touch either file (empty diffstat); (d) reference origin/feat/outlet-redesign has NO Vector 32 / no tool:invoke at all. It's a legacy-flavored KAT filler string; migrating requires regenerating pinned Merkle hashes. Suitable for a separate hygiene change, does not affect authorization correctness or PR-3 completeness.

clippy scp-protocol+scp-runtime --all-targets --features scp-runtime/testing = 0 diagnostics (21.86s). NOTE: without the testing feature, spawn_from_welcome_tests.rs:3162 fails (seed_peer_pseudonym is #[cfg(feature=testing)]) — an INVOCATION-flag omission, unrelated to PR-3 (file has 0 outlet refs, untouched). Base a65a778c9 NOT ancestor of origin/main (branch cut earlier).

LESSON: on capability-STRING re-ports the top residual class is FFI ERROR-STRING / DOC-COMMENT capability names (log "agent lacks X capability", err "does not have X capability") — they pass enum-variant greps because they are literal text, get migrated LAST. Grep the human-readable strings specifically. And event-log Vec<String> opaque-payload KATs may carry legacy cap spellings that are NOT capability-parse divergences (opaque bytes) — check the field type before flagging; if pre-existing + reference-absent + hash-pinned, it's separate-scope hygiene not a gap.
