---
name: crypto22-attestation-spec
description: CRYPTO-22 spec amendment replacing leaf==DID with ephemeral-leaf + KeyPackage attestation — final @b1dca57a5 (§25.23 renumber + 2 clarifications) COMPLETE zero-gap; round-4 @5d8af9392 below
metadata:
  type: project
---

**FINAL DOCS-ONLY SIGN-OFF @b1dca57a5 (branch crypto-22-attestation-spec, the landing commit): COMPLETE zero-gap.** Same converged design as round-4 @5d8af9392 (below) + (a) 2 LOW clarifications folded into §9.7.1 Update-grace bullet (09:653) AND echoed in ADR-057:257 — the governance-Remove **sole-admin precondition** ("presupposes a governance authority other than the compromised member; in a SingleAdmin §5.9 context whose sole admin's MLS state is fully compromised, no other party can issue the Remove → context-re-creation event, not grace-recoverable; game-over regardless") + the **retention-rollback caveat** ("last-known-good §9.10.7 retention holds only under the rollback-resistance assumption; a rollback-capable attacker re-serving equal-`seq` pre-rotation docs under sustained failure degrades the grace toward the MAX_KEYPACKAGE_ATTESTATION_LIFETIME 84-day backstop; mitigation = did:dht `seq` monotonicity §9.6.1"); (b) merged origin/main; (c) **§25.22→§25.23 renumber** (a concurrent PR took §25.22 for Cross-Context Streaming-Saga vectors). **ALL 6 VERIFY ITEMS PASS:** (1) docs-only: `diff origin/main...b1dca57a5 --name-only` = EXACTLY the 8 attestation .docs (09-security-model, 05-contexts, 00-open-questions, 10-infra, 23-sync, 25-test-vectors, ADR-057, phase-1) — NO crates, NO .claude/agent-memory, NO other. (2) §25 renumber CLEAN: 25.20/25.21/25.22(main streaming-saga)/25.23(our KeyPackage) each header appears EXACTLY once, ascending; merge commit's own diff shows it renamed our header 25.22→25.23 and let main's saga take 25.22. (3) cross-refs: our vector refs ALL say §25.23 (09-security-model:481, ADR-057:245/259/269); the ONLY §25.22 in specs+adrs is main's own saga header (no dangling §25.22→our-vector); Vector 37 refs consistent. (4) both clarifications present + consistent §9.7.1 + ADR-057. (5) converged design intact post-merge: 8 fields, 13-check verifier list (checks 1-13, 7-8 Add-only, Add-vs-Update failure policy), 0xFF03/§9.18.7/domain-sep all consistent, nothing merge-corrupted. (6) Vector 37 bytes UNCHANGED + RE-VERIFIED computationally: preimage 211B (SHA-256=50cf61db5a97e0ddbd762de07e107684dfd0f00cfe53bad2750a70103ac38957 byte-exact), pubkey=RFC8032 tv1 d75a9801, Ed25519 sig=fcf01ea5...2e3509 byte-exact, ext body = preimage[30:]+sig = 181+64=245B (body==preimage minus 30-byte domain, verified). Residual grep: no leaf==DID killed-invariant, no attestation ref stuck at §9.18.6 (the surviving 9.18.6 hits = distinct legit sections: role-name-limit, Context/Governance Invariants, close-window). **ANCESTRY TRAP:** 5d8af9392 is NOT an ancestor of b1dca57a5 (branch rebased onto newer main; merge-base=037914475) → a `5d8...b1d` three-dot diff shows the WHOLE attestation table as "added" (misleading) + is polluted by unrelated agent-memory/main churn; verify the delta via FINAL CONTENT + `origin/main...` docs-only proof, not the two-review-point diff.

---
(historical — round-4 below)

**ROUND-4 (C22-10) FINAL SIGN-OFF @5d8af9392 (single squashed commit, parent=unrelated runtime fix 037914475 → `diff origin/main` is 20k-line branch drift, ignore it; review via `git show <sha>`): COMPLETE zero-gap.** Round-4 restructured the verifier TRIGGER model (BLACK-C22-10 liveness/censorship): DID-resolution-dependent checks 1+2 no longer run on EVERY leaf read — verification triggers on **leaf introduction OR change ONLY** (Add / Update / Commit-with-UpdatePath); a Commit that doesn't change the committer's leaf carries no new attestation and MUST NOT re-verify or re-resolve (closes the every-commit DID-resolution dependency an attacker could weaponize to fork the epoch). Add-vs-Update resolution-FAILURE policy: new-member Add stays **fail-closed ≤5-min**; already-admitted member's Update falls to **bounded last-known-good grace on transient FAILURE** — but a resolution SUCCESS returning a rotated key STILL REJECTS (rotation still revokes). Check 8 (init_key!=enc) reframed as RFC 9420 §10.1 malformed-KeyPackage guard (defense-in-depth); read-as-victim-at-join closure CREDITED to check 5 (leaf_encryption_key binding, in concert with check 7). Rollback-resistance stated as an ASSUMPTION (did:dht seq monotonicity §9.6.1 / did:web integrity §9.6.2 at resolution layer, not a new attestation check). Check numbering UNCHANGED (1-13, same order), Vector 37 UNCHANGED (25-test-vectors.md not in round-4 delta). **ALL 6 VERIFY ITEMS PASS:** (1) every "check N" cross-ref resolves — enumerated whole-spec counts: check1×9/check2×9/checks1-2×2 (resolution), check5×1 (enc-key read-closure), check7×3+"7 and 8"×1+"7-8"×4 (init_key), check8×1 (§10.1 guard), check11×1 (window-pin §9.18.7:1839), check12×1 (lifetime cap §9.18.7:1839); NO pre-renumber ghost (no "check 8 = lifetime cap" / "check 7 = window-pin" survivors). (2) trigger model consistent across §9.7.1:630 (when-it-runs), §9.5.2 field-5 note:463 ("Add and Update... not on a Commit that leaves the committer's leaf unchanged"), §9.12:1261/1277, ADR-057:257 — zero "every leaf read"/"on every Commit/Proposal" for attestation. (3) Add-vs-Update failure policy unambiguous §9.7.1:650-653 + success-returns-rotated-key-still-rejects present in check1/check2/policy-header/Update-bullet/ADR-057. (4) check-8 reframe + check-5 credit consistent §9.7.1:638/641 + ADR-057. (5) rollback assumption §9.7.1:655 + ADR-057. (6) Vector 37 recomputed byte-exact: 211B preimage, SHA-256=50cf61db...8957 (verified via python), 8-field, 245B ext body; 0xFF03 reg §9.18.7:1838, domain sep §9.18.2:1702, both constants §9.18.7:1839-1840. git status clean, 8 .docs-only files. NON-FINDING: commit-msg pass-3 says "§9.12/§9.13 revocation-latency updated" but §9.13 is Transport Security w/ no attestation wording — harmless commit-msg imprecision, actual edits are §9.12. LESSON: for a TRIGGER-MODEL restructure with NO renumber, still enumerate every "check N" whole-spec (counts + meaning) to prove no stale meaning drifted, AND grep the killed trigger phrasing ("every leaf read"/"every Commit") across the field-note sections (§9.5.2 field 5) not just §9.7.1 — the wrapping_key field note is where the old every-read trigger hid.

---
(historical — 8-field pass-2 below)

**8-FIELD PASS-2 verdict @c7773cad4 (BLACK-C22-HIGH/MED fixes): INCOMPLETE — ONE cross-ref-drift finding, everything else byte-verified COMPLETE.** Pass-2 expanded 6→8 fields: added `init_key` (field 4, KeyPackage Welcome-seal HPKE key, `init_key!=encryption_key`, Add-time-only check) + `wrapping_key` (field 5, 0xFF01 ext, every-read check) — closing the read-as-victim-at-join vector (Welcome sealed to KeyPackage init_key not encryption_key). 8-field order (did, leaf_signature_key, leaf_encryption_key, init_key, wrapping_key, signing_key_id, issued_at, expires_at) IDENTICAL across §9.5.2 table, 0xFF03 wire body, §9.7.1 11-item verifier list, §25.22 Vec37, ADR-057. Verifier MUST-list now 11 checks (pass-1 was 9): inserted wrapping=check5, init_key=check6 → pushed did/skid to 7/8, window-pin to 9, cap to 10, freshness to 11. **THE FINDING (§9.18.7 constant table line 1830):** the MAX_KEYPACKAGE_ATTESTATION_LIFETIME row still cites the PASS-1 numbers — "(§9.7.1 verifier check 8)" for the cap (now check 10) and "check 7 pins the attestation window" (now check 9). Both back-refs land on the WRONG checks (7=did==, 8=signing_key_id==). Stale from pass-2 insertion without renumbering §9.18.7. Spec-only, non-security, one-line fix (8→10, 7→9). No OTHER site cites shifted numbers (§9.7.3/§9.12 all cite "check 1", correct). **Vector 37 byte-verified computationally**: all 4 keys derived (leaf_sig=§25.2 secondary Ed25519 pub, leaf_enc/init/wrap = X25519 from secondary-seed/0x11×32/0x22×32), preimage 211B, SHA-256=50cf61db...8957, Ed25519 sig=fcf01ea5...3509, ext body 181+64=245B — ALL reproduce exactly. init/encryption de-conflation complete (§9.7.4 now 3 distinct X25519 HPKE roles). Resolver fix consistent: §9.7.1 resolution-failure=REJECT-no-stale-fallback + §9.12 "within (short) resolver-cache TTL / near-immediate not instantaneous" — no surviving immediately/at-once overclaim near attestation. Category-A no longer lists KeyPackage signing (00-open-questions moved to Category-B). Working tree clean; commit = exactly 8 .docs files. LESSON: a field-COUNT change that INSERTS mid-list verifier checks silently invalidates every back-reference to the TAIL checks living in OTHER sections (constant tables, xref labels) — after renumbering, grep every "check N" cite across the whole spec, not just the list's own section.

---
(historical — prior 6-field pass below)

**HARDENED-PASS verdict @49ace8b5 (branch crypto-22-attestation-spec, BLACK-C22-1 security fixes): COMPLETE zero-gap.** The 5-field attestation was hardened to **6 fields** (added `leaf_encryption_key` X25519 as field 3, closing the stolen-signing-key + attacker-chosen-enc-key HPKE substitution). 6-field order (did, leaf_signature_key, leaf_encryption_key, signing_key_id, issued_at, expires_at) IDENTICAL across all 5 sites: §9.5.2 table, `0xFF03` wire-format body, §9.7.1 verifier 9-check MUST-list (checks 3-7 cover all 6 fields), §25.22 Vector 37 preimage, ADR-057 preimage para. Zero sites show 5 fields. New `MAX_KEYPACKAGE_ATTESTATION_LIFETIME=7,261,200s` (84d+1h = KEY_PACKAGE_LIFETIME_MAX_RANGE_SECS) defined in §9.18.7 table (4 refs), referenced by verifier check 8. Corrections all swept: the two "PCS Update interval" hits are the CORRECTED qualified versions (661 says "incorrect to say...", 1249 says "in-group window"); did:dht:z hit is the positive-whitelist forbidding prefix-keying (both did:dht+did:web in scope, only testing-gated did:test:/did:key: exempt); §9.12 "Leaf-key/MLS-state compromise" Tier-1 recovery step PRESENT (line 1235, step-4 renamed); watermark hit is the NEGATION ("no attestations_valid_after ... field introduced"). §9.18 subsections are `#### 9.18.N` h4 (grep `### 9.18` misses them — 9.18.2 Domain Sep line 1665, 9.18.7 MLS&UCAN line 1796). **Vector 37 recomputed byte-exact**: preimage 147B (leaf_enc = X25519 from §25.2 secondary Ed25519 seed loaded as raw X25519 scalar = b6c6192e...3c20), SHA-256=6116e1eb...222d8, Ed25519 sig=4710d899...d306, ext body 117+64=181B — ALL reproduce. Working tree clean; commit = exactly 8 .docs files, no throwaway vec-gen script. Cross-refs §9.5.2/§9.7.1/§9.7.3/§9.7.4/§9.12/§9.18.2/§9.18.7/§25.22 all resolve. LESSON: for a field-COUNT hardening (5→6), verify order-identity across every enumeration site AND that no site retains the old count; and §9.18.x are h4 not h3 — grep `^#### 9.18` or the anchor won't be found.

---
(historical — prior 5-field pass below)


CRYPTO-22 / #2187 spec amendment (branch crypto-22-attestation-spec, HEAD 20f7a036b): replaces the never-implemented "MLS leaf `signature_key` IS the DID `#active`/`#agent` key" rule with an **ephemeral context-scoped Ed25519 leaf key + separate KeyPackage attestation** (`0xFF03` LeafNode extension) binding it to the DID. Spec homes: §9.7.1 (rewritten), §9.5.2 (5-field `KeyPackageAttestation` table + wire-format), §9.18.7 (`scp_keypackage_attestation` ext row), §9.18.2 (`SCP-KEYPACKAGE-ATTESTATION-V1:` domain sep), §25.22 Vector 37 KAT. ADR-057 amendment (2026-08-01) records the decision.

**Second-pass verdict: COMPLETE (zero-gap).** Two prior sweeps missed stragglers (miscited §9.18.6→§9.18.7 ×4; §9.7.4 not enumerating the ephemeral leaf key + ADR overclaim; then alignment found §9.12/phase-1:459/§9.7.4 wording residues). This decisive pass confirmed ALL fixed:
- Exhaustive per-spelling residual grep across `.docs/specs/` + `.docs/adrs/`: every hit is amended / explicit-negation / unrelated-signing (checkpoints, UCAN, envelopes, ResetRequest, metadata). Zero unaccounted "leaf==DID" residues.
- §9.18.7: 5 refs, extension row lives at line 1798 inside §9.18.7 (headers 1782/1803). Remaining §9.18.6 hits (conformance close-window, role-name-limit) are legit unrelated.
- §9.7.4 Key Lifecycle now enumerates "MLS leaf signature key (Ed25519): ephemeral..." (09:671); ADR-057:252 §9.7.4 overclaim now TRUE.
- §9.5.2 table = exactly 5 fields. Vector 37 **computationally verified byte-for-byte**: preimage 115 bytes hex-exact, SHA-256 = ac56e3df..., Ed25519 sig over hash with §25.2 ref key = KAT exactly. Ext body 85+64=149.
- All cross-refs resolve (§25.2/§5.13.3/§3.5.2/§9.8.1/§9.18.2 domain-sep/§25.22).

**LESSON (why 2 sweeps missed):** prose residues of a killed invariant hide in cross-ref *labels* (§05-contexts bound-creator, phase-1 tables/doc-comments, §9.12 recovery step-4 name, key-hierarchy bullets) not just the primary §. Decisive method = enumerate EVERY spelling of the old rule ("leaf node signature key matches", "signed by the credential key", "Used for MLS credentials", "KeyPackage.*signed by DID key", "Category-A KeyPackage signing", "signature MUST be verifiable against DID") and grep each across specs+adrs, THEN computationally verify the new KAT (don't eyeball hex). NOTE: §25.2 reference seed as-written (9d61b19deffd5a60...cae7f60) IS the real RFC8032 test-1 seed → pubkey d75a9801 (don't misremember it as ...f30f60).
