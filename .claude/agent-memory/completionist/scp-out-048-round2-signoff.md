---
name: scp-out-048-round2-signoff
description: SCP-OUT-048 round-2 verification COMPLETE @8ac118069 — browser-cancel Option-A removal + 16 review fixes all wired
metadata:
  type: project
---

SCP-OUT-048 (WASM browser-invoker streaming-saga session) round-2 sign-off @8ac118069 (feat/outlet-xctx-048-wasm-session). Fix delta 4cdc78a89..8ac118069 (14 files). **COMPLETE zero-gap.**

**Why:** round-1 (9 reviewers) surfaced a DOA-class contradiction: browser cannot sign OutletCancel because §5.4.5:547 binds cancel next_seq to the runtime's live emission cursor ("never a value supplied by the caller") — unreadable by a remote browser. PRD CRITICAL #3 + ADR-057 already said cancel is N/A for WASM. Resolved via **Option A: remove the browser cancel surface entirely** (not a workaround — the drifted story AC/title had over-reached).

**How to apply (verification pattern for surface-REMOVAL fixes):**
- Atomic removal was CLEAN: outletStreamSignCancel/outletStreamComputeCancelPreimage TS wrappers, outlet_stream_sign_cancel/compute_cancel_preimage wasm predicates, OutletStreamCancel type, cancel re-exports, 2 CANCEL matrix rows, their 2 coverage aliases, cancel tests, cancel KAT fixture entries — ALL gone TOGETHER. Grep across bindings/typescript-wasm + crates/scp-client-wasm + scripts/check-sdk-coverage.py + sdk-capability-matrix.json = only deliberate out-of-scope DOC comments remain. Node-side cancel authority (scp-protocol/scp-runtime OutletStreamCancel/sign_cancel) correctly UNTOUCHED.
- Removing matrix rows + their aliases together is coverage-CONSISTENT, NOT an enforcement weakening (check-sdk-coverage PASS: 264 ops, 0 errors, unmatched-true 0). LESSON: row+alias paired removal is the legitimate shape; a lone row removal (alias orphaned) or lone alias removal (row unmatched) would be the finding.
- Story amendment (one-way flow honored): AC[2] was not just weakened — it was STRENGTHENED with a negative grep assertion (`grep outlet_stream_sign_cancel returns nothing`), turning the scope-correction into a machine-verifiable check. status pending→done, filename outlet-streaming.test.ts→outlets-streaming-invoker.test.ts, cancel-out-of-scope note cites ADR-057/§5.4.5/CRITICAL#3. validate-prd PASS 437 stories.

**16 review fixes all wired (verified in code, not self-report):** #2 6110 branch clears #pending+sets #error before throw (session.ts:512-524); #3 grantCredit TOCTOU double #throwIfClosed pre+post await open (341/347); #4 SCP-VALID-7028 construction guard rejects 2nd live session per (client,contextId) via WeakMap registry (236-257); #5 branded validating Credit newtype (new credit.ts, [1,2^32), grant.value); #6 credit sign/preimage wrappers → flat named-options objects (swap-footgun fix, client.ts:612/OutletStreamSignCreditParams); #7 concurrent-drain split to distinct SCP-VALID-7027 (not 6100); #8 types.ts OutletStreamCredit made wire-accurate (snake_case request_id/monotonic_seq, sig:number[]); #9 u64 seq >MAX_SAFE_INTEGER → SCP-VALID-7010 fail-closed (495-503); #10 lib.rs phantom-"native" comments reworded ("NO native FFI counterpart", 774/944); #11 operatorPk caller-trust doc (143-150); #12 caveatsBindingFor synonym deleted; #13 #throwIfClosed helper (280-287); #15 6131 gap test ([0,1,3]→6131, chunk 3 not yielded, no cancel routed) + 6110 in-session test (#pending cleared); #16 credit-preimage golden-pinned (fixture creditPreimageHex 1fef14cc..., Rust KAT re-derives via compute_credit_sig_preimage INDEPENDENTLY, TS test pins hex + WebCrypto-verifies real sig over it — closes "sign & compute share one builder so both drift together" blind spot). #1 gap-cancel HIGH resolved BY the Option-A removal (gap now surfaces StreamGap only; node credit-stall reclaims).

**Ran green:** bun test 27 pass/0 fail; cargo test -p scp-client-wasm all ok incl out048 KAT; tsc typecheck + biome clean; wasm fence holds (no scp_runtime dep, only fence-explaining comments). Verdict COMPLETE.

**FINAL MERGE SIGN-OFF @4d54f107c (rebased onto current main, R3+R4 fixes added, DEFINITIVE):** all gates re-run on rebased tree: check-error-codes PASS (4251 codes; SCP-VALID range 7000-7999 enforced, no central per-code registry — range+no-collision is the "registry"), check-sdk-coverage PASS (264 ops/0 err/0 unmatched-true), validate-prd 437, bun run check clean + bun test 36/0, wasm32 build exit0, cargo test -p scp-client-wasm all 6 binaries green (14+4+1[out048 KAT]+1+2+5=27; the plain `cargo test` HANGS at doctest/lock phase AFTER all binaries print ok — run binaries directly from ~/.cargo/shared-target/debug/deps/ to confirm, NOT a failure). All 8 ACs met, status=done. Cancel removal STILL holds (grep cancel across crates/scp-client-wasm+bindings/typescript-wasm = ZERO). SCP-VALID literals now 7010(Codec/malformed)/7025(not-initialized)/7026(WebSocketRelaySocket→create misuse)/7028(2nd live session per client,contextId)/7029(concurrent drain) — each single-purpose, literal matches its ValidationError code-arg; 7027→7029 rename left NO 7027 remnant. #2203 cited (story + session.ts:481 gap-close comment), #2204 CLOSED by spec commit 8d5da1912 which landed the §5.4.5:515-519 "Co-located vs. remote receiver (cancel locus)" carve-out FIRST (one-way flow) — code matches (session.ts:471-491: non-contiguous seq → StreamGap SCP-OUTLET-6131, no cancel, node reclaims via credit-stall). No rebase drop/dup: 4 wasm predicates each defined ONCE, wasm build compiles. Matrix line-677 outlet_stream_cancel = NATIVE co-located InvocationHandle.cancel (SCP-OUT-038), correctly RETAINED (only browser/wasm cancel removed); credit rows at matrix 2484/2499. Verdict COMPLETE — cleared for merge.
