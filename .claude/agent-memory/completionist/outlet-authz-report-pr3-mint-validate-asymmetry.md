---
name: outlet-authz-report-pr3-mint-validate-asymmetry
description: PR-3 outlet authorization re-port (feat/outlet-report-pr3) — §7.3.8 validator half ported but mint half (build_delegated_caveats) MISSING; mint emits nb:None that its own validator rejects at rule-4; no test covers non-root outlet delegation round-trip so gap passes CI
metadata:
  type: project
---

# Outlet capability-authorization re-port PR-3 (feat/outlet-report-pr3, a65a778c9..b4e97ff50)

Re-port of outlet-redesign's AUTHORIZATION model onto main's actor architecture. Reference end-state = `git show origin/feat/outlet-redesign:<path>` (@3d6313cee).

**CRITICAL finding — §7.3.8 mint/validate asymmetry (must-fix, INCOMPLETE):**
- Validator half FULLY ported+correct: `verify_leaf_outlet_stem_consistency` (validate.rs:1920) called from validate_ucan:807 + evaluate_ucan:1154; `verify_origin_kind_matches_stem_family` (:1870) called from verify_edge_attenuation:1769/1775; rule-4 rejects non-root outlet edge with nb=None → `OriginKindUnspecified` (validate.rs:1791). Shared classifier `att_set_has_outlet_stem` (capability.rs:240) used by validator (:1733).
- Mint half MISSING: runtime `delegate_ucan` (mint.rs:492) unconditionally sets `nb: None` (mint.rs:342/599) with comment "Delegation minting carries no §7.3.8 invocation caveats." Reference has `build_delegated_caveats` (mint.rs:199, CALLED at :963 `let nb = build_delegated_caveats(params)?`) + `infer_origin_kind_from_capabilities` (:123) that read parent_token.payload.nb, use shared att_set_has_outlet_stem, and MATERIALIZE origin_kind on non-root outlet edges (rule-4 mint obligation). **Neither fn exists anywhere in current branch (grep empty).**
- Consequence: minting a delegated UCAN over an outlet capability produces a token the SAME branch's validator rejects at rule-4. Validator comments (validate.rs:1787) assert "symmetric with the mint, which never emits an outlet child with nb=None (build_delegated_caveats always materializes...)" — assertion is FALSE on this branch (phantom provenance).
- **Why CI green despite gap:** no test delegates an outlet cap then validates it as outlet edge. full_pipeline_mint_delegate_parse_validate (ucan_validate_integration.rs:1308) mints root w/ outlet_call:assistant but DELEGATION NARROWS to messages:write+read only (drops outlet cap), validates messages:write → child_is_outlet_edge=false → rule-4 never fires. Reference tests (:1895-1998) DO cover non-root outlet delegation w/ materialized origin_kind:Some(Action). Coverage gap masks the wiring gap.

**Artifact divergence (must-fix, code-vs-spec):** §06 receipt/saga still uses tool_registration_id (9×) / tool_invoked_event_id, CrossContextToolReceipt/Invoke/InvocationPrepared type names — but CODE (renamed in PR-1 ae2c6f2da, cross_context_saga.rs UNTOUCHED by PR-3) uses outlet_registration_id (178×) + struct CrossContextOutletReceipt. §06 JCS envelope key line 249 `"tool_registration_id"` is WIRE-affecting (serde field has no rename → serializes outlet_registration_id). L7c commit (f256ca02f) touched §06 (24 lines) but only renamed capability/interface surface (tool:interface→outlet:interface, ProposeToolInterface→ProposeOutletInterface, ToolInvoke→OutletQuery/OutletCall), left saga-envelope/receipt field+type names stale. No artifact documents this as deferred/out-of-scope.

**Naming residual (LOW):** runtime `has_outlet_invoke_capability` (invoke.rs:1111, 4 prod callers session.rs:356/invoke.rs:256/447/682) NOT renamed; reference renamed it → has_outlet_call_capability (invoke.rs:4086, old name deleted). interface.rs comments already cite the NEW name (has_outlet_call_capability) which as a runtime fn doesn't exist on this branch (only protocol mod.rs:597 has it, a functionally-identical separate fn). Incomplete rename vs canonical target.

**Deliberate scope boundary (NOT a gap, but diverges from reference):** EventType::ToolInvoked/CrossContextToolInvoked KEPT tool-named (lib.rs:133/432); reference renamed → OutletInvoked (lib.rs:212) w/ tree tag 11→83 (wire-breaking). scp-event-log UNTOUCHED by PR-3. Python trust.py still refs ToolInvoked (consistent). phase-3.md:973 event_type="tool_invoked" is CONSISTENT with current code (NOT stale). So: authorization surface renamed (this PR); event-log/saga provenance surface NOT (deliberate half-boundary). Reference did both.

**CONFIRMED COMPLETE:** protocol enum OutletQuery/QueryAll/OutletCall/CallAll (roles.rs:81-88) matches ref exactly + hard-reject invoke (roles.rs:224-238, stricter than ref). All 4 SDKs constructible query+call: py outlet_query/outlet_call (types.py:253/263), ts outletQuery/outletCall (types.ts:54/63), swift (Types.swift:133/140 + QueryAll/CallAll consts), kotlin (Types.kt:65/72 + consts) — all emit outlet:query:/outlet:call: strings. Caveats WIRED (narrow() at validate.rs:1751/1767/1773, empty() origin_kind:None correct). NAPI+UniFFI+PyO3 outlet_call+PaidActionType::OutletCall present. WASM=scp-client-wasm participant-path-only (no outlet/capability/invoke surface, untouched) = justified N/A per ADR-034. §5.4.2 (05-contexts) + §7.3.8 (07-trust:781) specs authored matching code. No stubs (let _ = CaveatMintError block is #[test] exhaustiveness enumeration caveats.rs:2802, legit). check-error-codes.sh SCP-CODE-OK exemption = bounded additive (same-line, no whole-file), legit. Matrix untouched CORRECT (capabilities are strings, no new FFI ops).

**Cross-context saga_registration_id rename was PR-1 (ae2c6f2da), not PR-3.** cross_context_saga.rs untouched by PR-3 range.
