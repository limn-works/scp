---
name: outlet-group1-001-010-audit
description: Whole-outlet audit Group 1 (SCP-OUT-001..010 tool→outlet rename foundation) @ origin/main d5de8b153 — rename substance ~complete, but stories done-marked against an ORPHANED branch; re-port #2098 dropped several deliverables
metadata:
  type: project
---

Audit of SCP-OUT-001..010 (tool→outlet rename foundation) against origin/main d5de8b153 (worktree /Users/alec/Developer/limn/scp-wt-audit; local main was 154 behind).

**ROOT CAUSE (status-integrity):** outlet.json marks 001-010 `done`, but the story commits (f5236ef6a=OUT-007, b5a25f142=OUT-009, efe32cfdf=OUT-011, a2b99d71c=OUT-040, 23ec20c30=OUT-010 superseded_by) are on the ORPHANED `feat/outlet-redesign` branch — NONE are ancestors of HEAD. Main received the work via a wholesale RE-PORT (#2098 d4998aec3 "complete outlet re-port" + #2105 98e77fcd7) that DIVERGES from the stories' literal ACs and dropped several deliverables. So the done-marking is verified against unmerged work.

**Rename substance (FOCUS #1: residual tool naming) — essentially COMPLETE.** Only genuine residuals:
- (FUNCTIONAL BUG) bindings/typescript/src/trust.ts:707 filters `e.eventType === "ToolInvoked"` but EventType serializes as `OutletInvoked` (scp-event-log/src/lib.rs:133; NAPI event_log.rs:27 doc confirms) → `toolInvocations` array ALWAYS empty. Plus stale field `toolInvocations` (trust.ts:113) + comments 694/704. Python/Swift/Kotlin correctly use OutletInvoked. OUT-006 residual.
- (SPEC, no divergence) SCP-OFFER-ID-V1 NOT renamed to SCP-OUTLET-OFFER-ID-V1 as OUT-001 desc scoped: spec 25-test-vectors.md:662,676 + CODE interface.rs:428 + runtime test_vectors.rs both still old name (consistent → no wire break, but OUT-001 AC0 grep=2 not 0; offer-id sep absent from §9.18.2 registry so AC5 unmet). 
- Legit-remaining tool: FfiBridgeProvider/ContextProvider `invoke_tool`/`list_tools` (MCP execution trait, per outlet-pr3 decision), roles.rs hard-reject parser for pre-rename `tool:invoke:` strings (SCP-OUT-014), MCP wire `tools/list`/`tools/call`.

**Per-story:** 002 protocol COMPLETE (AC5/AC6 are AC-TEXT typos: const is `OUTLET_REGISTRATION_V2_DOMAIN` hash.rs:62 not `SCP_OUTLET_REGISTRATION_V2`; AC6 regex mistakenly includes the NEW string; roles.rs OutletInvoke hit=negative-assertion comment). 003 event-log rename done but AC2/3/4/8 mechanisms (OldFormatRejected/discriminant()/old_tool_event test/tool_*_total metrics) GONE — superseded by ADR-011 event-type unification. 004 runtime rename done but manager/ dir GONE (ADR-049 actor refactor; ops now context/outlets/invoke.rs). 005 FFI: PyO3+NAPI outlet surface w/ parity (names `outlet_register`/`outlet_invoke`/`outlet_verify`/session/interface/cross_context, NOT AC's `context_outlet_*`); WASM bridge dir ENTIRELY REMOVED (ADR-055 WASM-not-live → AC12-15 N/A); update/deregister/list/get exist in runtime but NOT exposed over PyO3. 006 SDK substance met (Outlets/outlets.ts/Outlets.swift/OutletBridge; class names diverge from AC OutletNamespace) + TS trust bug above. 007 MCP translator GAP: translator.rs + mcp_to_scp/scp_to_mcp + dot query./call. projection NOT on main; MCP uses namespace.rs slash `context/tool_name`. 008 enforcement COMPLETE (matrix 0 tool_, outlet_economy_wiring.rs exists, pipeline_wiring 29 outlet/0 tool_, validate-prd 437). 009 GAP: outlet_registration_v2.json (12 cross-bridge reg vectors) ABSENT; spec25 documents only STREAM vectors §5.4.5; V2 preimage only unit-tested protocol registration.rs:535. 010 GAP: details.superseded_by absent from all 5 stories (SCP-010/013/018/040/041) — AC0-4 unmet; AC5(no tool-era AC strings)=0 pass + AC6 validate-prd pass.

LESSON: when a PRD marks a group `done`, verify the story COMMITS are ancestors of HEAD (`git merge-base --is-ancestor <storySHA> HEAD`) — a done-marking can be pinned to an orphaned branch whose work was re-ported divergently. AC file-paths/symbol-names written against the orphaned branch will mismatch main even when rename substance landed.
