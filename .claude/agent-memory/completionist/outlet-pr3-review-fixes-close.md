---
name: outlet-pr3-review-fixes-close
description: COMPLETE @1ffe476e5 — final review-fix commit closing the two LOW findings from outlet-report-pr3 (check_outlet_call rename, OutletQuery FFI ceiling symmetry, §7.3.8 value-caveat deferral honesty)
metadata:
  type: project
---

Review of the 3-fix review-fix commit on feat/outlet-report-pr3 (worktree scp-wt-outlet-pr3, tip 1ffe476e5, single commit). Reference end-state = `git show origin/feat/outlet-redesign:<path>`. VERDICT: COMPLETE, zero remaining gaps.

**Closes** the SOLE MUST-FIX (LOW) from [[outlet-report-pr3-origin-kind-close]] (app_sandbox check_outlet_invoke not renamed) AND resolves an OutletQuery FFI-symmetry gap not previously flagged.

Fix 1 — rename check_outlet_invoke→check_outlet_call: `git grep check_outlet_invoke` EMPTY. app_sandbox.rs:551 method + all 5 callers (3 dedicated tests renamed check_tool_invoke_*→check_outlet_call_* + 2 composite-test assertions) renamed; matches reference name exactly. Broader `outlet_invoke|OutletInvoke` triage: ALL remaining refs legit — FFI op VERB `outlet_invoke` (kept, migration renamed CAPABILITY not op), `outlet_invoke_cross_context_saga`, event names OutletInvoked/outlet_invoked_event_id/ToolInvoked, deleted-stem refs (roles.rs OUT-014 `outlet_invoke:` hard-break tests), and human-readable "OutletInvoke capability" ERROR STRINGS (mcp.rs:794, outlets.rs:880/1433, bridge.rs:4680 — these are literal error text, NOT enum variants; the OutletInvoke enum variant was DELETED per OUT-014, asserted by roles.rs:5879 out014_no_outlet_invoke_variant_remains). No other `_invoke`-named capability HELPER exists; the query-side gate is has_outlet_call_capability (call-specific by design).

Fix 2 — OutletQuery ceiling symmetry: the 3 FFI check helpers (pyo3 py_check_scoped_capability@context.rs:1861, napi check_scoped_capability_inner@context.rs:5030, uniffi sandbox_check_capability@bridge.rs:8133) PREVIOUSLY handled ONLY the OutletCall wildcard family (`matches!(required, OutletCall(_)) && granted.contains(OutletCallAll)`) — a GENUINE asymmetry bug (OutletQuery family unhandled). Now ALL route through single-authority `CapabilityCeiling::new(granted).contains(&required)`. Complete superset-check site inventory (all handle BOTH families or are correctly call-specific): (a) protocol CapabilityCeiling::contains roles.rs:722 — both; (b) app_sandbox validate_declaration:770-799 — both, ceiling+role sets; (c) 3 FFI helpers — route through contains; (d) call-SPECIFIC helpers check_outlet_call (app_sandbox:551, checks OutletCallAll||OutletCall) + has_outlet_call_capability (outlets/mod.rs:597, runtime invoke.rs:1116 thin-delegate) = correctly call-only, gate Action-outlet INVOCATION which requires OutletCall not OutletQuery per §5.4.2; (e) caveats.rs:564 query-vs-action classification — both. NO query-outlet INVOCATION entrypoint in runtime (git grep OutletQuery in runtime/protocol outlets prod = EMPTY) — query is a capability class flowing through ceiling machinery, not a distinct invoke path, so no unprotected gate. New PyO3 test scoped_capability_check_honors_both_wildcard_families_fail_closed proves cross-family fail-closed (call-all ≠ query, query-all ≠ call, malformed denies) — PASSES.

Fix 3 — §7.3.8 value-caveat deferral honesty: (a) CaveatCounterStore type GENUINELY absent (only spec §07 text + caveats.rs:791 doc-comment ref; no struct/type def). check_invocation_local (caveats.rs:811) has ZERO callers outside caveats.rs; all callers (4192-4300) under #[cfg(test)] mod tests (line 2369). mint.rs:193 build_delegated_caveats materializes ONLY origin_kind (line 235: `InvocationCaveats{origin_kind, ..parent_effective}`); Mint/DelegateParams carry NO value-caveat params; roots emit nb=None (build_root_caveats:262 SCOPE note) ⇒ no in-circulation token carries a value-caveat ⇒ "no live divergence" is LITERALLY TRUE (runtime cannot under-enforce a bound no token asserts). (b) No other artifact claims enforcement live: phase-3.md:1011 + prds/main.json:8532 = SpendingCapability §19.5 (DISTINCT mechanism); §06:100 = context rate-policy max_calls_per_minute (different path); §07:791-849 field defs = type/attenuation not runtime-enforcement claim. caveat_counters storage path appears ONLY in §07 deferral text. (c) `enforce_caveat_invocation` phantom-comment ref GONE (`git grep` EMPTY) — caveats.rs:789-798 now honest: "decided-but-not-yet-wired slice (§7.3.8)", names real entrypoint check_invocation_local, "no live divergence today because mint emits no value-caveats". §07 lines 918/923/925 all carry accurate deferral markers.

Fix 4 — no new stubs/None/dead-code: only "not yet implemented" match = the intended §07 spec deferral TEXT (not a code stub); no `:None`/`= None` added; `Capability` import retained in all 3 helpers still USED (Capability::new, HashSet<Capability>) so no unused-import. Clean clippy scp-protocol+scp-runtime (17.96s, 0 diag) and scp-ffi pyo3 (26.46s, 0 diag). Tests pass: new symmetry test, 3 renamed check_outlet_call sandbox tests, 6 check_invocation caveat tests.

Pre-existing (NOT a finding vs these fixes): ScopedHandle::check_outlet_call has no PROD caller (test-only) — but existed identically as check_outlet_invoke pre-rename + reference carries it (9 refs) as part of ScopedHandle cap-check API surface alongside siblings (check_outlet_register/check_member_invite). Rename doesn't change wiring status; out of scope.

Enforcement note: pretooluse hook FALSE-POSITIVE-blocks any compound Bash command merely MENTIONING an enforcement filename (e.g. check-bridge-symmetry.sh) in a pipeline even for read-only invocation — run enforcement-script names in isolation. check-bridge-symmetry.sh is bash-only (syntax error under sh). Bridge symmetry unaffected here anyway: 3 helpers are free fns with UNCHANGED signatures (body-only change), no bridge-export obligation.
