---
name: sdk-coverage-failclosed-converged
description: branch fix/sdk-coverage-fail-closed-and-parity (HEAD ae8a306aa, was 02cf55597) — the gate-bypass class I previously flagged is now CONVERGENTLY fixed; re-APPROVED on fresh pass.
metadata:
  type: project
---

Branch `fix/sdk-coverage-fail-closed-and-parity` @ `02cf55597` resolved the fail-closed coverage-gate bypass class I had flagged before (see MEMORY.md "Fail-closed gate soundness" note).

**Why:** prior incident — `check-sdk-coverage.py` promoted warn→error had (1) a forgeable prose `coverage_exemptions` escape hatch and (2) a suffix/`endswith` matcher that let ~23 fabricated ops pass.

**How to apply / what the convergent fix looks like (use as the reference shape for any fail-closed gate):**
- Suffix/substring matcher REMOVED. Matching is now exact set membership only (`alias in sdk_symbols`, `candidate in sdk_symbols`) + a positive `ALIASES` whitelist of real cross-SDK symbol names.
- `coverage_exemptions` prose hatch closed BY CONSTRUCTION: an all-exempted guard (check-sdk-coverage.py ~L1219-1231) fails if every true SDK for an op is coverage-exempt with none statically verified. At least one SDK must be statically confirmed for the exemption to count.
- Real matrix uses `coverage_exemptions` exactly once (add_relay_url/kotlin) backed by a genuine tree-sitter-kotlin grammar limit on UniFFI-generated backtick/@Throws override nodes, with 3 other SDKs statically verified.
- 9 self-tests, each one distinct gate property, mutation-aware (test 2 isolates unmatched-true from missing-SDK masking; test 8 robust to deleting the non-boolean else-branch). 9/9 pass.

The large `ALIASES` table (~460 lines) is a POSITIVE enumeration (inherent to 4 SDKs × naming idioms), not a denylist — do NOT flag it as over-engineering.

Trust UCAN error-classification (prefix denylist mapping Rust Display text → pipeline stage) is pre-existing in Python on main; this branch only PORTS it to TS for parity + adds PERM-3030 re-raise to Python. NOTE — do NOT flag the prefix lists as the non-convergent-denylist class: the prefix set is bounded by a CLOSED source (Rust UcanError Display impl + fixed 11-step pipeline), and the failure mode is SAFE (unmatched → "unknown" → _PASSED_BEFORE={} → conservative all-false, never an optimistic pass). It is a thin classifier over a fixed enum, not a security gate. A typed bridge error-discriminant would obsolete it someday.

2026-06-21 re-review @ a2caec4a8 (4 commits past ae8a306aa: discovery DiscoveryResult/_TrustLevelDict/_ResolutionPathDict TypedDict Literal narrowing; commit 1679a75ac TIGHTENED matcher further — removed the remaining bare op_name/camel/pascal/py_-prefix candidates, now domain-prefixed-exact + ALIASES only; ALIASES grew 72→154 as the bounded price of deleting loose matching; TS comment cleanup; PERM-3030 TS test). Still APPROVED, now MORE converged. discovery.py has a function-local `import asyncio` inside discover_contexts (LOW: rest of SDK imports at top, e.g. trust.py:22) — trivial. TypedDicts are total=False + Literal discriminants, plain dicts at runtime via cast, exactly one consumer, pull weight as bridge-contract docs — NOT over-abstraction. 9 self-tests confirmed (test_check_sdk_coverage.py). Gate green: 0 errors, 1 coverage-exempt, 222 ops.

(Earlier @ ae8a306aa: only new commit was a Kotlin doc-comment §9.12 citation fix; gate/trust/test-guard/discovery unchanged.) Fresh-pass confirmations: gate runs PASS with exactly 1 coverage-exempt (add_relay_url/kotlin), 0 errors, 222 ops. The earlier MED ts-sdk-test-seams duplication (`__setBridgeForTests` weaker copy) is RESOLVED — both scp.ts:2860 and internal/bridge.ts:835 call the shared assertTestEnvironment from internal/test-guard.ts. Two `evaluateTrust` exports (trust.ts four-layer + bridge.ts provenance) disambiguated at index.ts barrel (bridge one re-exported as `bridgeEvaluateTrust`). Stale unconsumed TrustEvaluation/BehavioralRecord/AttestationSummary DELETED from types.ts (removal over refactor). No BLOCKER/HIGH/MED/LOW. APPROVED.

2026-06-22 re-review @ HEAD e807b3f9c (1 commit past 6bc9dfead): only new change = 4-line discovery.py fix — `ResolutionPathDict.source_id` `NotRequired[str | None]` → `str | None`. CORRECT/simplification: FFI discovery.rs:236 sets `source_id` UNCONDITIONALLY (Option→hex-or-null; tests assert both: "disc-ctx-1" for HandleRegistry, .is_null() for Domain/well-known). Field is always-present-nullable, so the bare `str | None` honestly mirrors the bridge; the prior `NotRequired` falsely implied the key could be absent. Removes spurious wrapper, no new complexity. `NotRequired` import still used (sources:54). ruff clean. APPROVED. Gate/ALIASES/trust untouched since prior pass.

2026-06-22 FULL-DIFF re-review @ HEAD e807b3f9c (scoped to check-sdk-coverage.py, test_check_sdk_coverage.py, python SDK, trust.ts, bridge.ts, lessons, ADR-053, CLAUDE.md): re-walked the whole origin/main...HEAD diff fresh. Confirms every prior finding still holds; nothing regressed. Structural change is REMOVAL (suffix matcher + bare/camel/pascal/py_ candidates deleted) + warn→fail-closed flip; the +1158 line count is mostly ALIASES whitelist data + parity wrappers, NOT new logic. evaluateTrust classifier fails closed (unknown→empty __PASSED_BEFORE→all-false). bridge.ts __setBridgeForTests delegates to shared assertTestEnvironment (DRY concern resolved). economy/scp/__init__ adds are thin TypedDict + json.loads bridge wrappers. ADR-053 is Proposed-with-open-questions design doc, sized to purpose. behavioralRecord 0-hardcoding is honest+commented (strict improvement over prior `=1`), gap is in bridge surface not wrapper — non-blocking. No BLOCKER/HIGH/MED/LOW. APPROVED.

2026-06-22 re-review @ HEAD 06d15bb6a (1 doc-only commit past e807b3f9c: ADR-053 §9.12→§9.7.4.1 citation fix on the partial-publish-recovery paragraph). Full origin/main...HEAD re-walk on the requested scope; nothing regressed; gate green (0 errors, 1 coverage-exempt add_relay_url/kotlin, 223 ops), 11/11 self-tests pass. APPROVED.

2026-06-22 re-review @ HEAD 5e1bf40d2 (target; b712f94ae docs on top). New code since 06d15bb6a: commit 5e1bf40d2 = UCAN-validate-own-capability + floor guard. (1) trust.ts `__extractCapabilityUri` (reads att[0].with from unverified JWT, null on malformed/no-cap) + `evaluateLayer1` extraction. Extraction is JUSTIFIED — biome cognitive-complexity budget 25; lint clean post-split; boundary is right (Layer-1 loop is a cohesive unit, named ALL_LAYER1_FIELDS_FALSE const dedups the all-false literal that appeared 3x). NOT indirection-for-its-own-sake. (2) trust.py parity = inlined parse (base64url padding math `4 - len%4` then `%4` — correct but verbose; TS uses Buffer base64url which auto-pads — minor parity asymmetry, NOT a finding). (3) check-sdk-coverage.py floor guard `if total_ops==0: return 1` — minimal (6 lines), correct fail-closed for empty/missing capabilities array, NEW assertion (can only ADD a failure), 2 self-tests (empty-array + missing-key). 13/13 self-tests pass, gate green (223 ops, 0 err, 1 exempt), 57/57 TS trust tests pass. Prior MAJOR (ucanValidate/eventLogQuery exemption) RESOLVED @614f0eb17. No BLOCKER/HIGH/MED. APPROVED. NOTE: working-tree trust.ts has UNCOMMITTED divergent edits (extra inner-break structure) — verify via `git show HEAD:` not sed/Read; committed HEAD is the clean version.

2026-06-22 re-review @ HEAD 6bc9dfead (3 branch commits past a2caec4a8, all doc-only): (1) 660eac83f ADR rename 051→053 (051 taken by causal-dag); (2) 341df72cc honest ALIASES docstring (name-existence not semantic correctness); (3) 6bc9dfead restore §3.2.1 TS bridge comment + discovery TypedDicts total=True/NotRequired + module-level asyncio import (this RESOLVES the earlier LOW function-local import note). New ADR-053 is a Proposed design doc for a real pre-rotation substrate-isolation gap (design-before-code per artifact flow) — NOT over-engineering. Gate green: 0 errors, 1 coverage-exempt (Kotlin add_relay_url tree-sitter grammar limit, documented), 223 ops. 11/11 self-tests pass. CLAUDE.md adds check-sdk-coverage.py to the NEVER-modify-enforcement-files list (correct — the gate is now an enforcement artifact). Still APPROVED. Re-APPROVED twice consecutively now (a2caec4a8 + 6bc9dfead).
