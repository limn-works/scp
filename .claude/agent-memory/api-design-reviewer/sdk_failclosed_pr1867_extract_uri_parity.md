---
name: sdk-failclosed-pr1867-extract-uri-parity
description: PR #1867 fix/sdk-coverage-fail-closed-and-parity — typed errors + capability-URI extraction parity review (HEAD b712f94ae)
metadata:
  type: project
---

PR #1867 (`fix/sdk-coverage-fail-closed-and-parity`, HEAD `b712f94ae` = `5e1bf40d2` + one lessons-doc commit) — typed-error rollout in `scp.ts` + trust capability-URI extraction. Verdict NEEDS REVISION (no CRITICAL/HIGH; all MEDIUM/LOW).

**RESOLVED since prior round (was the headline finding):** the `ucanValidate`/`eventLogQuery` mapBridgeError carve-out is GONE. Commit `614f0eb17` wraps both in `mapBridgeError` (scp.ts:2390, 2461). `evaluateLayer1`/`evaluateTrust` (trust.ts:477-485, 575-578) classify on the preserved `[SCP-PERM-NNNN]`/`[SCP-CTX-NNNN]` prefix in `.message` (mapBridgeError preserves the full bracketed message verbatim for ALL branches) and re-raise PERM-3030 by prefix-regex, not by-identity. So the prior `ts-vs-python-error-surface-exemption` asymmetry is closed — 203/203 wrapped, classification survives. The old memory file is now stale on its core claim.

**Still-open findings for next pass —**
- MEDIUM parity: TS exports/tests `__extractCapabilityUri` (trust.ts:308) but Python still INLINES att[0].with at trust.py:771-781 — no named `_extract_capability_uri` helper. Siblings `_extract_core_error`/`_classify_ucan_error`/`_PASSED_BEFORE` ARE mirrored 1:1. TS direction is better (test-covered); add the Python helper.
- MEDIUM contract clarity: `evaluateTrust(scp, subjectDid, context, capabilityTokens)` takes `subjectDid` but neither evaluateLayer1 (TS) nor evaluate_trust (Python) passes it as `presenting_agent_did` to ucanValidate — both call the 3-arg form, so the bridge defaults presenting agent = token's own `aud` (napi ucan.rs:221-223). PARITY IS FINE (TS==Python, shared choice). But `TrustEvaluation.capabilityValidation` is labeled per-`subjectDid` while it actually validates the token's-own-audience. Layer-1 result can be all-true for a token NOT presented by subjectDid. Doc/contract gap: TrustEvaluation doc + capabilityValidation field should state Layer 1 is token-self-validation, not "subjectDid presented this". (Subject↔token binding lives upstream, uncaptured here.)
- LOW standard drift (artifact-flow): sdk-common.md:13 still names canonical `PermissionError` (Python exception `UcanPermissionError`). TS now ALSO uses `UcanPermissionError` (PermissionError alias fully removed, verified zero non-Ucan refs). TS+Python agree with each other but BOTH diverge from the standard's canonical name. Code choice is superior (avoids global shadow in TS too) → update sdk-common.md:13 to make `UcanPermissionError` canonical. Pre-release, no breakage.
- LOW hierarchy drift: sdk-common.md:9-18 diagram lists 8 categories. TS errors.ts has 15 classes / 12-category table (adds Storage/Attestation/Mcp/Governance/Economy + 2 WASM-econ subclasses). Python errors.py has only the standard 8 (no Economy/Storage/etc.). Real cross-SDK category gap masked by stale standard. Decide: expand Python parity OR document TS-only categories in the standard.
- LOW surface minimality: `mapBridgeError` is a PUBLIC export (index.ts:242). It's an internal bridge→typed-error helper; every SDK method already wraps, so consumers never need it. Consider dropping from public surface (or document why exported — e.g. for consumers writing their own bridge calls).

APPROVED as-is: error hierarchy (errors.ts) is clean — every subclass sets `.name`, ERROR_CODE_OVERRIDES-before-prefix ordering correct, code-range table documented. ADR-053 `PreRotationCustodyProvider` (Proposed, design-only) is exemplary: locked canonical method-name table across all 6 bindings with explicit per-binding casing (snake_case Rust/UniFFI/PyO3, camelCase NAPI/Swift/Kotlin), separate-provider decision enforces §3 substrate isolation by construction, single-use handle invariant enforced adapter-side (not trusting foreign impls). No design defect.
