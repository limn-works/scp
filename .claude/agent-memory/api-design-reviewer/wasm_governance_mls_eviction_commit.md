---
name: wasm-governance-mls-eviction-commit
description: WASM governance RemoveMember `commit` result field review (fix/wasm-governance-mls-eviction) — @97c351df9 NEEDS REVISION (undoc'd relay obligation); @b98c5b1a9 + @66a2c6a5c APPROVED (docs on all 3 public surfaces)
metadata:
  type: project
---

RE-REVIEW @c3f7fe48b (FRESH independent pass — every claim re-verified IN-CODE, not from prior memory): VERDICT APPROVED, design SOUND, clean result valid+expected. Re-confirmed this pass: all 4 empty-commit causes map exactly to code (crypto.is_none() @manager.rs:3522-3523; self-DID short-circuit @group.rs:347-348; no-MLS-leaf @group.rs:352-364; genuine-MLS-failure fail-closed Err @group.rs:331-337). commit hex via encode_hex @manager.rs:3572 inside json!{action,did,commit}. Native auto-broadcast contrast TRUTHFUL (execute_remove_member @governance_helpers.rs:1231 → try_broadcast_commit_or_enqueue @:1320). Docs verbatim-accurate on all 3 surfaces (context.rs:738-749 per-backend, scp.ts:1361-1378 Native-NAPI-auto vs Browser-WASM-must-relay, internal/wasm.ts:1102-1107). manager.rs:3507 one-line pointer → state.rs:170-187 governance_rotate_sender_key honestly documents pre-existing sender-key-distribution gap + why eviction holds via epoch-advance alone. names own_did@group.rs:203 / leaf_index_for_did@:257 / remove_member_by_did@:338 / governance_remove_* @state.rs:152/165/188 exemplary. 2 cross-impl conformance tests added. Residual MED unchanged (untyped Promise<string> commit = binding-WIDE typed-result follow-up, not a point fix).

RE-REVIEW @c3f7fe48b (prior pass, doc-only delta on @66a2c6a5c): VERDICT APPROVED. The prior-pass LOW ("Browser callers MUST relay" phrasing on a NAPI-only method) is now FIXED: scp.ts:1357-1378 @returns is reworded to scope the relay obligation PER BACKEND — Native(NAPI)=auto-broadcasts/no-relay (the dominant path; SCP class is NAPI-only, throws SCP-VALID-7005 w/ no WASM fallback at scp.ts:518/556), Browser(WASM)=caller-must-relay. Accurate vs code: native try_broadcast_commit_or_enqueue re-verified in governance_helpers.rs; WASM `""`=encode_hex(Vec::new()) maps to exactly the 2 documented causes (crypto.is_none() broadcast/unencrypted @manager.rs:3522; remove_member_by_did no-op self-DID/missing-leaf @group.rs:348,364). manager.rs prose-dup collapsed to a one-line pointer to governance_rotate_sender_key (good — kills the 4x-restatement seam). Internal names own_did/leaf_index_for_did/remove_member_by_did/governance_remove_* re-confirmed exemplary. Residual MED (untyped Promise<string> commit = binding-wide typed-result follow-up, NOT point fix) unchanged. Clean confirming pass as expected.

RE-REVIEW @66a2c6a5c (fresh pass): VERDICT APPROVED — identical findings to @b98c5b1a9 below; docs unchanged and still on all 3 surfaces (context.rs:737-749, scp.ts:1361-1372, internal/wasm.ts:1093-1109). New LOW noticed this pass: scp.ts doc says "Browser (WASM) callers MUST relay" but `SCP.contextExecuteGovernanceAction` is NAPI-only (class throws SCP-VALID-7005 under WASM, scp.ts:115-122) — the warned caller actually goes through internal/wasm.ts; phrasing reads off (cosmetic, "WASM-bridge callers" better). Native auto-broadcast re-verified (try_broadcast_commit_or_enqueue present). Internal names own_did()/leaf_index_for_did()/remove_member_by_did() re-confirmed exemplary (own_did derives from own leaf, no stored field). Residual MED (untyped Promise<string> commit = binding-wide follow-up, NOT point fix) unchanged.

RE-REVIEW @b98c5b1a9 (remediation of the lone @97c351df9 blocker): VERDICT APPROVED. The relay-obligation doc now lives on all THREE relevant surfaces — public wasm-bindgen `# Returns` (context.rs:735), public SDK `@returns` (scp.ts:1357), internal wasm router comment (internal/wasm.ts:1098). Each covers: commit-field-exists-for-RemoveMember, caller-MUST-relay, silent-fork failure mode, no-broadcast/no-retry, empty-string no-commit contract, AND explicit contrast with native auto-broadcast. Discoverable at call site (JSDoc), not buried. Native auto-broadcast claim VERIFIED still-truthful (execute_remove_member→try_broadcast_commit_or_enqueue, governance_helpers.rs:1320). RESIDUAL (NOT blocking, NOT introduced here): return is still `Promise<string>` untyped JSON on bridge.ts:169 — ideal is typed RemoveMemberResult{commit:string} but EVERY governance-result method on this bridge returns string-JSON, so typing only this one = lone inconsistency; docs now discharge the SAFETY obligation a type would carry; track as governance-result-family follow-up, lift these docs into the field doc when done. Internal names re-confirmed sound.

--- PRIOR (superseded) ---
Reviewed branch `fix/wasm-governance-mls-eviction` @97c351df9 (WASM governance RemoveMember now MLS-evicts + appends durable MemberLeft leaf + returns eviction commit).

VERDICT: NEEDS REVISION (1 blocking design-completeness item). Encoding/naming/fail-closed all SOUND.

Key findings:
- `"commit"` field (hex) added to `context_execute_governance` JSON result (manager.rs:3550-3556). HEX IS CORRECT: this bridge is mixed-by-CHANNEL — `encode_hex` for bytes inside JSON result objects (Merkle roots/leaf/sibling hashes at manager.rs:2613-2721), base64 for message/ciphertext STRING fields, Uint8Array for binary returns/args (decrypt plaintext, key packages, welcome_bytes). Commit rides inside JSON → hex matches siblings. Uint8Array would be inconsistent (can't embed in serialized JSON).
- Empty-string commit for non-MLS (broadcast) path = CLEAN contract (empty never collides with a real commit; better than omitting the key) — but ONLY if documented.
- BLOCKING: `context_execute_governance` (context.rs:699-777) public #[wasm_bindgen] doc + `# Returns` + TS types.ts say NOTHING about `commit` or the caller's obligation to relay it. No TS wrapper method, no typed RemoveMemberResult. Remaining members never learn of eviction unless JS extracts+relays commit → silent group fork. This is the prior reviewer's "depends on JS doing the right thing, no retry" hazard, unaddressed at the API level.
- CROSS-BINDING ASYMMETRY (justified, not a defect): native/PyO3/UniFFI/NAPI RemoveMember return NO commit — runtime owns transport, `execute_remove_member` (governance_helpers.rs:1320-1332) auto-broadcasts via try_broadcast_commit_or_enqueue. WASM has no internal transport (ADR-034) so must hand commit to JS. WASM-only shape; SDK layer CANNOT paper over it. Document as intentional so nobody "harmonizes" it away.
- PRE-EXISTING asymmetry surfaced: governance `dispatch_add_member` (manager.rs:3407-3445) does ZERO MLS work (encrypted add = separate generate_key_package_for_join/join_context_encrypted two-step). So RemoveMember MLS-evicts but AddMember doesn't MLS-admit. Out of scope; track.
- Internal method names SOUND + hard to misuse: leaf_index_for_did (total, Ok(None) for non-member) vs remove_member_by_did (fail-loud on missing leaf — governance pre-verified membership). governance_remove_from_group/remove_sender_key/rotate_sender_key triad mirrors native MlsCrypto. executor_did:&str + timestamp_secs:u64 threaded explicitly (no transposition, distinct types), heavily doc'd.

LESSON reinforced: for WASM bridge, "where do MLS commit/welcome bytes go" differs from native BY ARCHITECTURE (no internal transport). When a fix surfaces MLS bytes to JS, the JS-relay obligation MUST be in the PUBLIC wasm-bindgen doc + ideally a typed SDK result — burying it in an untyped JSON key is the silent-fork footgun.
