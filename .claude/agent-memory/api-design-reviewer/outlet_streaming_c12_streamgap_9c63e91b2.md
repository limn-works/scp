---
name: outlet-streaming-c12-streamgap-9c63e91b2
description: C12 SDK-half review — new StreamGap error across 4 outlet-streaming SDKs (SCP-OUT-039, branch feat/outlet-streaming-ffi, b3a1dd3dc..9c63e91b2)
metadata:
  type: project
---

C12 delta adds receiver gap-detection: a new StreamGap error on the InvocationHandle drain of all 4 SDKs (§5.4.5 "Ordering and gaps"). Continues the [[outlet_streaming_python_sdk_c11a_3de014f40]] canonical-contract arc.

**Verdict: NEEDS REVISION — 1 MEDIUM, otherwise clean+uniform. → RESOLVED @b523535c4 (C12 review fixes): both findings fixed, no new divergence. See RE-REVIEW below.**

**RE-REVIEW @b523535c4:** MEDIUM fixed — Swift case renamed `.sequenceGap`→`.streamGap` at ALL sites (case decl :109, throw :534, streamGapError field :452, aggregate() re-throw :580, sendCancel doc :651, smoke test catch ×2). Zero `sequenceGap` remnants anywhere (bindings/crates/.docs). LOW fixed — enum header now says "four cases", streamGap doc'd as Execution-class SCP-OUTLET-6131 (§5.4.4 OutletErrorClass::Execution::StreamGap) surfaced at protocol-class depth. StreamGap surface still uniform ×4 (all use StreamGap/.streamGap). credit_exhaustion→credit_stall rename: vector key `credit_stall` consistent across JSON (7-vector set) + all 4 SDK expected-key assertions + spec §25; no new cross-SDK divergence. NOTE the residual "credit exhaustion (SCP-OUTLET-6131)" in 05-contexts.md:341 is the DISTINCT execution.credit-exhausted concept (6131), NOT the renamed 6133 credit-stall vector — correct + untouched, do not flag. Scenario key `sequence_gap` (unchanged) vs error type `StreamGap` is a uniform, pre-existing test-data-vs-type distinction, not divergence.

**MEDIUM — Swift enum case named `.sequenceGap`, all others `StreamGap`.** bindings/swift/Sources/SCP/Outlets+Streaming.swift:102. Spec + Rust core (`OutletErrorClass::Execution::StreamGap`) + Python/TS/Kotlin all use `StreamGap`. Swift's OWN sibling convention is lowerCamelCase-of-the-class-name (`StreamAlreadyClosed`→`.streamAlreadyClosed`, `InvalidGrant`→`.invalidGrant`), so the idiomatic form is `.streamGap` — the divergence is a changed WORD (Sequence vs Stream), NOT a casing/idiom difference. Grep for `streamGap` in Swift misses it. Even Swift-internally inconsistent: private field is `streamGapError` (line 445) holding a `.sequenceGap`. Fix = rename case to `.streamGap`. Violates agent-first "identical shape across all bindings."

**LOW — Swift OutletError enum header stale.** Lines 63/76: header still says "Protocol-class (§5.4.4 OutletErrorClass::Protocol) ... errors" and "the three conditions are cases at the SAME depth" — now FOUR cases, and sequenceGap is Execution-class. Python/TS/Kotlin StreamGap docstrings self-acknowledge the "execution code under a protocol base" tension; Swift's enum header doesn't.

**OBSERVATION (uniform, documented, sound — not a finding):** all 4 place StreamGap under the PROTOCOL-class SDK base (ProtocolError / OutletProtocolException / OutletError-enum) though spec classifies it Execution and it carries Execution code SCP-OUTLET-6131. Rationale (in all 4 docstrings): SDK only generates protocol-class errors locally; bridge-raised execution errors arrive as ScpError. StreamGap is SDK-generated defense-in-depth (same-context lossless pump never gaps in prod). Code 6131 is CORRECT — it's the shared CODE_EXECUTION_CREDIT covering execution.credit-exhausted / execution.stream-gap / execution.stream-cap-exhausted (error_codes.rs:149, three slugs one code); distinguished by TYPE not code.

**VERIFIED SOUND (don't re-flag):**
- Class depth uniform: StreamGap is a direct sibling of InvalidGrant/StreamAlreadyClosed under each SDK's protocol base. Python/TS: ScpError>OutletError>ProtocolError>StreamGap. Kotlin: Exception>OutletProtocolException>StreamGap (ScpException is sealed, can't extend — pre-existing). Swift: flat enum, same-depth case.
- Caught by type in all 4 (except StreamGap / instanceof / catch(e:StreamGap) / catch .sequenceGap). Message byte-identical: "outlet stream sequence gap: expected X, got Y (§5.4.5)".
- Fits handle contract: gap raises on the drain path (aggregate() + iterate both route through drainNext), does NOT yield the offending chunk, cursor starts 0 +1 per chunk across all kinds, gap takes priority over terminal Error, re-aggregate re-throws (Python/TS reuse _error field; Swift/Kotlin add separate streamGapError because terminalError is typed ScpError/ScpException which StreamGap isn't). Misuse-resistant: no silent truncation.
- Shared private cancel helper (_send_cancel/#sendCancel/sendCancel) extracted in all 4 so gap-teardown and public cancel() sign through the identical bridge path; best-effort cancel swallows cancel-path errors so StreamGap stays the surfaced terminal.
- Vector JSON (tests/conformance/vectors/outlet_stream_vectors.json) clean: `{version, vectors[]}`; @type discriminator data/end/error self-evident; sequence_gap vector = seq 0,1,3 → expected_end_status "Cancelled" + expected_error_code 6131 (distinguishes gap-cancel from plain cancel which has null code). Spec 25-test-vectors.md §25.21 documents schema + the two code traps (6131 gap vs 6133 credit-stall). Minor: `progress` @type untested; after_chunk_index=-1 sentinel (test-internal, not dev surface).
