---
name: sdk-coverage-failclosed-parity-r25
description: PR #2141 trust/UCAN SDK coverage + fail-closed parity. Round 9 @13aecbbbb (post 156-commit main merge; WASM GONE) NEEDS REVISION 1 SHOULD-FIX (TS-vs-Python error-typing parity). Round 8 @5d118e1a2 was APPROVED; the merge added new branch-specific TS mapBridgeError/getBridge work.
metadata:
  type: project
---

**ROUND 11 (HEAD d2c056ea4, /tmp/scp-2141, 2026-08-01). NEEDS REVISION, 1 MAJOR.** R10 5-method Py/TS divergence RESOLVED (commits 95bf99be4 + e9adb42a2 wrapped all 5 identity key-mutations + outlets w/ _coded_bridge_error; 14 scp.py methods now wrapped). R10 __all__ CONSIDER also resolved (_coded_bridge_error NOT in errors.py __all__). errors.ts change this PR = GOOD symmetric hardening (anchored `^\s*\[(SCP-[A-Z]+-\d+)\]` matching Py _SCP_CODE_RE). coverage extractor now public-only (non-`_`) = sound convergent tightening.
- **MAJOR (classification-strategy divergence → class/code contradiction, entrenched by this PR):** Py `_coded_bridge_error` classifies by BRIDGE-EXCEPTION-CLASS-NAME (BRIDGE_ERROR_MAP, 7 keys, DEFAULT→ContextError) then recovers code from msg. TS `mapBridgeError` classifies by CODE-PREFIX (ERROR_PREFIX_MAP, 12 entries incl STORAGE/ATTEST/MCP/GOV/ECON, DEFAULT→generic ScpError). CONFIRMED reachable: outlets.rs:453 doc "Raises `ContextError` (with embedded SCP-ECON-12010/12061/12090)" — this PR wraps outlet_invoke (scp.py:2540) w/ _coded_bridge_error → PyO3 class="ContextError"→SDK ContextError, code="SCP-ECON-12010" ⇒ Py yields ContextError whose CLASS CONTRADICTS its .code; TS outletInvoke yields EconomyError. Multi-lang: `except EconomyError` impossible in Py (class doesn't exist). Root: PyO3 bridge (error.rs:36-79) has only 6 domain classes (no Econ/Gov/Storage/Attest/Mcp); sdk-common.md:9-18 canonical hierarchy lists only 7 classes BUT code-table:40-44 lists 5 extra ranges w/ NO class ⇒ STANDARD GAP. TS invented 5 extra classes (pre-existing on main), Py didn't. Per artifact-flow fix sdk-common.md FIRST (add 5 classes + say which parent extra-range codes fold to), then align both SDKs + reclassify Py by code-prefix. governance_propose is NOT this bug (raises native PyValueError/RuntimeError w/ CTX codes 2040/2041 → ContextError+CTX = symmetric-consistent).
- **MINOR:** default-fallback asymmetry within the helper pair — Py defaults unmapped→ContextError (concrete, misleading), TS→generic ScpError (honest). MINOR/INFO: Py strips `[code]` prefix off .message (re-added by __str__), TS keeps it in .message → `err.message` differs cross-SDK. INFO: errors.ts md table SCP-OUTLET- row misaligned (cosmetic); TS deprecated `PermissionError=UcanPermissionError` alias has no Py analog (pre-existing).

**ROUND 10 (double-zero pass 1, HEAD 28623a226, /tmp/scp-review-r25, 2026-07-16). NEEDS REVISION, 1 SHOULD-FIX + 1 CONSIDER.** Two new commits since R9: a20eb5314 (python: wrap 8 methods w/ _coded_bridge_error — RESOLVES R9 SHOULD-FIX for those 8) + 28623a226 (ts: migrate 5 identity methods to getBridge "for structural error mapping"). Prior R9 hardcoded-code Python methods (suspend/resume) are NOT among the 8 and untouched — still a footnote, not this pass's focus.
- **SHOULD-FIX (5-method Py/TS error-typing divergence WIDENED by this PR, re-creating the exact gap the PR closes for the 8):** TS now maps errors on 5 identity KEY-MUTATION methods via getBridge Proxy — identityRotateKey/AddAgentKey/RotateAgentKey/RemoveAgentKey/Migrate (scp.ts 812/825/838/851/869) → typed ScpError w/ recovered .code. Their 5 Python counterparts (identity_rotate_key:935 / identity_add_agent_key:691 / identity_rotate_agent_key:928 / identity_remove_agent_key:867 / identity_migrate:850) stay RAW asyncio.to_thread w/ NO _coded_bridge_error → native _scp_core exc propagates (diff class tree, code-in-message-not-.code). The Python parity commit covered 8 of the 13 TS-mapped methods, omitting these 5. NOT a "family stays raw" design: TS identityCreate/Load/Resolve (684/794/875) are raw in BOTH SDKs (symmetric-OK); only the 5 key-mutations are TS-mapped-but-Py-raw. Violates identical-shape tenet. Fix: wrap the 5 Python methods (or, if intended raw, don't getBridge them in TS).
- **economy_verify_payment_receipts (NEW in this PR, scp.py:2201) is NOT the problematic gap — it is symmetric-raw:** its TS counterpart economyVerifyPaymentReceipts (scp.ts:2642, pre-existing, NOT in diff) is ALSO unmapped raw this.#native. Both raw → acceptable/symmetric. The task premise ("only unwrapped Py method w/ TS counterpart") is FALSE — the 5 identity methods are the real asymmetry.
- **CONSIDER (__all__ hygiene, low-impact):** `_coded_bridge_error` (underscore=private) was added to errors.py __all__:349 — self-contradictory (__all__ = public declaration). Its sibling cross-module private helpers `_saga_terminal_from_bridge` (consumed by scp.py:2643) + `_SCP_CODE_RE` are correctly OMITTED from __all__ → that's the precedent. Direct `from scp_sdk.errors import _coded_bridge_error` (trust.py:36 top-level, scp.py lazy ×8) works regardless of __all__, so nothing needs it there. NO namespace leak (scp_sdk/__init__.py:82 uses explicit imports, does NOT re-export it), so severity is cosmetic. Remove the line.
- **Lazy `from scp_sdk.errors import _coded_bridge_error` ×8 inside method bodies = NOT an API-design issue** (internal impl detail, not public surface; consistent w/ scp.py's pervasive lazy-import convention). Minor DRY note only: trust.py imports the SAME helper at module top-level, and errors.py imports only `re` (no cycle risk) → top-level would be safe+DRYer, but scp.py's lazy style is established. Non-blocking.

**ROUND 9 (HEAD 13aecbbbb, /tmp/scp-review-r25, 2026-07-16) — post 156-commit main merge (ADR-055 WASM removal, ADR-059 structured trust, ADR-060 decimal wire). NEEDS REVISION, 1 SHOULD-FIX + notes.** Surviving branch-specific surface = TS scp.ts (mapBridgeError added to 8 `this.#native` methods: contextSend/contextMemberCount/contextGovernancePropose/outletInvoke/identityRemove/identityExecuteRecovery/ucanValidate/eventLogQuery; getBridge(this) routing restored on 5 identity lifecycle methods) + TS errors.ts (regex anchored `/^\s*\[(SCP-[A-Z]+-\d+)\]/` + prefix format aligned) + Python trust.py `_SCP_CODE_RE` anchored.
- **Prior r25 sole MEDIUM (WASM ucan_validate fault-masking) MOOT — WASM crate DELETED** (crates/scp-ffi/wasm gone, 0 wasm refs in bindings/typescript/src). Resolved-by-deletion, not a finding.
- **mapBridgeError code→class mapping: NO HOLES for the 8 methods.** Verified: all 4-5-digit runtime prefixes (CTX/OUTLET/ECON/IDENT/PERM/TRANS/GOV/CRYPTO/STORAGE/ATTEST/VALID + SAGA) — every one except SAGA is in ERROR_PREFIX_MAP (12 entries); SAGA handled by separate mapSagaError, and the 8 are non-saga. Unmapped prefixes seen in grep (SCP-OUT-/PERSIST-/CAPINJECT-/CAC-/ACR-/BCH-/CRYPTOMOVE-/AB-/BA-) are all 3-digit STORY IDs in doc comments, never bracketed thrown codes → not a hole. Category table (errors.ts:10-23) matches the 12-entry map exactly.
- **Two bridge-access idioms in SCP class both converge on typed errors (coherent, but a discoverability smell = root cause of this PR):** `getBridge(this)` returns a `wrapBridgeErrors` Proxy (bridge.ts:821, applied at native.ts:2127) that auto-maps EVERY call via mapBridgeError — so the 5 restored identity methods get mapping for free (no try/catch, correctly). The 8 `this.#native.*` methods bypass the Proxy → need explicit try/catch{mapBridgeError}. mapBridgeError is idempotent on ScpError (errors.ts:356) so double-map is safe. Latent trap: a new `this.#native` method that forgets try/catch leaks a raw Error — exactly the bug class this PR fixes ("restored routing" + "added mapBridgeError to 8"). CONSIDER: unify on one dispatch path.
- **SHOULD-FIX (cross-SDK error-typing parity, tension w/ PR's stated 'parity' goal; pre-existing on Python side):** the 8 TS methods now throw SDK-typed ScpError subclasses carrying the REAL recovered `.code`. Their 8 Python equivalents in scp.py (context_send/context_member_count/context_governance_propose/outlet_invoke/identity_remove/identity_execute_recovery/ucan_validate/event_log_query) delegate RAW via asyncio.to_thread with NO wrapping → native `_scp_core.Scp*Error` propagates (different class tree from scp_sdk.errors.*, code embedded in message string not `.code`). Python's code-recovering helper `_coded_bridge_error` (the mapBridgeError analog) is used ONLY in trust.py, never in scp.py. Worse, the 2 scp.py methods that DO wrap (suspend→SCP-TRANS-5001, resume→SCP-CTX-2000; not among the 8) HARDCODE one fixed code, masking the real failure. So Python has 3 inconsistent error strategies; TS now has 1. PyO3 is the REFERENCE bridge yet is the least-structured here. This PR didn't touch these Python methods (out of literal diff) but widens the divergence relative to its parity objective — call it out.
- **CapabilityValidation (on ucanEvaluate, NOT ucanValidate — task premise mislabeled; ucanValidate returns void):** well-designed. camelCase 6-bool interface (types.ts:961), exported (index.ts:264), `allValid()` conjunction helper for the partial-true case (types.ts:1006), extensive "DIAGNOSTIC NOT AUTHORIZATION / intrinsic-mode skips step-6 grant-match" docs. Partial-true handled gracefully. LGTM.
- **Minor CONSIDER (pre-existing, not this PR):** ProtocolError + InvalidGrant + StreamAlreadyClosed all default to code SCP-OUTLET-6100 → 3 classes share 1 code, so `error.code===...` can't distinguish them (only `instanceof` can — which IS the documented idiom; StreamGap correctly distinct at 6131). Not produced by mapBridgeError (SDK-guard-constructed).

---

**ROUND 8 (double-zero pass 2, HEAD 5d118e1a2, /tmp/scp-review-r25) — API DESIGN SOUND / APPROVED, 0 findings.** No code change since Round 7; re-confirmed all four: (1) evaluateTrust/evaluate_trust well-designed+misuse-resistant; (2) Swift TrustEvaluation(from:) honest all-false Layer-1 on both TrustInput+TrustScoreResult inits, phantom-label doc bugs fixed by afad2b1af/105c19bbb; (3) verifyParticipationRequirements(profileJson:requirementsJson:) is SOLE resolution in Swift (UniFFI free-fn ScpBindings.swift:15097) AND Kotlin (Scp.kt:1714→uniffi.scp), insecure twins deleted (Swift Trust.swift −90 commit 23779139f, Kotlin Participation.kt −127 commit 7097938f5), scratch_trust_old.py removed by 58f6f06b5 (0 scratch files tracked); (4) coverage gate fail-closed+bounded (tree-sitter AST positive-extraction, over-capture=safe failure mode line 1250, missing-SDK-key=ERROR line 1536, empty-exemption-reason=ERROR), HEAD only anchored TEST regex _CODES_RETURN_RE to match-arm positions. See Round 7 detail below.

---

Re-review of `fix/sdk-coverage-fail-closed-and-parity` at HEAD a3c0e6efb (2026-07-15, /tmp/scp-review-r25).

**RESOLVED since earlier passes (now non-findings):**
- economyVerifyPaymentReceipts converged: TS now takes `readonly Record<string,unknown>[]` + returns typed `PaymentReceiptVerificationResult` (was `receiptsJson:string`→`unknown`). Prior parity smell closed.
- UcanFailureCategory no longer exported from index.ts (module-level export in trust.ts only, for tests). Prior minimality leak fixed.
- §9.12 citation now fixed in Rust FFI source (uniffi/napi), not just generated Swift. Prior HIGH resolved.
- mapBridgeError applied 204× in scp.ts; ERROR_CODE_OVERRIDES-then-prefix design is clean; errors.ts carries a documented category→range table. Good.
- Coverage gate fail-closed logic sound & bounded: unmatched-true / false-w/o-exempt / all-exempted-ops / empty-matrix / non-bool-cell all error. ALIASES honestly documented as name-existence not semantic. rotate_key (uniffi bridge.rs:2178) + register exemptions verified truthful.
- `__decodeBase64UrlToUtf8` correctly private (NOT exported); tested transitively via exported `__extractFirstCapabilityUri`. Keeping internal is right.

**NEW MEDIUM (fault-masking + cross-binding divergence):** commit 62bbf8e41/785aaf560 reworked WASM `run_validate_ucan` (crates/scp-ffi/wasm/src/ucan.rs) to wrap manager state-lookup faults (`ucan_context_state`/`ucan_seen_nonce_keys`/nonce writeback) as `UcanError::MalformedToken(...)` → routed via ucan_error_code → `[SCP-PERM-3001]`. trust.ts/trust.py evaluateLayer1 absorb ONLY PERM-3001 (closed allowlist) → classify "malformed token: context state lookup failed" as `token_parse` → all-false verdict, silently. NAPI's equivalent (`ensure_registered`, runtime.rs:1540) surfaces `[SCP-CTX-2023]` which trust.ts re-throws as a genuine fault. So: (1) same failure = thrown on NAPI, absorbed as all-false on WASM (violates identical-shape tenet); (2) infra/state fault masquerades as untrusted verdict on browser; (3) `MalformedToken` type is semantically wrong (token isn't malformed). Fix: WASM state-lookup faults should carry a CTX-class code so trust.ts propagates them, matching NAPI; only real token-pipeline failures → PERM-3001. The closed-allowlist absorption's soundness DEPENDS on PERM-3001 meaning only token-pipeline failures — this branch broke that invariant at the WASM bridge.

**LOW/INFO:** all-false CapabilityValidation returned for {no tokens, malformed, SCP-VALID-*, token_parse} — caller can't distinguish "no credential" from "bad credential" (documented, fail-closed, both SDKs agree; INFO). `register` bridge op public in Py/Kt/Swift but internal-only in TS (honest exemption; shape divergence). mapBridgeError regex `/\[…\]/` unanchored vs documented "position 0" + trust.ts's anchored `^\[SCP-PERM-3001\]` — anchoring would match the contract. `__decodeBase64UrlToUtf8` is the only `__`-fn in trust.ts not exported (convention marks exported test seams) — harmless.

**ROUND 2 (HEAD ~20c2178bd):** Prior NEW MEDIUM (WASM state-fault masking) RESOLVED — `WasmValidateError{Ucan,Context}` enum now routes context-state faults→CTX_2023 (matches NAPI ensure_registered), UCAN faults→PERM-3001. Enum internal, no public surface. Two NEW MEDIUMs in Python pre-flight (commit 6b39f6ac9):
- **Regex trailing-newline divergence** (trust.py:_CONTEXT_ID_RE `^[a-zA-Z0-9_-]{1,256}$`): Python `$` matches before a trailing `\n`, so `"ctx\n"` PASSES the pre-flight but Rust validate_context_id REJECTS it (reject_control_chars). Verified empirically. Re-opens the exact `[SCP-VALID-*]` absorption hole the pre-flight was added to close (bad ctx_id gets absorbed as all-false at the bridge instead of propagating). Fix: `re.fullmatch` or `\A...\Z`.
- **Pre-flight raises base `ScpError` + gated on `if capability_tokens:`**: same bad context_id → base `ScpError` (with tokens) vs `ValidationError` subclass from bridge event_log_query (no tokens, propagates since `except ContextError` doesn't catch VALID). Two exception classes for the same bad arg depending on optional param. Code SCP-VALID-7001 is CORRECT (matches Rust codes::VALID_7001 generic-validation code). Fix: raise `ValidationError` subclass + hoist check before the token-gate so both paths validate identically at entry.
LOW: `PIPELINE_ABSORBED_CODE_PREFIX` (trust.ts) exported for lockstep-test coupling — good single-source-of-truth pattern, NOT in index.ts (contained, not on package surface), but lacks `@internal`/`__` marker that sibling helpers (`__extractFirstCapabilityUri` etc.) carry; reads as public. Same for `UcanFailureCategory`. TrustEvaluation Layers 3-4 (attestations/endorsements/challengeResults/consequenceStructure) always empty/null in BOTH SDK facades but field docs describe them as populated — type overpromises (fn-level JSDoc says "reserved", field-level doesn't). TS `evaluateTrust(Context handle)` is misuse-resistant by construction; Python string context_id is the surface that NEEDS the pre-flight — the asymmetry is inherent to the signatures, TS design superior.

**ROUND 7 (HEAD 5d118e1a2) — API DESIGN SOUND (APPROVED). All prior findings resolved.**
- WASM fault-masking (r25 MEDIUM) stays fixed: `WasmValidateError{Ucan→PERM-3001, Context→CTX_2023}` on BOTH bridge paths (ucan_validate + validate_tool_ucan_wasm); latter's return type tightened Option<&'static str>→&'static str (code always-present, honest sig). Enum internal, not public surface.
- Absorption boundary uniform across all 3 async bridges: PyO3 `with_context` (CTX-*), NAPI `ensure_registered` (CTX-2023), WASM `WasmValidateError::Context` (CTX-2023). All SDK evaluate_trust absorb ONLY [SCP-PERM-3001] (TS `PIPELINE_ABSORBED_CODE_PREFIX`/validateOneCapUri trust.ts:531; Py `_PIPELINE_ABSORBED_CODES` frozenset trust.py:470), re-throw CTX. TS Layer-1 CTX fault propagates outside Layer-2 try (call site 703) — not masked.
- check-sdk-coverage.py gate: fail-closed + bounded (missing-key/unmatched-true/false-no-exempt/non-bool-cell/all-exempted-no-verified/empty-matrix-floor all→error; suffix matching removed for closed ALIASES allowlist). Correct API.
- Swift TrustEvaluation.init(from:) all-false Layer-1 documented on both TrustInput+TrustScoreResult inits. verifyParticipationRequirements insecure Kotlin/Swift twins DELETED; only UniFFI-generated secure free-fn remains (Scp.kt:1714).
- OBS (tracked separately, non-blocking): Swift evaluateTrust still takes no capability_tokens param (Py/TS do) → Swift Layer-1 always all-false. Typed-model parity follow-up.

**ROUND 3 BATCH 3 (HEAD 08fccffdc):** Prior R2 Python findings RESOLVED — de0077f13 hoists context_id pre-flight before token-gate + `re.fullmatch` (trailing-\n now rejected) + raises `ValidationError` subclass (code SCP-VALID-7001). ValidationError<ScpError so broad handlers still catch; strictly-better refinement, well-documented Raises clause. b49a5b64a added `@internal` to PIPELINE_ABSORBED_CODE_PREFIX (still module-only, not in index.ts barrel — TypeDoc strips @internal, no consumer confusion). att[0]-only caveat now surfaced at struct-docstring + within_ceiling field + evaluate_trust fn-doc (thorough). NEW SHOULD-FIX (introduced by this PR, Swift Trust.swift): the all-false Layer-1 fix (correct fail-closed) added doc "Use `evaluateTrust(tokens:)` to obtain a TrustEvaluation with Layer-1 fields populated" ×4 (lines 89/92/112/115) but `evaluateTrust(tokens:)` DOES NOT EXIST — only `evaluateTrust(subjectDid:contextId:)` (line 204) which routes trustQueryScore→TrustEvaluation(from:score)=the very all-false init. So Swift has ZERO token-validating path (Python/TS both do): its only evaluateTrust ignores tokens + always returns all-false Layer-1, and the doc misdirects an LLM to a non-compiling method (violates Agent-first authorability tenet). Root cause = cross-SDK parity gap: Swift missing the capabilityTokens param Python/TS carry (Swift is the odd-one-out — Py `evaluate_trust(scp,subject,context_id,tokens?)`, TS `evaluateTrust(scp,subject,Context,tokens?)`, Swift `evaluateTrust(subjectDid,contextId)`). Fix: either add token-taking Swift evaluateTrust (parity) or make doc point to real path/state Layer-1 unavailable in Swift facade. Behavior-change OBS: Python bad context_id now raises (was silent all-false absorption w/ tokens, or no-op w/o) — correct fail-loud on caller error.
