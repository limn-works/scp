# SCP-OUT-046 PR-A: streaming SagaPreparedState variant + MerkleFrontier serde (541a89e3b, feat/outlet-xctx-streaming-phase2)

CLEAN, 0 defects. One informational PR-B watch-item.

- **MerkleFrontier +Serialize/Deserialize/PartialEq/Eq (stream.rs:1220):** all 4 private fields (stack Vec<(u8,[u8;32])>, leaf_count u64, billed_count u64, ceiling u64) round-trip losslessly; root() is a pure fold over stack → reproduces bit-identically. Eq compares actual peaks, so no false-equal-with-different-root (equal peaks ⟹ equal root); irrelevant for restore anyway (exact fields restored). Test frontier_serde_round_trip_reproduces_root non-vacuous: ceiling=6→billed_count=7 over 10 leaves, cross-checked vs compute_chunk_manifest_root batch oracle.
- **from_prepared/into_prepared (saga_prepared_state.rs:466/508):** EXACT 14-field inverses. SagaId.0↔SagaId, DID.0↔DID, all else direct move/copy (Amount is Copy). Both match arms exhaustive, NO wildcard. Nothing dropped/reordered/defaulted.
- **~20 match sites:** Production if-lets at handlers/saga.rs:1520 (commit_b_reserve), 1661/1694 (commit_b_first_settle) LEFT UNCHANGED, match ONLY unary CrossContextOutletInvocation. They are FAIL-CLOSED: a streaming variant → explicit Err(SCP-SAGA-13030/13031), NOT a silent skip (corrects the task's "silently not-match" framing). In PR-A streaming variant is never constructed in prod (into_prepared is only pub-reachable constructor; production ctor lands PR-B), so never hit. Construction sites 1060/1714/1863 only re-insert an already-unary `prepared`. Abort path saga.rs:2495 does variant-agnostic remove(saga_id) → clears streaming fine.
- **PR-B WATCH-ITEM (informational, not a PR-A bug):** commit_b_reserve/commit_b_first_settle assume unary-only. PR-B MUST route streaming sagas through a separate seal-phase FSM command (disjoint dispatch), OR extend these 3 sites. If PR-B accidentally routes a streaming CommitB here it fails LOUD (13030/13031), not silent corruption — but still an integration seam to verify.
- **Class-S snapshot enum:** externally-tagged serde, distinct variant name CrossContextStreamingOutletInvocation → existing unary snapshot bytes unchanged, no tag collision. No golden regen needed (coder correct).
- **Test binds:** all let-else-panic conversions + supervisor.rs matches! guards use correct variant. 3 named tests pass, assert real values. clippy clean WITH and WITHOUT testing feature (into_prepared/from_prepared read every field in prod → no dead_code).
