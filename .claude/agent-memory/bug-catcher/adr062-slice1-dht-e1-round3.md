---
name: adr062-slice1-dht-e1-round3
description: ADR-062 Slice 1 (SCP-CAPINJECT-001) DHT E1 round-3 verification of round-2 fixes R2-1..R2-8 + GATEWAYS — CLEAN, 0 new bugs
metadata:
  type: project
---

ADR-062 Slice 1 / SCP-CAPINJECT-001 (branch feat/adr062-slice1-dht-e1, delta e54de4fae..5584761bc). Round-3 verify of round-2 fixes. **VERDICT: CLEAN — 0 new bugs.** All 6 checked fixes correct.

- **R2-1 (self_host publish-mode from config.dht):** CORRECT. `config.dht` → `dht_mode` (self_host.rs:1227) threads to BOTH `dispatch_hosted_site_by_dht_mode` (selects DID-method `D`) AND `ServeHostedSite.dht_mode` → `build_host_site_node(dht_mode)` → `NodeConfig.dht = dht_mode` (self_host.rs:2207/2240). `D` and publish-mode both derive from the ONE value → can never disagree. Test passes. LOW: the new `nat_traversal_disabled_dht_node_starts_without_publishing` builds NodeConfig directly (Node::start), bypassing build_host_site_node — guards the Node::start invariant but not the threading it claims to test (reverting the threading fix wouldn't fail it).
- **R2-2 (Domain honors DhtMode):** CORRECT. `build_domain_inner` gained `dht_mode`, replaced unconditional `did_method.publish().await?` with `publish_did_document_for_mode(dht_mode,...)` (lib.rs:3239). config.rs:948 TLS-success branch threads `dht` (already in scope). Disabled→Ok(skip, D-independent), Production/Memory→fatal. No residual unconditional node-start publish. Both tests pass.
- **R2-6 (hoist build_ffi_dht_client → scp_ffi_common::dht):** CORRECT. prod arm `#[cfg(not(testing))]`=into_client (real Pkarr), testing arm `#[cfg(feature="testing")]`=InMemory. Each bridge delegates + maps DhtInitError→own err w/ dedicated IDENT_1058. napi/uniffi InMemoryDhtClient import correctly narrowed to bare `#[cfg(test)]` (still used by their own unit tests: napi identity.rs:1936, uniffi bridge.rs:21103); pyo3 removed import (unused). Feature chains sound: each bridge dev-dep `scp-dht{features=["testing"]}` + `scp-ffi-common{features=["testing"]}` unifies testing on for its test build.
- **R2-7 (hoist invalidate_resolver_cache → CoreFields):** CORRECT. bridge_instance.rs:2205 internally calls same `resolver_cache()` (2174) resolver reads → same DidCache Arc. Pure body-hoist; all 5 rotation call-sites/bridge unchanged (migrate = old+new DID). pyo3 block_on, napi/uniffi .await.
- **R2-4 (A5 cfg tighten any(test,testing)→feature="testing"):** SOUND. DhtMode::Memory variant + all 4 match arms (lib.rs:3237, self_host.rs:1354, main.rs:491/570) gated feature="testing" identically → every match exhaustive in ALL cfgs. NO scp-node test code constructs Memory (only the gated arms reference it) → plain `cargo test -p scp-node` lib compiles. scp-dht self dev-dep `{path=".",features=["testing"]}` makes its own #[cfg(test)] tests (construct InMemoryDhtClient) compile. Workspace CI lanes get scp-node/testing via scp-ffi(default=server→scp-ffi-common/server enables optional scp-node)+scp-ffi-common/testing→scp-node?/testing. VERIFIED COMPILE: scp-dht --no-run OK; scp-ffi-common default (no testing) OK incl dht_capability_injection.rs tripwire; scp-node --tests --features testing OK. Plain `cargo test -p scp-node --tests` fails on `Node::start_for_testing` (E0599) — PRE-EXISTING (gated any(test,testing), untouched; integration tests always need --features testing), NOT from R2-4.
- **GATEWAYS (shared validate_gateway_url → scp-dht):** CORRECT. Moved to pub `scp_dht::validate_gateway_url` (byte-identical logic), GatewayUrlError→DhtInitError::InvalidGateway maps clean; both into_client + self_host build_pkarr_client fail-closed on it. LOW: self_host trims+skips-empty BEFORE validate; FFI into_client validates raw untrimmed → whitespace-padded gateway accepted by self_host, rejected by FFI. Both fail-closed (FFI stricter), benign; residual divergence vs the "identical contract" goal.

3 new node tests pass (domain_disabled_starts_without_publishing, domain_production_publish_failure_fails_closed, nat_traversal_disabled_dht_node_starts_without_publishing). LESSON re-confirmed: verify cfg-gate hypotheses with real cargo check; the A5 tighten did NOT orphan helpers into hard errors because no test constructs the gated variant + helpers are pub.
