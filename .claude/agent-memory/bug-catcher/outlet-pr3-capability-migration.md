### outlet-report-pr3 capability migration (b4e97ff50, Jul 2026)
Migration Capability::ToolInvoke/All→OutletCall/All, ToolRegister→OutletRegister, ToolInterface→OutletInterface; PaidActionType::ToolInvoke→OutletCall; UCAN stems tool_invoke→outlet_call; Capability::new→Option; new nb/InvocationCaveats/CaveatResolver in validate.rs.

**VERDICT: clean on correctness.** scp-protocol 3499 tests pass; scp-protocol/scp-runtime/cross-context-bridge template all compile. Event names PRESERVED correctly (EventType::ToolInvoked/CrossContextToolInvoked/ToolInterfaceEstablished untouched; scp-event-log not in diff; derive_tool_invoked_entry_id → "ToolInvoked:{}" intact). No residual old enum variants. per_outlet_call/OutletCall wire rename consistent across all 4 SDKs + FFI + templates (intentional hard-break, no serde alias — matches ADR-049 §1 policy). mint.rs Capability::new now fail-loud via ok_or_else. All filter_map(Capability::new) sites are fail-CLOSED direction (drop from ceiling/granted narrows authority).

**Findings (all LOW/MEDIUM, non-blocking):**
- MEDIUM (spec-internal contradiction, not code): §08 prose example says app declares `outlet_call(...)` but the normative declaration-format table (§08 lines 79-80,100) + app_sandbox.rs to_capabilities() parser STILL key on `scp:ctx/tools/{id}` + action "invoke". Parser matches the normative table (self-consistent), but an author following the prose writes `outlets/x`+`call` → falls to Capability::Custom("outlets:call") silently → later authz fail. Fix: migrate the §08 prose back to tools/invoke OR migrate table+parser to outlet/call (pick one).
- MEDIUM (doc/behavior mismatch): FFI media bridges (scp-ffi/src/media.rs:154,197; napi/uniffi peers) doc "Raises ValidationError if the capability string is invalid" for ceiling param but use filter_map → silently drops malformed ceiling entries. Fail-closed direction (deny), but violates doc contract + inconsistent with build_context_params which ok_or_else?-fails-loud. Same for validate_declaration bridge (ceiling/role_caps filter_map).
- LOW (naming inconsistency): two semantically-IDENTICAL fns in different crates — has_outlet_call_capability (scp-protocol/outlets/mod.rs, renamed) vs has_outlet_invoke_capability (scp-runtime/outlets/invoke.rs, kept old name, body migrated). Both check OutletCallAll then OutletCall(id). Agree. accept_tool_interface_with_kind / accept_tool_interface (interface.rs) also keep "tool" in internal fn names (not wire).

**Observation (NOT a regression — deferred wiring, pre-existing):** cross-context saga Prepare-B re-validation (saga.rs:1202) uses NoCaveatResolver, so §7.3.8 nb caveats (max_calls/amount_max/origin_kind/allowed_target_dids/time-box) are NOT enforced on the stored delegation proof there. validate.rs:487 doc explicitly says switching runtime/bridge sites from NoCaveatResolver→TokenNbCaveatResolver is "a separate wiring step" (SCP-OUT-019/021). Production FFI outlet-invoke sites (napi/outlets.rs:60, src/outlets.rs:362, src/mcp.rs:749, uniffi bridge.rs:4021/4609) correctly use TokenNbCaveatResolver. Field didn't exist before diff → adding NoCaveatResolver preserves prior behavior, no regression. Flag for the SCP-OUT-019 wiring PR: verify saga path is/needs TokenNbCaveatResolver (spec §7.3.8 line 927: cross-context guard = OutboundPolicy ∧ InboundPolicy ∧ caveat).

verify_edge_attenuation (None,None)/(Some,None) outlet-edge launder-rejection logic is correct & well-tested; child_is_outlet_edge derived from child att (mirrors mint). ENV: worktree /Users/alec/Developer/limn/scp-wt-outlet-pr3.

---

### FOLLOW-UP PR feat/outlet-report-pr3 @7512e2159 (diff b4e97ff50..7512e2159, Jul 2026) — CLEAN, NO BUGS
This PR RESOLVES all 3 findings above. NO scp-protocol changes (validator + resolvers + caveats pre-existed on base). Changes are runtime + FFI only.
- **mint.rs origin_kind materialization:** new infer_origin_kind_from_capabilities / build_delegated_caveats / build_root_caveats. mint_ucan/delegate_ucan now compute nb instead of hardcoding None. VERIFIED SOUND: (a) mint↔validate classification symmetric — both use uri.resource() "outlet_query"/"outlet_call" (att_set_has_outlet_stem, is_outlet_stem, classify_outlet_stem_family all agree); (b) inference only runs AFTER child_is_outlet_edge guard, so it never returns Ok(None) on the child path → inherited_origin_kind always Some → narrow never trips OriginKindUnspecified on a faithful child; (c) escalation (outlet_call child from outlet_query parent) blocked upstream at delegate_ucan Step 2 capability-subset `matches()` (resource must match exactly) BEFORE build_delegated_caveats; (d) child always inherits parent verbatim (`..parent_effective`) so narrow(parent==child) passes; parent always outlet-edge when child is (subset). build_root_caveats delegates to try_new_for_root (mixed-family reject covered by protocol test root_mint_rejects_mixed_kind_stems).
- **saga.rs NoCaveatResolver→TokenNbCaveatResolver at validate_ucan_rebind:** CORRECT + REQUIRED. ZST resolver, no lifetime issue. This is the SCP-OUT-019 wiring flagged in the prior note. required_cap=outlet_call:{tool} → genuine outlet-invocation site. Positive saga tests (prepare_b_accepts_*) pass (root token, nb=None resolves None, leaf gate accepts). Delegated-token materialization covered by new ucan_validate_integration.rs tests.
- **has_outlet_invoke_capability→has_outlet_call_capability rename + delegate to protocol fn:** wrapper body byte-identical semantics (OutletCallAll then OutletCall(id)); zero leftover old-name refs anywhere (crates+bindings); both scp-core export paths (outlets::* glob + invoke module) compile.
- **app_sandbox.rs:** new OutletQueryAll⊇OutletQuery(_) arm symmetric+disjoint+correct (mirrors CapabilityCeiling::contains). tools/→outlets/ parser migration resolves the prior §08 MEDIUM. NOTE: to_capabilities() still can't EMIT OutletQuery (outlets/{id}+invoke→OutletCall only), so the new QueryAll arm is defensive-only from this parser — harmless, not a bug.
- **FFI fail-loud (media.rs/context.rs + napi/uniffi peers):** filter_map(Capability::new)→ok_or_else+collect::<Result>()? — resolves the prior media MEDIUM. Error types correct (PyValueError/ScpPyError::ValidationError VALID_7000; NapiError::from_reason/ScpNapiError::Validation; ScpError::Validation). `?` early-return is intended fail-loud on malformed ceiling entry (media ceiling strings are protocol "media:voice" form, Capability::new-parseable — only genuine junk errors). context_params.rs change is message-text-only (already fail-loud).
- Preserved names intact: EventType::ToolInvoked/CrossContextToolInvoked, tool_invocation_count. derive_tool_invoked_entry_id NEVER existed on base/reference (not a regression).
- **GATES:** scp-runtime lib 1896 pass; ucan_validate_integration 46 pass (incl 3 new origin_kind roundtrip tests, well-asserted + negative nb=None-still-rejected); saga integration 14 pass; saga inline 45 pass; phase2_integration pass. clippy -D warnings clean (runtime, testing+saga-witness-test-mint). scp-ffi + napi + uniffi all build.
- **Minor test-coverage gap (NOT a bug):** no mint-boundary (mint_ucan) test for mixed-family outlet root rejection — covered at protocol try_new_for_root level; wrapper is trivial passthrough.
