---
name: eventlog-unification-phase2
description: ADR-011 event-log unification Phase 2 crypto review (round 4 APPROVE) — runtime onto scp_event_log RFC 6962 substrate, export-root migration, §9.9.3 equivocation dedup
metadata:
  type: project
---

## FINAL MERGE GATE (HEAD 3d96058f5, 2026-06-20) — SOUND, no blocking findings. Double-zero confirmed.
- WASM hardened clock (time.rs): production wasm32 now_ms()=captured_date_now() UNCHANGED. now_ms_u64 negative clamp + cast guards + WasmClock UNCHANGED byte-for-byte. Only added: cfg(wasm32) on inline-JS extern + prod now_ms, plus cfg(not wasm32) SystemTime native-test fallback compiled OUT of browser build. wasm32 cargo check + clippy clean.
- Leaf parity native↔WASM: identical Event struct + leaf_hash()=SHA-256(0x00‖rmp_serde(Event)). Governance proposal/vote/withdrawn/resolved leaves EMPTY both sides (b""==EventPayload::default().data); params ride on buffer-only ContextEvent. consequence_event_payload()=serde_json::json! BTreeMap (no preserve_order anywhere) → sorted-key deterministic; shared producer both sides.
- Export binding: recompute_event_log_root replays via RFC6962 tree::root, ct_eq vs SIGNED snapshot.event_log_merkle_root (sole authority). verify_strict used.
- participation tool_invocation_count_anchored: 1-byte fixed-width INTO signed preimage; signature_binds test proves binding. No length/domain-sep regression.
- Tag 59 retired gap (76→75); KAT byte-stable; name-string serialization can't shift leaves.
- MINOR non-blocking: ignored marker test wasm_native_full_governance_eventtype_parity_pending stale-lists GovernanceProposalCreated as "unappended by WASM" but WASM manager.rs:3990 appends it (empty); covered by active cross_impl_governance_proposal_vote_leaf_is_empty — hides nothing.
- Tests green: scp-event-log 200/200 (--features testing; bare fails 116 on did:key gate — known), participation 79/79, eventlog_convergence 6/6 + negative controls, wasm_conformance 55 + checkpoint byte-identical, cross_impl 6/6.

ADR-011 amendment / event-log unification Phase 2. Round-4 confirmation review = APPROVE, no blocking findings. Resolves the prior finding that runtime used a hash-CHAIN, not the RFC 6962 tree.

**Why:** runtime ContextManager event log diverged from the protocol RFC 6962 Merkle substrate (`scp-event-log/tree.rs`). Native↔WASM equivocation latent; #1540/#1535 blocked. Alec chose FULL unification.

**How to apply:** single root authority is `scp_event_log::tree::root` (RFC 6962: leaf=SHA-256(0x00‖rmp_serde(Event)), interior=SHA-256(0x01‖L‖R), odd-node promotion, empty=SHA-256("")). All three paths use it identically: provider `merkle_root` (providers/event_log.rs:278), checkpoint create (:623), exporter+importer via `verify_merkle_chain`.

Verified sound this round:
- **Export-root binding migration**: `verify_merkle_chain` (export_import.rs) REPLAYS entries through `tree::append_unsigned_event` (per-leaf sequence-from-0 + prev_hash chain, genesis for entry[0]) then returns `tree::root`. Exporter binds it into signed snapshot (create_export:890); importer recomputes + ct_eq. Prefix-truncation rejected (nonzero seq); suffix/reorder/middle-removal → different root. Truncation forgery CLOSED.
- **§9.9.3 equivocation dedup**: received app messages + EquivocationDetected + (former) MessageReceived NO LONGER appended to durable log — receiver-minted, sender-unauthenticated leaves would make honest receivers diverge roots and false-positive detection. No MessageReceived/EquivocationDetected EventType variant exists; both in-memory ContextEvent only. Detection unchanged (Equal count + ct_eq root false ⇒ Divergent). Dedup = per-sender `(event_count, merkle_root)` HashSet (record_equivocation_if_fresh, queries_helpers.rs:867) — sole mechanism now append no longer advances local_count. New root at seen count = fresh. Bounded at MAX_SEQUENTIAL_COMMITS: alert still EMITS when full, only stops inserting (never misses).
- **Governance decoupled**: run_buffered_post_delivery runs velocity/consequence/checkpoint-counter UNCONDITIONALLY; event_name:Option<EventType> Some only for sender-authenticated events. Fixes prior buffered-drain governance-skip regression.
- **Consequence typed-payload decode** (consequence.rs): payload_target_did decodes exactly 2 live encodings — positional MessagePack typed struct (rmpv first array element) + JSON object target_did. rmpv array-vs-map disambiguates. Legacy null-terminated fallback removed (pre-release, correct).
- **Typed API**: append_context_event takes EventType + EventPayload; no string-literal event names remain.
- **76 EventType tags**: 0-35 pinned (EconomicPolicyApplied=33 historical gap-fill), 36..=75 unification variants, all distinct (test-enforced).

HEAD bf9266777 re-review (commit-ordered leaf determinism + 75-variant) — SOUND, no blocking findings:
- **76→75**: PseudonymAnnounced REMOVED (routing-bootstrap ContextEvent, not durable). Tag 59 RETIRED as a permanent GAP (not renumbered) so every other variant's tag + §25 KAT preimage stays byte-stable. EventType now derives Copy. all_event_type_tags_are_distinct asserts !contains(59). KAT root byte-unchanged.
- **Leaf determinism / convergence is the crux**: leaf=SHA-256(0x00‖rmp_serde(Event)); Event={event_type,actor_did,timestamp,sequence,payload,prev_hash,signature=empty}. The two non-trivially-convergent fields:
  - timestamp: now COMMITTER-ASSIGNED (envelope created_at copied by every member, §7.3.1/§9.9.3), NOT per-member now(). create_context takes creation_timestamp_secs; append_event takes timestamp_secs; consequence leaves use convergent_consequence_timestamp(consequence)=timestamp of highest-sequence triggering evidence (drawn from convergent log). Timer events use pre-computed convergent deadline. Negative-control test wasm_and_native_members_diverge_under_per_member_local_timestamps proves this is load-bearing.
  - consequence payload: consequence_event_payload(payload.rs) = serde_json::to_vec of sorted-key BTreeMap object {action_type,rule_index,target_did,trigger_kind}. preserve_order NOT enabled so keys sorted=deterministic. trigger_kind_str emits stable "Custom:{key}" NOT Debug; consequence_action_type stable. Durability gate is_convergent_trigger (enum-keyed): only WarningCount/Custom mint durable leaves; MessageVelocity/ToolRateExceeded never (rate needs convergent clock protocol lacks).
- **WASM shares the SAME scp-event-log crate** (wasm/Cargo.toml path dep) — imports Event/EventLog/append_unsigned_event/root/leaf_hash/consequence_event_payload directly. NO re-implementation of tree or Event. Eliminates encoding-drift class entirely. Native consequence actor_did="system", WASM append_consequence_leaf actor_did="system" — match.
- **Empty-root convention is symmetric by construction**: create_export binds root via verify_merkle_chain(&data) (export_import.rs:872), the SAME fn the importer calls — so empty→[0u8;32] on both sides, never diverges. Provider merkle_root returns SHA-256("") for empty (checkpoint path, native+WASM both via provider). The two conventions ([0;32] export vs SHA-256("") checkpoint) never meet in a compare. No bug.
- **Proof seam**: state.merkle_tree TWIN DELETED + sync_merkle_tree helper (pushed entry.hash as raw leaf — old divergence source) GONE. Provider overrides prove_event_inclusion/consistency build directly against own canonical EventLog via with_log seam — no second tree, no replay. Trait-default rebuild_event_log_for_proof replays via append_unsigned_event = byte-identical tree.
- **prune_before_checkpoint**: truncate_log_keeping_tail RE-CHAINS retained tail to genesis (first re-anchors GENESIS_PREV_HASH, renumber seq from 0) so pruned export is itself a valid genesis-rooted prefix that verify_merkle_chain accepts. prune_event_log (keep_last_n) DELETED.
- **participation anchored byte**: tool_invocation_count_anchored (=false until ADR-051) inserted into signable_bytes at FIXED position (after 8 u64s, before 2×32B keys), 1 byte u8::from(bool). Test signature_binds_tool_invocation_count_anchored proves sig binds it. Unambiguous (fixed-width between fixed-width). Sound.
- **checkpoint cosig UNCHANGED**: SCP-CHECKPOINT-V2 preimage (compute_checkpoint_canonical_hash) untouched; build_checkpoint(queries_helpers:677) now feeds it tree::root + entries.len() event_count from same provider read = consistent. wasm_and_native_checkpoint_signatures_are_byte_identical passes.
- SCP-EXPORT-ENTRY: domain separator RETIRED (chain hash gone). compute_entry_hash/verify_chain/EventLogEntry all deleted.
- Tests: 200/200 scp-event-log --lib (needs --features scp-primitives/testing; bare run = 116 fail on hex did:key gating, NOT a regression). export_import + eventlog_convergence + wasm_conformance all green. One #[ignore]: wasm_native_full_governance_eventtype_parity_pending (~40 governance EventTypes not yet appended by WASM — honest documented gap, where both append leaves converge).

FINAL double-zero confirming review (HEAD 4cad781e5, 2026-06-20) — SOUND, NO BLOCKING FINDINGS. Re-verified at final state after 4 additional commits past prior round:
- leaf_hash() extraction (tree.rs:296) = pure refactor, byte-identical to old inline (0x00 || rmp_serde(event)). append + append_unsigned both call it.
- PseudonymAnnounced removal: tag 59 RETIRED as permanent gap (not renumbered); all_event_type_tags_are_distinct asserts !contains(59); 75 variants; EventType now Copy; serde-by-name so removal can't shift other leaves; §25 KAT root byte-stable.
- Notification-window non-backdatable floor (4cad781e5, NEW): is_effective now max(effective_at, observed_at+PERIOD). CRITICAL CHECK PASSED: durable leaf timestamp = pending.effective_at (CONVERGENT, committer-assigned), NOT observed_at/current (governance_helpers.rs:463 ceiling, :509 econ-policy). observed_at is member-LOCAL APPLY-GATE only — never written to leaf. Honest members all mint byte-identical CeilingModified/EconomicPolicyApplied leaf at effective_at; floor only delays malicious-proposer early activation. Transient cross-member apply-timing skew = Behind (fewer events), not Divergent — no §9.9.3 false-positive. Freeze deadlock deadline intentionally NOT floored (liveness safety valve, not authz; backdate only ENDS deadlock earlier, needs 2 colluding signers) — documented residual, accepted.
- Consequence convergence end-to-end: durable leaf gate = is_convergent_trigger (enum-keyed; WarningCount/Custom only). matches_trigger: WarningCount/Custom match ONLY EventType::GovernanceAction events → evidence is always Source-1 (durable log). convergent_consequence_timestamp draws highest-seq evidence timestamp (convergent). Buffer-sourced events project to MessageSent ONLY → can never be evidence for a convergent trigger → member-local estimated_ts never reaches a durable leaf. EL01 fix: buffer no longer re-projects convergent events (would double-count by member-local buffer_len) — convergent_governance_count_is_independent_of_buffer_length test proves it.
- WASM shares merge_consequence_events + consequence_event_payload + trigger_kind_str + consequence_action_type via ConsequenceDispatcher::append_durable_consequence_leaf override; actor_did="system" both sides; preserve_order absent workspace-wide (sorted-key JSON deterministic). No re-impl.
- Export seam: recompute_event_log_root (export_import.rs:464, renamed from verify_merkle_chain) replays via append_unsigned_event (validates seq+prev_hash chain) → tree::root → ct_eq vs SIGNED snapshot.event_log_merkle_root (:638). Empty→[0u8;32] both sides (inside signed JCS), no all-zeros bypass. Prefix-trunc rejected (nonzero seq/non-genesis prev_hash); suffix/reorder/middle/forge → root mismatch. exporter binds via same fn (:844).
- Proof seam: state.merkle_tree TWIN fully GONE from runtime (grep 0 hits). prove_event_inclusion/consistency build directly against canonical EventLog via with_log (providers/event_log.rs:760) — no second tree, no replay. merkle_root = tree::root.
- Checkpoint cosig: checkpoint.rs UNCHANGED (empty diff) — SCP-CHECKPOINT-V2 preimage byte-stable. build_checkpoint feeds tree::root + entries.len() from same provider read = consistent. wasm_and_native_checkpoint_signatures_are_byte_identical passes.
- participation tool_invocation_count_anchored byte at FIXED position (after 8 u64s, before 2×32B keys), u8::from(bool); signature_binds_tool_invocation_count_anchored proves sig binds it; unambiguous between fixed-width fields.
- EquivocationDetected/MessageReceived: NO EventType variant exists (grep 0); record_equivocation_if_fresh has 0 append_context_event calls — type-level un-appendable, receiver-minted leaves can't enter Merkle tree.
- Tests green: scp-event-log 200/200; eventlog_convergence 6/0 (+1 ignored ADR-051 fwd); wasm_conformance 54/0 (+1 ignored governance-parity fwd).
