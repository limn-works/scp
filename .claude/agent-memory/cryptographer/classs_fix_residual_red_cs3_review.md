---
name: classs-fix-residual-red-cs3-review
description: ADR-049 §9 RED-CS3 suspension durability fix review — authorization-monotonicity under crash; SOUND, no findings
metadata:
  type: project
---

# ADR-049 §9 Class-S suspension durability fix (RED-CS3) — branch classs-fix-residual, base 272c4d079 — CRYPTO-SOUND, no HIGH/MED/LOW

**Why:** consequence engine auto-suspends a member's capability (`ContextRoleState.suspended_capabilities`, app-level downward-auth blocklist enforced by `member_has_capability`, roles.rs:924 — checks suspended FIRST, returns false even when role grants). Previously rode best-effort coalesced persist; ≤50ms crash window lost it (NOT re-derived on respawn) → transient re-grant of a DENIED capability = authorization-monotonicity break.

**How to apply:** the fix is the correct convergent reframe — strengthen the rare suspension OUTCOME to fail-closed, leave EVALUATION coalesced. If re-reviewed, all 5 axes resolve in fix's favor:

1. CLASSIFICATION right class: suspension rollback = re-grant (security regression); velocity/earned_capacity rollback = relaxed rate limit only (accepted §10 residual, explicitly EXCLUDES re-grant). Asymmetry real.
2. KEEP-DIRECTION correct: `persist_state_fail_closed` (messaging_helpers.rs:2403) takes SHARED &PerContextState, serializes already-suspended state, returns PersistenceFailed on write fail WITHOUT un-applying. Suspension STAYS in mem. Un-suspending would be the unsafe (fail-OPEN) direction. Test `periodic_sweep_suspension_persists_fail_closed` (governance.rs) drives failing-persistence provider, asserts (a) PersistenceFailed surfaces (b) suspended_capabilities RETAINED.
3. H10 escalation: process_one_triggered_consequence failure path applies suspend_all (strictest) then `return true` — correctly owes fail-closed persist (suspend_all mutates suspended_capabilities). Success path returns enforcement.suspended (false for AssignRole + empty SuspendCapability — genuinely no mutation, correct).
4. PRUNE carve-out SOUND (the subtle one): only prod role_state_mut→system_assign_role callers = member-ADD (execute_add_member governance_helpers.rs:1162; spawn/import lifecycle_helpers.rs:1130; class_s.rs:4806 is TEST). system_assign_role (roles.rs:1101) REPLACES member_capabilities with new grants THEN prune_suspensions_to_role_grants retain(cap ∈ new grants) — BOTH ride SAME persist. Crash-rollback reverts both maps in LOCKSTEP → member_has_capability reverts to EXACT prior value, no split-brain. Prune only removes suspensions for caps ∉ new grants = caps the member no longer holds (member_has_capability false via 2nd clause anyway) = inert no-op suspensions. Net effect at worst over-restrictive, NEVER re-grant. ADR prose "never the downward-auth pair" loose in letter (prune DOES touch suspended_capabilities) but governance_helpers.rs:1152 comment accurate ("not a downward-auth bypass"); conclusion correct. Genuine sound carve-out, correctly left best-effort. DOC NIT (cosmetic, not finding): class_s.rs role_state_mut doc claims "never suspended_capabilities" — align to governance_helpers framing if tightened.
5. CONVERGENCE §9.9.3 preserved: fix adds only SNAPSHOT persist (build_snapshot_for_persist messaging_helpers.rs:2333 — reads state + exports crypto, NO event_log append, NO checkpoint_events_since mutation). Durable Merkle leaves go to SEPARATE store deps.event_log via append_consequence_event (governance_logic.rs:55), synchronous inside enforce_triggered_consequences, counter++ only on append — UNCHANGED (only doc-comment hunks near it). Snapshot persist runs AFTER enforce returns → counter consistent with leaves in independent store. No snapshot↔leaf divergence. Strictly ADDS durability, never narrows.

Flag threading exhaustive: all 9 enforce_triggered_consequences + 6 run_buffered_post_delivery sites OR-accumulate or return up; #[must_use] honored. Governance-exec path (governance_helpers.rs:4710/4725) `let _ = suspension_applied` correct because whole finalize_governance_action runs inside token.discharge_with (4903) = single fail-closed persist. 4 prod consequence sites all covered: send (finalize_send/persist_finalized_send), receive (handle_deliver_incoming owns bool at cell boundary, persists after class_c_view drops), tool-settle (settle_tool_economy), periodic sweep. Borrow lifetimes correct (persist after borrowing view drops). Whitelist tripwire unaffected (no new no-persist ClassSCell mutator). Residual correctly recharacterized BEHAVIORAL-closed / STRUCTURAL-disclosed (fields nameable via whole &mut but hole closed); cross-crate pub(in) blocker on privatizing ContextRoleState fields accurately stated. VERDICT: SOUND.
