---
name: outlet-pr3-capability-redesign
description: PR3 Capability Query/Call redesign + §7.3.8 InvocationCaveats attenuation algebra + UCAN origin_kind enforcement — crypto assessment (feat/outlet-report-pr3 @b4e97ff50)
metadata:
  type: project
---

# Outlet PR3 capability redesign (feat/outlet-report-pr3, a65a778c9..b4e97ff50) — crypto assessment

## TIP UPDATE @1ffe476e5 (delta over 7512e2159) — SOUND, no new crypto finding
Single commit; 6 files. mint/validate/caveats-exec/resolver core BYTE-IDENTICAL to 7512e2159 (git diff --name-only proves none in delta). (1) 3 FFI check_scoped_capability helpers (pyo3 context.rs:1862, napi context.rs:5031, uniffi bridge.rs:8133) now route through CapabilityCeiling::new(granted).contains(&required) instead of inline exact+OutletCallAll. contains (roles.rs:722) = exact-membership OR family-scoped wildcard: OutletQuery(_)⇐OutletQueryAll ONLY, OutletCall(_)⇐OutletCallAll ONLY, disjoint (query≠call), else false fail-closed. Capability derives Hash/Eq so OutletCall("a")≠OutletCall("b") exact. Refactor is a STRENGTHENING not a widening: old code had NO OutletQueryAll⇒OutletQuery(id) branch (asymmetric UNDER-auth) — new adds the missing query symmetry, matching the UCAN validator (verify_ceiling_compliance→CapabilityUri::is_within_ceiling capability.rs:214, wildcard keyed on exact resource string outlet_call vs outlet_query = same disjoint families). "in lockstep" claim TRUE. Malformed required parses to Custom(str) (roles.rs:289 catch-all, not None) → requires exact grant, still denies. FFI symmetry test PASSES (1/1). (2) caveats.rs delta = DOC-COMMENT ONLY (verified: every ±line is ///; zero executable change; narrow/check_invocation_local bodies untouched). (3) app_sandbox.rs = pure rename check_outlet_invoke→check_outlet_call, identical body + test renames. (4) spec §07 §7.3.8 now marks value-caveat post-input enforcement + CaveatCounterStore as DEFERRED/not-yet-wired while origin_kind (Step7b per-edge narrow, Step7c leaf/terminus stem-consistency, Step11b time-box) LIVE. This LIVE/DEFERRED split is CRYPTOGRAPHICALLY ACCURATE: origin_kind family IS enforced at mint+validate (prior review); value bounds genuinely NOT enforced at invocation. SAFE because mint emits ONLY origin_kind: build_root_caveats returns None (mint.rs:305); build delegated materializes InvocationCaveats{origin_kind, ..parent_effective} where parent_effective=empty() (all value fields None, caveats.rs:361) for roots and inductively value-None for interior edges → NO in-circulation token carries amount/rate/count bound → no unenforced assertion. This RESOLVES the prior HIGH's phantom-provenance concern: the divergence is now DOCUMENTED as deferred (artifact-flow: spec leads code), not silent. Note: value-caveat ATTENUATION (child≤parent in narrow()) IS still enforced at mint/validate edges — only runtime INVOCATION-time value enforcement is deferred; spec split correctly scopes this. caveats(106/106) + roles(154/154) + FFI sym(1/1) PASS. Pre-existing unrelated test-compile breaks in worktree (FfiKeyCustody::InMemory needs allow_in_memory_custody feature; seed_peer_pseudonym in spawn_from_welcome_tests) NOT in caveat path; scp-runtime + scp-protocol LIBS build clean.



Range b4e97ff50 (9 commits L0..L7c). Reference = origin/feat/outlet-redesign @3d6313cee (DIVERGED branch: 252 commits in ref not in PR3, 237 in PR3 not in ref — parallel, NOT subset). VERDICT: attenuation algebra + validate-time gates SOUND; ONE HIGH (spec/code divergence: post-input runtime caveat enforcement absent, latent in PR3).

## SOUND (verified, reasoning)
- **narrow() (caveats.rs:669) monotone.** All directions correct: amount_max_per_call/cumulative child<=parent (narrow_le_amount, compares .value() u64 not string — ADR-060 Amount-as-JSON-string doesn't break it), max_calls child<=parent, valid_from child>=parent (later=restrictive), valid_until child<=parent, hours/days subset bitmask (child&parent==child), rate_window both fields downward. None→Some=introduce-bound OK; Some→None=FieldRemoved REJECT everywhere. origin_kind: (_,None)→OriginKindUnspecified always, mismatch→OriginKindMismatch. narrow() logic byte-identical to reference (caveats.rs diff = 24 lines, only scp_primitives→scp_did import + 1 ADR-060 test fixture).
- **JSON schema narrowing (json_schema_narrows:1196)** closed whitelist (enum/const/min/max/minLength/maxLength/pattern/required/additionalProperties) + structural (type/properties/items). Unknown child keyword→reject. pattern=byte-equality (regex containment undecidable, correct). additionalProperties:false locks key set (descent 1642). NOTE minor: numeric min/max via as_f64 loses precision >2^53 — symmetric both sides so still monotone, worst case ~1 ULP relax of a SECONDARY schema bound (outlet's own input_schema is primary). LOW/defensible.
- **UCAN origin_kind enforcement (validate.rs) sound at EVERY edge.** verify_chain_recursive:1517 calls verify_edge_attenuation(token,parent) at every recursion level → interior edges covered, not just leaf. (None,None) arm rejects outlet edges with OriginKindUnspecified (anti-laundering: all-nb=None outlet chain closed). verify_leaf_outlet_stem_consistency (1920) closes forged depth-1 no-proof token (mixed-family reject + origin_kind-vs-stem). classify_outlet_stem_family (1819) rejects mixed-family (true,true) UNCONDITIONALLY. att_set_has_outlet_stem fail-closed on unparseable att.
- **caveat_resolver classification (finding #3) SAFE.** All outlet-invocation sites use TokenNbCaveatResolver (napi/outlets.rs:60, uniffi/bridge.rs:4021/4609, ffi/outlets.rs:362, mcp.rs:749). Generic validate/evaluate + broadcast/saga use NoCaveatResolver. Safe because: NoCaveatResolver returns None for BOTH parent+child at every edge → (None,None) arm REJECTS any outlet edge (child_is_outlet_edge=true) with OriginKindUnspecified → a caveated multi-edge outlet chain CANNOT validate through a NoCaveatResolver path (fails closed). Depth-1 root outlet token has no edges but its per-call caveats are enforced at INVOCATION not validate (see HIGH). No bypass. evaluate_ucan is read-only mirror (same verify_delegation_chain/leaf-consistency/time-box, threaded caveat_resolver); Step-6 skip when required=None cannot flip false→true.
- **Signing preimages byte-identical (finding #4).** outlets/hash.rs diff vs reference = EMPTY. OUTLET_REGISTRATION_V2_DOMAIN=b"SCP-OUTLET-REGISTRATION-V2:", kind bytes 0x00 Query/0x01 Action, cost sentinel SHA-256(0x00) all unchanged. registry/interface/mod.rs diffs = comments + test labels + re-export lists only, NO domain/preimage/tag change. No sig-compat break.
- **Hard-reject completeness (finding #5) COMPLETE.** Capability::new (roles.rs:220) hard-rejects outlet:invoke:/outlet_invoke:/tool:invoke:/tool_invoke:/tool:register/tool:interface BEFORE any prefix split → cannot reach Custom catch-all. Custom masquerade defense (validate_as_ceiling_entry:545) re-resolves Custom string through Capability::new, rejects if it names any built-in in any spelling (single authority, spelling-agnostic). ceiling_strings built from Capability::ucan_capability_name (enum-gated, runtime.rs:1499/1534/1736) → no legacy stem in ceiling. Economy has NO tool:invoke parse path (PaidActionType::OutletCall only). CapabilityUri::from_str (wire) is permissive but a forged tool_invoke:* att fails is_within_ceiling + fails matches(outlet_call). No legacy path survives.
- **Capability::new→Option + filter_map (finding #6) SAFE (under-auth only).** ALL production filter_map(Capability::new) sites (ffi context/media, napi, uniffi) build GRANT sets (ceiling/role_caps/granted) — dropping unparseable entry SHRINKS authority = under-auth. `required` check always fail-closed→deny. No deny/block/ban LIST in the model (MemberBan is a grant, not a list entry); model is purely additive (ceiling caps grant, roles grant subsets) per §7.3.8 "additive deny-surface". mint.rs:269 production caller uses .ok_or_else (FAILS LOUD, not silent drop) — correct for mint. No over-auth path.

## UPDATE @7512e2159 (commit f6fd5705a — origin_kind materialization; POST-b4e97ff50)
NEW commit f6fd5705a materializes §7.3.8 origin_kind in mint (`build_delegated_caveats`/`build_root_caveats` in mint.rs) + switches saga.rs cross-context re-validation NoCaveatResolver→TokenNbCaveatResolver. VERDICT: mint↔validate SYMMETRY SOUND. Verified:
- infer_origin_kind_from_capabilities (mint.rs:124) exhaustive/fail-closed: (q,a)=(T,T)→Err mixed, (T,F)→Query, (F,T)→Action, (F,F)→None; unparseable→Err. SAME resource() classifier as validator's classify_outlet_stem_family + att_set_has_outlet_stem (is_outlet_stem = EXACT "outlet_query"|"outlet_call"). Classifiers CANNOT disagree.
- KEY INVARIANT PROVEN: if child_is_outlet_edge (att_set_has_outlet_stem=true) then infer≠Ok(None) (exact negation), so inherited_origin_kind=parent.or(inferred) is ALWAYS Some for outlet child → mint NEVER emits outlet child with origin_kind=None. Inherited kind can't disagree with child stems (child caps ⊆ parent caps via delegate step-2 matches(), both single-family) → NO wrong-kind token mintable.
- narrow() origin_kind arm (caveats.rs:686): (_,None)→OriginKindUnspecified ALWAYS (even parent None); (Some p,Some c) p≠c→Mismatch. build_delegated_caveats runs try_new(mint-limit)+narrow(parent-or-empty). Materialized struct = {origin_kind, ..parent_effective} — ONLY origin_kind ever set (Q5 boundary: NO amount/max_calls/rate/adapter/target/schema bound emitted; build_root_caveats always None; sole InvocationCaveats{} literal mint.rs:235). Deferred post-input enforcement stays safe — mint emits no value-caveat to go unenforced. Q5 SOUND.
- Q4 saga TokenNbCaveatResolver STRICTLY-MORE-CORRECT: required_cap=outlet_call:{tool} is genuine outlet-invocation gate; nb is signature-covered so resolver can't surface unsigned value; confused-deputy defense = presenting_agent_did=caller_did + step-5 audience match, ORTHOGONAL to resolver. Old NoCaveatResolver REJECTED legit depth≥2 outlet chains (None,None→OriginKindUnspecified); new resolver passes materialized chains, still rejects mixed/wrong-kind/missing/widened. depth-1 root (nb=None) passes leaf-consistency correctly (Some-guard skipped, only mixed-family classify runs).
- evaluate_ucan read-only mirror routes SAME verify_delegation_chain+verify_leaf_outlet_stem_consistency threaded w/ resolver (validate 757/807, evaluate 1114/1154) — cannot diverge.
- Tests: 46 integration pass incl both Query+Action depth-2 round-trips + hand-built nb=None-still-rejected negative. 38 validate unit incl interior-edge widen rejects.
- **NEW FINDING (LOW, PRE-EXISTING, out-of-PR3-scope): depth-3+ chains DON'T VALIDATE for ANY capability.** delegate_ucan accumulates FULL ancestor prf (mint.rs:785 proofs=parent.prf.clone()+parent_cid, byte-identical on origin/main) but verify_chain_recursive (validate.rs:1485) requires EVERY prf entry's aud==token.iss — true only for direct parent. Confirmed by adversarial test: BOTH outlet_call AND messages:write depth-3 fail identically w/ DelegationChainBroken("parent aud X != child iss Y"). NOT introduced/worsened by PR3 (mint depth-3 correctly materializes+inherits Action at D1 AND D2; only the general chain-validation model caps effective validation at depth-2). MAX_CHAIN_DEPTH implies deeper chains intended → flag to human as repo-wide UCAN chain-model bug.

## HIGH — spec/code divergence (finding #3 deep dive): post-input caveat runtime enforcement ABSENT in PR3
Spec §07 §7.3.8:923-925 (ADDED in THIS PR, normative present-tense) mandates "Post-input checks, inside invoke_outlet: input_schema conformance, amount_max_per_call vs cost, allowed_adapters, allowed_target_dids, and CaveatCounterStore for max_calls/amount_max_cumulative/rate_window (durable, persisted §17.3)". PR3 reality:
- check_invocation_local (caveats.rs:807) has ZERO production callers (only tests). enforce_caveat_invocation named only in a doc comment. NO CaveatCounterStore anywhere in PR3 tree.
- invoke_outlet_execute_and_validate (invoke.rs:422) does steps 1-6 (active/capability/registry/input-schema/execute/output-schema) — NO check_invocation_local, NO counter store.
- REFERENCE branch FULLY wires this (manager/outlets.rs:3466/3721 call check_invocation_local, IncrementCaveatCounter mutation intents, CaveatCounterApi::release across scp-core+all FFI+runtime). PR3 uses manager_methods.rs structure and omits it entirely.
LATENCY (why not CRITICAL): PR3 runtime mint.rs hardcodes nb:None at both sites (342,599) — NO PR3 path mints caveats into a token. So within PR3's closed loop nothing produces caveats and nothing bypasses the missing check. BUT TokenNbCaveatResolver reads token.payload.nb, so an externally/reference-minted token carrying nb caveats would have its per-call amount / allowed_adapters / allowed_target_dids / max_calls / rate_window / cumulative-amount UNENFORCED at invocation (validate-time gates narrow/time-box/origin_kind DO fire; only post-input is missing). Artifact-flow invariant VIOLATED: spec shipped in same PR asserts behavior code lacks. FIX: wire check_invocation_local + CaveatCounterStore into invoke_outlet per §7.3.8:923, OR mark §7.3.8 runtime-enforcement bullets as a forward story with explicit slice ID and add a mint-side guard/test pinning nb=None so no caveated token is silently under-enforced. Recommend escalate — this is the reference's design intent not yet ported.
