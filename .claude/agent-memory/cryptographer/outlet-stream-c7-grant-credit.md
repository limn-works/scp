---
name: outlet-stream-c7-grant-credit
description: C7 grant_credit(u32) internal-signing + grant-time escrow reserve review — crypto SOUND, one HIGH crash-conservation gap
metadata:
  type: project
---

# Outlet streaming C7 grant_credit revision (feat/outlet-streaming-ffi @665e1838a, delta atop a96940ecf)

Reviewed 3-commit delta: a6bafe451 (GIL-deadlock fix), 665e1838a (grant_credit(u32) internal sign + grant escrow reserve), be7f76acd (rename opener → outlet_stream_open, crypto/economy-INERT pure rename+PRD).

CRYPTO SOUND (item 1):
- compute_credit_sig_preimage (scp-protocol/.../stream.rs:627) byte-exact §5.4.5 SCP-OUTLET-CREDIT-V1: SHA256("SCP-OUTLET-CREDIT-V1:" || len_be32(ctx)||ctx || len_be32(outlet)||outlet || request_id[16] || grant_be32 || monotonic_seq_be64 || stream_epoch_be64 || caveats_binding[32]). Bridge signs over pinned StreamEntry fields; runtime verify_credit_signature uses SAME identity pins (grant()/grant_with_identity in runtime stream.rs) → divergence fails-closed, not money bug.
- Key never leaves custody: BridgeCustodyStreamSigner.sign → custody.sign(handle, preimage); only verifying_key (public) cached. Ed25519-over-32B-digest, sign==verify consistent (mirrors cancel).
- monotonic_seq: per-StreamEntry AtomicU64 fetch_add(SeqCst), first=0, strictly increasing; runtime rejects <=seen_seq CreditReplay (seen only advances on accept). Failed-grant seq gaps harmless.
- Non-invoker → SCP-PERM-3001 (caller_not_invoker_err) BEFORE resolve_stream_signer/reserve. Correct order.

CONSERVATION mostly SOUND (item 2): reserve DEBITS budget + escrow.apply_reserved_top_up extend ledger in lockstep; close settle refund = reserved−billed; net charge = billed. billed+refund==reserved holds open+grants. Caveat-ceiling backstop claim ACCURATE: max_billable=min(credit_window,max_calls) via replenish_clamped + cumulative_ceiling_reached; hard bill-cap regardless of escrow. reverse can't double/skip (reverse_spend saturating; apply sync under session RwLock, Ok→no reverse / Err→one reverse). Item 3 escrow DOC-comment ACCURATE (names caveat ceiling not billed<=reserved).

RESOLVED @66b3fe424 (re-verified): prior HIGH FIXED, no new conservation hole. reserve_stream_grant_escrow now bumps record.reserved_escrow += cost×grant ATOMICALLY with budget debit; combinator swapped keep_compensating→commit_class_s_compensating. VERIFIED: (1) stream_reservations ∈ ClassSState (state.rs:937), captured by snapshot()/restore() (1013/1034) → the compensating combinator SNAPSHOT-RESTORES the record bump on persist-fail; budget_tracker ∈ governance TOP-LEVEL (state.rs:1299, Class-C, NOT governance.class_s) → reversed by the Class-C compensate closure. Two legs genuinely DISJOINT, both undone together. keep_compensating GENUINELY could not un-bump (compensate gets ClassCMut, structurally can't reach class_s.stream_reservations). NO new partial-failure window: compensate runs ONLY on persist-FAIL where durable==pre-grant already; the compensate async block has no real await suspension. (2) reverse_stream_grant_escrow: credit+un-bump in ONE commit_class_s_keep f closure, no await between, ONE persist unit → crash can NEVER split them durably; record==budget always consistent → no double-refund. saturating → double-reverse no-op. (3) Durability chain CONFIRMED: reserved_escrow serialized into ContextSnapshot (state.rs:1190) via build_snapshot_from_state→stream_reservations_snapshot (messaging_helpers.rs:2788), restored lifecycle_helpers.rs:3128; persist_state_fail_closed uses that builder. reconcile (outlets_helpers.rs:1858) refunds FULL reserved_escrow + mem::take clears. Conservation: clean=−billed, crash=0 (refund==debit==open+Σaccepted_grants), apply-reject symmetric. (4) crash test grant_then_hard_crash asserts total_spent 90→0, FAILS pre-fix (not vacuous). (5) record-create: no race (serial actor, reconcile is restore-time); persist-fail on 1st grant removes just-created record via full-assignment restore. 5/5 grant tests + 2140 runtime lib tests PASS.
LOW findings (none money-breaking): (a) settle_outlet_stream:1578-1580 comment calls `reserved` "the open-time escrow HOLD" — WRONG, it's open+grants (billed+refund, struct doc invoke.rs:1575 + both build sites dispatch.rs:1937/3310 correct). This is the very field record_persisted (1718) gates record-removal on; the wrong comment nearly caused a false-positive double-refund finding. FIX. (b) commit_class_s_compensating still #[allow(dead_code)] (class_s.rs:2913) despite real caller now — stale, remove. (c) bridge outlet_stream.rs reverse-Err comment says Err "means no live actor" — also covers live-actor PersistenceFailed (both safe, reason incomplete).
--- ORIGINAL HIGH (now fixed) ---
- HIGH: grant-time escrow debit is durable (commit_class_s_keep_compensating→persist_state_fail_closed on budget_tracker) BUT the crash-recovery `stream_reservations` record.reserved_escrow is written ONCE at open (outlets_helpers.rs ~1182) and NEVER updated on grant. StreamEscrow ledger (SharedSessionState) that DOES track grant top-ups is ephemeral (dies with pump on hard crash). reconcile_stream_reservations (outlets_helpers.rs:1758) refunds only record.reserved_escrow = open-time hold. → HARD CRASH after a grant before clean settle: budget debited (open+grants) but reconcile refunds only open portion, no settle fires → invoker PERMANENTLY OVER-CHARGED by cumulative grant top-ups. Breaks the sweep's stated "invoker is never over-charged" invariant. NEW regression: pre-revision grant passed reserved_top_up=Amount(0) (no debit, so no crash-strand; but under-billing). Fix: grant reserve must bump the durable record.reserved_escrow (or persist a grant delta) so reconcile refunds open+grants.
- LOW: NEW warn! comment in grant_credit_impl ("debited hold is reconciled at stream close by the settlement/reconcile sweep") is INACCURATE — no sweep covers grant top-ups. Reverse-failure path is actually safe for a DIFFERENT reason (reverse_spend is KEEP'd in-memory by commit_class_s_keep regardless of persist; only truly fails if context/actor gone → budget also gone → moot). Comment misattributes the safety.
- LOW/info: concurrent grant_credit on same handle — distinct seqs via atomic, but apply order not guaranteed = seq order; a reordered lower-seq apply spuriously CreditReplay-rejects (then reverses its reserve, conservation intact). Liveness quirk, GIL+single-caller makes rare.

Tests: 16 runtime grant tests + 3 bridge outlet_stream (incl live-poll grant control-plane: non-invoker PERM-3001, self-grant sig never rejected) PASS. grant_reserve_then_reverse_conserves_budget only covers in-memory net-zero, NOT the crash-record asymmetry (HIGH untested).

Feature flags: scp-ffi tests need `scp-ffi/allow_in_memory_custody,scp-ffi/testing` (scp-ffi has NO direct scp-runtime/testing). runtime-only: `scp-runtime/testing`.
