# Crypto Agent Memory — SCP Protocol Core

Index only. One line per entry; detail lives in the topic file.

## Orientation

- [Key files index](key-files-index.md) — where the crypto lives: Merkle, envelopes, sender keys, UCAN, DID/relay, FFI bridges, platform adapters.
- [Spec-level crypto audit (2026-03-05)](spec-audit-findings.md) — 9 CRITICAL / 11 HIGH / 8 MED / 5 LOW across specs 09, 03, 07. Root pattern: only the migration proof uses length prefixes + domain sep; 8+ other hash constructions don't. BroadcastEnvelope is the ONLY signature with no domain separator. "Ed25519_keygen(seed)" undefined ⇒ cross-platform breakage. Sender-key HPKE, nonce gen, wire format, encrypted routing_id, participation-key derivation, and canonical serialization for signed structures are all MISSING.

## Standing cross-cutting findings

- [Merkle + canonical-hash weaknesses](event-log-merkle-and-canonical-hash.md) — RFC 6962 leaf/interior separation is consistent; but no domain separators or length prefixes on the event/claim/attestation/checkpoint hashes, `hash_pair()` copied in 3 files, and TWO incompatible attestation canonical forms (claiming.rs BE+prehash vs trust/attestation.rs LE+raw). Also the §8.4 AppBound/AppUnbound leaf encoding (SOUND).
- [Absence-proof decision, costed](absence-proof-decision-rescope.md) — verified @8b7cbe7f8: no `verify_absence` exists, `AbsenceProof` has no serde (no wire type), bridges drop the paths, `verified` is always-true, and PyO3+UniFFI prove against a near-empty bridge-local tree. Committing a sorted root does NOT reach soundness alone (the R_s↔R_a binding gap). Recommend C now + D (exhaustive-leaf-set verifier) permanently.
- [Cross-platform pseudonym parity + platform adapters](platform-adapters-and-pseudonym-parity.md) — Rust / Kotlin-BC / Swift-CryptoKit each derive DIFFERENT pseudonyms (Kotlin HMACs the PUBLIC key; CryptoKit treats input as a clamped scalar, not a seed). Unify per SCP-214. Includes the Apple PR #86 and Android PR #118 audits and the randomness policy (OsRng in prod).
- [ADR-039 persona binding + signature verification](adr039-persona-binding-and-signatures.md) — `signing_key_id` IS inside the signed InnerEnvelope preimage (persona un-flippable). SOUND. Open gap: the only prod KeyResolver returns None for every (DID,kid), so #agent is non-functional outside in-crate tests.
- [UCAN + FFI bridge audit](ucan-and-ffi-bridge-audit.md) — CRITICAL: UniFFI `ucan_revoke` revokes by token_id, not the content-hash CID ⇒ revocations are no-ops on mobile/desktop. HIGH: WASM duplicates `UcanPayload` with no field-order enforcement. Envelope/AES-GCM/mint/nonce-tracker all SOUND.
- [Pruning, economy, credentials](pruning-economy-credentials.md) — SCP-126 pruning verifies neither the checkpoint root nor its signature; SCP-157 pricing is integer-only and fail-closed; SCP-162 credential storage notes.
- [Phase 0/1 readiness + white paper](phase01-readiness-and-whitepaper.md) — sender-key MessagePack migration SOUND; PRE-EXISTING HIGH: HPKE `info` carries only the domain prefix, not `context_id||sender_did||epoch`, and AES-GCM has no AAD.

- [CRYPTO-22 four-key binding verdict](crypto22-four-key-binding-verdict.md) — "bind only signature_key" REFUTED: the transitive LeafNodeTBS/KeyPackageTBS chain roots in the *ephemeral leaf key* (RFC 9420 §16.5 / spec §9.12 threat). SCP's `self_update` keeps `signature_key` and rotates `encryption_key` every epoch ⇒ a 1-key attestation never re-vouches the live decryption key. Also: §9.7.1 check 11 unsatisfiable on Update leaves (no `Lifetime`); §23.10 stale; ADR-057 shim Decision-1 "order-independent" is false.

## Reviews by workstream

- [HPKE RFC 9180 conformance](hpke-rfc9180-conformance.md) — custom-ECIES finding FIXED: one hand-rolled RFC 9180 core in scp-protocol/src/crypto/hpke.rs (A.1 KAT + hpke-rs oracle), custody Decap variant, 60→48 wire, all 5 paths conformed.
- [SCP-1717 pre-rotation custody](scp1717-prerotation-custody.md) — round-10 SOUND. Step ordering, rotated_at bounds, 4-bridge commitment invariant. Open: `verify_migration` doesn't bind old_document→old_did (STRONG-bypass if a caller skips resolution); CallbackKeyCustody import Unsupported; step-7 no recovery handle.
- [ADR-051 pre-rotation substrate](adr-051-prerotation-substrate.md) — substrate isolation (Proposed); separate-provider model sound per spec §9.7.4.1 §3.
- [Bridge relay auth + CRYPTO-22 S4](bridge-relay-auth-and-crypto22-s4.md) — PR #255 63-byte fixed BRIDGE_REGISTER preimage SOUND; CRYPTO-22 S4 Add-only attestation verifier seam SOUND. PRE-EXISTING: migration proof hash has `old_did||new_did` concat ambiguity.
- [RELAYRES-003 DID-record validation](relayres003-did-record-validation.md) — relay-side `classify_did_record_frame` + `DidSlotRegistry` (da1f610ae); binding(2nd-preimage) → BEP44 sig cheapest-first, blob_id=SHA-256 idempotency, key confusion closed. SOUND.
- [RELAYRES-004 WRITE path + re-seed](relayres004-write-path-reseed.md) — @5b89baada SOUND: publish returns the signed `RepublishEntry` (DHT read-back deleted), live watch slots, tier-change re-seed, one routing-ID family. 5 LOW/INFO residuals.
- [Event-log unification Phase 2](eventlog-unification-phase2.md) — ADR-011 runtime onto the RFC 6962 substrate; export-root migration (truncation forgery CLOSED) + §9.9.3 equivocation dedup. Round-4 APPROVE.
- [PR #2235 §8.4 AppBound/AppUnbound](pr2235-appbound-appunbound.md) — tags 74/75; positional-rmp encoding sound; BUT node-local appends (ADR-011 convergence violation), unsigned leaves, caller-supplied timestamp+actor_did, actor_did trim divergence, non-strict Ed25519 verify, fabricated §8.4.1 limits.
- [0xFF02 genesis creator sig (#2027)](ff02-genesis-creator-sig.md) — 0xFF02 commits `creator_did` as an UNSIGNED string (the leaf uses a fresh key, not #active); embedding a creator #active-key signature is feasible and strictly better than a separate GenesisAttestation artifact.
- [#1900 PR-2b WASM engine-adoption BLOCKER](pr2b-wasm-engine-adoption-blocker.md) — "transient engine + replay stored votes" NOT implementable: PR-2a added keyless ingest_approve/reject only, no keyless propose/seed. Unblock = `TrustedVoteIngest::ingest_proposal` on the shared engine. Items A/B/C/D/H unblocked; E/F/G blocked.
- [Trust UCAN classification](trust-ucan-classification.md) — TS `evaluateTrust` UCAN error → CapabilityValidation classification; fail-closed; faithful port of the reviewed Python layer.
- [Nonce dedup / saga removal](nonce-dedup-saga-removal.md) — NonceDedup configurable-TTL API removed in lockstep with the cross-context saga deletion; no replay regression; Python parity confirmed.

## Standalone verdicts (no topic file needed)

- **Signed context-export (export_import.rs, 16a2cd42b) APPROVE** — removing the unsigned `ContextExport.merkle_root` field + the step-6 self-check is strictly stronger. Signed preimage = `SHA-256("SCP-CONTEXT-EXPORT-V1:" || scope.tag_byte() || JCS(snapshot))`, and `snapshot.event_log_merkle_root` is INSIDE the signed JCS. Step 5 (recompute the RFC 6962 root over `event_log_data` via `recompute_event_log_root`, `ct_eq` vs the signed root) is the sole authoritative binding; step 6 compared two attacker-visible copies and gated nothing. Prefix truncation is rejected in `append_unsigned_event` (seq/prev_hash); suffix/middle/reorder/substitute/forge → root mismatch. Empty log signs `[0u8;32]`, no all-zeros bypass. The removed field was never in the hash → no 2nd-preimage or domain-sep regression.
- **PseudonymAnnounced removal (f438acf0f) APPROVE** — taxonomy 76→75; tag 59 RETIRED as a gap (no renumber) so every other `event_type_tag` is stable ⇒ §25 KAT 32/33 root `39e50b87` byte-unchanged (verified). `EventType` serializes by NAME-string via rmp_serde (no int repr), so removal cannot shift other leaves. Convergence RESTORED: `deliver_plaintext_or_announcement` returns `None` on all 3 arms; the Some-append channel is dead in prod. The 3 non-convergent classes have no `EventType` variant ⇒ un-appendable at the type level. **GOTCHA**: bare `-p scp-event-log` FAILS 116 tests (hex did:key is gated behind the scp-primitives `testing` feature, identity.rs:118) — run with `--features testing`.
