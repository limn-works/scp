---
name: outlet-stream-chunk3-settlement-wiring
description: Chunk-3 outlet-streaming settlement/billing actor wiring crypto review (355f50a8c~1..c166ba953) — receipt↔manifest binding gap, release math sound, generation guard sound, snapshot fallback sound
metadata:
  type: project
---

# Outlet-stream chunk-3 settlement wiring (worktree scp-wt-streaming, range 355f50a8c~1..c166ba953)

Reviewed ONLY the chunk-3 ACTOR WIRING of settlement/billing (primitives — RFC6962 manifest, SCP-OUTLET-CHUNK-SIG-V1, credit grants — pre-reviewed SOUND, out of scope).

**Verdict: settlement integrity SOUND modulo ONE MEDIUM manifest↔receipt binding gap (deferred verify_chunks_billed).**

## RE-REVIEW of fix delta c166ba953..941c81d97 (Fix-B anchoring + Fix-D crash recovery) — VERDICT: SOUND, prior MED RESOLVED.
- **Fix-B (dispatch.rs:3188,3281-3306,3345 `anchor_settlement_receipt_to_manifest`)** — pump now feeds StreamSettlement `billed_count = manifest_reference = reference_chunks_billed(summary.manifest, cancel_ack_seq)` (SAME value the audit event anchors to; receiver-acked cursor caps billing), `billed_amount = min(cost×manifest_reference, reserved)` overflow→0 fail-closed, `refund = reserved − billed` sat. Ledger self-count (summary.billed_count/billed_amount) NO LONGER reaches sink. billed+refund==reserved holds all directions. Honest path byte-identical to ledger split. Receipt CANNOT out-run signed manifest (sink re-caps at cost×billed_count=cost×manifest_reference). reference_chunks_billed only COUNTS Data leaves ≤ cancel_ack_seq — anchors to operator's own signed emission record + receiver-acked cursor (the non-repudiation property); verify_chunks_billed is recorded==reference tautology (defensive), not a sig-verify.
- **Q2 Fix-D mismatch path (outlets_helpers.rs:1402-1406,1424-1475)** — inherits anchored amount: destructures settlement.billed_amount (already Fix-B-anchored) then re-caps `min(billed_amount, cost×billed_count)` with billed_count=manifest_reference → bills SAME anchored value, NOT re-derived ledger count. No-actor fallback (supervisor.rs:10774-10832) also uses settlement.billed_amount (anchored). CONFIRMED.
- **Q3 recovery sweep (outlets_helpers.rs:1638-1679)** — releases exactly record.reserved_escrow + record.amount_cumulative_reserved (open-time values, saturating reverse_spend/release) → NEVER releases MORE than reserved. Literal property SOUND. Sweep NEVER captures a receipt. Clean settle clears record in-same-persist (double-release guard); mutually exclusive with sweep. INFO residual: KEEP-then-crash AFTER clean-settle-capture window → durable record survives, restore sweep releases FULL reserved incl. receipt-billed portion → §7.3.8 cumulative cap regenerated for billed portion (bounded by reservation, conservative/invoker-favoring, pre-existing KEEP+external-capture non-atomicity, NOT introduced by fix). Pure hard-crash (pump dies w/ process, no settle fires) → pre-crash rendered service UN-billed (operator eats it) = safe direction, no manifest→no bill.
- **Q4 double-capture** — capture is single-drive: pump fires settle EXACTLY once (pump_exited+single spawn) → routes to EXACTLY ONE of {no-actor fallback | gen-mismatch CapturedWithoutMutation | clean Settled}, all capture once. reconcile sweep NEVER captures. No second settle source. Record key = hex::encode(request_id) consistent across persist(handlers/outlets.rs:81)/remove(outlets_helpers.rs:1539)/sweep. record.generation diagnostic-only (sweep ignores). NO double-bill.
- Fix-D record persist (dispatch.rs:2340-2385) awaited BEFORE pump spawn, gated on reserved_escrow>0||amount_cumulative_reserved>0 (zero-cost skip), best-effort warn-on-fail (documented net-loss-only-on-crash-during-window). origin_admission operator-scoped tracker plumbed through gate/release/pump-close in lock-step (per-context admission first, origin leaf innermost — no cycle) — orthogonal to billing, sound.

- **MEDIUM — receipt billed_count/billed_amount NOT anchored to signed manifest.** `settle_outlet_stream` (outlets_helpers.rs:1359) and the no-actor fallback (supervisor.rs:10716) capture the receipt from `StreamSettlement.billed_amount`/`billed_count`, which the pump fills from `guard.escrow.settle_at_close()` running counters (dispatch.rs:3038, sink fired 3168). The EVENT path reconciles pump-recorded vs `reference_chunks_billed(manifest, cancel_ack_seq)` and gates on `verify_chunks_billed` (dispatch.rs:3098-3146); the SETTLEMENT path does NOT — fires unconditionally with pump values even when the event was DROPPED for a malformed manifest. `StreamSettlement` carries NO manifest / NO cancel_ack_seq, so chunk-3 settle CANNOT re-derive/verify — fix locus is the pump→sink boundary (dispatch.rs, out of chunk-3 file scope): thread reference count into StreamSettlement or gate settle on verify_chunks_billed. Honest single-node = self-mismatch only (no external adversary controls billed_count; both derive from same operator emission), so no live exploit — but the receipt is the money-moving non-repudiation artifact and is the ONE not checked against the signed manifest while the lower-stakes audit event is. Deferral acceptable ONLY if tracked + pump reconciles before firing.
- **LOW/defense-in-depth** — settle trusts `billed_amount == billed_count × cost_per_chunk` (receipt uses billed_amount; §7.3.8 counter debit uses billed_count×cost). Never asserted. Equal in honest pump.
- **Concern #2 release math SOUND:** `u64::from(billed_count).checked_mul(cost).map_or(0, |b| amount_cumulative_reserved.saturating_sub(b))` (1395). Overflow→release 0 (fails closed, over-charges counter never under). Result ≤ amount_cumulative_reserved (=what open incremented) so can't regenerate spent cap beyond reservation. `CaveatCounters::release` (caveat_counters.rs:207) saturating_sub. Single-fire (pump_exited + generation guard + once-per-command), KEEP-on-persist-fail applies mutation once.
- **Concern #3 generation guard SOUND:** `spawn_generation.fetch_add(1)+1` per spawn (supervisor.rs:4213), single supervisor-wide monotonic counter, unique per incarnation, ≥1 for live (default 0 never matches). Confused-deputy threat is in-process only (pump dies on process restart), so local counter is right primitive — not cryptographic, doesn't need to be. Handler: Dropped→ok() no persist, Settled→ok_mutated() persists (handlers/outlets.rs:344).
- **Concern #4 snapshot fallback SOUND:** economic_policy_snapshot = process-internal clone of live policy at open (reserve_outlet_stream_economy:1148), never serialized/wire/attacker-supplied. Staleness is deliberate H8 (price-at-open); live preferred when context up. No forgery vector.
