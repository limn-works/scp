---
name: project-adr049-phase2b-watchdog
description: ADR-049 Phase 2B per-actor watchdog/respawn/poison — design as built + two non-obvious gotchas (respawn-Active-transition bug, dead-handle-lingering test race)
metadata:
  type: project
---

Phase 2B of ADR-049 actor-per-context: per-actor watchdog + 3-crashes-in-60s respawn budget + poison. Branch `feat/actor-2b-watchdog-respawn` off main (HEAD 35f694595). 4 commits.

**Why:** Pre-change `spawn_actor_with_state` dropped the actor JoinHandle (supervisor.rs), so a panic was silently swallowed and the context wedged (every later send → ActorBusy). ADR-049 §10 mandates a watchdog that detects panics, logs payload-free (key-material safety), enforces a budget, and respawns from snapshot.

**How to apply (design as built):**
- `spawn_actor_with_state(self: &Arc<Self>)` now derives `owning_did` (local_dids min, else `DID(ctx_id)` seed) and delegates to `spawn_actor_with_watchdog`, which KEEPS the JoinHandle and starts the watchdog. Every spawn path (create/import/restore/respawn) is watched uniformly.
- `spawn_actor_watchdog_task` is a FREE fn (not inline `tokio::spawn`) — REQUIRED to break the self-referential async opaque-type cycle (`spawn_actor_with_watchdog → actor_watchdog → respawn_from_snapshot → restore_context → spawn_actor_with_state`). Inline spawn = "fetching the hidden types of an opaque inside of the defining scope is not supported". Don't try `Box::pin`/`dyn+Send` at the call site — only the free-fn boundary works.
- `actor_watchdog`: Ok / non-panic JoinError = no crash, no respawn. Panic → record crash, log PAYLOAD-FREE (`is_panic()` bool ONLY; never `into_panic()`/`payload()`/`{:?}` on JoinError), then poison-and-despawn (budget) or respawn. `panic_location="unknown"` hardcoded — a global last-location store would be racy across threads/Supervisors AND a mutable global (forbidden); ADR-049 §10 floor accepts "unknown".
- Poison surfacing: `lookup_miss_error(ctx_id, msg)` → ContextPoisoned vs ContextNotRegistered, wired into messaging/governance/ttl_close `ok_or_else` + lifecycle direct arms.
- CrashWindow pure methods take `now_ms` PARAM (no clock inside) → unit-testable without clock.

**GOTCHA 1 (real bug found by test):** `respawn_from_snapshot` must `handle.transition_to(Active)` BEFORE calling `restore_context` — `restore_context` does NOT transition the handle itself (the RestoreContext direct dispatch arm transitions externally). Without it the respawned context is stuck in `Creating` (responsive but never Active).

**GOTCHA 2 (test race):** a just-crashed actor's handle LINGERS in the `actors` registry until the watchdog's `respawn_from_snapshot` despawns it. So `wait_until(lookup().is_some())` matches the DEAD handle → next panic lands on a closed mailbox and never crashes. Watchdog tests MUST wait on a RESPONSIVE actor (`read_context_state == Active` via mailbox), not a bare registry lookup. See [[feedback-read-tool-stale-verify-with-awk]] for the general "verify behavior not surface" theme.

Test seam: `LifecycleControlCommand::TestInducePanic` is `#[cfg(feature="testing")]` and handled in `actor/mod.rs` `dispatch_state` (NOT in `handlers/*.rs`, so the new `scripts/check-handler-no-panic.sh` ban stays green). The handler + skeleton matches get gated no-op arms for exhaustiveness.

Panic-redaction test (the security regression test) captures tracing via a hand-rolled `tracing::Subscriber` set as PROCESS-GLOBAL default once (`std::sync::Once`) — the watchdog logs on a tokio worker thread, so a thread-local `set_default` would miss it. No `tracing-subscriber` dep added.

Gates all green: CI-exact clippy (0 warnings workspace), check-error-codes (SCP-CTX-2134/2135), check-handler-no-panic (new, wired into ci.yml next to deleted-primitives), no-mutable-globals, block-in-place, deleted-primitives. scp-runtime+scp-protocol nextest 4934/4934.

**REMEDIATION ROUND (5 commits atop c17331886, branch feat/actor-2b-watchdog-respawn; full workspace nextest 9161/9161):** 15 review fixes. Key non-obvious decisions:
- **read_context_state is the poison index, NOT the handle.** Once the watchdog despawns a poisoned actor, the handle is dropped — you canNOT drive `Active→Poisoned` through the dead mailbox. The DURABLE poison signal is the sticky `crash_windows` flag. `read_context_state` was rewired: on a `lookup` MISS, check `is_context_poisoned` → return `Some(Poisoned)`; else `None`. A live actor masks the flag (mailbox read wins). Don't attempt a mailbox transition in the poison branch — it's dead weight.
- **Two reap semantics, two methods.** `reap_crash_window` (clean despawn: remove only if `!is_poisoned`, via `DashMap::remove_if`, so poison survives shutdown for reporting) vs `reset_crash_window` (explicit (re)create/import/standing-recreate: UNCONDITIONAL `remove`, including poison — a deliberate recreate is a fresh budget). Conflating them re-poisons a recreated standing-pair id on its first panic. reset wired at the 3 bootstrap arms UNDER bootstrap_spawn_lock; reap at shutdown_all_contexts. close/expire do NOT despawn (actor stays registered post-Close) so no reap needed there.
- **Respawn must NOT reap/reset** — the running crash count must accumulate across respawns or a reliably-crashing snapshot never poisons.
- **Active-only precondition returns ContextClosed** and the watchdog logs it at info (expected dormancy, not a respawn failure). Snapshot `.state` comes from `ctx.handle.try_read_state()` in `snapshot_context`; test drives handle Active→Closing then snapshots.
- **owning_did is NOT a mis-scoping vector** (security finding CONFIRMED sound, code unchanged + documented + 2-DID test). `build_actor_deps(owning_did)` uses it ONLY for `key_package_store_for`; crypto/transport/event_log/key_resolver are shared supervisor providers; `local_dids` is `local_dids_shared()` (full set). Broadcast author-gate keys off caller `author_did` in `publish_broadcast_two_phase`, NOT owning_did. MLS rehydrated from snapshot, not re-derived.
- **dispatch_queries_direct had to become `&self`** (was `fn`, no self) to call `self.lookup_miss_error`. Tool-economy settle has TWO sites (the `reply_tools_not_registered`-adjacent settle + the `settle_tool_economy_via_actor` pre-check); both route through lookup_miss_error keeping the rich SCP-TOOL-6089 not-registered message; escrow-void side-effect stays BEFORE the error build.
- **check-handler-no-panic.sh dispatch-hub scan** tracks `#[cfg(feature="testing")]`-gated regions by brace depth (attribute marks NEXT item; floor = depth before item opens; region ends when depth returns to floor) — skips banned macros inside the gate, flags everything else. Self-tested with a planted production panic.
- **clippy gotchas this round:** `PyO3`/`UniFFI` need backticks (doc_markdown) but `NAPI` is in the dict; CrashWindow marker methods must be `const fn`; clock-absent branch must be `map_or_else` not `match` (option_if_let_else) even with a side-effecting None arm; error_codes.rs const docs hit `too_long_first_doc_paragraph` (need a short first line).
- WASM panic redaction: read ONLY `info.location()` for file:line + CONSTANT message; never `info.to_string()`.
- ADR-049 §10 rewritten to as-built (option a: watchdog panic-free by construction, no JoinSet health-sweep backstop).

**ROUND-2 REVIEW REMEDIATION (5 commits atop 94d09ea99; full nextest 9168/9168):** items A–G.
- **A (dead CI gate):** `scripts/check-handler-no-panic.sh` awk scanners detected `/* */` BEFORE stripping `//` tails, so the `//` comment at `actor/mod.rs:589` containing literal `handlers/*.rs` set `in_block=1` permanently → swallowed the rest of `dispatch_state` → a production `panic!` planted after 589 vacuously PASSED. Fix: move `//`-tail strip ABOVE block-comment detection in BOTH `scan_file` and `scan_dispatch_hub`; add SCANNED-count vacuity guard (hub < 100 lines = fail) + a self-test that plants panics in synthetic fixtures. GOTCHA: awk programs are inside shell `'...'` single quotes — NO apostrophes in awk-embedded comments (`gate's`/`file's` break the shell with `(` syntax errors).
- **B (security rollback) — REVIEW PREMISE WAS MOSTLY WRONG.** Verified by reading code: member removal (`execute_remove_member` gh:1081), capability/access/ceiling revocation (`execute_revoke` gh:769, `apply_pending_ceiling_modification` gh:401), role demotion — ALL already call `persist_state_best_effort` SYNCHRONOUSLY inside their leaf helpers BEFORE returning (hence before the handler reply). ADR-049 §9 already MANDATES this (downward-auth transitions sync-persist). Lifecycle close→Closing ALSO sync-persisted (`close_context_with_key` lifecycle_helpers gh:568). The ONE real defect: `restore_context` (lifecycle_helpers gh:~1897) restored crypto via BARE `restore_crypto_state`, bypassing the §23.17 floor guard → a respawn from a coalesce-lagged snapshot could regress a per-sender replay floor. FIX: route through `restore_crypto_state_with_floor_guard` (captures LIVE floors — still in the provider, despawn only drops mailbox/handle — then rejects regression + max-merges). Negative tests added anyway per CLAUDE.md.
- **C:** wrap respawn `restore_context` in `timeout(Self::LIFECYCLE_TIMEOUT)` (assoc const, dedup w/ dispatch arm); on elapse `record_respawn_failure` + TransportTimeout. Extracted `respawn_rebuild_and_restore` tail (too_many_lines). NOTE: C elapsed-path NOT unit-tested — `deps.crypto` is concrete `Arc<MlsCryptoProvider>` (no trait seam to inject a hang); happy path covered by respawn tests; documented as residual.
- **D:** `standing_context` emits distinct `tracing::warn!("poisoned standing context auto-revived on re-contact")` before the unconditional `reset_crash_window` when the cleared window was poisoned.
- **F:** `record_respawn_failure` mirrors `actor_watchdog`'s clock-absent `map_or_else` warn (was silent `map_or(0,..)`).
- **G:** new `CrashWindow.respawning` transient marker set before despawn in `respawn_from_snapshot`, cleared on EVERY exit (failure via record_respawn_failure, success, terminal-skip). `lookup_miss_error` returns retryable `ActorCrashed` while respawning. GOTCHA: `mark_respawning` creates a `crash_windows` entry even for clean terminal-skip → must `remove_if(is_empty_except_respawning)` to preserve the "no window for clean terminal context" invariant (`respawn_skips_terminal_snapshot` asserts it).
- **E:** poison NOT pushed to per-FFI-handle cached `Mutex<ContextState>` — surfaced via SCP-CTX-2134/2135 error code on next op. DON'T make `state()` a live read (kills cheap-cache perf). ADR-049 §10 boundary subsection (mirrors §12a webhook deferral) + PyO3 docstring + Swift: `Context.swift` map extracted to `mapStateString` (SwiftLint cyclomatic_complexity ≤10 — adding cases to an already-busy init() trips it), fail-safe default `.poisoned` not `.active`; hand-add `Poisoned` case 8 to generated `ScpBindings.swift` ContextState enum+converter (CI regenerates via uniffi-bindgen, no diff-check gate, hand-edit matches). Rust enum order: Creating1..Tombstoned7, Poisoned8.
