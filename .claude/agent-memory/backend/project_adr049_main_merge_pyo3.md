---
name: project-adr049-main-merge-pyo3
description: ADR-049 origin/main→actor MERGE, bridge conflict resolution. PyO3 GREEN + NAPI GREEN (cargo check -p scp-ffi-napi = 0 errors/warnings). Mailbox-dispatch MAP (19 ops) for the remaining UniFFI agent. Key NAPI gotchas: free-fn→_on(bi) shape shift, custody.as_ref() not &custody.0, ProtocolRepoVariant::InMemory→Arc<BridgeInMemoryRepo> fix in scp-ffi-common.
metadata:
  type: project
---

# ADR-049 origin/main → actor-per-context MERGE — PyO3 bridge resolution

Active MERGE (`MERGE_HEAD` = a94b742376...) in worktree `.claude/worktrees/actor-per-context/`. Do NOT commit/abort — multi-agent (PyO3 / NAPI / UniFFI resolved separately).

## MERGE COMPILES GREEN (this session) — workspace `cargo check --all-targets` = 0 errors
UniFFI `bridge.rs` (31 errs) + scp-ffi-common `bridge_instance.rs` (10 errs) type-completeness DONE; `git add`'d, no commit/abort, main checkout untouched.
- **bridge.rs 31 ops re-pointed** to mailbox dispatch (in-file GREEN template = `governance_execute` @8919 + `finalize_close` TtlCloseCommand::FinalizeClose @9378). `bi.context_manager_or_error()?`/`context_manager_expect()?` accessors are FINE (both already return `&Arc<Supervisor>`) — only the BODY method-calls were broken. Ops: gov approve/reject (`VoteOnProposalPayload{...signing_key:SigningKeyBytes::from_signing_key(&sk)}` — `resolve_uniffi_signing_key`→`ed25519_dalek::SigningKey`), ApplyPendingCeilingModification, Create/AddCheckpoint (TrustRecovery; cosig drops `&mut checkpoint`→owned `Box`, uses returned `updated_checkpoint`), Tombstone/MigrationState, RestoreContext (deleted manual load_persisted+handle path → actor loads own snapshot, `params:ContextParams::default()`), broadcast sub/unsub/block/unblock/keyreq + 3 read-only (count/is_sub/admission swallow err→None/false via `if dispatch.is_err(){return}` + `match rx.await`), publish/publish_asset/publish_assets (custody: `dispatch_broadcast_command_with_custody(cmd, cb.as_ref())` for callback / `&imc.0` for in-memory — UniFFI custody = `identity.in_memory_custody.0` tuple, NOT NAPI's `.as_ref()`; `signing_key_handle` made owned `KeyHandle`(Copy); assets-loop clones `did` per-iter), access-key gen/revoke/restore (LifecycleCommand), ttl expiry(ExecuteTtlClose)/extend(ExtendTtl)/reset(ResetTtlTimer fire-and-forget→`let _=rx.await`), export/import (LifecycleCommand).
- **Also fixed (mechanical, recipe-sanctioned, beyond the 2 named files but blocked the `--all-targets` gate):** `is_local_did` now returns `Result<bool>` → `.unwrap_or(false)` (E0308 @13010); `register_local_did` must_use Result → `.map_err(ScpError::from)?`; **`has_context_manager`→`has_supervisor`** at 4 bridge.rs test sites + **3 in `crates/scp-ffi/src/transport.rs` (PyO3)** — PyO3's `cargo check -p scp-ffi` (no `--all-targets`) missed these test-only callsites; `has_supervisor` exists @bridge_instance.rs:799.
- **Deleted dead `NoOp{DidResolver,NonceTracker,RevocationChecker,ProofResolver}` structs** in bridge.rs (~4860-4931) — subscribe_broadcast was the SOLE consumer; gone after dispatch swap (else `-D warnings`).
- **bridge_instance.rs (10 errs):** `CoreFields::with_context_manager(test_context_manager())`→`with_supervisor(test_supervisor())` at 5 callsites (3801/3895/3945/3977/4031). Both targets pre-exist: `with_supervisor(Arc<Supervisor>)` @539, `test_supervisor()->Arc<Supervisor>` @2763.
- **Remaining non-blocking warns (NOT mine):** `runtime.rs` (UniFFI) 3 unused imports (`ShutdownError`/`ShutdownOutcome`/`Duration`/`SqliteStorage`) + scp-runtime `HashMap` — belong to still-unresolved UniFFI runtime.rs resolution; don't fail the `^error` count but WILL fail `-D warnings` clippy until that file is finished.

**Why:** main advanced (per-instance `&self`/`bi` bridge frame, #1543 PR-C storage-at-construction, fs2 advisory locking) while the actor branch replaced `ContextManager` with `Arc<Supervisor>` + actor mailbox dispatch. The merge target = **main's per-instance `&self`/`bi` frame** + **actor branch's `Supervisor` dispatch bodies**.

**How to apply (RECIPE):** per conflicted file, keep main's method frame (per-instance `&self`, `bi`, `supervisor(bi)` not process-global), then re-point `ContextManager::X` / `context_manager(bi)` / `mgr` → `Supervisor` / `supervisor(bi)` / `sup`; `has_context_manager`→`has_supervisor`. Drop obsolete #1491 storage hunks; KEEP `mls_storage` provider-seam (runtime.rs `derive_mls_storage`). `init_context_manager*` runtime.rs fn NAMES are retained (bodies build the Supervisor).

## PyO3 bridge (`crates/scp-ffi/src/` + CLAUDE.md) — COMPLETE
All resolved, zero conflict markers, zero scp-ffi-internal compile errors, `git add`'d:
- `runtime.rs`, `context.rs` (prior agent resolved-on-disk; this session re-pointed the 3 broken broadcast-publish sites + re-staged), `transport.rs`, `mcp.rs`, `server.rs`, `CLAUDE.md`.

**Key gotchas the NAPI/UniFFI agents WILL hit (same divergence):**
- **`publish_broadcast` / `publish_broadcast_content` do NOT exist on Supervisor.** Main called `sup.publish_broadcast(ctx,did,payload,custody,key)` directly — WRONG, won't compile. Actor shape: build `BroadcastCommand::PublishBroadcast{payload:Box<PublishBroadcastPayload{context_id,author_did,payload,signing_key_handle}>, reply:tx}` (or `PublishBroadcastContent`+`PublishBroadcastContentPayload{...content...}`), then `sup.dispatch_broadcast_command_with_custody(cmd, custody.as_ref()).await`, then `rx.await??`. Reply type `PublishBroadcastReply` yields `BroadcastEnvelope` (content/asset paths use it for blob_id; raw-publish path discards it). Cmd types: `use scp_core::context::actor::commands::{BroadcastCommand, PublishBroadcast{,Content}Payload};`. `KeyHandle` is `Copy`; `DID` is `Clone` (clone per-iteration in the batch `publish_assets` loop).
- **CORRECTION (this session, verified by compiler):** the prior claim that broadcast/governance/ttl/lifecycle ops "ARE real direct methods on Supervisor" was WRONG. `Arc<Supervisor>` has NO direct `export_context`/`import_context`/`execute_governance_action`/`tombstone_migrated_context`/`migration_state`/`approve_governance_proposal`/`reject_governance_proposal`/`apply_pending_ceiling_modification`/`create_governance_checkpoint`/`add_checkpoint_cosignature`/`load_persisted_context_state`/`subscribe_broadcast`/`unsubscribe_broadcast`/`block_broadcast_subscriber`/`unblock_broadcast_subscriber`/`handle_broadcast_key_request`/`handle_ttl_expiry`/`propose_ttl_extension`/`reset_ttl_timer` methods (E0599). All 19 are MAILBOX-DISPATCH-ONLY. See the dispatch MAP below.
- Methods that DO still exist directly on `impl Supervisor`: `member_dids` (no—use `QueriesCommand::MemberDids`), `dispatch_query`, `dispatch_governance_command`, `dispatch_broadcast_command`, `dispatch_broadcast_command_with_custody`, `dispatch_lifecycle_command`, `dispatch_ttl_close_command`, `dispatch_trust_recovery_command`, `dispatch_economy_command`, `register_local_did` (returns `Result`, must_use), `event_log_entries` (used by event_log.rs `query_manager_entries`), `restore_context` (exists but unused — RestoreContext dispatch supersedes it).
- **transport.rs:** `init_context_manager_with` now takes `crypto: Arc<MlsCryptoProvider>` (was `Box`) — change `Box::new(MlsCryptoProvider::new(...))` → `Arc::new(...)`. Take main's per-instance method bodies; delete HEAD free-fn duplicates (`py_configure_relay_transport`, `py_transport_add_relay`, `init_supervisor_with`, `init_supervisor_for_test`).
- **server.rs:** `Supervisor::register_local_did` returns `Result<(),ContextError>` (must_use) — use HEAD's `if let Ok(sup)=supervisor(bi) && let Err(e)=rt.block_on(sup.register_local_did(...))` body, NOT main's `rt.block_on(mgr.register_local_did(...))` (would `-D warnings` on unused Result). `resolve_broadcast_key` (in scp-ffi-common, already actor-shaped) takes `&Supervisor` — pass `&supervisor` (clone the `Arc` from `supervisor(&self.bi)`).
- **CLAUDE.md:** keep HEAD storage block (superset: per-instance `with_storage_py` + `mls_storage` consumer + supervisor seam); drop origin/main block.
- Per-fn local `use scp_core::context::actor::commands::{...}` is the in-file convention (not module-top).

## SOLE GATE BLOCKER (NOT PyO3): `crates/scp-platform/src/sqlite/mod.rs`
`cargo check -p scp-ffi` fails ONLY here (unclosed delimiter from 2 unresolved markers @ ~194-490 and ~717-975). scp-ffi lib itself compiles clean up to this upstream dep.
- **Genuine storage-core divergence, NOT mechanical** (storage-foundation task #22/#33 scope). HEAD adds Argon2id **passphrase mode** (`with_passphrase`, `load_or_init_salt`/`atomic_write_salt`/`sync_parent_dir`, salt-sidecar fail-closed, 8+ tests); origin/main adds **fs2 advisory locking** (`lock_file: Mutex<Option<File>>` struct field, `try_lock_exclusive` in `new()`, `close()` method called from bridges' `bridge_specific_shutdown`, lock-release tests). Both features must SURVIVE (union) — both deps wired, both referenced by code, both security-load-bearing (salt fail-closed + advisory-lock fail-closed).
- Correct merge: struct gets `lock_file`; `new()` keeps HEAD pragma path AND acquires origin/main's fs2 lock; `with_passphrase` added AND acquires lock; both test suites coexist. STOPPED here (not PyO3, high regression risk to hand-merge security paths).
- **Already resolved this session (safe, mechanical):** `crates/scp-platform/Cargo.toml` `sqlite` feature → union `["dep:rusqlite","dep:hex","dep:argon2","dep:rand","dep:fs2"]` (all three confirmed used); `sqlite/mod.rs` import hunk @25 → both `use fs2::FileExt;` + `use rand::RngCore;`. These are NOT git-add'd (UU left for the storage agent to finish the file).

## PyO3 GREEN — final 19-op mailbox dispatch MAP (this session)

`cargo check -p scp-ffi --features scp-ffi/allow_in_memory_custody,scp-core/testing` = **0 errors** (warnings only: the wasm-profile note + 4 pre-existing). The 22 type errors the prior agent left (naive `ContextManager::X`→`sup.X` renames against the actor Supervisor, which has no such direct methods) are resolved by re-pointing each to the actor mailbox. Pattern per op: keep main's per-instance `&self`/`bi`/`sup = supervisor(bi)?.clone()` frame + FFI orchestration, replace the body with `(tx,rx)=oneshot::channel(); cmd=<Command>{...,reply:tx}; sup.dispatch_<X>_command(cmd).await.map_err(..)?; rx.await.map_err("shim reply dropped")?.map_err(..)?`. Per-fn local `use scp_core::context::actor::commands::{...}` inside the `block_on` closure.

| FFI method (main) | Command + dispatch fn | Reply / notes |
|---|---|---|
| `context_export` | `QueriesCommand::MemberDids`(via `dispatch_query`)+`LifecycleCommand::ExportContext`{context_id,exporter_did,reply}→`dispatch_lifecycle_command` | exporter_did from MemberDids query first; then serialize_export |
| `context_import` | `LifecycleCommand::ImportContext`{export:Box,reply}→`dispatch_lifecycle_command` | keep main's `init_context_manager[_for_test]` precondition |
| `governance_execute` | `GovernanceCommand::ExecuteGovernanceAction`{payload:Box<ExecuteGovernanceActionPayload{context_id,proposal}>,reply}→`dispatch_governance_command` | role-state resync via `QueriesCommand::GetRoleState`(dispatch_query)→`with_ffi_state(bi,...)`; result match uses `scp_core::context::state::GovernanceActionResult` (NOT `::manager::` — that module is deleted) |
| `tombstone_migrated_context` | `GovernanceCommand::TombstoneMigratedContext`{context_id,reply}→`dispatch_governance_command` | |
| `migration_state` | `GovernanceCommand::MigrationState`{context_id,reply}→`dispatch_governance_command` | reply is `Result<Option<MigrationState>>` (main's old fn returned bare Option) |
| `governance_approve` | `GovernanceCommand::ApproveGovernanceProposal`{payload:Box<VoteOnProposalPayload{context_id,proposal_id,voter_did,signing_key:SigningKeyBytes::from_signing_key(&sk)}>,reply}→`dispatch_governance_command` | then `sync_role_state_from_manager(bi,..)` |
| `governance_reject` | `GovernanceCommand::RejectGovernanceProposal`{payload:Box<VoteOnProposalPayload>,reply}→`dispatch_governance_command` | |
| `apply_pending_ceiling_modification` | `GovernanceCommand::ApplyPendingCeilingModification`{context_id,current_timestamp,reply}→`dispatch_governance_command` | reply `bool` |
| `create_governance_checkpoint` | `TrustRecoveryCommand::CreateGovernanceCheckpoint`{payload:Box<CreateGovernanceCheckpointPayload{...8 fields...}>,reply}→`dispatch_trust_recovery_command` | |
| `add_checkpoint_cosignature` | `TrustRecoveryCommand::AddCheckpointCosignature`{context_id,checkpoint:Box,cosignature:Box,reply}→`dispatch_trust_recovery_command` | reply `(updated_checkpoint,status)` — use `updated_checkpoint` (drop main's `&mut checkpoint`; make it immutable) |
| `restore_context` (was `load_persisted_context_state`) | `LifecycleCommand::RestoreContext`{payload:Box<RestoreContextPayload{context_id,params:ContextParams::default()}>,reply}→`dispatch_lifecycle_command` | REPLACES main's manual load_persisted+ContextHandle::new+transition_to+restore_context block; handler loads its own snapshot |
| `broadcast_subscribe` | `BroadcastCommand::SubscribeBroadcast`{payload:Box<SubscribeBroadcastPayload{context_id,subscriber_did,ucan:None,timestamp}>,reply}→`dispatch_broadcast_command` | deletes the dead `NoOp{DidResolver,NonceTracker,RevocationChecker,ProofResolver}` turbofish structs (were ONLY used here) |
| `broadcast_unsubscribe` | `BroadcastCommand::UnsubscribeBroadcast`{payload:Box<UnsubscribeBroadcastPayload{context_id,subscriber_did,rotate_keys}>,reply}→`dispatch_broadcast_command` | |
| `broadcast_block_subscriber` | `BroadcastCommand::BlockBroadcastSubscriber`{payload:Box<BroadcastBlockPayload{context_id,author_did:blocker,subscriber_did}>,reply}→`dispatch_broadcast_command` | NOTE payload field is `author_did` = the blocker |
| `broadcast_unblock_subscriber` | `BroadcastCommand::UnblockBroadcastSubscriber`{payload:Box<BroadcastBlockPayload{context_id,author_did:unblocker,subscriber_did}>,reply}→`dispatch_broadcast_command` | |
| `broadcast_handle_key_request` | `BroadcastCommand::HandleBroadcastKeyRequest`{context_id,author_did,requester_did,reply}→`dispatch_broadcast_command` | reply Debug-formatted decision |
| `context_handle_ttl_expiry` | `TtlCloseCommand::ExecuteTtlClose`{payload:Box<TtlContextPayload{context_id,params}>,reply}→`dispatch_ttl_close_command` | REPLACES manual ContextHandle::new+transition+handle_ttl_expiry; then set handle state "expired" |
| `context_propose_ttl_extension` | `TtlCloseCommand::ExtendTtl`{context_id,member_did,proposed_duration,reply}→`dispatch_ttl_close_command` | reply `bool` (unanimous) |
| `context_reset_ttl_timer` | `TtlCloseCommand::ResetTtlTimer`{payload:Box<TtlTimerPayload{context_id,params,duration}>,reply}→`dispatch_ttl_close_command` | HEAD propagates the reply error (main's old fn was fire-and-forget `;`) |

**Non-context.rs fixes:**
- `event_log.rs` `query_manager_entries`: `crate::runtime::context_manager(bi)` → `crate::runtime::supervisor(bi)` (accessor renamed; `Supervisor::event_log_entries` exists).
- `economy.rs` `py_economy_verify_payment_receipts`: the prior agent left it as a trailing FREE `#[pyfunction]` calling global `supervisor()` (0-arg) — but in main the global accessor + DEFAULT_BRIDGE_INSTANCE are DELETED (#1695 Phase D), so a free fn has no `bi`. MOVED it INTO the `#[pymethods] impl PyScp` block as `economy_verify_payment_receipts(&self,...)` with `#[pyo3(name="economy_verify_payment_receipts")]`, `bi=&*self.inner`, `supervisor(bi)`. Command `EconomyCommand::VerifyPaymentReceipts`{receipts:Box,reply}→`dispatch_economy_command`. NOT in `register_economy` (methods register via the class).
- `runtime.rs`: removed now-unused `use scp_core::context::ContextError;` (only doc-comment refs remained).
- `context.rs` create path: `sup.register_local_did(...).await` returns `Result` (must_use) — added `.map_err(|e| ContextError::CreationFailed(format!("register_local_did failed: {e}")))?` to un-swallow (matches HEAD + NAPI).

**KEY LESSON for NAPI/UniFFI:** do NOT trust "this is a direct Supervisor method" — grep `impl Supervisor` / the dispatch fns first. The actor migration moved nearly all per-context lifecycle/governance/broadcast/ttl ops behind `dispatch_*_command` + a `Command` enum; only `dispatch_*`, `register_local_did`, `event_log_entries`, query helpers remain direct. The Command variant + payload struct names are stable across bridges (defined in `crates/scp-runtime/src/context/actor/commands.rs`).

## NAPI bridge (`crates/scp-ffi/napi/src/`) — GREEN (this session)

`cargo check -p scp-ffi-napi --features scp-ffi-napi/allow_in_memory_custody,scp-core/testing` = **0 errors, 0 warnings**. `context.rs` (77 hunks) + `runtime.rs:563` resolved, zero markers, all `git add`'d (mid-merge, NOT committed).

**Structural difference from PyO3 that the NAPI agent MUST know:** main converted every per-context op from a public `#[napi]` free fn (HEAD shape) into a `pub(crate) async fn ..._on(bi, ...)` per-instance fn; the public `#[napi]` wrappers live in `scp.rs` (already resolved) and call the `_on` fns. So the resolution is NOT "keep HEAD's free fn" — it is **produce `_on(bi,...)` fns with HEAD's mailbox-dispatch BODY** (`supervisor(bi)` not global `supervisor()`, `napi_check_handle!(&bi.core, handle)`, `init_supervisor(bi, did)`). Whole multi-fn conflict regions (queries, drain+access-key, broadcast-read, etc.) were rebuilt wholesale as `_on` fns rather than line-surgical.
- `runtime.rs:563`: broken `context_manager(&self)->Option<Arc<ContextManager>>` calling non-existent `try_context_manager` → replaced with `try_supervisor(&self)->Option<&Arc<Supervisor>>` mirroring PyO3 (CoreFields has `try_supervisor`, not `try_context_manager`; `ContextManager` type is DELETED from scp-core). No consumers of the old inherent method.
- subscribe spawned-task: captured `supervisor_for_task = supervisor(bi).ok().cloned()` (replacing main's `manager_for_task`); both in-task uses (LocalPseudonym query + DeliverIncoming) re-pointed to it.
- **publish_broadcast custody (3 sites: publish/publish_asset/publish_assets):** `dispatch_broadcast_command_with_custody(cmd, custody.as_ref())` — NOT HEAD's `&custody.0`. NAPI `custody` = `&Arc<NapiKeyCustody>` (`handle.in_memory_custody.as_ref()`); `NapiKeyCustody: KeyCustody`, dispatch wants `&C`, so `.as_ref()` derefs `&Arc<T>`→`&T`. (PyO3's `.0` was a tuple-custody type — bridge-specific, do NOT copy.)
- **Cross-cutting impediment fixed in `scp-ffi-common/src/bridge_runtime.rs`:** storage-foundation Step 3a (#23) un-swallow changed `BridgeInMemoryStorageHandle = Arc<EncryptingAdapter<BridgeInMemoryStorage>>` and `build_event_log_provider`→`Arc<BridgeInMemoryRepo>`, but left `ProtocolRepoVariant::InMemory(Arc<ProtocolRepository<EncryptingAdapter<BridgeInMemoryStorage>>>)` STALE (non-Arc inner) → E0308 at NAPI runtime.rs `InMemory(protocol_repository)` (lines 321/350). Fix = variant inner → `Arc<BridgeInMemoryRepo>`. Safe: PyO3 has 0 `ProtocolRepoVariant` usages; UniFFI uses the identical `InMemory(protocol_repository)` ctor and needs this SAME fix; consumers (`event_log_provider`/`close`/trust.rs/bridge.rs) are generic over `S: Storage`. **UniFFI agent: this is already fixed — do not re-introduce the non-Arc variant.**
- Pre-existing unused-import warns in NAPI runtime.rs (`MerkleEventLogProvider`, `SqliteStorage` — only doc/fully-qualified refs) removed while staging (would fail workspace `-D warnings`).

See [[project-adr-049-storage-foundation-step1]], [[project-adr-049-storage-foundation-step3]] for the storage ladder context.
