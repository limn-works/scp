---
name: project-out038-python-streaming-reference
description: SCP-OUT-038 C11a — the Python SDK single-verb outlet-streaming reference shape that TS/Swift/Kotlin mirrors must match; plus the Python-SDK mypy/CI-gate reality
metadata:
  type: project
---

C11a committed `3de014f40` + review-fixes `6d6b98ff7` on `feat/outlet-streaming-ffi` (NOT pushed): the Python SDK is the REFERENCE for SCP-OUT-038's single-verb outlet streaming. Review-fix deltas (6d6b98ff7): data-plane bridge errors NOW translated (open in `_ensure_open`, poll in `__anext__`, both via `_translate_bridge_error` → UcanPermissionError/ValidationError/ContextError) — matches control plane; concurrent-consumer guard `_draining` flag → 2nd concurrent driver raises ProtocolError; `cancel()` on never-opened handle = LOCAL no-op close (no open, sets _closed) while grant_credit still opens; module docstring de-staled; mock poll returns `list[int]` (PyO3 Vec<u8>→list) not bytes. PRD SCP-OUT-038 amended (Swift sketch EAGER→LAZY; canonical cross-language contract added; Credit accessor harmonized to `.value` superseding OUT-031's `.raw` Swift sketch — OUT-031 line ~1825 left as-is, OUT-038 explicitly supersedes). PRD is ASCII-escaped (`§`/`—`) — edit raw text, keep additions ASCII; validate-prd.py passes (419 stories). Full suite 728 passed. The exact public shape the TS/Swift/Kotlin mirrors must match:

- `ctx.outlets.invoke(outlet_id, input, *, ucan_token, caller_did=None, proof_tokens=None, spending_ucan=None, timeout_ms=None, estimated_chunk_count=None) -> InvocationHandle`. `invoke` is SYNC + non-blocking (stream opens LAZILY on first await/iterate/control-call via asyncio.to_thread over the sync PyO3 block_on). `caller_did` defaults to the context's identity_did.
- `InvocationHandle` is BOTH awaitable (`await handle` / `handle.aggregate()` -> `Aggregate(value, provenance, execution_time_ms)`) AND async-iterable (`async for chunk in handle` -> `OutletStreamChunk`, Progress surfaced not filtered). Terminal chunk is YIELDED (counts toward the visible sequence) then closes. Terminal Error -> `await` raises typed `OutletError(code,message)`; stream ending w/o End -> `ProtocolError`. Both surfaces share one drain (`_aggregate`/`_error` captured in `__anext__`), so iterate-then-await returns cached aggregate.
- `handle.grant_credit(grant: Credit)` and `handle.cancel()` — control plane wrapped BEHIND the handle. `StreamAlreadyClosed` (lifecycle guard) once terminal reached (checked before touching bridge). Bridge signs credit/cancel internally under pinned invoker key; SDK passes only caller_did + u32.
- `Credit` newtype (`scp_sdk.outlets.Credit`): non-zero u32 `[1, 2**32)`; `Credit(0)/-1/2**32/bool/non-int` raise `InvalidGrant` at construction. `grant_credit(Credit)` — a raw int is a mypy --strict arg-type error (verified) and also raises InvalidGrant at runtime (Python types not runtime-sound, so the guard is real, not redundant).
- NO public `invoke_stream`/`poll_next`/`grant_credit` free functions (grep-assert test scans bindings/ excluding tests + comment lines + Internal/ generated bridges).
- Error hierarchy (errors.py): `ProtocolError(OutletError)`, then `InvalidGrant(ProtocolError)` + `StreamAlreadyClosed(ProtocolError)` (round-5 sibling-depth symmetry). SCP-OUT-031's full 8-class sealed OutletError hierarchy was NEVER actually built (only Saga* subclasses exist) despite OUT-031 status=done — C11a introduced only ProtocolError + the 2 lifecycle/grant subclasses it needed. Do NOT add ProtocolError to BRIDGE_ERROR_MAP: it is not a bridge exception variant name; test_types.py::test_bridge_map_covers_all_variants enforces the map == exact bridge variant set and will fail.

Wiring facts: `Context` gained an `outlets` property + a `_scp` back-ref (threaded via the 3 `Context(raw, ..., scp=self)` sites in scp.py); it was previously a pure handle wrapper. capability-matrix: added Outlets `stream_grant_credit` + `stream_cancel` (python=true; ts/swift/kotlin=false+exemption citing the pending mirror slices). check-sdk-coverage.py needed ADDITIVE ALIASES for both (matcher only auto-generates domain-prefixed candidates like `outlets_stream_grant_credit`, but the real symbols are the bare handle methods `grant_credit`/`cancel`). `.pyi` set-parity gate (check-pyi-generated.sh) was RED on this branch pre-C11a — SCP-OUT-037 added the 7 `outlet_stream_*` PyO3 exports without stubs; C11a hand-added the stubs (generator reconciles but does NOT synthesize missing methods).

Live-test approach: contract-only (scripted mock `_native` replays JSON chunk bytes in §5.4.5 wire shape — request_id/sig are serde_bytes int-arrays; SDK hex-encodes them). Same mock-`_native` convention as every other Python outlet test. The LIVE wire path (funded escrow + granted capability over MLS) is the Rust PyO3 live-poll test (C7); NOT re-faked in Python.

Python SDK mypy reality (so nobody chases it): `mypy --strict scp_sdk/` has a PRE-EXISTING baseline of 40 errors at c66a8b225 (mostly `no-any-return` from the pervasive `_native: Any` pattern, in economy/media/discovery/trust/server/identity/bridge/sync/scp/context). C11a REMOVED one (fixed a real `.did`-on-None union-attr at to_dict) -> 39, and the streaming code adds ZERO. mypy is NOT a CI gate — the Python CI lint job runs ONLY `ruff format --check .` + `ruff check .`. Aggregate-schema validation is executor-side (§5.4.5); the SDK deliberately does NOT re-validate (no jsonschema dep; redundant per CLAUDE.md anti-over-engineering) — it surfaces the validated aggregate + discriminates End vs Error terminals.
