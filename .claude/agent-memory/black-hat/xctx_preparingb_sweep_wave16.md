# §6.2.4 PreparingB crash-recovery wave-16 (commit d161e8fd9) — REAPER econ-clean; NTTEST gate has regression + FP

Branch chore/fuzz-pin-nightly worktree xctx-blackhat (detached d161e8fd9). Diff base 6b7c8b658.
Files: crates/scp-runtime/src/context/supervisor/supervisor.rs, scripts/check-class-s-fail-closed.sh

## (1) REAPER over-charge — RESISTS for LOCAL economy; residual external-escrow strand is pre-existing
- Reaper `caller_context_deleted_from_persistence` reaps terminal-Aborted ONLY on `load_context==Ok(None)`; `Ok(Some)`→leave-non-terminal, `Err`→treat-present, no-backend(`helper_persistence` None)→false. Correct posture.
- `helper_persistence` = Arc::clone of SAME persistence backend that holds context snapshots (supervisor.rs:1276-1301). So reaper reads the SAME store. InMemoryPersistence load==Ok(None) iff genuinely absent. Same-store invariant HOLDS.
- `CallerReservationRecord` (xctx_caller_reservations) lives INSIDE the ContextSnapshot (messaging_helpers.rs:2215). So Ok(None) ⇒ LOCAL economy (budget/velocity/hard-rate-limit) state died WITH the snapshot — nothing to over-charge locally. SAFE.
- RESIDUAL (not wave-16-introduced): the `escrow_authorization: PaymentAuthorization` (external payment hold) ALSO lives only in the in-snapshot record. `CrossContextToolInvocationPrepared` journal evidence does NOT carry it (saga_prepared_state.rs:258). close→finalize_close(ttl_close_helpers.rs:374) deletes snapshot WITHOUT reconciling outstanding xctx_caller_reservations. So a deleted caller's outstanding external escrow is irrecoverable — but it was ALREADY irrecoverable pre-wave-16 (the only void path is the co-resident caller actor's Abort handler, gone on deletion). Wave-16 converts infinite-non-terminal→terminal-Aborted; escrow strand identical in both. Reaper writes terminal-Aborted ("fully compensated") which is a slightly-false assertion when external escrow held, but it cannot be voided anyway.
- Deleted-then-recreated-same-deterministic-id collision: Abort{None} handler keyed by saga_id on xctx_caller_reservations (saga.rs:1850); fresh ctx has empty map ⇒ remove→None ⇒ clean no-op, NO double-refund/spurious-reversal. RESISTS.
- Startup replay-before-restore: doc (supervisor.rs:5476-5478) ADMITS restore-vs-replay ordering not enforced in-tree. InMemory backend has no stored-but-unreadable window. RESIDUAL risk only for a FUTURE on-disk lazy backend whose load_context could transiently Ok(None) a present snapshot → mis-reap recoverable caller's LOCAL deduction. Documented assumption, not enforced invariant.
- TEST GAP: all 3 saga test mocks (actor_saga_crash_recovery/coordinator/concurrent) return load_context==Ok(None) UNCONDITIONALLY. The Ok(Some)→leave-non-terminal branch (the over-charge-prevention branch) and Err→present branch have ZERO coverage. A future arm-inversion would pass all tests while over-charging.

## (2) NTTEST convergent guard — REAL regression (bypass) + REAL false-positive
Verified with real scan_file via cloned runner (/tmp/nttest_dump.sh, SCAN_DIR repointed).
- BYPASS (coverage regression vs wave-15): `is_column0_item_start` returns 0 for `unsafe impl` and `unsafe trait` (and `default impl`). Root cause: clause-1 matches bare item-kw with only `pub` vis; the qual-run (async/const/unsafe/extern) feeds ONLY clause-2 which ends in `fn`. So unsafe/const-qualified NON-fn items fall through. Wave-15 denylist HAD `unsafe[[:space:]]+impl` (6b7c8b658 line 860) — wave-16 refactor DROPPED it. Fixture nontrailing test mod + `unsafe impl {...}` ⇒ NO NTTEST line (un-scanned vacuum over the indented impl body). `extern "C" fn`, `async unsafe fn`, `pub(in path) fn`, `impl<T>`, `mod`, `pub(crate) mod` resume all correctly flagged.
- FALSE POSITIVE (CI break on valid Rust): second-test-module branch accepts mod via `^(pub[[:space:]]+)?mod[[:space:]]` which REJECTS `pub(crate) mod`/`pub(in path) mod`. So `#[cfg(test)] pub(crate) mod more_tests` after a first test mod ⇒ is_column0_item_start==1 but inner mod-check==0 ⇒ else branch ⇒ NTTEST HIT on a legit second test module. Asymmetric with main `vis` regex (which accepts pub(...)). Fixture confirmed end-to-end.
- NEITHER has a live occurrence in crates/scp-runtime/src/context (no col-0 unsafe impl; no file w/ 2+ #[cfg(test)] gates; no pub(crate) test mod). Latent. Live gate PASSES. Self-test PASSES.

Fix sketch: add `unsafe`/`const`/`default`-qualified non-fn items to clause-1 (or generalize qual-run to all item kws); widen second-module mod-accept to the same `vis` regex as is_column0_item_start.
