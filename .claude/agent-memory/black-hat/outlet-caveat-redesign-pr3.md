---
name: outlet-caveat-redesign-pr3
description: BLACK-hat attack on OutletQuery/OutletCall split + §7.3.8 InvocationCaveats (branch feat/outlet-report-pr3). FINAL @1ffe476e5: ALL 5 attacks BLOCKED, HEADLINE phantom-provenance now CLOSED — spec §7:918/923/925 honestly marks value-caveat family + CaveatCounterStore as DEFERRED/not-yet-wired with "no live divergence" argument. No exploitable findings.
metadata:
  type: project
---

# FINAL PASS @1ffe476e5 (1 commit past 7512e2159; tip of feat/outlet-report-pr3, 2026-07-10)

## VERDICT: could not break. All 5 attacks BLOCKED. HEADLINE (phantom provenance) now CLOSED at this tip.
- **Attack #1 (query↔call ceiling crossing): BLOCKED.** `CapabilityCeiling::contains` (roles.rs:722) — `OutletQuery(_)` only satisfied by `OutletQueryAll`; `OutletCall(_)` only by `OutletCallAll`; else exact-set membership. Disjoint by construction. All 3 native bridges (PyO3 context.rs:1881, NAPI context.rs:5051, UniFFI bridge.rs:8153) route IDENTICALLY through `CapabilityCeiling::new(granted).contains(&required)` with same `filter_map(Capability::new)` granted + fail-closed `else return false` required. WASM has ZERO outlet-capability logic (grep=0) — no surface to diverge.
- **Attack #2 (origin_kind forgery / mid-chain flip): BLOCKED.** origin_kind is derived from signature-covered stems, never trusted as free data. `verify_leaf_outlet_stem_consistency` (validate.rs:1920, called UNCONDITIONALLY validate.rs:807) + `verify_origin_kind_matches_stem_family` (validate.rs:1870, edge Some/Some & None/Some arms) pin nb.origin_kind==stem family; `classify_outlet_stem_family` (validate.rs:1819) rejects mixed (true,true) unconditionally. narrow() origin_kind arm (caveats.rs:686) rejects child=None + parent≠child. Flipping origin_kind buys nothing: authorization is 100% stem-based (still need outlet_call stem; if you have it, validator FORCES origin_kind=Action).
- **Attack #3 (saga TokenNbCaveatResolver confused-deputy): BLOCKED.** saga.rs:1207 now TokenNbCaveatResolver. required_cap = `outlet_call:{tool}` on B's OWN target_hex (saga.rs:1158); presenting_agent_did=req.caller_did (audience gate rejects proof delegated to different principal); target_context_id must == B's context (saga.rs:1092); root issuer must == B's creator (step 4); proof resolved from B's OWN store. A-scoped token can't match B's target_hex. caller_did forge is inert — required cap re-derived from B's ctx, not attacker assertion.
- **Attack #4 (value-caveat token misleads consumer into "enforced"): BLOCKED + spec now HONEST.** Structural proof mint can't emit value-caveats: MintParams/DelegateParams carry NO caveat-value params; build_root_caveats→None; build_delegated_caveats materializes ONLY origin_kind (`InvocationCaveats { origin_kind, ..parent_effective }`) inheriting parent's value fields — by induction from root(None) no value field ever introduced. No FFI path sets payload.nb from caller input (both UcanPayload{nb:..} are TEST-only). check_invocation_local has ZERO prod callers (only caveats.rs tests); CaveatCounterStore grep struct/trait=0 (doesn't exist). **Spec §7.3.8 lines 918/923/925 NOW explicitly say value-caveat family + CaveatCounterStore are "deferred"/"NOT YET WIRED; the spec leads here"/"type does not exist on the current branch" WITH the "no live divergence" argument.** The prior HEADLINE phantom-provenance finding (spec claimed present-tense enforcement) is CLOSED at this tip.
- **Attack #5 (legacy-stem smuggle / cross-bridge asym): BLOCKED.** Capability::new (roles.rs:225-238) hard-rejects outlet:invoke:/outlet_invoke:/tool:invoke:/tool_invoke:/tool:register|interface BEFORE Custom fallback. mint routes through it (mint.rs:477). All bridges share it; WASM has no reimpl. filter_map drop only affects GRANTED (fail-closed: dropped grant can't grant); required uses fail-closed else-deny.

## Structural properties that stop the attacks
- **origin_kind = RESTRICTION invariant, never GRANT** (no allow-decision keys off it; stem-based auth).
- **Ceiling query/call disjointness by construction** (contains has 2 explicit arms + exact set).
- **Mint = local convenience, Validate = authority** (mint trusts parent.nb but validator re-walks every edge stem-pinned; forged parent's chain rejected at validate).
- **Value-caveat family genuinely deferred AND spec honestly says so** — no phantom provenance; the "no live divergence" argument is sound (mint emits none → runtime can't under-enforce what no token asserts).

## Only nit (LOW, non-actionable): context.rs:8403 test comment says "Malformed required capability denies (fail-closed)" for input `"not-a-capability"`, but that parses as Custom(name) via roles.rs:289 catch-all (Some, not None) — deny happens because granted set lacks it, not because required=None. Test passes correctly; comment rationale slightly off. Cosmetic.

---

# RE-ATTACK @7512e2159 (3 commits past b4e97ff50: f6fd5705a origin_kind mint materialization + saga resolver align; 4af0af916 dedup/symmetry; 7512e2159 docs)

## VERDICT: the fix is SOUND for escalation. Only HEADLINE (phantom provenance) + prior secondary remain.
- **origin_kind is a RESTRICTION invariant, never a GRANT.** Grep confirms NO allow-decision anywhere keys off `origin_kind`/nb presence/OutletKind::Query (runtime + all bridges). Authorization is 100% stem-based: invoke.rs:449 `has_outlet_call_capability` (role state) + invoke.rs:1164 required_cap=`outlet_call:{name}` subset via `matches()` (exact resource match, capability.rs:177). So flipping origin_kind buys nothing — you still need the `outlet_call` stem, and if you have it `verify_origin_kind_matches_stem_family` (validate.rs:1870) FORCES origin_kind=Action. Attacks #1/#2/#3 = BLOCKED as escalations.
- **NEW validator guards (this fix) close the forge surface:** `verify_origin_kind_matches_stem_family` (validate.rs:1870, called in edge (Some,Some)+(None,Some) arms) + `verify_leaf_outlet_stem_consistency` (validate.rs:1920, called UNCONDITIONALLY on presenting token at validate.rs:807) both pin nb.origin_kind==stem-family and reject mixed-family unconditionally. `classify_outlet_stem_family` (validate.rs:1819) rejects (true,true) mixed. narrow() origin_kind arm (caveats.rs:686): child=None→OriginKindUnspecified always; parent Some & child Some differ→Mismatch; parent None child Some→OK. verify_edge_attenuation (None,None) outlet-edge arm→reject (validate.rs:1791).
- **mint trusts parent_token.payload.nb WITHOUT re-validating the parent chain (mint.rs:198).** NOT a hole: mint is local convenience; the VALIDATOR re-walks every edge stem-pinned. An attacker-forged parent (Action nb + query stems) yields a child whose chain is rejected at validate (edge or leaf stem check). Correct arch: mint=convenience, validate=authority.
- **saga @1207 now TokenNbCaveatResolver (was NoCaveatResolver — prior finding #2 FIXED).** Newly ACCEPTS honest depth-N caveated x-ctx chains that were DoS'd before; no reject→accept escalation because nb only adds constraints + gate still stem-based + confused-deputy binding (presenting_agent_did=caller_did, AudienceMismatch) intact. saga re-runs full validate_ucan rebound to caller_did+outlet_call:{tool}+B's ctx.
- Legacy-stem (attack #5): still hard-rejected Capability::new (roles.rs:225-231) before Custom fallback; mint routes through it (mint.rs:474). BLOCKED.
- **Time-box IS runtime-enforced** (Step 11b verify_caveat_time_box validate.rs:843-844: valid_from/until/hours/days). So the deferred slice is PARTIAL not total — the 4th spec bullet (post-input value checks) is the only unwired one.
- EMPIRICAL: cargo test scp-runtime ucan_validate_integration --features testing = 46 pass 0 fail (after clean rebuild; a STALE test binary showed 2 phantom failures `adversarial_depth3_*` whose names exist NOWHERE in source — red herring). caveats lib 114 pass, mint delegate/origin tests pass.

## HEADLINE (HIGH, phantom provenance — WORSENED this branch): §7.3.8 post-input value-caveats + CaveatCounterStore UNWIRED, spec asserts present-tense enforced
- Spec `.docs/specs/07-trust-validation-and-capabilities.md:923-925` — commit **f256ca02f6 (this branch, 2026-07-10, NOT on origin/main; grep origin/main = 0)** — added the "Post-input checks, inside invoke_outlet" bullet ("checks input_schema conformance, amount_max_per_call vs cost, allowed_adapters, allowed_target_dids, consults CaveatCounterStore for max_calls/amount_max_cumulative/rate_window") + the `CaveatCounterStore` paragraph ("durable... persisted under context/{id}/caveat_counters/{ucan_cid} per §17.3"). Spec says caveats "enforced at THREE points" but lists FOUR bullets; 3 (Step 7b/7c/11b) ARE wired, the 4th is NOT.
- REALITY unchanged from b4e97ff50: `check_invocation_local` (caveats.rs:807, COMPLETE + tested) called ONLY from caveats.rs tests. `CaveatCounterStore` still grep struct/trait = 0. `invoke_outlet` (invoke.rs:440-499 and siblings) does ctx-active + has_outlet_call_capability + registry input_schema (the OUTLET's, not the caveat's) + execute — ZERO calls to check_invocation_local, ZERO amount/target/adapter/count/rate enforcement.
- IMPACT: silent-restriction-bypass. A delegator who hand-crafts nb {max_calls:1, amount_max_per_call:5, allowed_target_dids:[X]} (mint won't emit these — DelegateParams has no caveat params, mint always inherits empty for value fields) gets a token that VALIDATES (narrow ok, time-box absent-fields ok) but whose amount/count/target bounds are NEVER enforced at invocation. The delegation LOOKS enforced, isn't. Also violates artifact-flow INVARIANT (code→spec): spec edited to claim enforcement code doesn't do. Fix: wire invoke_outlet post-input + build CaveatCounterStore, OR mark the 4th bullet + CaveatCounterStore para as a future/deferred slice explicitly.

## SECONDARY (unchanged, LOW-MED): mint value-caveat materialization still deferred
- mint (`build_delegated_caveats`/`build_root_caveats`) materializes ONLY origin_kind; value fields inherit-from-empty (roots) / no DelegateParams caveat params. So no HONEST token ever carries a value-caveat — only self-signed/hand-crafted. Combined with headline (never enforced anyway) the practical delta is small, but it means the entire value-caveat half of §7.3.8 (attenuation table columns amount/count/rate/adapters/targets/schema) is inert end-to-end today.

---
# (PRIOR PASS) Outlet capability redesign (OutletQuery/OutletCall + §7.3.8 caveats) — attack pass @b4e97ff50

Branch `feat/outlet-report-pr3`, worktree scp-wt-outlet-pr3. Diff a65a778c9..b4e97ff50.

## HEADLINE FINDING (HIGH, spec-vs-code divergence): §7.3.8 value-caveats never enforced
- Spec `.docs/specs/07-...md:918-925` mandates FOUR enforcement points incl. "Post-input checks inside invoke_outlet" (amount_max_per_call, allowed_adapters, allowed_target_dids, input_schema, CaveatCounterStore for max_calls/amount_max_cumulative/rate_window; durable under context/{id}/caveat_counters/{ucan_cid}).
- REALITY: `check_invocation_local` (caveats.rs:807) called ONLY from caveats.rs tests. `CaveatCounterStore` type DOES NOT EXIST (grep struct = 0; only in doc comments). `invoke_outlet` (invoke.rs) has ZERO caveat calls.
- Mint (`crates/scp-runtime/src/crypto/ucan/mint.rs:342,599`) always emits `nb: None`. `build_delegated_caveats`/`build_root_caveats` referenced in validate.rs docstrings (1702/1787/1915) but DO NOT EXIST (grep def = 0). So no honest token carries caveats; even the wired `TokenNbCaveatResolver` time-box (Step 11b) never fires on real tokens.
- NET: value-caveats (amount/count/rate/adapters/target-dids/input_schema) are validated for per-edge NARROW MONOTONICITY only (token-vs-token), NEVER against an actual invocation's amount/target/count. A delegator who "restricts to max_calls:1, amount_max:5, allowed_target_dids:[X]" gets NO runtime enforcement of those bounds.
- Scope judgement: attenuation plumbing landed; runtime enforcement + mint materialization appear DEFERRED but spec §7.3.8 was edited IN THIS PR to assert they're enforced → phantom provenance. Fix: implement invoke_outlet post-input + CaveatCounterStore, or mark spec as future.

## SECONDARY (MED): cross-context saga re-validation uses NoCaveatResolver on an outlet_call gate
- `crates/scp-runtime/src/context/actor/handlers/saga.rs:1150-1205` (`validate_ucan_rebind`, §6.2.4 confused-deputy). required_cap = `outlet_call:{outlet_registration_id}` on B's ctx, resolver = `&NoCaveatResolver` (line 1202).
- This IS an outlet-invocation authorization gate but skips caveat resolution. Consequence: a DELEGATED (depth>=2) caveated outlet proof is REJECTED here (verify_edge_attenuation (None,None) arm + child_is_outlet_edge=true → OriginKindUnspecified) = fail-closed DoS for legit x-ctx caveated calls; a DEPTH-1 root outlet proof PASSES with caveats ignored. Combined with headline (no runtime enforcement anyway) the practical delta is small, but the resolver choice is inconsistent with the outlet-open sites.

## BLOCKED / SOUND (could not break)
- **Attack #1 query→call escalation: BLOCKED.** `CapabilityUri::matches()` (capability.rs:175) requires EXACT resource match; `outlet_query`≠`outlet_call`. Wildcard action `outlet_query:*` cannot match `outlet_call:X`. `validate_outlet_invocation_ucan` requires `outlet_call:{name}`. app_sandbox.rs superset only OutletCallAll⊇OutletCall(_); no query↔call crossing. `has_outlet_invoke_capability` checks OutletCall/OutletCallAll only.
- **Attack #3 origin_kind laundering: BLOCKED.** `narrow()` (caveats.rs:686) `(_,None)→OriginKindUnspecified` rejects any non-root missing origin_kind. `(None,None)` outlet-edge arm in verify_edge_attenuation rejects. `verify_leaf_outlet_stem_consistency` runs UNCONDITIONALLY on presenting token (validate.rs:807) catching forged depth-1 mixed-family. `classify_outlet_stem_family` rejects mixed (true,true) unconditionally. try_new_for_root rejects mixed-stem root.
- **Attack #4 legacy-stem smuggling: BLOCKED (no cross-bridge asym).** `Capability::new` (roles.rs:220) hard-rejects tool:invoke:/outlet:invoke:/tool_invoke:/outlet_invoke: BEFORE any Custom fallback. ALL 4 bridges route ceiling parse through shared scp-protocol Capability::new (context_params.rs:136,294); no bridge has its own outlet parser; wasm doesn't reimplement. `Custom("outlet_call:*")` deserialized-direct caught by `validate_as_ceiling_entry` re-resolution (roles.rs:558: `!matches!(Self::new(name), Some(Self::Custom(_)))` → reject).
- **Attack #5 filter_map drop: BLOCKED on cap path.** `parse_granted_caps` (validate.rs:623) + `verify_edge_attenuation` parent parse (1668) both `.collect::<Result>()?` — fail-closed, no filter_map/ok() that drops a DENY/ceiling entry.
- **Attack #6 SCP-CODE-OK marker: LOW/acceptable.** check-error-codes.sh skips whole LINE on `*SCP-CODE-OK:*`. Could let a new prod code append `// SCP-CODE-OK: x` to dodge range/collision check, but marker is grep-visible in review + only bypasses Phase-1 range (not the enforcement-file protections). Reviewable, bounded. Not a smuggle of colliding code past humans.

## Caveat resolver call-site map (the security-critical wiring)
- TokenNbCaveatResolver (caveat-aware): scp-ffi/src/outlets.rs:362, mcp.rs:749; napi/outlets.rs:60; uniffi/bridge.rs:4021,4609. = outlet-invocation open sites.
- NoCaveatResolver (caveat-blind): generic ucan.rs validate/evaluate (all bridges), broadcast/*, saga.rs:1202, spending.rs:1079. Key: verify_edge_attenuation (None,None) arm STILL rejects outlet edges → NoCaveatResolver is fail-closed for delegated outlet chains (not a launder), but resolves nothing for depth-1.
