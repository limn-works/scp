[project]
name = "scp-chat"
version = "0.1.0"
description = "Two-party encrypted chat over SCP"
requires-python = ">=3.12"
dependencies = [
    "scp-python",
]

[project.scripts]
scp-chat = "chat:main"
