name: Bad
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: workspace-debug
          save-if: false
  clippy:
    runs-on: ubuntu-latest
    steps:
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: workspace-debug
          save-if: false
