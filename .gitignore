# OS
.DS_Store

# Rust
target/
rust_out

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# TypeScript
node_modules/
dist/

# Swift
bindings/swift/.build/

# Kotlin
bindings/kotlin/.gradle/
bindings/kotlin/**/build/
# UniFFI-generated Kotlin bindings (regenerated from Rust source)
bindings/kotlin/scp-kt/src/main/kotlin/works/limn/scp/internal/*.kt

# Native build artifacts
*.node
*.wasm
*.so
*.dylib

# Misc
.fastembed_cache/

.claude/worktrees
.worktrees

.mcp.json
.mcp_data
bindings/kotlin/scp-kt/src/main/kotlin/works/limn/scp/internal/
bindings/python/tests/bridge_parity/helpers/kotlin_bridge_runner/.gradle/
bindings/python/tests/bridge_parity/helpers/kotlin_bridge_runner/build/
bindings/python/tests/bridge_parity/helpers/swift_bridge_runner/.build/

# ADR-049 Decision 14 perf_baseline harness — per-environment runtime artifact
# recorded by the default `--test perf_baseline` run; never committed (one box's
# numbers must not become a cross-machine gate reference).
crates/scp-runtime/tests/perf_baseline.json

# Audit-skill scratch output (.claude/skills/audit/SKILL.md §5.1 writes a
# checkpoint, a coverage matrix, and per-finding notes here on every run).
# Those files are an inventory of this codebase's unfixed defects, so this
# public repository never tracks them.
.claude/agent-memory/audit/
