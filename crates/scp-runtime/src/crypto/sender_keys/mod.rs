//! Per-sender AES-256 symmetric key layer — async runtime.
//!
//! Pure types are in scp-protocol::crypto::sender_keys. This module retains
//! the async `key_protocol` module.

pub mod key_protocol;
