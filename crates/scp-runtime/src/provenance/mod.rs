//! Data provenance — async runtime.
//!
//! Pure types are in scp-protocol::provenance. No async modules remain
//! in scp-runtime for this module.

// Re-export everything from scp-protocol::provenance.
pub use scp_protocol::provenance::*;
