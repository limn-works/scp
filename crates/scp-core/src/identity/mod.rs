//! Identity re-exports removed.
//!
//! Import directly from [`scp_identity`] instead of `scp_core::identity`.
//! SCP has not shipped, so no backward compatibility is needed.
//!
//! See ADR-003 in `.docs/adrs/phase-1.md` for the full design.
