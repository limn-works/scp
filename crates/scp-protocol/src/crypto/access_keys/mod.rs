//! Per-member access key types for SCP — pure protocol types.
//!
//! AccessKey, AccessKeyError, ContentEncryptionKey types.
//! The `lifecycle` and `wire` modules (async key operations) stay in scp-runtime.

pub mod wrapping;
